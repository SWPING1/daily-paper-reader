---
title: "FedTaste: Topology-Aware Structural Transfer for Multimodal Federated Learning with Missing Modalities"
title_zh: "FedTaste: 面向缺失模态的多模态联邦学习的拓扑感知结构迁移"
authors: "Haochen Liang, Jie Zhang, Hideya Ochiai"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23245v1"
tags: ["query:mm"]
score: 9.0
evidence: 通过结构迁移处理缺失模态
tldr: 多模态联邦学习面临模态缺失与非独立同分布数据导致的表征漂移。现有生成式填补或辅助数据方法通信成本高且有隐私风险。FedTaste提出参数高效框架，利用冻结基础模型提取全模态客户端的联合拓扑结构，服务器整合为全局蓝图，并通过模态自适应结构提示与谱一致性正则化实现缺失客户端的轻量级适应。实验表明，该方法在多个数据集和非独立同分布场景下性能显著优于现有方法，同时大幅降低通信开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23245v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 862, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23245v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1793, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23245v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1796, \"height\": 972, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23245v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 835, \"height\": 443, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23245v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 848, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23245v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 836, \"height\": 451, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23245v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 850, \"height\": 356, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23245v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1665, \"height\": 443, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23245v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1661, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23245v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 855, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23245v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 853, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23245v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 850, \"height\": 381, \"label\": \"Table\"}]"
motivation: 现有方法依赖生成式填补或辅助数据，成本高且存在隐私风险，亟需无需显式填补的轻量级联邦学习方案。
method: 冻结基础模型提取全模态客户端的联合拓扑，服务器整合为全局蓝图，再通过模态自适应结构提示和谱一致性正则化对齐缺失模态客户端的表征。
result: 在多个数据集与非独立同分布设置下，FedTaste性能显著优于现有方法，且通信开销大幅降低。
conclusion: FedTaste有效解决模态缺失问题，兼顾性能与效率，适用于隐私敏感的多模态联邦场景。
---

## 摘要
多模态联邦学习通常面临任意模态缺失和非独立同分布数据分布的挑战，这会导致严重的表示漂移，阻碍客户端之间的有效协作。现有方法通常依赖于生成式插补、外部辅助数据或孤立单模态训练来弥合模态差距，这往往会产生大量的通信和计算成本以及潜在的隐私风险。为解决这些限制，我们提出FedTaste，一种面向缺失模态的多模态联邦学习的拓扑感知结构迁移参数高效框架。FedTaste不关注脆弱的一阶特征对齐，而是聚焦于更稳定的组级语义关系。具体而言，FedTaste利用冻结的基础模型从全模态客户端提取联合多模态拓扑，再由服务器整合为全局结构蓝图。为适配缺失模态的客户端，我们引入模态自适应结构提示与谱一致性正则化，实现轻量级分支特定适应，使局部部分表示与共享蓝图对齐。通过这种方式，FedTaste避免了显式模态插补，同时保留了客户端间共享的语义结构。大量实验表明，FedTaste在多个数据集和具有挑战性的非独立同分布设置中始终取得优越性能，同时相比现有方法显著降低通信开销。

## Abstract
Multimodal Federated Learning is often challenged by arbitrary modality missingness and Non-IID data distributions, which lead to severe representation drift and hinder effective collaboration across clients. Existing methods typically rely on generative imputation, external auxiliary data, or isolated unimodal training to bridge modality gaps, often incurring substantial communication and computational costs as well as potential privacy risks. To address these limitations, we propose FedTaste, a parameter-efficient framework for topology-aware structural transfer in Multimodal Federated Learning with missing modalities. Instead of aligning fragile first-order features, FedTaste focuses on more stable group-level semantic relations. Specifically, FedTaste leverages frozen foundation models to extract a joint multimodal topology from full-modality clients, which is then consolidated by the server into a global structural blueprint. To adapt clients with missing modalities, we introduce Modality-Adaptive Structural Prompts together with spectral consistency regularization, enabling lightweight branch-specific adaptation that aligns local partial representations with the shared blueprint. In this way, FedTaste avoids explicit modality imputation while preserving shared semantic structure across clients. Extensive experiments demonstrate that FedTaste consistently achieves superior performance across multiple datasets and challenging Non-IID settings, while substantially reducing communication overhead compared with existing methods.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态联邦学习（MFL）面临两个主要挑战：一是任意模态缺失（客户端可能缺少图像或文本模态），二是非独立同分布（Non-IID）数据分布。这两种挑战导致局部表示空间剧烈漂移，传统一阶特征对齐脆弱不可靠，阻碍客户端间有效协作。
- **现有方法局限**：现有三类主流方法各有缺陷：
  - 生成式插补（如FL-MRSG、FedMobile）：计算开销大，可能引入语义幻觉，不适合资源受限设备。
  - 依赖外部辅助数据（如CreamFL）：引入域偏移和隐私泄露风险，破坏联邦闭环。
  - 直接一阶特征或原型对齐（如FedCola）：在严重Non-IID下易因表示漂移而失效。
- **本文动机**：需要一种轻量、鲁棒、隐私保护的多模态对齐机制，避免显式模态填补或依赖外部数据，转而利用更稳定的二阶语义关系（拓扑结构）进行跨客户端知识迁移。

## 2. 论文提出的方法论

### 2.1 核心思想
- 放弃脆弱的一阶绝对坐标对齐，转而对齐更稳定的组级二阶语义关系（相对距离/拓扑）。利用冻结的预训练多模态基础模型（CLIP ViT-B/32）提取全模态客户端的联合多模态拓扑，服务器整合为**置信感知全局结构蓝图**，缺失模态客户端通过**模态自适应结构提示（MASP）**与**谱一致性正则化**进行轻量级适应。

### 2.2 关键技术细节

#### 教师端语义结构蒸馏
- 全模态教师客户端使用冻结的CLIP编码器，附加可学习的MASP。
- 对每个语义组（类或检索单元）计算模态特定原型并融合为联合语义锚点。
- 构建组间亲和矩阵（基于余弦相似度），并计算归一化拉普拉斯矩阵，提取K个最小非平凡特征值作为谱描述符。
- 上传负载：亲和矩阵 \(M_k\)、特征向量 \(\Lambda_k\)、支持掩码 \(B_k\)。

#### 服务端全局结构蓝图构建
- 聚合所有教师上传的亲和矩阵，计算支持感知均值 \(\bar{M}\) 和跨客户端方差。
- 通过置信掩码过滤：保留支持数>0、方差≤阈值、行内Top-\(K_n\) 的边，得到全局蓝图 \(\hat{M}_g\)。
- 同时计算平均谱描述符 \(\Lambda_g\) 和全局活动掩码 \(A_g\)。

#### 学生端轻量级结构适应
- 缺失模态学生仅使用一个活跃分支（图像或文本），同样冻结编码器，只更新MASP。
- 局部任务采用组监督对比损失（基于可用模态）。
- 拓扑损失：对齐局部亲和矩阵与全局蓝图，仅作用于同时满足全局可靠、全局活跃、局部支持的边。
- 谱一致性损失：对齐局部拉普拉斯小特征值与全局谱描述符。
- 参数正则化防止过度扰动。
- 总损失：\(L_{total} = L_{task} + \beta_r L_{topo} + \gamma L_{spec} + \eta L_{reg}\)，其中拓扑权重预热。

#### 非对称联邦优化
- 教师上传结构负载，学生下载蓝图，均不更新全模型，只更新轻量MASP。
- 通信开销低，教师上传MASP参数+稀疏拓扑负载，学生只同步活跃分支MASP。

## 3. 实验设计

- **数据集**：
  - **图像-文本检索**：Flickr30k（29k训练图，1k测试图；145k/5k文本）、MS-COCO（113k/5k图；566k/25k文本）。
  - **医学域（域偏移测试）**：OrganCMNIST（12,975图像，8,216文本？原文表1显示OrganCMNIST为图像12,975，文本8,216，但分类栏标注为Classification；Medical Abstr.为文本11,550/2,888，标注Classification）——训练与测试集划分未详细说明。
- **场景**：
  - 默认：32个客户端（8全模态教师，12图像-only，12文本-only），Dirichlet α=0.5，参与率 r=0.25。
  - 更异构（α=0.1）、更低参与率（r=0.125）。
  - 倾斜模态分布（更多图像客户端、更多文本客户端、更少全模态客户端）。
  - 域偏移场景（用医学数据集模拟OOD，对比理想域内数据辅助 vs 噪声域外辅助数据）。
- **对比方法**：
  - FedAvg（2017）、FedProx（2020）、FedIoT（2022）、CreamFL（2023）、FedCola（2024）——涵盖经典优化、模态感知聚合、辅助数据驱动、一阶对齐范式。
- **实现细节**：
  - 统一使用CLIP ViT-B/32（冻结），仅MASP可训练。
  - AdamW优化器（lr=2e-4, weight decay=0.01, batch=64），50轮通信，每轮客户端本地5个epoch。
  - 谱保留K=5个特征值，拓扑权重β=0.5，预热10轮。

## 4. 资源与算力

- 论文明确提到：“All experiments are conducted on a server equipped with an NVIDIA H100 GPU.” 但未说明具体GPU数量（可能是单卡），也未给出训练时长。
- 总结：文中仅提及一台配备NVIDIA H100 GPU的服务器，算力细节不完整。

## 5. 实验数量与充分性

- **实验组数**：
  - 主表（表2、表3）包含6种场景（默认、更异构、更少参与、三种倾斜模态分布），在两个数据集（Flickr30k, MS-COCO）上共12组结果。
  - 通信效率对比（表4）：在Flickr30k上对比6种方法。
  - 域偏移实验（表5）：在医学设置下对比4种方法（Ideal vs Noisy），含相对性能下降百分比。
  - 超参数敏感性（图4）：两个参数（K和β）的若干取值。
  - 消融实验（表6）：6种配置（A～F），逐步添加组件。
  - 定性可视化（图5、图6）：热力图和t-SNE投影。
- **充分性评价**：
  - 实验覆盖了多种异构性、参与率、模态缺失分布，并额外测试域偏移，设计较为全面。
  - 消融实验充分验证各组件贡献（MASP、拓扑损失、谱损失、正则化），并对比了一阶对齐替代。
  - 对比方法覆盖多个代表性范式，且所有方法共享同一数据分区和通信轮次，公平性较好。
  - 不足：未在更多模态（如音频、传感器）上验证，也未涉及异步联邦场景；医学域偏移实验仅一个设置，可扩展性有限。

## 6. 论文的主要结论与发现

- FedTaste在所有测试场景下显著优于现有方法：在默认Flickr30k上R@1 sum达99.64 vs FedCola 91.96；在更异构α=0.1时仍保持领先。
- 在稀疏参与、倾斜模态分布、教师稀缺等困难设置下，FedTaste表现稳定，表明二阶拓扑结构对齐比一阶坐标对齐更鲁棒。
- 通信效率高：Flickr30k上通信开销174.58 MB（最低），同时性能最高。
- 域偏移下性能下降极小（-2.10% vs 其他方法下降-35.13%至-4.05%），证明无需外部数据即可维持分布内泛化。
- 消融实验证实每个模块（MASP、拓扑损失、谱损失、正则化）均贡献显著；用一阶对齐替代拓扑损失后性能大幅下降。
- 可视化（热力图和t-SNE）显示FedTaste产生更集中的对角线和更紧凑的聚类结构。

## 7. 优点

- **方法论创新**：将二阶语义拓扑迁移引入MFL，规避一阶对齐的脆弱性，是重要的范式转变。
- **参数高效**：仅更新轻量MASP，冻结CLIP，既保留预训练知识又降低通信/计算成本。
- **无需显式模态填补或外部数据**：避免生成开销和隐私泄露，适合边缘设备。
- **置信感知蓝图**：通过跨客户端方差和Top-K筛选过滤不可靠边，提升全局结构质量。
- **实验设计严谨**：覆盖多种异构性、模态缺失模式、域偏移，并与多个强基线公平对比。
- **效率与性能兼得**：通信开销最低同时性能最优，实用价值高。

## 8. 不足与局限

- **实验覆盖**：
  - 仅验证图像-文本两种模态，未扩展到音频、传感器等更多模态（论文未来工作中提到，但未实现）。
  - 域偏移实验仅用一个医学场景，且未提供详细数据集划分和统计，可复现性略弱。
  - 缺少对更极端教师数量（如仅1个全模态客户端）的鲁棒性实验。
  - 未讨论异步参与或动态客户端的场景。
- **算法假设**：
  - 要求预先定义语义组（类别或检索单元），在无强监督信号的开放域场景可能不适用。
  - 谱损失依赖于拉普拉斯特征值分解，组数G较大时计算复杂度O(G^3)可能成为瓶颈（虽论文声明实际客户端仅维护局部子集，但全局蓝图构建仍在服务器端）。
- **对比方法公平性**：FedTaste使用了冻结CLIP，而部分基线可能使用了不同的骨干网络（虽未明确说明），可能影响比较的绝对公平。
- **隐私风险**：虽然不依赖外部数据，但服务器聚集了教师的亲和矩阵和特征值，理论上可能泄露组级分布信息，需进一步差分隐私分析。
- **算力描述不足**：仅提及H100 GPU，未报告训练时长、GPU数量，难以评估资源投入。

（完）
