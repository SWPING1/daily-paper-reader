---
title: Multimodal Ambivalence and Hesitancy Recognition via Cross-Attention and Gated Fusion
title_zh: 通过交叉注意力与门控融合的多模态矛盾与犹豫识别
authors: "Oussama Berhili, Yassine Ouzar, Larbi Boubchir"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15779v1"
tags: ["query:mm"]
score: 8.0
evidence: 多模态矛盾/犹豫识别
tldr: "视频中的矛盾与犹豫识别需要多模态信息互补。本文提出融合文本、音频和视频特征的多模态框架，使用预训练编码器，并通过双向交叉注意力与门控多模态单元实现跨模态交互。验证集Macro F1达0.7394，较最佳单模态提升11%。该工作证明显式跨模态交互能捕获互补线索，并开源代码。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15779v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1089, \"height\": 1290, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15779v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1094, \"height\": 557, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15779v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 917, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15779v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 825, \"height\": 659, \"label\": \"Table\"}]"
motivation: 视频中的矛盾/犹豫识别挑战中，单一模态信息有限，需要多模态互补以提升鲁棒性。
method: 使用F2LLM、WavLM、VideoMAE提取文本、音频、视频特征，经双向交叉注意力融合后由门控多模态单元整合。
result: "验证集Macro F1达0.7394，相对最佳单模态提升11%，远超过零样本Video-LLaVA基线。"
conclusion: 显式跨模态交互有效提升多模态矛盾/犹豫识别性能，代码已开源。
---

## 摘要
我们提出了一个用于视频中矛盾/犹豫（A/H）识别的多模态框架，该框架为ECCV 2026的ABAW11挑战赛而开发。所提出的方法融合了从BAH数据集中提取的文本、声学和视觉模态，使用了三个预训练编码器：用于文本转录的F2LLM-v2-0.6B（1024维）、用于音频的WavLM-Large（1024维）和用于面部视频的VideoMAE V2（768维）。我们首先使用经典分类器（MLP、随机森林、GBDT）建立了全面的单模态基线，每个分类器均通过Optuna优化，并在测试集上使用仅文本特征获得了最佳单模态宏F1分数为0.6659——大幅优于零样本Video-LLaVA基线（宏F1: 0.2827）。在这些基线的基础上，我们提出了一种多模态融合架构，该架构结合了所有三种模态的双向交叉注意力以及门控多模态单元（GMU），并通过50次Optuna搜索选择了架构和优化超参数。该模型在验证集上达到了0.7394的宏F1分数，相对于最佳单模态基线相对提升11.0%，证实了显式的跨模态交互能够捕捉单一模态无法独立提供的互补线索。最终对官方未标注的私有测试集的预测使用该模型生成，并按照挑战协议提交。代码公开于https://github.com/yassineouzar/IUSD_AH/

## Abstract
We present a multimodal framework for Ambivalence/Hesitancy (A/H) recognition in video, developed for the ABAW11 challenge at ECCV 2026. The proposed approach fuses textual, acoustic, and visual modalities extracted from the BAH dataset using three pretrained encoders: F2LLM-v2-0.6B for transcripts (1024-d), WavLM-Large for audio (1024-d), and VideoMAE V2 for facial video (768-d). We first establish comprehensive unimodal baselines using classical classifiers (MLP, Random Forest, GBDT), each optimized via Optuna, and obtain a best unimodal Macro F1 of \textbf{0.6659} on the test set using text features alone -- substantially outperforming the zero-shot Video-LLaVA baseline (Macro F1: 0.2827). Building on these baselines, we propose a multimodal fusion architecture that combines bidirectional cross-attention across all three modalities with a Gated Multimodal Unit (GMU), with both architectural and optimization hyperparameters selected through a 50-trial Optuna search. This model achieves a Macro F1 of \textbf{0.7394} on the validation set, a relative improvement of 11.0\% over the best unimodal baseline, confirming that explicit cross-modal interaction captures complementary cues that no single modality provides in isolation. Final predictions on the official, unlabeled private test set are generated using this model and submitted according to the challenge protocol. Code is publicly available at https://github.com/yassineouzar/IUSD_AH/

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：矛盾/犹豫（Ambivalence/Hesitancy, A/H）是一种微妙的情感状态，表现为同时存在冲突的情绪或动机，导致个体在行动与拒绝之间摇摆。自动识别A/H对数字行为干预和心理健康监测具有直接应用价值。
- **背景**：ABAW11挑战赛提供了BAH数据集，包含1,427个视频的二分类标注（存在/不存在A/H）。单一模态（如仅文本、仅音频或仅视觉）难以捕捉这种复杂信号，因为矛盾可能跨模态出现（如积极词汇伴随消极语调），也可能在同一模态内微妙呈现。因此需要多模态融合，且需显式建模跨模态交互。
- **整体含义**：本文旨在构建一个端到端的多模态框架，通过显式的**双向交叉注意力**和**门控融合**机制，整合文本、音频、视频特征，提升A/H识别性能，并证明多模态互补性的重要性。

## 2. 论文提出的方法论

### 核心思想
- 使用三个独立预训练编码器提取各模态的**全序列token**（避免全局池化损失细粒度信息），再通过**双向交叉注意力**让每对模态相互查询与交互，最后用**门控多模态单元（GMU）**动态加权融合三个模态的池化向量，最终由MLP分类。

### 关键技术细节
1. **特征提取**：
   - **文本（F2LLM-v2-0.6B）**：解码器模型，输出变长序列 \(N_t \times 1024\)。
   - **音频（WavLM-Large）**：输出序列 \(N_a \times 1024\)。
   - **视频（VideoMAE V2）**：将视频切分为16帧的块，处理后拼接，输出 \(N_v \times 768\)。
2. **投影与位置编码**：将各模态特征线性投影到共享维度 \(d_{\text{model}}\)（Optuna搜索得到256），加正弦位置编码保留时序。
3. **跨模态注意力**：
   - 实现**六个方向**的交叉注意力模块（text←video, text←audio, video←text, video←audio, audio←text, audio←video），每个包含多个预归一化交叉注意力层+残差+前馈网络。
   - 使用key-padding mask屏蔽填充位置。
4. **池化**：每个模态的上下文token序列通过可配置策略（均值、最大值、注意力池化）聚合成单个向量，后再加层归一化。
5. **门控多模态融合（GMU）**：将三个池化向量拼接，通过一个网络生成每个模态的sigmoid门控值，将每个模态向量乘以其门控后求和，得到融合表示。GMU能动态抑制噪声模态。
6. **分类**：融合表示输入可配置MLP（深度、宽度、激活、dropout均通过Optuna搜索），输出一个logit，用二元交叉熵训练。

### 无公式说明
整个流程为：特征提取 → 投影+位置编码 → 双向交叉注意力 → 池化 → GMU融合 → MLP分类。超参数通过Optuna 50次搜索优化。

## 3. 实验设计

- **数据集**：BAH数据集（ABAW11挑战）。共1,427视频，按参与者划分为训练集778、验证集124、标注测试集525。另有一个未标注私有测试集152视频。二分类标签（0无A/H，1有A/H）。
- **基准**：
  - **零样本基线**：Video-LLaVA（仅视觉提示），Macro F1=0.2827。
  - **单模态基线**：对每个模态分别训练MLP、随机森林、GBDT，每个分类器Optuna 100次调优，在标注测试集上评估。最佳为文本+随机森林，Macro F1=0.6659。
- **对比方法**：
  - 本文多模态模型（交叉注意力+GMU）在验证集上评估，Macro F1=0.7394，与最佳单模态（0.6659）和零样本（0.2827）对比。
  - 另外提及ABAW10优胜系统BROTHER（Macro F1=0.7465），但明确说明不同届挑战无法直接对比。

## 4. 资源与算力

- **原文描述**：“所有实验在单个NVIDIA GPU服务器上运行”。**未明确GPU型号、数量、具体训练时长**。
- **估算**：特征提取一次完成并缓存；单模态分类器在CPU上运行；多模态模型训练约28轮（早停触发），但具体GPU算力信息缺失。

## 5. 实验数量与充分性

- **单模态实验**：3种模态 × 3种分类器 = 9组基线，每组Optuna 100次搜索，在独立测试集上评估，结果充分。
- **多模态实验**：1个最终架构，Optuna 50次搜索选择最佳配置，在验证集上评估。最终模型训练100轮（早停15轮）。
- **消融分析**：通过对比单模态与多模态结果间接体现跨模态交互贡献，但**未进行内部消融**（如去除交叉注意力、仅用拼接、仅用GMU、不同池化策略等对比）。
- **公平性**：与零样本基线比较合理；与先前优胜系统比较时谨慎声明不可直接对比。训练时使用训练+测试集（1303视频）最大化数据量，验证集独立，设计合理。
- **充分性评价**：实验基本充分覆盖核心对比，但缺少更细粒度的消融和多模态融合方式对比，可认为中等充分。

## 6. 论文的主要结论与发现

- 单模态中**文本**最具判别力（Macro F1=0.6659），远优于零样本（0.2827），说明领域内训练的必要性。
- 多模态融合模型（交叉注意力+GMU）在验证集上达到**Macro F1=0.7394**，相对最佳单模态提升**11.0%**，证明显式跨模态交互能够捕捉互补线索。
- 混淆矩阵分析表明单模态有不同误分类模式，多模态融合有助于纠正单一模态的偏差。
- 代码和管道已开源，利于复现和扩展。

## 7. 优点

- **保留细粒度时序信息**：特征提取时保留全序列token而非池化，为交叉注意力提供丰富上下文。
- **系统化基线构建**：对每个模态采用多种经典分类器并统一Optuna调优，提供可靠下限。
- **架构设计合理**：双向交叉注意力覆盖所有模态对，GMU动态抑制噪声，可解释性强。
- **超参数搜索统一**：使用Optuna同时对架构和优化参数搜索，避免手动调参偏倚。
- **公开可复现**：代码、特征提取流程模块化且开源，降低复现门槛。

## 8. 不足与局限

- **缺少内部消融实验**：未单独验证交叉注意力的贡献、GMU vs 简单拼接/平均的效率、不同池化策略影响等，使得架构设计决策的因果证据较弱。
- **验证集评估**：最终结果仅在验证集上报告（124样本），标注测试集用于训练（与验证集不重叠但样本量较大），未给出测试集上的最终分数，私有测试集无标签，因此泛化性能不够透明。
- **与BROTHER系统比较不直接**：虽然接近其Macro F1（0.7465），但不同届挑战可能数据集版本或划分不同，无法断定优劣。
- **过拟合风险**：训练Macro F1（0.9498）与验证（0.7394）差距较大，暗示可能过拟合，文中也指出需要进一步正则化或数据增强。
- **未探索更复杂的时序建模**：如Transformer编码器进一步建模跨模态时序，或使用多模态大语言模型微调。
- **仅二分类**：未考虑A/H的强度或程度，可能限制实际应用粒度过粗。
- **无数据增强**：对视频、音频的增强（如时域扰动、噪声注入）未使用，可能影响鲁棒性。

（完）
