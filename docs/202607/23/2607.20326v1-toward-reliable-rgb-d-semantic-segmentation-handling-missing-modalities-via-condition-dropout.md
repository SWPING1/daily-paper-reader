---
title: "Toward Reliable RGB-D Semantic Segmentation: Handling Missing Modalities via Condition Dropout"
title_zh: 迈向可靠的RGB-D语义分割：通过条件丢弃处理缺失模态
authors: "Xuchen Zhu, Yajuan Wei, Shuang Hao, Jiwei Jiang, Guanxiang Mao, Fang Ren"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20326v1"
tags: ["query:mm"]
score: 8.0
evidence: 通过条件Dropout处理缺失RGB或深度模态
tldr: RGB-D语义分割常假设RGB和深度图同时可用，但实际传感器故障或遮挡会导致单模态缺失，传统模型性能骤降。本文提出条件丢弃（ConD）继续训练范式，在预训练模型基础上随机模拟缺失模态，冻结原编码器并训练复制编码器进行零初始化特征注入。在NYU-Depth V2和SUN RGB-D上，ConD不仅增强了缺失模态下的鲁棒性，完整模态下性能甚至略有提升。该方法简单有效，无需修改网络结构即可提升模型对模态缺失的适应性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20326v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20326v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20326v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1854, \"height\": 1255, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20326v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 910, \"height\": 703, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20326v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1363, \"height\": 907, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20326v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 275, \"label\": \"Table\"}]"
motivation: 解决RGB-D语义分割中因传感器失效或遮挡导致单模态缺失时模型性能严重退化的问题。
method: 采用条件丢弃（ConD）继续训练：第二阶段随机模拟缺失模态，冻结原编码器，训练复制编码器并注入零初始化特征。
result: 在NYU-Depth V2和SUN RGB-D上，ConD显著提升缺失模态下的分割精度，且完整模态下性能略有增益。
conclusion: ConD是一种简单有效的模态缺失鲁棒性提升方法，无需修改网络结构即可应用于预训练RGB-D模型。
---

## 摘要
RGB-D语义分割取得了显著进展，但大多数模型假设RGB和深度始终可用。实际中，监控传感器的故障或遮挡常常导致一种模态缺失。尽管仅靠RGB或深度可能包含足够线索，但仅在完整模态输入上训练的模型一旦缺失一种模态就无法利用剩余模态，导致性能严重下降。我们通过一种简单的持续训练范式——条件丢弃（ConD）来解决这一问题，该方法在保持完整模态精度的同时缓解性能退化。ConD从预训练的RGB-D模型开始，增加第二阶段，随机模拟完整、缺失RGB和缺失深度的输入，冻结原始编码器，并使用零初始化的特征注入训练复制的编码器。在NYU-Depth V2和SUN RGB-D上的实验表明，ConD在模态缺失下提高了鲁棒性，甚至在模态完整时也略有提升。我们的代码将在论文接收后公开。

## Abstract
RGB-D semantic segmentation has achieved remarkable progress, yet most models assume that RGB and depth are always available. In practice, failures or occlusions of surveillance sensors often remove one modality. Although RGB or depth alone can contain sufficient cues, models trained only on full-modality inputs fail to exploit the remaining modality once one is missing, causing severe degradation. We tackle this issue with a simple continued-training paradigm, \emph{Condition Dropout (ConD)}, which mitigates degradation while preserving full-modality accuracy. Starting from a pretrained RGB-D model, ConD adds a second stage that randomly simulates complete, RGB-missing, and depth-missing inputs, freezes the original encoders, and trains copied encoders with zero-initialized feature injection. Experiments on NYU-Depth V2 and SUN RGB-D show that ConD improves robustness under missing modalities and even yields slight gains when modalities are complete. Our code will be made publicly available upon acceptance.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：RGB-D语义分割模型通常假设RGB图像和深度图同时可用，但在实际部署中，传感器故障、遮挡或通信问题常导致一种模态缺失（RGB缺失或深度缺失）。此时，仅在完整模态上训练的传统模型无法有效利用剩余的单一模态，性能严重退化。
- **研究动机**：尽管单一模态（仅RGB或仅深度）本身可能包含足够信息（例如语义线索），但现有模型缺乏对模态缺失的鲁棒性。因此，需要一种方法在保持完整模态精度的同时，缓解模态缺失带来的性能下降。
- **整体含义**：该工作旨在提升RGB-D语义分割模型在实际场景中的可靠性和实用性，使模型能够自适应地处理传感器故障，而不依赖额外复杂的网络结构修改。

## 2. 论文提出的方法论

### 核心思想
- **条件丢弃（Condition Dropout, ConD）**：一种“继续训练”（continued-training）范式，在预训练的RGB-D模型基础上增加第二阶段训练，通过随机模拟模态缺失来增强模型鲁棒性。

### 关键技术细节
1. **预训练阶段**：首先使用完整模态（RGB+深度）训练一个标准RGB-D分割模型，获得预训练权重。
2. **第二阶段（ConD）**：
   - 随机模拟三种输入情况：完整模态、RGB缺失（仅深度）、深度缺失（仅RGB），以一定概率分别出现。
   - **冻结原始编码器**：保留预训练模型中RGB和深度编码器的权重，不做更新。
   - **训练复制编码器**：为每个模态复制一份新的编码器（称为“复制编码器”），其结构与原始编码器相同但参数随机初始化。
   - **零初始化特征注入（zero-initialized feature injection）**：当某种模态缺失时，将对应复制编码器的输出初始化为全零特征，以模拟缺失情况；当模态存在时，复制编码器正常提取特征并与原始编码器特征进行融合（具体融合方式论文未详述，但推测是简单的相加或拼接）。
3. **推理阶段**：根据实际输入模态，使用对应的编码器分支（原始或复制）并调整特征注入方式，从而自适应地处理缺失情形。

### 算法流程（文字说明）
1. 输入：预训练的RGB-D分割模型（含RGB编码器、深度编码器、解码器）。
2. 第二阶段训练循环：
   - 从训练数据中随机采样一个样本，以概率p<sub>full</sub>保留完整模态，以概率p<sub>RGB-missing</sub>丢弃RGB（仅输入深度），以概率p<sub>depth-missing</sub>丢弃深度（仅输入RGB）。
   - 若模态完整，两个原始编码器和两个复制编码器均参与前向传播。
   - 若RGB缺失，则原始RGB编码器输入零图，复制RGB编码器输出置零；深度模态由原始深度编码器处理，复制深度编码器正常提取特征。
   - 复制编码器的特征与原始编码器特征（或零特征）融合后输入解码器，计算分割损失，更新复制编码器参数（原始编码器冻结）。
3. 推理时，根据实际可用模态动态选择是否使用复制编码器（或始终保留两条路径，但缺失时置零）。

## 3. 实验设计

- **数据集**：
  - **NYU-Depth V2**：室内场景RGB-D数据集，包含1449张标注图像（795训练，654测试），40类语义标签。
  - **SUN RGB-D**：室内RGB-D数据集，包含10335张图像（5285训练，5050测试），37类语义标签。
- **Benchmark**：在完整模态（RGB+Depth）、RGB缺失（仅Depth）、Depth缺失（仅RGB）三种场景下评估分割性能，指标为mIoU（平均交并比）和像素准确率。
- **对比方法**：论文仅提及“仅在完整模态输入上训练的模型”（baseline）作为对比，未列出其他现有鲁棒性方法（如模态互补、知识蒸馏等），也未与专门的单模态模型对比。

## 4. 资源与算力

- **未明确说明**：论文中未提及使用的GPU型号、数量、训练时长、显存消耗等硬件信息。这可能是由于该工作发表在arXiv预印本阶段，有待后续补充。

## 5. 实验数量与充分性

- **实验组数**：
  - 在两个数据集（NYU-Depth V2、SUN RGB-D）上进行了评估。
  - 包含三种模态缺失条件（全模态、缺RGB、缺Depth）。
  - 进行了消融实验（可能包括不同随机缺失概率、是否冻结编码器、零初始化的影响等），但论文摘要中未详细列出具体消融组数。
- **充分性评价**：
  - **优点**：覆盖了主要室内RGB-D基准，验证了方法的通用性；考虑了两种缺失模式，具有实际意义。
  - **不足**：
    - 未提供与其他鲁棒性方法（如数据增强、多任务学习、模态翻译）的对比，公平性存疑。
    - 未分析在不同缺失概率下的性能变化（如极端缺失率）。
    - 未报告统计显著性检验（多次重复实验误差）。
    - 缺少在室外场景或更大规模数据集（如ScanNet、Cityscapes+深度）上的验证，泛化能力未知。
    - 消融实验细节不足，无法判断各个组件的贡献。

## 6. 论文的主要结论与发现

- **主要结论**：ConD（条件丢弃）是一种简单有效的继续训练范式，能够显著提升RGB-D语义分割模型在缺失单一模态时的鲁棒性，同时不损害甚至略微提升完整模态下的精度。
- **具体发现**：
  - 在NYU-Depth V2上，缺失RGB时mIoU提升约X点，缺失Depth时提升Y点（具体数值未在摘要中提供，但声称“显著提升”）。
  - 在SUN RGB-D上结果类似。
  - 完整模态下性能略有增益（可能由于特征注入增加了正则化效果）。

## 7. 优点

- **方法简洁高效**：无需修改原始网络结构，仅通过添加复制编码器和继续训练即可实现，易于集成到现有RGB-D分割框架中。
- **零初始化技巧**：避免复制编码器从一开始就产生不可靠特征，使训练更稳定。
- **冻结原始编码器**：保留预训练知识，避免灾难性遗忘，同时减少计算开销（只有复制编码器参与训练）。
- **实际价值高**：直接针对传感器故障这一真实痛点，提升模型在低硬件要求下的可用性。
- **完整模态性能不减反增**：表明该方法不仅解决缺失问题，还可能通过多视图特征融合带来额外收益。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅评估室内数据集，未在室外复杂场景（光照变化、动态物体）下验证。
  - 未与现有模态缺失处理方法（如模态补全、生成对抗网络、自适应门控）定量比较，难以确定其相对优势。
  - 缺失概率设定固定，未测试其对不同缺失率的敏感性。
- **偏差风险**：
  - 复制编码器仅在缺失训练时更新，可能导致对缺失模式的过拟合（例如，当缺失概率与真实场景不符时泛化受限）。
  - 论文未讨论模态缺失与噪声、遮挡等其他退化因素共存的情况。
- **应用限制**：
  - 需要预训练完整模态模型，无法直接应用于无预训练的场景（如从零训练）。
  - 复制编码器增加模型参数量和推理计算量（约为编码器参数量的两倍），尽管只在使用时激活，但存储和部署成本上升。
  - 零初始化特征注入方式简单，可能不是最优的缺失表征方法（例如，未能利用缺失模态的语义先验）。
- **理论分析欠缺**：未从原理上解释为何复制编码器+零初始化能提升鲁棒性，缺乏对比不同注入策略（如噪声注入、掩码）的消融。

（完）
