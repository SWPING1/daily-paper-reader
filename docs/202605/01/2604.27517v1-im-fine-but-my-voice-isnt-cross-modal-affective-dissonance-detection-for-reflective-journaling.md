---
title: "I'm Fine, But My Voice Isn't: Cross-Modal Affective Dissonance Detection for Reflective Journaling"
authors: Sumin Lee
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27517v1"
tags: ["query:missing-modality"]
score: 8.5
evidence: 跨模态情感不一致检测与情绪调节
tldr: 本研究针对数字日志中用户常掩饰真实情感的现象，提出了跨模态情感失调检测（CADD）任务，旨在识别文本与语音间的情感不一致性。研究构建了CADD-Journal数据集，并开发了采用非对称跨模态注意力的DACM模型。实验表明该模型能有效识别掩饰和应对等情感状态，并揭示了合成语音与真实语音间的领域差距，最后通过iOS应用ReflectJournal实现了该框架的落地。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27517v1/fig-001.webp\", \"caption\": \"Figure 1: DACM architecture. Frozen text (blue, XLMRoBERTa) and audio (green, WavLM) encoders feed Asymmetric Cross-Modal Attention (pink), in which each modality’s pooled vector queries the opposing modality’s full sequence; this design resolves the scalar-softmax gradient degeneracy of pooled-to-pooled attention (§3.3). The Dissonance Interaction Module (orange) computes gated difference, Hadamard product, cosine similarity, and a stop-gradient unimodal-disagreement vector, producing the CADD class label and the interpretable mismatch score 𝑆 ∈ [0, 1] (purple).\", \"page\": 2, \"index\": 1, \"width\": 438, \"height\": 702}]"
motivation: 旨在解决数字日志中用户倾向于修饰文本内容，导致文本叙述与真实语音情感不一致的“真实性鸿沟”问题。
method: 定义了包含掩饰、应对和一致三类的CADD任务，并设计了基于非对称跨模态注意力的双编码器模型DACM。
result: DACM模型在CADD-Journal数据集上取得了0.711的宏F1值，消融实验证明非对称注意力机制对性能提升起到了主导作用。
conclusion: 研究量化了合成语音与自然语音间的领域差距，并通过ReflectJournal应用为未来真实场景下的情感失调数据采集提供了平台。
---

## Abstract
Digital journaling creates an authenticity gap: users consciously translate raw emotions into text, often sanitizing narratives even in private writing. We formalize this as Cross-Modal Affective Dissonance Detection (CADD), a directional three-way classification distinguishing Masking (positive text, negative acoustics), Coping (negative text, positive acoustics), and Congruent utterances, grounded in Gross's process model of emotion regulation. We present three further contributions: (i) CADD-Journal, a 1,800-sample TTS dataset with a shared-sentence-pool design that provably isolates acoustic signal from textual content; (ii) DACM, a dual-encoder model with asymmetric cross-modal attention that re-solves a gradient degeneracy in pooled fusion, achieving macro-F1 0.711 - with a four-step ablation demonstrating that asymmetric attention is the dominant driver (+ 0.242) while the DIM is effective only on cross-modal features (+0.033); and (iii) a domain gap quantification: zero-shot evaluation across three naturalistic corpora reveals a substantial gap between TTS-trained models and real speech, and we identify two concrete requirements for future in-the-wild corpus construction. ReflectJournal, a proof-of-concept iOS application, operationalizes the framework and provides a deployment platform for naturalistic data collection.

---

## 论文详细总结（自动生成）

这是一份关于论文《I'm Fine, But My Voice Isn't: Cross-Modal Affective Dissonance Detection for Reflective Journaling》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：数字日志（Digital Journaling）中存在“真实性鸿沟”。用户在记录私密日志时，往往会下意识地修饰文字内容以维持正面形象（印象管理），导致文字叙述与真实的心理状态脱节。
*   **核心问题**：现有的多模态情感分析模型通常假设模态间是“对齐”的，在面对文字与语音情感冲突时，往往会出现“文本主导（Lexical Dominance）”现象，即忽略语音中的真实情感。
*   **整体含义**：论文正式提出了**跨模态情感失调检测（CADD）**任务，旨在识别文本与语音之间的不一致性，并将其分为：**掩饰（Masking）**（文字积极但语音消极）、**应对（Coping）**（文字消极但语音积极）和**一致（Congruent）**三类。

### 2. 方法论
论文提出了 **DACM（Dissonance-Aware Cross-Modal model）** 架构，核心思想是捕捉并放大模态间的冲突信号。
*   **双编码器结构**：使用冻结的 **XLM-RoBERTa-base** 处理文本，**WavLM-base-plus** 处理音频，并通过加权层池化（WLP）提取特征。
*   **非对称跨模态注意力（Asymmetric Cross-Modal Attention）**：这是关键技术创新。作者发现传统的“池化对池化”注意力会产生梯度退化（Softmax 变为标量 1）。DACM 采用一侧的池化向量作为 Query，去查询另一侧的完整序列（Key/Value），从而获得语义上下文相关的跨模态特征。
*   **失调交互模块（DIM）**：计算四个特征：
    1.  **g（门控差异）**：捕捉模态间的向量差。
    2.  **m（哈达玛积）**：捕捉模态间的共同激活。
    3.  **s（余弦相似度）**：衡量整体一致性。
    4.  **d（单模态逻辑差）**：使用 `stop-gradient` 确保捕捉的是原始预测冲突。
*   **训练目标**：结合了交叉熵损失、双极余弦边际损失（拉近一致对、推开失调对）、辅助分类损失和一致性损失。

### 3. 实验设计
*   **数据集**：
    *   **CADD-Journal**：自建数据集，包含 1,800 个样本。采用**共享句子池设计**（100 个种子句子在所有情感条件下重复使用），确保模型无法仅靠文本内容分类，必须依赖语音。
    *   **自然语料库**：CMU-MOSEI、IEMOCAP、CH-SIMS（用于跨领域评估）。
*   **Benchmark 与对比方法**：
    *   **TextOnly / AudioOnly**：单模态基准。
    *   **DACM-Base**：使用简单池化融合（拼接/相减）的基准模型。
    *   **消融变体**：DACM-noAttn（无非对称注意力）、DACM-noDIM（无失调交互模块）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 AdamW 优化器，学习率 $5 \times 10^{-4}$，Batch size 为 16，采用了早停机制（Patience=7）和梯度裁剪。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在自建数据集上进行了完整的消融实验（4 个步骤的归因阶梯）。
    *   进行了**留一说话人（LOVO）**评估，验证了模型对不同声音的泛化力。
    *   在 3 个外部自然语料库上进行了**零样本（Zero-shot）**迁移实验。
*   **充分性与客观性**：实验设计较为严密，特别是通过“共享句子池”排除了文本偏见，确保了多模态推理的必要性。所有结果均基于 3 个随机种子的均值和标准差，具有一定的统计说服力，但种子数量（3个）相对较少。

### 6. 主要结论与发现
*   **注意力机制是核心**：非对称跨模态注意力贡献了最大的性能增益（F1 值提升 +0.242），解决了简单融合导致的音频信号被抑制问题。
*   **性能表现**：DACM 在 CADD-Journal 上达到 **0.711** 的宏 F1 值。
*   **巨大的领域差距**：这是最重要的发现——在合成 TTS 数据上训练的模型，在真实自然语音语料库上表现接近随机（F1 约 0.33-0.51）。这量化了合成情感与真实人类情感调节之间的巨大鸿沟。

### 7. 优点
*   **任务建模新颖**：将情感不一致性从“噪声”转变为“信号”，具有很强的心理学基础（基于 Gross 的情绪调节模型）。
*   **架构针对性强**：专门设计了非对称注意力机制来解决多模态融合中的梯度退化和文本主导问题。
*   **闭环设计**：不仅有算法研究，还开发了 iOS 应用原型 **ReflectJournal**，为未来收集真实世界的失调数据提供了平台。

### 8. 不足与局限
*   **TTS 数据的局限性**：CADD-Journal 使用合成语音，虽然控制变量严格，但无法完全模拟人类真实的压抑或应对情感时的复杂声学特征。
*   **泛化能力弱**：跨领域实验显示模型目前难以直接应用于真实场景，存在严重的领域偏移。
*   **标注依赖**：外部语料库评估使用的是“伪标签”（Silver labels），可能引入额外的噪声。
*   **用户研究缺失**：ReflectJournal 应用尚未进行用户研究，无法确定这种“情感失调提醒”是否真的对用户心理健康有益，或是否会引起焦虑。

（完）
