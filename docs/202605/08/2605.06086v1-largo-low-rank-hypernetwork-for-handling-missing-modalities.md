---
title: "LARGO: Low-Rank Hypernetwork for Handling Missing Modalities"
title_zh: LARGO：用于处理缺失模态的低秩超网络
authors: "Niels Vyncke, Pooya Ashtari, Aleksandra Pižurica"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06086v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 用于处理缺失模态的低秩超网络
tldr: 针对多模态图像分析中模态缺失的挑战，本文提出了一种名为 LARGO 的低秩超网络。不同于传统在特征空间构建鲁棒表示的方法，LARGO 在权重空间操作，利用 CP 张量分解将所有可能的模态组合模型压缩进单一网络。在 BraTS 2018 和 ISLES 2022 数据集上的实验表明，该方法在多数配置下优于现有 SOTA 模型，具有极强的泛化能力和性能优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06086v1/fig-001.webp\", \"caption\": \"Figure 2: Illustration of LARGO. The dedicated models Fθ(m) for each missing-modality case m = 1, ...,M are compressed into a single hypernetwork Hφ, by representing the kernel weights W of their convolutional layers using CPD decomposition, which we refer to as low-rank convolutional layers (LRConv). The stem and head layers remain uncompressed. During inference, the hypernetwork reconstructs the convolutional layer weights of the target dedicated model from the factor matrices A,B,C and D.\", \"page\": 2, \"index\": 1, \"width\": 782, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2605.06086v1/fig-002.webp\", \"caption\": \"Table 1: Comparison of Dice scores (%) ↑ on the BraTS 2018 dataset using 5-fold cross-validation for baseline and proposed models. Symbols • and ◦ denote the presence and absence of a modality, respectively. The best and second-best scores per tumor region are highlighted in bold and underlined, respectively.\", \"page\": 7, \"index\": 2, \"width\": 830, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2605.06086v1/fig-003.webp\", \"caption\": \"Figure 6: Configuration of the fusion architecture used for the avMNIST dataset. The image and audio encoders independently extract features. Features from available modalities are concatenated and projected on a 64-dimensional feature vector. Three fusion blocks with residual connections process the combined features, followed by layer normalization and a classifier producing digit predictions.\", \"page\": 14, \"index\": 3, \"width\": 822, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2605.06086v1/fig-004.webp\", \"caption\": \"Figure 3: Qualitative comparison on BraTS 2018. Ground truth (GT) and predictions from different methods under single-modality and all-modality settings are shown. Yellow indicates enhancing tumor, red indicates the necrotic and non-enhancing tumor regions, and green indicates edema. Dice scores are reported for each prediction.\", \"page\": 3, \"index\": 4, \"width\": 682, \"height\": 609}, {\"url\": \"assets/figures/arxiv/2605.06086v1/fig-005.webp\", \"caption\": \"Figure 5: Configuration of the nnU-Net architecture used for the BraTS 2018 dataset. The encoder path (left) progressively downsamples the input through conv blocks with increasing channels. Skip connections transfer encoder features to the decoder path (right), which upsamples back to the original resolution. Numbers indicate channels×spatial resolution.\", \"page\": 13, \"index\": 5, \"width\": 388, \"height\": 541}, {\"url\": \"assets/figures/arxiv/2605.06086v1/fig-006.webp\", \"caption\": \"Figure 4: Qualitative comparison on the ISLES 2022 dataset. Predictions from different methods under single-modality and all-modality settings are shown. Green denotes true positives, red false positives, and blue false negatives. Dice scores (%) are reported for each example.\", \"page\": 4, \"index\": 6, \"width\": 756, \"height\": 574}]"
motivation: 现有的多模态缺失处理方法通常依赖复杂的特征空间架构，难以在不同数据集间灵活迁移或调整。
method: 提出 LARGO 超网络，通过 CP 张量分解对卷积权重建模，将所有可能的模态组合模型压缩为单一网络。
result: 在 BraTS 2018 和 ISLES 2022 的 52 种配置中，LARGO 在 47 种情况下排名第一，显著优于 mmFormer 等 SOTA 基准。
conclusion: LARGO 通过权重空间建模有效解决了模态缺失问题，且其应用潜力可从医疗影像扩展至非医疗的异构多模态任务。
---

## 摘要
处理缺失模态是多模态图像分析中的一个重要挑战，通常依赖于复杂的架构，而这些架构在不进行架构修改或超参数调整的情况下，往往难以迁移到不同的数据集。虽然大多数现有方法通过在特征空间中设计对缺失输入具有鲁棒性的表示来解决这一问题，但我们转而在权重空间中进行操作。我们提出了 LARGO，这是一种超网络，通过使用正则多项（CP）张量分解对卷积权重进行建模，将 $2^N-1$ 个专门的缺失模态模型压缩到单个网络中。在 BraTS 2018（4 种模态，15 种场景）和 ISLES 2022（3 种模态，7 种场景）上的广泛实验验证表明，我们的方法在 52 种配置中的 47 种里排名第一，与最先进的基准模型（mmFormer、M³AE、ShaSpec、SimMLM）相比，平均 Dice 分数分别提升了 +0.68% 和 +2.53%。在 avMNIST 上的概念验证实验表明，LARGO 可能会从医学成像扩展到异构的非医学模态。

## Abstract
Addressing missing modalities is an important challenge in multimodal image analysis and often relies on complex architectures that do not transfer easily to different datasets without architectural modifications or hyperparameter tuning. While most existing methods tackle this problem in feature space by engineering representations that are robust to missing inputs, we instead operate in weight space. We propose LARGO, a hypernetwork that compresses the $2^N-1$ dedicated missing-modality models into a single network by modelling the convolutional weights using the Canonical Polyadic (CP) tensor decomposition. Extensive experimental validation on BraTS 2018 (4 modalities, 15 scenarios) and ISLES 2022 (3 modalities, 7 scenarios) shows that our method ranks first in 47 out of 52 configurations, achieving average Dice improvements of +0.68$\%$ and +2.53$\%$ over state-of-the-art baselines (mmFormer, M$^{3}$AE, ShaSpec, SimMLM). A proof-of-concept experiment on avMNIST suggests that LARGO may extend beyond medical imaging to heterogeneous non-medical modalities.

---

## 论文详细总结（自动生成）

## 论文总结：LARGO - 用于处理缺失模态的低秩超网络

### 1. 核心问题与研究动机
*   **核心问题**：在多模态学习（尤其是医学影像）中，由于设备限制或临床协议，经常会出现某些模态数据缺失的情况。
*   **研究动机**：
    *   **现有方法局限**：目前主流方法多在“特征空间”操作（如特征融合、生成缺失模态或知识蒸馏），往往需要复杂的架构调整或多阶段训练。
    *   **扩展性矛盾**：为每种模态组合训练独立模型（共 $2^N-1$ 个）虽然性能最强，但参数量随模态数 $N$ 呈指数级增长，无法实际部署。
    *   **LARGO 的思路**：观察到不同模态组合的模型之间存在高度的参数冗余，提出在“权重空间”通过张量分解将所有可能的模型压缩进一个单一的超网络中。

### 2. 方法论
*   **核心思想**：将所有 $2^N-1$ 个专用模型的卷积核权重堆叠成一个高阶张量，并利用**正则多项分解（Canonical Polyadic Decomposition, CPD）**进行低秩重参数化。
*   **关键技术细节**：
    *   **权重张量构建**：将第 $l$ 层的权重表示为 $\mathcal{W} \in \mathbb{R}^{M \times C_{in} \times C_{out} \times K}$，其中 $M$ 是模型索引维度（对应不同的模态组合），$C$ 是通道，$K$ 是卷积核空间维度。
    *   **CP 分解**：将 $\mathcal{W}$ 分解为四个因子矩阵 $A, B, C, D$。其中 $A$ 矩阵的每一行对应一个特定模态组合的权重组合系数。
    *   **动态重建**：在推理时，根据输入的模态组合索引 $m$，从因子矩阵中实时收缩（Contract）生成该组合专用的卷积核权重 $W^{(m)}$。
    *   **训练流程**：采用随机模态丢弃（Modality Dropout）策略，并结合“全模态引导”损失函数，使超网络在训练过程中同时学习处理部分模态和完整模态的能力。

### 3. 实验设计
*   **数据集**：
    1.  **BraTS 2018**：脑肿瘤分割（4 模态：FLAIR, T1, T1c, T2），包含 15 种缺失场景。
    2.  **ISLES 2022**：缺血性中风病灶分割（3 模态：DWI, ADC, FLAIR），包含 7 种缺失场景。
    3.  **avMNIST**：视听数字分类（图像 + 音频），用于验证非医学、异构模态的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了四种最先进的（SOTA）缺失模态处理模型：**mmFormer**、**M³AE**、**ShaSpec** 和 **SimMLM**。
    *   分割任务以 **nnU-Net** 作为基础骨干架构。

### 4. 资源与算力
*   **算力平台**：使用了比利时 VSC（Flemish Supercomputer Center）的 Stevin 超算基础设施。
*   **训练细节**：BraTS 2018 训练了 750 个 epoch，ISLES 2022 训练了 500 个 epoch。
*   **明确性说明**：论文**未明确列出**具体的 GPU 型号（如 A100/V100）及具体使用的 GPU 数量。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。在两个医学数据集上测试了共 52 种模态组合场景（BraTS 15x3 区域 + ISLES 7 场景）。
*   **消融实验**：
    *   对比了 **CP 分解**与 **Tucker 分解**的性能差异（CP 表现更优）。
    *   研究了**秩（Rank）**的大小对模型性能与压缩率的权衡。
    *   对比了 LARGO 与“独立专用模型”的性能，证明了低秩约束甚至能起到正则化作用提升效果。
*   **客观性**：所有基准模型均在相同的预处理、后处理和数据分割线下从头训练，确保了比较的公平性。

### 6. 主要结论与发现
*   **性能卓越**：LARGO 在 52 种配置中的 47 种里排名第一。在 BraTS 和 ISLES 上分别比 SOTA 平均提升了 **0.68%** 和 **2.53%** 的 Dice 分数。
*   **参数高效**：LARGO 仅需约 22.6M 参数（相当于单个模型的量级），就能实现原本需要 $2^N-1$ 个模型才能达到的专用化性能。
*   **跨领域潜力**：在 avMNIST 上的成功验证了该方法处理异构数据（图像+音频）的有效性。

### 7. 优点
*   **权重空间创新**：避开了复杂的特征对齐问题，通过参数共享直接建模模型间的相关性。
*   **极高的参数效率**：在不增加存储负担的前提下，为每种模态组合提供了“定制化”的权重。
*   **无需辅助模块**：不需要生成器、判别器或复杂的教师网络，训练逻辑简单直接。

### 8. 不足与局限
*   **计算开销**：虽然参数量没变，但动态重建权重增加了推理时的计算量（GFLOPs 增加约 24.6%）和训练时的显存占用（增加约 25.1%）。
*   **架构限制**：目前主要针对卷积层（CNN）设计，虽然文中提到未来会扩展到 Transformer，但目前尚未验证在注意力机制上的表现。
*   **模态数量上限**：虽然解决了权重爆炸，但未压缩的 Stem（输入层）和 Head（输出层）仍随 $2^N-1$ 增长，当模态数极多（如 $N > 10$）时仍会面临挑战。

（完）
