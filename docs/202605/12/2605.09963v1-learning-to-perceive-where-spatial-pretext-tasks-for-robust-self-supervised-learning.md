---
title: "Learning to Perceive \"Where\": Spatial Pretext Tasks for Robust Self-Supervised Learning"
title_zh: 学习感知“何处”：用于鲁棒自监督学习的空间代理任务
authors: "Yang Shen, Yusen Cai, Weronika Hryniewska-Guzik, Qing Lin, Mengmi Zhang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09963v1"
tags: ["query:missing-modality"]
score: 6.0
evidence: 用于空间结构的解耦局部视图和解耦插件
tldr: 本研究针对现有自监督学习（SSL）忽视空间结构的问题，提出了空间预测（SP）预训练任务。SP通过预测同一图像中局部视图间的相对位置和比例，建模部分与整体的几何关系。作为可插拔插件，SP能显著提升模型在图像识别、分割、深度估计及分布外鲁棒性方面的表现，为SSL引入了有效的空间归纳偏置。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09963v1/fig-001.webp\", \"caption\": \"Figure 1: Problem setting and task overview for visual representation learning from partial observations. The left panel illustrates the problem setting: two cropped and resized views (orange and blue) are sampled from the same image, and the SSL model predicts the relative position and scale of the orange patch with respect to the blue reference. The right panel summarizes the evaluation tasks for benchmarking SSL models. (a) Image recognition includes in-domain classification on C100 [1] and IN-1K [2], out-of-distribution robustness on IN-C [3], IN-R [4], Sketch [5], and Occlusion, and cross-dataset transfer learning on Flowers [6], C100 [1], DTD [7], and Food [8]. (b) Dense prediction tasks include semantic segmentation on PASCAL VOC [9] and depth estimation on NYU [10]. (c) Spatial prediction evaluates the ability of SSL models to estimate relative position and scale between two local views. (d) Jigsaw understanding evaluates patch reordering, reconstruction, and subsequent recognition. (c) and (d) correspond to our newly proposed benchmarks and tasks on IN-1K [2].\", \"page\": 2, \"index\": 1, \"width\": 821, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2605.09963v1/fig-002.webp\", \"caption\": \"Table 1: Performance comparison across all downstream tasks except spatial reasoning. Each column corresponds to a task, together with its dataset and evaluation metric. Rows are grouped in pairs for SSL models with and without SP, where each pair shares the same backbone. All reported Acc values denote Top-1 classification accuracy (%). For IN-C, VOC, and NYU, we report mCE↓, mIoU↑, and RMSE (scaled by ×102)↓, respectively. See Sec. 4 for experimental details. Each experiment is repeated three times, and we report the mean performance with standard deviations in (± std). Best results are shown in bold.\", \"page\": 7, \"index\": 2, \"width\": 825, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2605.09963v1/fig-003.webp\", \"caption\": \"Figure 3: Example visualizations of learned representations by SSL models with and without SP. (a) Qualitative comparison of spatial attention maps. Following [31], we visualize attention by computing the attention weights between the classification token z and all patch tokens Z, reshaping them into a 2D spatial map, and upsampling to the input image size. Each row corresponds to an input image, and columns show attention maps overlaid on the images for MAE and DINO, with and without SP. Heatmaps are normalized to [0,1]; see the colorbar for scale. (b) Qualitative comparison of spatial reasoning results. Predicted relative positions and scales between two local views are visualized on the original image. Each row corresponds to an input image, and columns show results for MAE and DINO, with and without SP. Blue boxes denote reference views, red boxes denote predicted positions and scales, and green dashed boxes denote ground truth; arrows indicate predicted (red) and ground-truth (green) displacement vectors. See Fig. S3 for examples on MoCo v3.\", \"page\": 8, \"index\": 3, \"width\": 820, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.09963v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of Spatial Prediction (SP). Given a reference view Ir and a target view It sampled from the same image, both views are encoded by a shared Vision Transformer (ViT) [50]. The class token ([CLS]) of the reference view serves as the query (Q), while patch tokens from both reference and target views act as keys (K) and values (V ) to compute a cross-attention-like interaction (⊗), producing reference and target features. This operation is parameter-free, as it directly uses token embeddings without additional projections for Q, K, and V . The resulting features Zr and Zt are concatenated and fed into a two-layer MLP to regress the relative position and scale, supervised using ℓ2 loss against ground-truth targets. As illustrated in the right panels, the ground-truth spatial relationship is defined by the relative offset (∆x,∆y) from the reference view to the target view, along with the relative scale normalized by the reference view dimensions (Hr,Wr), where (Hr,Wr) and (Ht,Wt) denote the height and width of the reference and target views, respectively.\", \"page\": 4, \"index\": 4, \"width\": 829, \"height\": 266}]"
motivation: 现有的自监督学习方法主要关注类别不变性，往往忽略了物体内部各部分之间的空间结构和几何关系。
method: 提出一种名为空间预测（SP）的回归任务，通过预测图像内成对局部视图的相对坐标和缩放比例来学习空间依赖。
result: 该方法在图像分类、语义分割、深度估计等多个任务上取得一致提升，并显著增强了模型的分布外鲁棒性。
conclusion: 显式建模空间信息能为自监督学习提供强大的归纳偏置，使模型获得更具结构化和泛化能力的表征。
---

## 摘要
现有的自监督学习（SSL）方法主要学习对象不变性表示，但往往忽略了对象各部分之间的空间结构和关系。为了解决这一局限性，我们引入了空间预测（Spatial Prediction, SP），这是一种具有空间感知能力的代理回归任务，旨在预测来自同一图像的一对解耦局部视图之间的相对位置和比例。通过在连续几何空间中建模部分与部分之间的关系，SP 促使表示捕捉超越不变类别语义的细粒度空间依赖关系，从而学习视觉场景的组合结构。SP 作为一个解耦插件实现，可以无缝集成到各种 SSL 框架中。广泛的实验表明，该方法在图像识别、细粒度分类、语义分割和深度估计方面均有一致的提升，并在对象识别的分布外（out-of-distribution）鲁棒性方面取得了显著收益。为了评估空间推理能力，我们引入了：（1）图像块对的位置和比例预测任务；（2）要求在重建后进行图像块重排序和识别的拼图理解任务。在这些任务上的强劲表现表明了空间结构和几何感知能力的提升。总的来说，显式建模空间信息为 SSL 提供了一种有效的归纳偏置，从而实现了更具结构化的表示和更好的泛化能力。代码和模型将公开发布。

## Abstract
Existing self-supervised learning (SSL) methods primarily learn object-invariant representations but often neglect the spatial structure and relationships among object parts. To address this limitation, we introduce Spatial Prediction (SP), a spatially aware pretext regression task that predicts the relative position and scale between a pair of disentangled local views from the same image. By modeling part-to-part relationships in a continuous geometric space, SP encourages representations to capture fine-grained spatial dependencies beyond invariant categorical semantics, thereby learning the compositional structure of visual scenes. SP is implemented as a decoupled plug-in and can be seamlessly integrated into diverse SSL frameworks. Extensive experiments show consistent improvements across image recognition, fine-grained classification, semantic segmentation, and depth estimation, as well as substantial gains in out-of-distribution robustness for object recognition. To evaluate spatial reasoning, we introduce (1) a position and scale prediction task on image patch pairs and (2) a jigsaw understanding task requiring patch reordering and recognition after reconstruction. Strong performance on these tasks indicates improved spatial structure and geometric awareness. Overall, explicitly modeling spatial information provides an effective inductive bias for SSL, leading to more structured representations and better generalization. Code and models will be released.