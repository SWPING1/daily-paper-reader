---
title: Unified Multimodal Visual Tracking with Dual Mixture-of-Experts
title_zh: 基于双混合专家机制的统一多模态视觉跟踪
authors: "Lingyi Hong, Jinglun Li, Xinyu Zhou, Kaixun Jiang, Pinxue Guo, Zhaoyu Chen, Runze Li, Xingdong Sheng, Wenqiang Zhang"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03716v1"
tags: ["query:missing-modality"]
score: 8.5
evidence: 解耦跨模态依赖并处理任何输入模态进行跟踪
tldr: OneTrackerV2是一个统一的多模态视觉跟踪框架，旨在解决现有方法在处理不同模态（如RGB和RGB+X）时效率和扩展性不足的问题。该框架通过Meta Merger将多模态信息嵌入统一空间，并引入双混合专家网络（DMoE）来解耦跨模态依赖并建模时空关系。实验表明，OneTrackerV2在12个基准测试中达到SOTA性能，且在模型压缩和模态缺失情况下表现出极强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03716v1/fig-001.webp\", \"caption\": \"Table 2. Comparison with state-of-the-art trackers. OneTrackerV2 outperforms existing methods across 5 tasks and 12 benchmarks with a single training process, unified architecture, and shared parameters. We also compare whether each method supports multimodal inputs (MultiModal), employs a unified parameter (Unified Param), and is trained with a single step (Onestep Training).\", \"page\": 6, \"index\": 1, \"width\": 1012, \"height\": 735}, {\"url\": \"assets/figures/arxiv/2605.03716v1/fig-002.webp\", \"caption\": \"Table 3. Comparison on missing-modality benchmarks. We evaluate OneTrackerV2 and existing RGB+X trackers under scenarios where certain modalities are absent. Thanks to the proposed Meta Merger, OneTrackerV2 consistently outperforms prior methods, demonstrating superior robustness to modality missing in multimodal tracking.\", \"page\": 7, \"index\": 2, \"width\": 1012, \"height\": 238}]"
motivation: 现有的多模态跟踪方法通常需要为不同模态训练独立模型，导致效率低下且难以扩展。
method: 提出Meta Merger进行统一空间嵌入，并利用双混合专家网络（T-MoE和M-MoE）分别处理时空关系和多模态知识。
result: 在5类跟踪任务和12个基准测试中均取得了最先进的性能，且推理效率高。
conclusion: OneTrackerV2通过统一架构和端到端训练，实现了高效、鲁棒且可扩展的多模态视觉跟踪。
---

## 摘要
多模态视觉目标跟踪根据输入模态可分为多种任务（例如 RGB 和 RGB+X 跟踪）。现有方法通常为每种模态训练单独的模型，或依赖预训练模型来适应新模态，这限制了效率、可扩展性和可用性。因此，我们推出了 OneTrackerV2，这是一个统一的多模态跟踪框架，支持对任何模态进行端到端训练。我们提出了 Meta Merger，将多模态信息嵌入到统一空间中，实现了灵活的模态融合和鲁棒性。我们进一步引入了双混合专家机制（DMoE）：T-MoE 建模用于跟踪的时空关系，而 M-MoE 嵌入多模态知识，解耦跨模态依赖并减少特征冲突。通过共享架构、统一参数和单次端到端训练，OneTrackerV2 在五种 RGB 和 RGB+X 跟踪任务以及 12 个基准测试中均达到了最先进的性能，同时保持了较高的推理效率。值得注意的是，即使在模型压缩后，OneTrackerV2 仍保持着强劲的性能。此外，OneTrackerV2 在模态缺失的情况下表现出卓越的鲁棒性。

## Abstract
Multimodal visual object tracking can be divided into to several kinds of tasks (e.g. RGB and RGB+X tracking), based on the input modality. Existing methods often train separate models for each modality or rely on pretrained models to adapt to new modalities, which limits efficiency, scalability, and usability. Thus, we introduce OneTrackerV2, a unified multi-modal tracking framework that enables end-to-end training for any modality. We propose Meta Merger to embed multi-modal information into a unified space, allowing flexible modality fusion and robustness. We further introduce Dual Mixture-of-Experts (DMoE): T-MoE models spatio-temporal relations for tracking, while M-MoE embeds multi-modal knowledge, disentangling cross-modal dependencies and reducing feature conflicts. With a shared architecture, unified parameters, and a single end-to-end training, OneTrackerV2 achieves state-of-the-art performance across five RGB and RGB+X tracking tasks and 12 benchmarks, while maintaining high inference efficiency. Notably, even after model compression, OneTrackerV2 retains strong performance. Moreover, OneTrackerV2 demonstrates remarkable robustness under modality-missing scenarios.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **OneTrackerV2** 的统一多模态视觉跟踪框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态视觉跟踪（如 RGB+深度、RGB+热成像等）存在“碎片化”问题。不同模态组合通常需要设计特定的网络架构、进行多阶段的微调，或者依赖于不可扩展的预训练模型。
*   **研究动机**：
    1.  **训练效率低**：多阶段训练（先训练 RGB 再微调多模态）导致收敛效果不佳。
    2.  **架构不统一**：需要为不同任务手动设计分支。
    3.  **特征冲突**：在统一模型中，时空运动信息与异构模态特征往往交织在一起，产生优化干扰。
    4.  **鲁棒性差**：当某种模态缺失时，现有模型性能往往大幅下降。
*   **整体含义**：OneTrackerV2 旨在通过一个**统一的架构、统一的参数和单步端到端训练**，实现对任意模态组合的高效、鲁棒跟踪。

### 2. 方法论：核心思想与关键技术
OneTrackerV2 的核心在于将多模态输入映射到统一空间，并利用解耦的专家机制处理不同类型的特征。
*   **Meta Merger（元合并模块）**：
    *   **核心思想**：引入一个可学习的“元嵌入”（Meta Embedding）作为全局信息枢纽。
    *   **流程**：通过轻量级的空间和通道注意力增强 RGB 和辅助模态（X）的特征，然后让元嵌入与这些特征交互，从而吸收、对齐并重新分配跨模态信息。这种设计使得模型在模态缺失时依然能通过元嵌入保持稳定。
*   **Dual Mixture-of-Experts (DMoE，双混合专家机制)**：
    *   **T-MoE (时空专家)**：专门负责建模目标在视频序列中的时空匹配和运动规律。
    *   **M-MoE (多模态专家)**：专门负责多模态知识的融合与嵌入。
    *   **解耦技术**：
        1.  **专家解耦损失 ($L_{dis}$)**：通过惩罚 T-MoE 和 M-MoE 输出之间的余弦相似度，强制两者进入正交的功能子空间，避免特征纠缠。
        2.  **路由聚类正则化 ($L_{cluster}$)**：强制 M-MoE 的路由器对相同模态产生相似的专家选择，对不同模态产生差异化选择，从而学习到模态特异性的表示。
*   **统一训练**：采用单步训练方案，在包含 RGB 和多种 RGB+X 数据的混合数据集上直接训练。

### 3. 实验设计
*   **数据集与任务**：涵盖了 5 种跟踪任务和 12 个主流基准测试：
    *   **RGB 跟踪**：LaSOT, TrackingNet, GOT-10k, UAV123, NFS。
    *   **RGB+D (深度)**：DepthTrack。
    *   **RGB+T (热成像)**：LasHeR, RGBT234。
    *   **RGB+E (事件)**：VisEvent。
    *   **RGB+N (自然语言)**：TNL2K, OTB99。
*   **对比方法**：对比了包括 SUTrack, ARTrackV2, MixFormerV2, SeqTrack, ViPT 等在内的最新 SOTA（最先进）方法。
*   **特殊场景**：专门设计了**模态缺失（Missing Modality）**实验，验证模型在只有部分传感器工作时的表现。

### 4. 资源与算力
*   **模型配置**：提供了从 Base 到 Large 不同规模的变体（如 B224, L384）。
*   **训练细节**：使用 AdamW 优化器，训练 300 个 epoch，每个 epoch 包含 100,000 个图像对。
*   **算力说明**：文中**未明确列出具体的 GPU 型号和数量**（例如未提及使用了多少张 A100 或 H100），但给出了推理速度（如 B224 版本在特定硬件上可达 72.4 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 12 个数据集的性能对比、针对 Meta Merger 和 DMoE 的消融实验、专家超参数（秩 $r$ 和专家数 $K$）的敏感性分析、模型压缩实验以及模态缺失的鲁棒性测试。
*   **充分性与公平性**：实验覆盖了目前主流的所有多模态跟踪场景，对比基准统一，且通过可视化路由器分布直观展示了专家分工的合理性。整体实验设计客观、严谨，具有很强的说服力。

### 6. 主要结论与发现
*   **性能领先**：OneTrackerV2 在所有 12 个基准测试中均达到或超过了现有 SOTA 水平，证明了统一框架的优越性。
*   **解耦有效性**：DMoE 成功将运动建模与模态融合分离，解决了异构特征冲突问题。
*   **极强鲁棒性**：在模态缺失情况下，OneTrackerV2 的性能下降远小于同类模型（如 SUTrack）。
*   **高效性**：模型在保持高精度的同时，推理速度快，且在压缩至 6 层后仍能保持极高的性能（159 FPS）。

### 7. 优点
*   **架构优雅**：通过 Meta Merger 和 DMoE 实现了真正的“一个模型走天下”，无需为新模态重新设计。
*   **解耦创新**：引入正交损失和聚类损失来引导 MoE 专家分工，这是一个非常直观且有效的创新点。
*   **实用性强**：单步训练简化了开发流程，且对硬件故障（模态缺失）具有天然的防御能力。

### 8. 不足与局限
*   **文本编码器开销**：虽然模型主体很轻量，但在处理 RGB+N 任务时依赖 CLIP-L 文本编码器，这部分的计算开销在 FLOPs 统计中被省略了。
*   **超参数敏感性**：DMoE 的性能依赖于专家数量和秩的选择，虽然文中给出了推荐值，但在全新模态上的泛化可能仍需调优。
*   **训练数据依赖**：作为一个统一模型，其性能高度依赖于多模态混合数据的质量和多样性，对于极罕见的模态组合可能仍需数据增强。

（完）
