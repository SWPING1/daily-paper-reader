---
title: "BALM: A Model-Agnostic Framework for Balanced Multimodal Learning under Imbalanced Missing Rates"
title_zh: BALM：一种针对不平衡缺失率下平衡多模态学习的模型无关框架
authors: "Phuong-Anh Nguyen, Tien Anh Pham, Duc-Trong Le, Cam-Van Thi Nguyen"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19718v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 不平衡缺失率下的平衡多模态学习
tldr: 针对多模态学习中因各模态缺失率不均衡（IMR）导致的优化失衡问题，本文提出了BALM框架。该框架包含特征校准模块（FCM）和梯度重平衡模块（GRM），分别从特征表示和梯度动力学角度解决模态间的不平衡。BALM作为一种模型无关的插件，能显著提升多模态情感识别模型在复杂缺失场景下的鲁棒性与性能。
selection_source: fresh_fetch
motivation: 现实场景中各模态缺失率不一，导致模型优化被主导模态支配，弱势或高缺失率模态贡献不足。
method: 提出由特征校准模块（FCM）和梯度重平衡模块（GRM）组成的通用框架，通过全局上下文校准特征并动态调节梯度。
result: 在多个多模态情感识别基准测试中，BALM在不同缺失率和不平衡设置下均显著提升了模型的性能。
conclusion: BALM为解决不均衡缺失率下的多模态学习提供了一种灵活且有效的通用解决方案。
---

## 摘要
从多个模态中学习通常会面临不平衡问题，即信息丰富的模态主导优化过程，而较弱或部分缺失的模态贡献较少。在具有不平衡缺失率（IMR）的现实场景中，这种不平衡变得更加严重，其中每个模态以不同的概率缺失，从而扭曲了表示学习和梯度动力学。我们从训练过程的角度重新审视这一问题，并提出了 BALM，这是一个模型无关的插件式框架，旨在实现 IMR 下的平衡多模态学习。该框架包含两个互补模块：特征校准模块（FCM），利用全局上下文重新校准单模态特征，以在异构缺失模式之间建立共享的表示基础；梯度重平衡模块（GRM），通过从分布和空间两个维度调节梯度的大小和方向，来平衡各模态之间的学习动力学。BALM 可以无缝集成到包括多模态情感识别（MER）模型在内的各种骨干网络中，而无需改变其架构。在多个 MER 基准测试上的实验结果证实，BALM 在各种缺失和不平衡设置下都能持续增强鲁棒性并提高性能。代码可在以下网址获取：https://github.com/np4s/BALM_CVPR2026.git。

## Abstract
Learning from multiple modalities often suffers from imbalance, where information-rich modalities dominate optimization while weaker or partially missing modalities contribute less. This imbalance becomes severe in realistic settings with imbalanced missing rates (IMR), where each modality is absent with different probabilities, distorting representation learning and gradient dynamics. We revisit this issue from a training-process perspective and propose BALM, a model-agnostic plug-in framework to achieve balanced multimodal learning under IMR. The framework comprises two complementary modules: the Feature Calibration Module (FCM), which recalibrates unimodal features using global context to establish a shared representation basis across heterogeneous missing patterns; the Gradient Rebalancing Module (GRM), which balances learning dynamics across modalities by modulating gradient magnitudes and directions from both distributional and spatial perspectives. BALM can be seamlessly integrated into diverse backbones, including multimodal emotion recognition (MER) models, without altering their architectures. Experimental results across multiple MER benchmarks confirm that BALM consistently enhances robustness and improves performance under diverse missing and imbalance settings. Code available at: https://github.com/np4s/BALM_CVPR2026.git

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **BALM** 的模型无关（Model-Agnostic）框架，旨在解决多模态学习在**不平衡缺失率（Imbalanced Missing Rates, IMR）**场景下的性能退化和优化失衡问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在现实应用中，不同模态（如音频、视觉、文本）的缺失概率往往是不同的（即 IMR），而非学术假设中常见的相同缺失率（SMR）。
*   **研究动机**：
    *   **表示失衡**：异构的缺失模式会扭曲单模态特征分布，导致跨模态融合不一致。
    *   **学习失衡**：缺失率低的模态会主导梯度更新，而缺失率高的模态由于监督信号稀疏，导致模型收敛缓慢或陷入局部最优。
*   **整体含义**：论文指出，现有的缺失模态学习方法大多忽略了训练过程中的梯度动力学失衡。BALM 作为一个插件式框架，试图从特征表示和优化策略两个层面恢复模态间的平衡。

### 2. 方法论：核心思想与关键技术
BALM 由两个互补的模块组成，其核心思想是“特征校准”与“梯度重平衡”：

*   **特征校准模块 (FCM)**：
    *   **全局上下文提取**：通过平均池化获取可用模态的全局描述符，并利用全连接层提取跨模态共享上下文。
    *   **自适应校准**：利用提取的上下文生成模态特定的校准权重（类似 Squeeze-and-Excitation 机制），对单模态特征进行门控缩放，以消除因缺失模式不同带来的表示偏差。
*   **梯度重平衡模块 (GRM)**：
    *   **分布驱动调节**：引入辅助单模态预测头，计算单模态预测与多模态融合预测之间的 **KL 散度**。根据散度的变化率（学习进度）动态调整各模态编码器的梯度步长（系数 $\mu_m$）。学习快的模态会被抑制，学习慢的模态会被加强。
    *   **空间驱动调节**：计算单模态梯度与全局梯度之间的**余弦相似度**，通过正则化损失 $L_{mod}$ 强制各模态的优化方向向平衡的多模态目标对齐。
*   **理论支撑**：论文通过引理证明了在 IMR 条件下，模态梯度期望值会按其可用率 $(1-r_m)$ 进行缩放，从而为 GRM 的梯度补偿提供了数学依据。

### 3. 实验设计
*   **数据集**：
    *   **IEMOCAP**：交互式情感对话捕捉数据库（6 类情感）。
    *   **CMU-MOSEI**：大规模多模态情感分析数据集（情感极性回归/分类）。
*   **Benchmark 与对比方法**：
    *   **缺失模态专用模型**：MMIN, SDR-GNN, Mi-CGA, MoMKE, GCNet。
    *   **不平衡学习模型**：Ada2I。
    *   **综合处理模型**：RedCore, MCE。
    *   **标准 MER 骨干网络**：MMGCN, MMDFN（作为 BALM 嵌入的基座）。
*   **实验场景**：设置了 6 种不同的 IMR 配置（缺失率在 0.3, 0.5, 0.7 之间交替），以及极端的严重缺失测试（缺失率达 0.9）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 PyTorch 框架，并在 Comet.ml 上进行实验追踪。代码已开源，通常此类多模态情感识别任务在单张主流显卡（如 RTX 3090 或 A100）上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了详尽的对比。
    *   针对每种 IMR 配置进行了多次独立运行并报告了标准差。
    *   **消融实验**：分别验证了 FCM 和 GRM 的有效性，并分析了不同模态组合下的表现。
    *   **鲁棒性测试**：在 SMR（相同缺失率）和极端缺失率下也进行了验证。
*   **充分性评价**：实验设计非常全面，不仅对比了同类先进方法，还证明了 BALM 作为插件提升现有强力模型（如 MMDFN）的能力，客观性较强。

### 6. 主要结论与发现
*   **性能提升**：BALM 在所有 IMR 设置下均显著提升了基准模型的准确率和 F1 值，尤其是在文本（最关键模态）缺失严重时，提升效果尤为明显。
*   **鲁棒性**：即使在 SMR 这种相对简单的场景下，BALM 依然优于专门为 SMR 设计的模型。
*   **优化平衡**：可视化分析（t-SNE 和梯度曲线）表明，GRM 有效缩小了模态间的学习差异，使模型不再被单一主导模态“带偏”。

### 7. 优点
*   **模型无关性**：作为插件设计，不需要修改原有的多模态融合架构（如 Transformer 或 GNN），易于集成。
*   **双重平衡**：同时从特征空间（FCM）和优化动力学（GRM）两个维度解决不平衡问题，比单一的重采样或损失加权更深入。
*   **理论与实践结合**：提供了梯度缩放的理论分析，并辅以直观的实验验证。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在多模态情感识别（MER）领域，尚未在更广泛的多模态任务（如视觉问答 VQA、多模态检索等）上验证其通用性。
*   **超参数敏感性**：GRM 引入了调节强度 $\rho$ 和权衡因子 $\tau$ 等超参数，虽然论文做了敏感性分析，但在不同任务中可能需要精细调优。
*   **计算开销**：虽然框架轻量，但引入辅助预测头和计算梯度相似度会略微增加训练时的内存占用和计算耗时。

（完）
