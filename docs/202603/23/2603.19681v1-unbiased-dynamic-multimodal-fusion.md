---
title: Unbiased Dynamic Multimodal Fusion
title_zh: 无偏动态多模态融合
authors: "Shicai Wei, Kaijie Zhang, Luyi Chen, Tao He, Guiduo Duan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19681v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 模态质量变化下的动态多模态融合
tldr: 针对动态多模态融合中噪声评估不准和模态依赖偏差导致难学模态受双重抑制的问题，本文提出无偏动态多模态学习（UDML）框架。该框架引入噪声感知不确定性估计器，通过预测受控噪声强度实现精准的质量评估，并利用模态丢弃量化固有依赖偏差以优化权重分配。实验证明，UDML在多种基准任务上均优于现有动态和静态融合方法，具有极强的通用性。
selection_source: fresh_fetch
motivation: 现有动态融合方法在极端噪声下质量评估失效，且忽视了模态依赖偏差对难学模态的过度抑制。
method: 提出UDML框架，结合噪声感知不确定性估计器和基于模态丢弃的偏差量化机制，实现更公平、准确的动态加权。
result: 在多个多模态基准任务上的广泛实验验证了该方法在提升性能、通用性和泛化能力方面的有效性。
conclusion: UDML通过解决噪声评估和模态偏差问题，显著提升了动态多模态融合的鲁棒性与公平性。
---

## 摘要
传统多模态方法通常假设模态质量是静态的，这限制了它们在动态现实场景中的适应性。因此，研究者提出了动态多模态方法来评估模态质量并相应地调整其贡献。然而，这些方法通常依赖于经验指标，在噪声水平极低或极高时无法准确衡量模态质量。此外，现有方法通常假设每个模态的初始贡献是相同的，忽略了内在的模态依赖偏差。结果，难以学习的模态会受到双重惩罚，导致动态融合的性能可能劣于静态融合。为了应对这些挑战，我们提出了无偏动态多模态学习（UDML）框架。具体而言，我们引入了一个噪声感知不确定性估计器，它向模态数据添加受控噪声，并根据模态特征预测其强度。这迫使模型学习特征损坏与噪声水平之间的清晰对应关系，从而在低噪声和高噪声条件下都能实现准确的不确定性度量。此外，我们通过模态丢弃（modality dropout）量化了多模态网络中固有的模态依赖偏差，并将其纳入权重机制。这消除了对难学模态的双重抑制效应。在多种多模态基准任务上的广泛实验验证了所提 UDML 的有效性、通用性和泛化能力。代码可在 https://github.com/shicaiwei123/UDML 获取。

## Abstract
Traditional multimodal methods often assume static modality quality, which limits their adaptability in dynamic real-world scenarios. Thus, dynamical multimodal methods are proposed to assess modality quality and adjust their contribution accordingly. However, they typically rely on empirical metrics, failing to measure the modality quality when noise levels are extremely low or high. Moreover, existing methods usually assume that the initial contribution of each modality is the same, neglecting the intrinsic modality dependency bias. As a result, the modality hard to learn would be doubly penalized, and the performance of dynamical fusion could be inferior to that of static fusion. To address these challenges, we propose the Unbiased Dynamic Multimodal Learning (UDML) framework. Specifically, we introduce a noise-aware uncertainty estimator that adds controlled noise to the modality data and predicts its intensity from the modality feature. This forces the model to learn a clear correspondence between feature corruption and noise level, allowing accurate uncertainty measure across both low- and high-noise conditions. Furthermore, we quantify the inherent modality reliance bias within multimodal networks via modality dropout and incorporate it into the weighting mechanism. This eliminates the dual suppression effect on the hard-to-learn modality. Extensive experiments across diverse multimodal benchmark tasks validate the effectiveness, versatility, and generalizability of the proposed UDML. The code is available at https://github.com/shicaiwei123/UDML.

---

## 论文详细总结（自动生成）

以下是对论文《Unbiased Dynamic Multimodal Fusion》（无偏动态多模态融合）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在现实场景中，多模态数据的质量是动态变化的（如光照变化影响视觉、环境噪声影响音频）。现有的动态融合方法旨在评估模态质量并调整其权重。
*   **核心问题**：
    1.  **不确定性估计偏差**：现有方法依赖经验指标（如能量分数或概率嵌入），在噪声极低或极高时失效，无法准确反映模态真实质量。
    2.  **双重抑制效应（Dual Suppression）**：模型天然倾向于依赖“易学”模态。现有的动态方法假设初始权重相同，导致“难学”模态在面临高不确定性时，既受优化偏差抑制，又受权重下调抑制，性能甚至不如静态融合。
*   **研究动机**：开发一种能够准确感知噪声并消除模态依赖偏差的通用动态融合框架。

### 2. 论文提出的方法论（UDML）
UDML 框架由两个核心组件和一个优化策略组成：
*   **噪声感知不确定性估计器（NUE）**：
    *   **核心思想**：通过主动注入受控噪声并训练模型预测噪声强度，建立特征损坏与噪声水平的清晰映射。
    *   **技术细节**：采用概率表示技术，将模态特征映射为高斯分布。均值编码语义信息，方差反映噪声特性。估计器（轻量级两层 MLP）直接从方差中推导噪声强度 $\sigma$，从而在全噪声区间实现精准度量。
*   **模态依赖计算器（MC）**：
    *   **核心思想**：量化模型对各模态的固有依赖程度。
    *   **技术细节**：利用**模态丢弃（Modality Dropout）**机制，计算移除某一模态后输出逻辑值（Logits）的变化量。变化越大，依赖度 $\alpha$ 越高。通过将该系数引入权重分配公式，抵消模型对易学模态的过度偏好。
*   **渐进式优化策略（POS）**：
    *   **流程**：第一阶段在干净数据上预训练，学习稳定的多模态表示；第二阶段引入受控扰动训练噪声估计器，同时阻断噪声损失对编码器的梯度回传，避免干扰主任务学习。

### 3. 实验设计
*   **数据集**：涵盖 5 个多模态基准，包括情感分析（CMU-MOSI, CMU-MOSEI）、图像-文本分类（MVSA-Single）、音频-视觉情感识别（CREMA-D）和动作理解（Kinetics-Sounds）。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括静态融合（Late Fusion）、经典动态方法（TMC, QMF）以及最新的 SOTA 方法（EAU, LDDU）。
    *   **测试场景**：除了标准测试集，还专门在加入不同强度高斯噪声和椒盐噪声的污染数据集上进行了鲁棒性评估。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 Adam 和 SGD 优化器，学习率在 $1 \times 10^{-5}$ 到 $1 \times 10^{-3}$ 之间，Batch Size 分别为 16 或 64。代码已开源，表明其计算开销主要集中在标准 ResNet-18 或 BERT 等骨干网络上，UDML 增加的 MLP 模块计算量极小。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个不同领域的任务上进行了性能对比。
    *   针对噪声鲁棒性做了多梯度（$\sigma=0, 5, 10$）和多类型（高斯、椒盐）的对比实验。
    *   进行了消融实验（验证 NUE、MC、POS 的必要性）。
    *   进行了架构泛化性实验（集成到 MMTM 和 mmFormer 中）。
*   **客观性与公平性**：实验设置了统一的训练轮数（Stage 1 和 Stage 2 各占一半），确保与对比方法在总训练时长上公平；使用了多种评估指标（Acc, F1, Corr），实验设计较为全面、客观。

### 6. 论文的主要结论与发现
*   **性能提升**：UDML 在所有数据集上均优于现有 SOTA 方法，尤其在噪声环境下表现出极强的鲁棒性。
*   **解决偏差**：可视化结果证实，UDML 能在极高噪声下将模态权重降至接近 0，并能准确识别并修正模型对特定模态（如音频）的固有依赖偏差。
*   **通用性**：该框架是“架构无关”的，可以无缝集成到 CNN 或 Transformer 等不同结构的多模态模型中。

### 7. 优点（亮点）
*   **洞察深刻**：首次揭示了动态融合中的“双重抑制效应”，并提出了通过量化固有依赖来修正偏差的方案。
*   **噪声感知能力强**：通过主动预测噪声强度的训练方式，解决了传统经验指标在极端噪声下失效的问题。
*   **轻量且通用**：不改变原有骨干网络结构，仅通过特征层面的加权实现优化，易于部署。

### 8. 不足与局限
*   **样本级偏差未覆盖**：作者在文中提到，UDML 主要关注模态级别的偏差，尚未显式处理样本级别的偏差（例如长尾分布导致的样本不确定性）。
*   **噪声类型限制**：虽然在椒盐噪声上表现出泛化性，但训练时仍依赖于注入高斯噪声，对于更复杂的现实环境干扰（如遮挡、语义错配）的有效性有待进一步验证。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于评估大规模部署时的成本。

（完）
