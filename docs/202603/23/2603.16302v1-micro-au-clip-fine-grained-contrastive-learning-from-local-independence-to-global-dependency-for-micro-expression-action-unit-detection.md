---
title: "Micro-AU CLIP: Fine-Grained Contrastive Learning from Local Independence to Global Dependency for Micro-Expression Action Unit Detection"
title_zh: Micro-AU CLIP：从局部独立性到全局依赖性的细粒度对比学习，用于微表情动作单元检测
authors: "Jinsheng Wei, Fengzhou Guo, Yante Li, Haoyu Chen, Guanming Lu, Guoying Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16302v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 细粒度真实情感分析
tldr: 本研究针对微表情动作单元（Micro-AU）检测中局部特征感知不足的问题，提出了Micro-AU CLIP框架。该框架通过局部语义独立性（LSI）建模捕捉特定肌肉运动，并利用全局语义依赖性（GSD）建模AU间的关联。此外，引入微AU对比损失（MiAUCL）实现细粒度的视觉-文本对齐。实验证明，该方法在Micro-AU检测和无标签微表情识别任务上均达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有方法多从全脸学习特征，忽略了AU固有的局部独立性以及在特定情感状态下的全局依赖性。
method: 提出Micro-AU CLIP框架，通过PTA模块建模局部独立性，利用GDA和GDLoss建模全局依赖，并设计MiAUCL损失增强视觉与文本的细粒度对齐。
result: 实验结果表明，Micro-AU CLIP在微表情AU检测任务上取得了当前最优性能，并能有效应用于无标签的微表情识别。
conclusion: 通过从局部独立到全局依赖的建模路径，该方法显著提升了微表情细粒度特征的学习能力，为情感分析提供了更客观的线索。
---

## 摘要
微表情（ME）动作单元（Micro-AUs）为细粒度的真实情感分析提供了客观线索。大多数现有的 Micro-AU 检测方法从整个面部图像或视频中学习 AU 特征，这与 AU 固有的局部性相冲突，导致对 AU 区域的感知不足。事实上，每个 AU 独立地对应于特定的局部面部肌肉运动（局部独立性），而在特定情感状态下，某些 AU 之间存在固有的依赖关系（全局依赖性）。因此，本文探讨了“独立到依赖”模式的有效性，并提出了一种新型的微表情动作单元检测框架 Micro-AU CLIP，该框架独特地将 AU 检测过程分解为局部语义独立性建模（LSI）和全局语义依赖性（GSD）建模。在 LSI 中，设计了补丁令牌注意力（PTA），将 AU 区域内的多个局部特征映射到同一特征空间；在 GSD 中，提出了全局依赖注意力（GDA）和全局依赖损失（GDLoss），以建模不同 AU 之间的全局依赖关系，从而增强每个 AU 特征。此外，考虑到 CLIP 在微语义对齐方面的固有局限性，设计了微表情动作单元对比损失（MiAUCL），通过视觉和文本特征的细粒度对齐来学习 AU 特征。此外，Micro-AU CLIP 还被有效地应用于无情感标签的微表情识别。实验结果表明，Micro-AU CLIP 能够充分学习细粒度的 Micro-AU 特征，达到了最先进的性能。

## Abstract
Micro-expression (ME) action units (Micro-AUs) provide objective clues for fine-grained genuine emotion analysis. Most existing Micro-AU detection methods learn AU features from the whole facial image/video, which conflicts with the inherent locality of AU, resulting in insufficient perception of AU regions. In fact, each AU independently corresponds to specific localized facial muscle movements (local independence), while there is an inherent dependency between some AUs under specific emotional states (global dependency). Thus, this paper explores the effectiveness of the independence-to-dependency pattern and proposes a novel micro-AU detection framework, micro-AU CLIP, that uniquely decomposes the AU detection process into local semantic independence modeling (LSI) and global semantic dependency (GSD) modeling. In LSI, Patch Token Attention (PTA) is designed, mapping several local features within the AU region to the same feature space; In GSD, Global Dependency Attention (GDA) and Global Dependency Loss (GDLoss) are presented to model the global dependency relationships between different AUs, thereby enhancing each AU feature. Furthermore, considering CLIP's native limitations in micro-semantic alignment, a microAU contrastive loss (MiAUCL) is designed to learn AU features by a fine-grained alignment of visual and text features. Also, Micro-AU CLIP is effectively applied to ME recognition in an emotion-label-free way. The experimental results demonstrate that Micro-AU CLIP can fully learn fine-grained micro-AU features, achieving state-of-the-art performance.

---

## 论文详细总结（自动生成）

以下是对论文《Micro-AU CLIP: Fine-Grained Contrastive Learning from Local Independence to Global Dependency for Micro-Expression Action Unit Detection》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：微表情（ME）是短暂且不自主的面部动作，能揭示真实情感。动作单元（AU）检测是微表情分析的基础，比主观的情感标签更客观。
*   **核心问题**：现有的微表情 AU 检测方法多从全脸提取特征，忽略了 AU 的两个关键特性：
    1.  **局部独立性**：每个 AU 对应特定的局部肌肉运动，无需其他区域信息即可识别。
    2.  **全局依赖性**：在特定情感下，某些 AU 会协同出现（如快乐时的 AU6 和 AU12）。
*   **研究动机**：利用 CLIP 的视觉-文本对齐能力，探索一种从“局部独立”到“全局依赖”的细粒度学习模式，以解决微表情动作极其微弱、难以捕捉的问题。

### 2. 方法论
论文提出了 **Micro-AU CLIP** 框架，核心思想是将检测过程分解为局部建模和全局关联：
*   **视觉与文本骨干**：使用 CLIP 的 ViT-B/32 作为视觉编码器，输入起始帧与顶点帧之间的光流图（Optical Flow）；文本编码器输入针对每个 AU 定义的正负描述（如“眉毛下压”与“眉毛未下压”）。
*   **局部语义独立性建模 (LSI)**：
    *   **细粒度局部划分**：利用 68 个面部关键点，根据解剖学对应关系将视觉特征图划分为多个 AU 相关的局部区域。
    *   **补丁令牌注意力 (PTA)**：设计 PTA 模块对 AU 区域内的多个 Patch Token 进行加权融合，提取具有语义一致性的局部 AU 特征。
*   **全局语义依赖性建模 (GSD)**：
    *   **全局依赖注意力 (GDA)**：通过自注意力机制建模不同 AU 特征之间的相互影响。
    *   **全局依赖损失 (GDLoss)**：利用 AU 标签的共现关系约束注意力权重，确保模型关注有依赖关系的 AU 组合。
*   **微 AU 对比损失 (MiAUCL)**：针对 CLIP 在细粒度任务上的局限，设计了基于 AU 标签的对比损失，使相同激活状态的样本在特征空间更接近，增强视觉与文本的细粒度对齐。
*   **无标签微表情识别 (MER)**：通过计算检测到的 AU 特征与情感类别文本特征的相似度，实现无需情感标签监督的零样本识别。

### 3. 实验设计
*   **数据集**：使用了两个主流微表情数据集：**CASME II**（255 个样本）和 **SAMM**（159 个样本）。
*   **评估协议**：采用留一受试者交叉验证（LOSO），使用 **F1-score** 和 **Accuracy (ACC)** 作为评价指标。
*   **对比方法**：
    *   **AU 检测**：对比了传统方法（SP）、深度学习方法（ResNet18）、图模型（ME-GraphAU）、Transformer 架构（AUFormer）以及最新的大语言模型方法（AU-LLM）。
    *   **MER 任务**：对比了有监督学习（SME, TFT, CoT）和弱监督学习（Micron-BERT）方法。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4060 Ti (16G)** 的工作站上完成。
*   **软件环境**：使用 PyTorch 2.6.0。
*   **训练细节**：Epoch 设为 80，Batch Size 为 8；使用 SGD 优化器，视觉/文本编码器学习率为 0.001，其他模块为 0.01。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括：
    1.  **SOTA 对比**：在两个数据集上与 8 种以上先进方法对比。
    2.  **消融实验**：针对 LSI、GSD、PTA、GDA 以及 MiAUCL 损失函数分别进行了拆解验证。
    3.  **参数敏感性分析**：对损失函数的权衡系数 $\alpha$ 和 $\beta$ 进行了梯度测试。
    4.  **可视化分析**：包括注意力热图、t-SNE 特征聚类图和相似度矩阵可视化。
*   **充分性评价**：实验设计严谨，LOSO 协议保证了结果的客观性，消融实验覆盖了所有核心创新点，可视化结果直观地证明了模型确实关注到了正确的面部区域。

### 6. 主要结论与发现
*   **模式有效性**：从局部独立到全局依赖的建模路径显著优于单一的全局或局部建模。
*   **性能领先**：在 SAMM 数据集上 F1 分数达到 0.730，大幅领先现有方法；在跨数据集表现上比基于 LLM 的方法更稳健。
*   **跨任务潜力**：即使不使用情感标签进行训练，该模型在微表情识别任务上也达到了与有监督学习方法相当的水平。
*   **微调必要性**：实验发现微调 CLIP 的文本编码器对于区分“激活”与“未激活”这种细粒度语义至关重要。

### 7. 优点
*   **逻辑清晰**：将解剖学先验（局部肌肉运动）与统计学规律（AU 共现）有机结合。
*   **细粒度对齐**：MiAUCL 解决了标准 CLIP 在多标签、微小差异分类任务中的“粗粒度”问题。
*   **可解释性强**：通过关键点定位和注意力机制，使模型决策过程具有明确的生物学含义。
*   **泛化性好**：在无标签 MER 任务上的成功尝试展示了其强大的特征迁移能力。

### 8. 不足与局限
*   **依赖先验**：AU 区域的划分依赖于预定义的关键点映射表，若关键点检测失效或面部遮挡严重，性能可能受限。
*   **预处理需求**：仍需依赖起始帧和顶点帧的标注来计算光流，未实现完全端到端的原始视频处理。
*   **数据规模限制**：尽管在现有数据集上表现优异，但微表情数据集整体规模较小，模型在更大规模、更复杂环境下的鲁棒性仍待验证。

（完）
