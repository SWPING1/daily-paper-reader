---
title: Integrating Multimodal Large Language Model Knowledge into Amodal Completion
title_zh: 将多模态大语言模型知识集成到非遮挡补全中
authors: "Heecheol Yun, Eunho Yang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28333v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 重建人与物体的遮挡部分
tldr: 针对自动驾驶和机器人领域中物体遮挡还原的挑战，本文提出 AmodalCG 框架。该框架创新性地引入多模态大语言模型（MLLM）的常识知识来指导非全貌补全。通过评估遮挡程度，在重度遮挡时利用 MLLM 推理缺失区域的范围与内容，并结合视觉生成模型进行迭代优化。实验证明，该方法在处理复杂遮挡场景时显著优于现有技术，展示了 MLLM 在物理常识推理中的潜力。
selection_source: fresh_fetch
motivation: 现有的非全貌补全方法缺乏对现实世界实体的物理常识和先验知识，难以准确推断重度遮挡下的隐藏区域。
method: 提出 AmodalCG 框架，利用 MLLM 对重度遮挡物体的缺失部分进行范围和内容推理，并由视觉生成模型整合引导信息进行迭代修复。
result: 在多种真实世界图像上的实验结果表明，该方法在补全质量上显著超越了现有的所有先进算法。
conclusion: 将多模态大语言模型的常识推理能力引入非全貌补全任务，是解决复杂遮挡问题的有效且极具前景的方向。
---

## 摘要
随着自动驾驶汽车和机器人技术的广泛应用，非遮挡补全（amodal completion，即重建图像中人物和物体的被遮挡部分）变得日益重要。正如人类根据先验经验和常识推断隐藏区域一样，这项任务本质上需要关于现实世界实体的物理知识。然而，现有方法要么仅依赖于缺乏此类知识的视觉生成模型的图像生成能力，要么仅在分割阶段利用此类知识，导致其无法显式地引导补全过程。为了解决这一问题，我们提出了 AmodalCG，这是一个利用多模态大语言模型（MLLM）的现实世界知识来引导非遮挡补全的新型框架。我们的框架首先评估遮挡程度，以便仅在目标物体被严重遮挡时选择性地调用 MLLM 引导。如果需要引导，该框架会进一步结合 MLLM 来推理缺失区域的 (1) 范围和 (2) 内容。最后，视觉生成模型整合这些引导信息，并迭代地优化可能由不准确的 MLLM 引导产生的不完善补全结果。在各种现实世界图像上的实验结果显示，与现有所有工作相比，该方法都有显著改进，这表明 MLLM 是解决具有挑战性的非遮挡补全任务的一个极具前景的方向。

## Abstract
With the widespread adoption of autonomous vehicles and robotics, amodal completion, which reconstructs the occluded parts of people and objects in an image, has become increasingly crucial. Just as humans infer hidden regions based on prior experience and common sense, this task inherently requires physical knowledge about real-world entities. However, existing approaches either depend solely on the image generation ability of visual generative models, which lack such knowledge, or leverage it only during the segmentation stage, preventing it from explicitly guiding the completion process. To address this, we propose AmodalCG, a novel framework that harnesses the real-world knowledge of Multimodal Large Language Models (MLLMs) to guide amodal completion. Our framework first assesses the extent of occlusion to selectively invoke MLLM guidance only when the target object is heavily occluded. If guidance is required, the framework further incorporates MLLMs to reason about both the (1) extent and (2) content of the missing regions. Finally, a visual generative model integrates these guidance and iteratively refines imperfect completions that may arise from inaccurate MLLM guidance. Experimental results on various real-world images show impressive improvements compared to all existing works, suggesting MLLMs as a promising direction for addressing challenging amodal completion.

---

## 论文详细总结（自动生成）

以下是对论文《Integrating Multimodal Large Language Model Knowledge into Amodal Completion》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：非遮挡补全（Amodal Completion）旨在重建图像中被遮挡物体的完整外观。这项任务极具挑战性，因为模型需要具备关于现实世界实体的物理常识（如：被挡住的公交车车头长什么样）。
*   **研究动机**：
    *   **现有方法局限**：现有的基于扩散模型（如 Stable Diffusion）的方法往往缺乏物理常识，容易生成与目标无关的物体（幻觉）；或者仅将多模态大模型（MLLM）用于分割，而未将其知识引入生成过程。
    *   **人类直觉**：人类能根据经验推断隐藏区域，论文试图将 MLLM 蕴含的丰富常识转化为显式的几何和语义引导，以提升补全的准确性。

### 2. 方法论（AmodalCG 框架）
AmodalCG 包含五个核心组件，形成了一个从感知到推理再到生成的流水线：
*   **遮挡检测模块**：利用语义分割（SAM）和几何顺序预测网络识别遮挡物，确定初步的待补全区域（Inpainting Mask）。
*   **引导决策模块**：使用轻量级 MLLM（InternVL3.5-8B）评估遮挡程度。仅在物体被严重遮挡时才调用昂贵的大型 MLLM，以平衡效率与效果。
*   **几何引导模块**：利用 GPT-4o 预测目标物体的完整边界框（Bounding Box）。为了应对不确定性，模型会输出“紧凑、中等、粗略”三个尺度的预测框，防止生成范围过大导致无关物体的产生。
*   **语义引导模块**：生成详细的文本描述（Long Prompt），说明遮挡部分应有的内容。**关键技术细节**：在输入 MLLM 前会遮盖（Mask）遮挡物的外观，防止 MLLM 错误地描述遮挡物（如把挡住马的人也描述进马的特征里）。
*   **多尺度扩张补全模块**：使用 Stable Diffusion v2 进行修复。采用**迭代验证策略**：从最紧凑的预测框开始生成，若生成的物体触及边界则认为未完成，自动切换到更大尺度的框，直到物体被完整重建。

### 3. 实验设计
*   **数据集**：
    *   **非遮挡分割（Amodal Segmentation）**：COCO-A、BSDS-A、MP3D-A（涵盖真实世界多种物体）。
    *   **遮挡物体识别（Occluded Object Recognition）**：Occluded COCO、Separated COCO（评估重建外观的语义一致性）。
*   **对比基准（Baselines）**：
    *   **pix2gestalt**：基于微调扩散模型的方法。
    *   **MC Diffusion**：无需训练的渐进式扩散方法。
    *   **OWAAC**：利用 MLLM 进行分割但补全阶段仅用类别标签的方法。
    *   **SD Inpainting**：标准的稳定扩散修复模型。
*   **评估指标**：mIoU（分割准确度）、Top-1/Top-3 Accuracy（利用 CLIP 评估识别率）。

### 4. 资源与算力
*   **模型使用**：
    *   **决策模块**：InternVL3.5-8B（开源轻量级）。
    *   **推理模块**：GPT-4o（闭源 API）。
    *   **生成模块**：Stable Diffusion v2 Inpainting。
*   **算力说明**：文中**未明确给出**具体的 GPU 训练时长或显存消耗。由于该框架主要基于预训练模型和 API 调用，属于 **Training-free（无需训练）** 框架，其算力消耗主要集中在推理阶段的 MLLM API 调用和扩散模型的多次采样迭代上。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流分割数据集和两个识别数据集上进行了全面测试，涵盖了从简单到困难（遮挡率 > 50%）的不同场景。
*   **消融实验**：详细分析了几何引导、语义引导、多尺度扩张策略以及遮挡物遮盖处理对最终结果的贡献。
*   **客观性**：通过定量指标（mIoU、识别率）和定性可视化对比，证明了方法在处理重度遮挡时的优越性。实验设计较为公平，对比了当前该领域最前沿的开放世界补全模型。

### 6. 主要结论与发现
*   **性能提升**：在非遮挡分割任务上比基准模型提升了 **5.49%**，在遮挡物体识别任务上提升了 **2.92%**。
*   **几何引导的重要性**：准确预测物体的完整范围（边界框）是防止扩散模型产生“幻觉”的关键。
*   **语义引导的价值**：详细的文本描述能显著改善物体的姿态和外观细节，使其更符合逻辑。
*   **MLLM 的潜力**：证明了 MLLM 的常识推理能力可以有效弥补视觉生成模型在物理逻辑上的短板。

### 7. 优点与亮点
*   **选择性调用**：引入决策模块，避免了对轻微遮挡样本浪费高昂的 MLLM 算力。
*   **多尺度策略**：巧妙地解决了 MLLM 预测几何尺寸不精确的问题，通过生成模型的反馈进行闭环优化。
*   **视觉提示优化**：通过遮盖遮挡物来消除 MLLM 的视觉偏差，这是一个简单但非常有效的工程创新。
*   **无需训练**：能够直接应用于开放世界物体，具有很强的通用性。

### 8. 不足与局限
*   **API 依赖**：核心推理依赖于 GPT-4o 等闭源模型，可能存在成本高昂和无法离线部署的问题。
*   **共现偏差（Co-occurrence Bias）**：MLLM 有时会根据常识臆断图中不存在的物体（例如看到碗就描述里面有水果，即使原图并非如此）。
*   **效率瓶颈**：多尺度扩张策略需要多次运行扩散模型采样，推理延迟可能较高，难以满足自动驾驶等实时性要求极高的场景。
*   **几何预测限制**：对于极长或形状极不规则的物体（如长围栏），MLLM 的尺寸预测仍可能保守或失效。

（完）
