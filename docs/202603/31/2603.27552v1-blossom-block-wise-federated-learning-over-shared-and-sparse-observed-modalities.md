---
title: "BLOSSOM: Block-wise Federated Learning Over Shared and Sparse Observed Modalities"
title_zh: BLOSSOM：基于共享且稀疏观测模态的分块式联邦学习
authors: "Pranav M R, Jayant Chandwani, Ahmed M. Abdelmoniem, Arnab K. Paul"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27552v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 缺失模态下的联邦学习
tldr: 针对现实中多模态联邦学习面临的模态缺失和客户端异构问题，本文提出了 BLOSSOM 框架。该框架支持具有任意模态子集的客户端，并引入了块级聚合策略，通过选择性聚合共享组件并保留任务特定私有块来实现部分个性化。实验表明，BLOSSOM 在模态稀疏场景下表现优异，显著提升了模型在不完整数据下的泛化能力和性能。
selection_source: fresh_fetch
motivation: 现有联邦学习方法通常假设模态完整，难以应对现实中客户端模态缺失和异构的挑战。
method: 提出 BLOSSOM 框架，通过块级聚合策略选择性地聚合共享组件，同时保留任务特定的私有块以实现部分个性化。
result: "在多种多模态数据集上，BLOSSOM 在模态不完整和模态排他场景下分别比全模型聚合提升了 18.7% 和 37.7% 的性能。"
conclusion: 块级学习和部分个性化对于解决实际多模态联邦学习中的模态稀疏性问题至关重要。
---

## 摘要
多模态联邦学习（FL）对于自动驾驶系统和医疗保健等实际应用至关重要，在这些应用中，数据分布在具有不同且通常缺失模态的异构客户端上。然而，大多数现有的联邦学习方法假设模态可用性是统一的，这限制了它们在实践中的适用性。我们介绍了 BLOSSOM，这是一个任务无关的多模态联邦学习框架，旨在共享且稀疏观测的模态条件下运行。BLOSSOM 支持具有任意模态子集的客户端，并实现了模型组件的灵活共享。为了应对客户端和任务的异构性，我们提出了一种分块聚合策略，该策略在保持任务特定块私有的同时，选择性地聚合共享组件，从而实现部分个性化。我们在多个不同的多模态数据集上评估了 BLOSSOM，并分析了缺失模态和个性化的影响。我们的结果表明，分块个性化显著提高了性能，特别是在模态稀疏严重的情况下。在模态不完整的情况下，BLOSSOM 相比全模型聚合实现了 18.7% 的平均性能提升，而在模态排他性设置中，提升幅度增加到 37.7%，突显了分块学习对于实际多模态联邦学习系统的重要性。

## Abstract
Multimodal federated learning (FL) is essential for real-world applications such as autonomous systems and healthcare, where data is distributed across heterogeneous clients with varying and often missing modalities. However, most existing FL approaches assume uniform modality availability, limiting their applicability in practice. We introduce BLOSSOM, a task-agnostic framework for multimodal FL designed to operate under shared and sparsely observed modality conditions. BLOSSOM supports clients with arbitrary modality subsets and enables flexible sharing of model components. To address client and task heterogeneity, we propose a block-wise aggregation strategy that selectively aggregates shared components while keeping task-specific blocks private, enabling partial personalization. We evaluate BLOSSOM on multiple diverse multimodal datasets and analyse the effects of missing modalities and personalization. Our results show that block-wise personalization significantly improves performance, particularly in settings with severe modality sparsity. In modality-incomplete scenarios, BLOSSOM achieves an average performance gain of 18.7% over full-model aggregation, while in modality-exclusive settings the gain increases to 37.7%, highlighting the importance of block-wise learning for practical multimodal FL systems.

---

## 论文详细总结（自动生成）

以下是对论文《BLOSSOM: Block-wise Federated Learning Over Shared and Sparse Observed Modalities》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实的多模态联邦学习（FL）场景中，客户端往往由于硬件限制、成本或环境差异，只能观测到部分模态（即**结构化模态缺失**）。
*   **研究动机**：
    *   现有 FL 方法大多假设所有客户端拥有统一的模态，忽略了模态异构性。
    *   现有的多模态 FL 研究多关注样本级的随机缺失，而非客户端级别的完全缺失（模态排他性）。
    *   传统的全模型聚合在模态稀疏时性能大幅下降，且难以平衡全局知识共享与本地个性化需求。

### 2. 方法论
*   **核心思想**：提出 **BLOSSOM** 框架，采用**分块式（Block-wise）结构**和**后期融合（Late-fusion）策略**，将模型解构为：模态特定编码器（Encoders）、融合模块（Fusion）和任务预测头（Head）。
*   **关键技术细节**：
    *   **分块聚合策略**：服务器根据客户端拥有的模态，仅聚合对应的编码器块。对于融合层和预测头，提供灵活的聚合选项。
    *   **三种个性化模式**：
        1.  **FM (Full Model)**：全模型聚合（基准）。
        2.  **PH (Private Head)**：聚合编码器和融合层，预测头保持本地私有。
        3.  **PHF (Private Head-Fusion)**：仅聚合编码器，融合层和预测头均保持私有。
    *   **融合机制**：支持连接融合（ConcatFusion）和注意力融合（AttentionFusion）。在模态缺失时，通过将缺失模态的编码输出置零来保持架构一致性。
*   **算法流程**：在每一轮通信中，客户端下载相关的全局块，进行本地训练，随后仅上传其拥有的模态编码器及（根据模式确定的）融合/头部参数，服务器按块进行加权平均。

### 3. 实验设计
*   **数据集与场景**：涵盖 6 个多样化数据集：
    *   **人类活动识别 (HAR)**：KU-HAR, UCI-HAR。
    *   **医疗保健**：PTB-XL（心电图分类）。
    *   **多媒体**：AV-MNIST（图像+音频）。
    *   **情感识别**：MELD, IEMOCAP（音频+文本）。
*   **模态配置**：采用 `a-b-c` 表示法（如 5-5-0 表示 5 个客户端仅有模态 A，5 个仅有模态 B，0 个有双模态），模拟从模态完整（0% 缺失）到模态排他（50% 缺失）的极端场景。
*   **对比基准**：
    *   **FedAvg**（作为全模型聚合的代表）。
    *   **FedMultimodal**（最先进的多模态 FL 基准测试框架）。
    *   对比了 IID 和 Non-IID（基于 Dirichlet 分布 $\alpha=0.5$）的数据划分。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **软件框架**：提到使用了 Flower 联邦学习框架和 Hydra 配置管理工具，实验设置包含 10 个客户端，进行 60 轮通信，每轮本地训练 1 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集上分别测试了 3 种模态配置、2 种融合方式、3 种聚合模式以及 IID/Non-IID 两种分布，总实验组数较多。
*   **充分性评价**：实验设计较为**充分且客观**。通过引入“个性化增益（PG）”指标量化了不同模式的提升，并针对模态不足（Modality-insufficient）的任务进行了专门分析，验证了框架在极端稀疏情况下的鲁棒性。

### 6. 主要结论与发现
*   **个性化至关重要**：在模态不完整场景下，部分个性化（PH/PHF）平均提升性能 18.7%；在模态排他场景下，提升高达 **37.7%**。
*   **PHF 模式占优**：特别是在使用**注意力融合**时，将融合层私有化（PHF）能让客户端更好地根据自身拥有的模态调整权重，性能优于仅私有化预测头（PH）。
*   **应对异构性**：在标签异构（Non-IID）和模态稀疏同时存在时，个性化带来的增益最大（平均 25.82%）。
*   **贡献验证**：即使是模态不完整的客户端，通过 BLOSSOM 也能对全局编码器的学习做出积极贡献，提升整体泛化能力。

### 7. 优点
*   **灵活性高**：任务无关（Task-agnostic）设计，可适配多种多模态任务和聚合算法（如 FedAdam 等）。
*   **处理极端情况**：有效解决了现有方法难以处理的“模态完全排他”问题。
*   **隐私与效率平衡**：无需像某些方法那样重建缺失模态（保护隐私），通过块级聚合降低了不必要的通信开销。
*   **开源贡献**：提供了开源框架和标准化的实验划分，利于社区复现。

### 8. 不足与局限
*   **规模限制**：实验仅在 10 个客户端的小规模设置下完成，未探讨在大规模客户端或极低参与率（Low participation）下的表现。
*   **模态深度不足**：测试的数据集大多仅包含 2 个模态，对于拥有 3 个及以上模态的复杂场景（如视觉+音频+文本+传感器）的验证相对较少。
*   **算力细节缺失**：缺乏训练资源消耗的具体数据，难以评估其在资源受限设备上的部署成本。

（完）
