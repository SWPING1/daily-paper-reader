---
title: Learning to See through Illumination Extremes with Event Streaming in Multimodal Large Language Models
title_zh: 在多模态大语言模型中利用事件流学习在极端光照条件下进行视觉感知
authors: "Baoheng Zhang, Jiahui Liu, Gui Zhao, Weizhou Zhang, Yixuan Ma, Jun Jiang, Yingxian Chen, Wilton W. T. Fok, Xiaojuan Qi, Hayden Kwok-Hay So"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27558v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 通过多模态融合补偿极端光照下丢失的信息
tldr: 针对多模态大语言模型（MLLM）在极端光照下表现不佳的问题，本文提出Event-MLLM。该模型通过动态融合事件流与RGB帧，利用事件相机的高动态范围特性弥补视觉信息缺失。核心贡献包括光照指示器、光照校正损失函数以及首个多光照事件指令微调数据集，显著提升了模型在极端环境下的感知与推理能力。
selection_source: fresh_fetch
motivation: 传统MLLM在极端光照（过暗或过亮）下因RGB图像语义丢失而失效，需引入事件流来增强视觉鲁棒性。
method: 提出Event-MLLM，通过光照指示器自适应融合事件与RGB特征，并利用光照校正损失在潜空间对齐正常光照语义。
result: 在包含多种亮度变化的全新基准测试中，Event-MLLM在推理、计数和细粒度识别任务上均显著优于现有基准模型。
conclusion: 结合事件流与MLLM是解决极端光照下视觉推理难题的有效途径，为全天候鲁棒感知提供了新方向。
---

## 摘要
多模态大语言模型（MLLMs）在标准条件下表现出强大的视觉语言推理能力，但在极端光照条件下会失效，因为此时 RGB 输入会丢失不可恢复的结构和语义信息。我们提出了 Event-MLLM，这是一种事件增强模型，通过动态融合事件流与 RGB 帧来实现全光照视觉推理。驱动我们方法的两个关键组件是：光照指示器（Illumination Indicator）——一个源自 DINOv2 分支的可学习信号，用于表示曝光退化并自适应地调节事件-RGB 融合；以及光照校正损失（Illumination Correction Loss），用于在潜空间中将融合特征与未退化（正常光照）的语义对齐，从而补偿极端光照下丢失的信息。我们为 MLLMs 构建了首个多光照事件指令语料库，包含跨越不同场景和 17 种亮度倍率（0.05x - 20x）的 2,241 个事件-RGB 样本（每个样本约包含 6 个问答对），以及一个用于极端光照下推理、计数和细粒度识别的指令遵循基准。实验表明，Event-MLLM 显著优于通用型、光照自适应型和仅事件型的基线模型，在挑战性光照条件下的鲁棒多模态感知与推理方面达到了新的最先进水平。

## Abstract
Multimodal Large Language Models (MLLMs) perform strong vision-language reasoning under standard conditions but fail in extreme illumination, where RGB inputs lose irrevocable structure and semantics. We propose Event-MLLM, an event-enhanced model that performs all-light visual reasoning by dynamically fusing event streams with RGB frames. Two key components drive our approach: an Illumination Indicator - a learnable signal derived from a DINOv2 branch that represents exposure degradation and adaptively modulates event-RGB fusion - and an Illumination Correction Loss that aligns fused features with non-degraded (normal-light) semantics in the latent space, compensating for information lost in extreme lighting. We curate the first multi-illumination event-instruction corpus for MLLMs, with 2,241 event-RGB samples (around 6 QA pairs each) across diverse scenes and 17 brightness rates (0.05x - 20x), plus an instruct-following benchmark for reasoning, counting, and fine-grained recognition under extreme lighting. Experiments show that Event-MLLM markedly outperforms general-purpose, illumination-adaptive, and event-only baselines, setting a new state of the art in robust multimodal perception and reasoning under challenging illumination.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to See through Illumination Extremes with Event Streaming in Multimodal Large Language Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态大语言模型（MLLM）在极端光照（如极暗或过度曝光）环境下表现极差。这是因为传统的 RGB 传感器在这些条件下会发生严重的语义和结构信息丢失，导致模型产生幻觉或推理错误。
*   **研究背景**：虽然已有图像增强或外部工具调用等方案，但它们多属于被动修复，且容易引入人工伪影。本文提出利用**事件相机（Event Camera）**的高动态范围（>120dB）和微秒级响应特性，通过融合事件流来主动补偿 RGB 信息的缺失，实现“全光照”视觉推理。

### 2. 方法论
*   **核心思想**：构建一个事件增强的 MLLM（Event-MLLM），通过自适应融合模块将事件特征注入 RGB 特征中，并在特征空间内将其与正常光照下的语义进行对齐。
*   **关键技术细节**：
    *   **光照指示器（Illumination Indicator）**：利用冻结的 DINOv2 编码器提取 RGB 图像的光照特征，通过一个可学习的轻量级 MLP 生成信号，用于量化曝光退化程度并引导融合权重。
    *   **自适应特征融合**：将极端光照下的 RGB 特征、事件特征与光照指示器信号进行拼接，通过融合 MLP 生成统一的视觉表示。
    *   **光照校正损失（Illumination Correction Loss, $L_{IC}$）**：在训练阶段，引入正常光照图像作为参考，计算融合特征与正常光照特征之间的均方误差（MSE），强制模型在潜空间（Latent Space）中恢复丢失的语义。
    *   **两阶段训练流程**：
        1.  **特征融合训练**：仅训练融合模块，使融合特征在语义上接近正常光照特征。
        2.  **指令微调**：使用 LoRA 技术对整个 MLLM 进行端到端的指令遵循微调。

### 3. 实验设计
*   **数据集与场景**：构建了首个**多光照事件指令数据集**，包含 2,241 个视觉三元组（正常 RGB、极端 RGB、事件帧），涵盖室内外多种场景。
*   **Benchmark**：设计了包含 10,129 个问答对的基准测试，分为**多选题（场景理解）**和**物体计数**两大任务。
*   **对比方法**：
    *   **通用模型**：LLaVA-7B, DeepSeek-VL, InternVL。
    *   **事件专用模型**：EventGPT。
    *   **光照感知模型**：Q-Instruct。
    *   **基线模型**：基于 Qwen-3B/7B 的基础实现。
*   **亮度设置**：系统性地测试了从 0.05x（极暗）到 20.0x（极亮）共 17 个亮度等级。

### 4. 资源与算力
*   **硬件设备**：使用了 **NVIDIA RTX 5090D** 和 **NVIDIA A800** GPU。
*   **训练参数**：
    *   第一阶段：Adam 优化器，Batch size 4，学习率 0.001，训练 30 个 epoch。
    *   第二阶段：LoRA 微调，训练 1 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 17 种亮度倍率下进行了详尽测试，确保了结果在不同退化程度下的稳定性。
*   **消融实验**：针对光照校正模块、LoRA 微调、以及不同的融合策略（前融合、后融合、自适应融合）进行了对比实验。
*   **特征分析**：通过 **t-SNE 可视化**和**余弦相似度测量**，定量证明了融合特征确实向正常光照特征靠拢。
*   **评价**：实验设计全面，对比了不同参数规模（3B 和 7B）的模型，验证了方法的扩展性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：Event-MLLM 在所有亮度等级下均显著优于现有 SOTA 模型，尤其在极端曝光下，准确率提升幅度巨大。
*   **语义恢复**：通过 $L_{IC}$ 损失，模型能够学会在 RGB 信息完全丢失时，仅凭事件流和残余 RGB 信号重建出稳定的语义流形。
*   **模型规模效应**：更强大的语言模型（如 7B vs 3B）对残余特征偏差的容忍度更高，能更可靠地整合多模态线索。

### 7. 优点
*   **方案新颖**：首次将事件相机与 MLLM 结合，解决了视觉语言模型在极端环境下的鲁棒性“盲点”。
*   **高效对齐**：采用特征空间对齐而非像素级图像恢复，降低了计算负担并避免了伪影干扰。
*   **资源贡献**：提供了高质量的多光照事件指令数据集，填补了该领域的空白。

### 8. 不足与局限
*   **硬件依赖性**：模型高度依赖事件相机输入，在仅有传统 RGB 相机的设备上无法部署。
*   **泛化限制**：目前主要针对光照极端情况，对于大雾、雨雪或高速运动导致的动态模糊等其他退化场景的泛化能力尚待进一步验证。
*   **数据稀疏性**：未深入探讨在事件流信号极度稀疏（如静态场景且相机静止）时的失效风险。

（完）
