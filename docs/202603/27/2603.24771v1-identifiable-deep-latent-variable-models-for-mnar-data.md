---
title: Identifiable Deep Latent Variable Models for MNAR Data
title_zh: 针对非随机缺失（MNAR）数据的可识别深度潜变量模型
authors: "Huiming Xie, Fei Xue, Xiao Wang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24771v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 针对非随机缺失(MNAR)数据归补的深度潜变量模型
tldr: 针对现实中常见的非随机缺失（MNAR）数据，本文提出了一种基于深度潜变量模型的识别框架。通过引入“给定潜变量下无自审查”的假设，建立了数据分布的可识别性理论，并利用重要性权重自编码器（IWAE）开发了高效的估计算法。实验证明该方法能准确恢复真实分布，在缺失值填补任务中优于现有主流方法。
selection_source: fresh_fetch
motivation: 传统的缺失数据处理方法多基于MAR假设，而现有针对MNAR的深度学习方法往往忽略了非参数可识别性问题，导致结果不可靠。
method: 提出一种深度潜变量模型，在给定潜变量下无自审查的假设下确立可识别性，并采用重要性权重自编码器进行参数估计。
result: 理论和实验均表明，该方法在特定条件下能准确恢复联合分布，且在模拟和真实数据集上的填补效果优于多种基准方法。
conclusion: 本研究解决了MNAR数据下的可识别性难题，为深度学习在复杂缺失数据场景下的应用提供了坚实的理论基础和有效的算法支持。
---

## 摘要
缺失数据是数据分析中普遍存在的挑战，通常会导致偏差和不准确的结果。传统的填补方法通常假设缺失机制是随机缺失（MAR），即缺失情况与缺失值本身无关。这一假设在现实场景中经常被违背，这促使了近期利用深度学习解决该挑战的填补方法的进展。然而，这些方法忽略了非随机缺失（MNAR）数据中非参数可识别性这一关键问题，这可能导致有偏且不可靠的结果。本文旨在通过提出一种基于深度潜变量模型的 MNAR 数据新框架来填补这一空白。基于给定潜变量时条件无自审查（conditional no self-censoring）的假设，我们确立了数据分布的可识别性。这一关键的理论结果保证了我们方法的可行性。为了有效估计未知参数，我们开发了一种利用重要性权重自动编码器（importance-weighted autoencoders）的高效算法。我们在理论和实证上都证明了，在特定的正则性条件下，我们的估计过程能够准确地恢复真实的联合分布。广泛的仿真研究和真实世界数据实验展示了我们提出的方法与各种经典及最先进的缺失数据填补方法相比的优势。

## Abstract
Missing data is a ubiquitous challenge in data analysis, often leading to biased and inaccurate results. Traditional imputation methods usually assume that the missingness mechanism is missing-at-random (MAR), where the missingness is independent of the missing values themselves. This assumption is frequently violated in real-world scenarios, prompted by recent advances in imputation methods using deep learning to address this challenge. However, these methods neglect the crucial issue of nonparametric identifiability in missing-not-at-random (MNAR) data, which can lead to biased and unreliable results. This paper seeks to bridge this gap by proposing a novel framework based on deep latent variable models for {MNAR data}. Building on the assumption of conditional no self-censoring {given} latent variables, we establish the identifiability of the data distribution. This crucial theoretical result guarantees the feasibility of our approach. To effectively estimate unknown parameters, we develop an efficient algorithm utilizing importance-weighted autoencoders. We demonstrate, both theoretically and empirically, that our estimation process accurately recovers the ground-truth joint distribution under specific regularity conditions. Extensive simulation studies and real-world data experiments showcase the advantages of our proposed method compared to various classical and state-of-the-art approaches to missing data imputation.

---

## 论文详细总结（自动生成）

### 针对非随机缺失（MNAR）数据的可识别深度潜变量模型论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在数据分析中，缺失数据是普遍挑战。大多数传统方法基于“随机缺失”（MAR）假设，即缺失机制与缺失值本身无关。然而，现实中常出现“非随机缺失”（MNAR），例如高收入者不愿透露收入。处理 MNAR 数据的核心难点在于**非参数可识别性（Nonparametric Identifiability）**：即从观测数据中唯一确定原始完整数据分布的可能性。现有的深度学习填补方法往往忽略了这一理论基础，导致模型可能学习到多个不唯一的分布，从而产生有偏且不可靠的结果。本文旨在建立一个既有理论可识别性保证，又能处理复杂高维数据的深度潜变量模型框架。

#### 2. 论文提出的方法论：IM-IWAE
*   **核心思想**：提出“**给定潜变量下的条件无自审查**”（Conditional no self-censoring given latent variables）假设。该假设认为：在给定其他观测变量和潜在因子的条件下，某个变量的缺失指示符与其自身的值以及其他变量的缺失状态相互独立。
*   **关键技术细节**：
    *   **双潜变量结构**：引入潜变量 $Z$ 建模数据 $X$ 的生成过程，引入另一个潜变量 $\tilde{Z}$ 捕捉缺失机制 $R$ 中无法被 $X$ 解释的相关性。
    *   **模型分解**：将全数据分布分解为 $p(x, r) = \int \int p(x|z) p(r|x, \tilde{z}) p(z) p(\tilde{z}) dz d\tilde{z}$。
    *   **IM-IWAE 算法**：基于重要性权重自编码器（IWAE）开发。通过最大化观测数据的对数似然下界（ELBO 的变体 $L_K$），利用多个重要性样本来获得更紧致的似然估计。
    *   **理论保证**：论文证明了在该假设下，全数据分布具有非参数可识别性，且 IM-IWAE 能够渐近恢复真实的联合分布。

#### 3. 实验设计
*   **数据集/场景**：
    *   **仿真实验**：3维非线性数据、高维数据（30D, 50D, 100D）、高斯混合模型（用于均值估计）。
    *   **真实数据集**：6个 UCI 机器学习库数据集（如红酒质量、混凝土强度等）、博茨瓦纳 HIV 感染女性医疗记录、Yahoo! R3 音乐评分数据。
*   **Benchmark 与对比方法**：
    *   **深度学习 MNAR 方法**：GINA（需辅助变量）、not-MIWAE（不保证可识别性）。
    *   **经典填补方法（基于 MAR）**：MICE（多重填补）、missForest（随机森林填补）。
    *   **评估指标**：填补均方根误差（RMSE）、最大均分差异（MMD）、参数估计偏差。

#### 4. 资源与算力
*   论文提到使用 **TensorFlow Probability** 库和 **Adam 优化器** 进行实现。
*   **具体算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到在 Yahoo! 数据集上训练了 100 个 epoch，批大小为 100；在仿真实验中训练了 10k 到 20k 个 epoch。

#### 5. 实验数量与充分性
*   **实验规模**：仿真实验和 UCI 数据实验均进行了 **50 次重复**以保证统计显著性；HIV 数据使用了 1000 次自助法（Bootstrap）抽样计算置信区间。
*   **充分性评价**：实验设计较为充分。涵盖了从低维到高维、从合成到真实、从填补任务到下游统计推断（均值估计、优势比计算）的多个维度。
*   **公平性**：对比方法均采用了相同的神经网络架构和超参数设置，并针对潜变量维度进行了交叉验证选择，体现了实验的客观性。

#### 6. 主要结论与发现
*   **可识别性的重要性**：在 MNAR 场景下，缺乏可识别性保证的方法（如 GINA, not-MIWAE）在某些设置下表现不稳定且偏差较大。
*   **性能优越性**：提出的 IM-IWAE 在几乎所有 MNAR 实验中均取得了最低的 RMSE，且能更准确地恢复原始数据的概率密度分布。
*   **鲁棒性**：即使在违反“无自审查”假设（即存在自审查）的极端情况下，IM-IWAE 依然表现出比基准方法更好的适应性，这归功于其对变量间相关性的有效建模。

#### 7. 优点
*   **理论与实践结合**：不仅给出了严谨的非参数可识别性证明，还提供了可扩展到高维数据的深度学习算法。
*   **无需辅助变量**：与 GINA 等方法不同，该方法不需要额外的辅助变量即可实现识别，适用范围更广。
*   **灵活的缺失建模**：通过引入独立的缺失机制潜变量 $\tilde{Z}$，能够处理缺失指示符之间的复杂相关性。

#### 8. 不足与局限
*   **假设依赖**：“条件无自审查”虽然比传统假设宽松，但在某些极端自审查场景（如缺失仅由变量自身决定）下仍可能失效。
*   **计算开销**：作为基于 IWAE 的方法，需要多个重要性样本（实验中使用 $K=20$），在大规模数据集上的计算成本高于简单的 VAE 或传统填补方法。
*   **超参数敏感性**：虽然论文提供了交叉验证选择潜变量维度的方法，但在极高缺失率（如 98% 以上）的情况下，模型性能对架构选择的敏感性可能增加。

（完）
