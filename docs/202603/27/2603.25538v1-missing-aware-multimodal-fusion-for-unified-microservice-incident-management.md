---
title: Missing-Aware Multimodal Fusion for Unified Microservice Incident Management
title_zh: 面向统一微服务故障管理的缺失感知多模态融合
authors: "Wenzhuo Qian, Hailiang Zhao, Ziqi Wang, Zhipeng Gao, Jiayi Chen, Zhiwei Ling, Shuiguang Deng"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25538v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 针对缺失模态场景设计的鲁棒框架
tldr: 针对微服务故障管理中多模态数据（指标、日志、追踪）经常缺失的问题，本文提出ARMOR框架。该框架通过非对称编码器隔离分布差异，并利用缺失感知的门控融合机制和可学习占位符来减少缺失数据带来的干扰。ARMOR采用自监督学习，在异常检测、故障分类和根因定位任务上均表现出色，即使在严重数据缺失下也能保持高诊断精度。
selection_source: fresh_fetch
motivation: 现有的多模态微服务故障管理方法假设数据完整，但在实际场景中网络波动常导致模态缺失，使用静态占位符会引入噪声并降低性能。
method: 提出ARMOR框架，采用非对称编码器处理不同模态分布，并结合缺失感知的门控融合与掩码引导的自监督重构技术。
result: 实验表明ARMOR在完整数据下达到SOTA水平，且在模态严重缺失的情况下仍能保持稳健的诊断准确率。
conclusion: ARMOR通过缺失感知的融合机制有效解决了微服务监控数据不完整带来的挑战，实现了统一且鲁棒的故障管理。
---

## 摘要
自动化故障管理对于微服务的可靠性至关重要。虽然最近的统一框架利用多模态数据进行联合优化，但它们不切实际地假设了数据的完美完整性。在实践中，网络波动和代理故障经常导致模态缺失。现有依赖静态占位符的方法会引入插值噪声，从而掩盖异常并降低性能。为解决这一问题，我们提出了 ARMOR，这是一个专为模态缺失场景设计的鲁棒自监督框架。ARMOR 的特点包括：(i) 一种特定于模态的非对称编码器，用于隔离指标（metrics）、日志（logs）和追踪（traces）之间的分布差异；(ii) 一种缺失感知的门控融合机制，利用可学习的占位符和动态偏差补偿来防止不完整输入带来的跨模态干扰。通过采用带有掩码引导重构的自监督自回归，ARMOR 联合优化了异常检测 (AD)、故障分类 (FT) 和根因定位 (RCL)。AD 和 RCL 不需要故障标签，而 FT 仅依赖于下游分类器的故障类型标注。广泛的实验表明，ARMOR 在数据完整的情况下达到了最先进的性能，并且在严重的模态缺失下仍能保持鲁棒的诊断准确性。

## Abstract
Automated incident management is critical for microservice reliability. While recent unified frameworks leverage multimodal data for joint optimization, they unrealistically assume perfect data completeness. In practice, network fluctuations and agent failures frequently cause missing modalities. Existing approaches relying on static placeholders introduce imputation noise that masks anomalies and degrades performance. To address this, we propose ARMOR, a robust self-supervised framework designed for missing modality scenarios. ARMOR features: (i) a modality-specific asymmetric encoder that isolates distribution disparities among metrics, logs, and traces; and (ii) a missing-aware gated fusion mechanism utilizing learnable placeholders and dynamic bias compensation to prevent cross-modal interference from incomplete inputs. By employing self-supervised auto-regression with mask-guided reconstruction, ARMOR jointly optimizes anomaly detection (AD), failure triage (FT), and root cause localization (RCL). AD and RCL require no fault labels, while FT relies solely on failure-type annotations for the downstream classifier. Extensive experiments demonstrate that ARMOR achieves state-of-the-art performance under complete data conditions and maintains robust diagnostic accuracy even with severe modality loss.

---

## 论文详细总结（自动生成）

### 论文总结：面向统一微服务故障管理的缺失感知多模态融合 (ARMOR)

#### 1. 核心问题与整体含义（研究动机和背景）
微服务架构的复杂性使得自动化故障管理（包括异常检测 AD、故障分类 FT 和根因定位 RCL）成为保障系统可靠性的关键。现有的统一多模态框架通常假设监控数据（指标 Metrics、日志 Logs、追踪 Traces）是完整且对齐的。然而，在实际工业生产环境中，由于网络波动、采集插件（Agent）崩溃或配置错误，**模态缺失（Missing Modality）**是常态而非特例。
现有的处理方式通常采用静态占位符（如补零）进行填充，这会产生“伪正常”信号，掩盖真实的故障特征，导致诊断性能剧烈下降。本文提出的 **ARMOR** 旨在构建一个鲁棒的自监督框架，在模态不完整的情况下依然能实现高效的统一故障管理。

#### 2. 方法论：核心思想与关键技术
ARMOR 的核心思想是通过**非对称编码**隔离模态差异，并利用**缺失感知机制**动态调整融合权重。其主要模块包括：
*   **特定模态非对称编码器 (Modality-Specific Asymmetric Encoder)：** 针对指标（稠密连续）与日志/追踪（稀疏离散）的结构不对称性，设计了不同容量的 ModernTCN 块。通过深度卷积捕获时间趋势，逐点卷积捕获通道相关性，从而在融合前独立提取各模态特征，防止缺失模态污染幸存模态。
*   **缺失感知门控融合 (Missing-Aware Gated Fusion)：** 
    *   **可学习占位符：** 使用可学习的嵌入向量 $E_{miss}$ 替代静态补零，向网络明确传递“数据缺失”的信号。
    *   **动态偏差补偿：** 在计算注意力门控权重时，为缺失模态引入一个负向的可学习偏差 $b_{miss}$，主动降低缺失模态在全局表示中的贡献。
*   **统一表示与自监督优化：** 
    *   **自监督重构：** 在离线阶段，通过随机丢弃模态并尝试重构完整数据的自回归任务进行训练，使模型学习跨模态的不变量。
    *   **故障签名：** 将重构误差与潜在嵌入向量拼接，形成统一的故障表示，同时支持下游的 AD、FT 和 RCL 任务。

#### 3. 实验设计
*   **数据集：** 
    *   **$\mathcal{D}_1$：** 云部署的电商模拟系统，包含 46 个节点、210 个故障案例。
    *   **$\mathcal{D}_2$：** 2021 AIOps 挑战赛数据集（某大型商业银行管理系统），包含 18 个异构实例、133 个故障案例。
*   **Benchmark 与对比方法：** 
    *   **多任务框架：** ART, TrioXpert, Eadro, DiagFusion, Dejevu。
    *   **单任务方法：** Hades, ChronoSage (AD); MicroCBR (FT); DeepHunt (RCL)。
*   **缺失场景模拟：** 设计了 6 种缺失场景（如仅缺失日志、缺失指标、同时缺失指标和追踪等），对比 ARMOR 与最强基线 ART 在数据不完整时的表现。

#### 4. 资源与算力
*   **硬件环境：** 专用服务器，配备 20-vCPU Intel Xeon Platinum 8470Q 处理器。
*   **GPU 资源：** 单张 **NVIDIA RTX 4090 GPU (24GB)**。
*   **软件框架：** Python 3.9.13, PyTorch 1.12.1, DGL 0.9.0。
*   **训练时长：** 文中未明确给出具体的训练总时长，但通过推理延迟对比（Table 2）显示，ARMOR 的在线诊断速度优于基线方法 ART。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **全面性：** 在两个不同背景的工业级数据集上进行了测试，涵盖了故障管理的三个核心阶段。
*   **鲁棒性测试：** 针对 6 种不同的模态缺失组合进行了详尽对比，证明了模型在极端数据缺失下的稳定性。
*   **消融实验：** 设计了 12 组变体（A1-A9, B1-B3），分别验证了非对称编码、门控融合、可学习占位符、图神经网络以及统一表示策略的有效性。
*   **客观性：** 对基线方法进行了针对性的重训练（加入模态丢弃增强），确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能领先：** 在数据完整的情况下，ARMOR 在所有任务上均达到 SOTA，尤其在故障分类（FT）上 F1 分数提升显著（最高达 15.5%）。
*   **极强鲁棒性：** 当指标（Metrics）缺失时，传统模型性能会崩溃，而 ARMOR 通过动态偏差补偿，其 RCL 准确率比基线高出 60% 以上。
*   **偏差学习：** 实验证实模型自动学习到了负向偏差，且对高维指标缺失的惩罚最为严重，符合工业直觉。
*   **低延迟：** 共享编码器和并行融合机制使得 ARMOR 能够满足在线实时监控的需求。

#### 7. 优点
*   **切中痛点：** 首次系统性地解决了微服务监控中“模态缺失”这一极具现实意义的问题。
*   **架构科学：** 非对称编码器尊重了不同监控数据的物理特性（稠密 vs 稀疏），避免了特征污染。
*   **标签友好：** 采用自监督学习，异常检测和根因定位无需任何故障标签，极大地降低了部署成本。

#### 8. 不足与局限
*   **训练依赖：** 离线训练阶段仍假设历史存档中存在相对完整的模态数据作为重构目标，若历史数据本身长期缺失某模态，模型效果可能受限。
*   **拓扑动态性：** 虽然使用了图神经网络，但对于微服务实例频繁销毁/创建的极端动态拓扑演进，处理机制尚显简单。
*   **实验环境：** 尽管使用了工业数据集，但仍属于受控的测试床环境，在超大规模（万级节点）生产环境中的长尾效应和级联故障表现仍需进一步验证。

（完）
