---
title: "DMMRL: Disentangled Multi-Modal Representation Learning via Variational Autoencoders for Molecular Property Prediction"
title_zh: DMMRL：基于变分自编码器的解耦多模态表示学习，用于分子性质预测
authors: "Long Xu, Junping Guo, Jianbo Zhao, Jianbo Lu, Yuzhong Peng"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21108v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 通过变分自编码器进行解耦多模态表示学习
tldr: 本研究针对分子属性预测中表征纠缠和多模态融合不充分的问题，提出了DMMRL框架。该方法利用变分自编码器将分子表征解耦为共享（结构相关）和私有（模态特定）潜空间，并通过正交与对齐正则化确保特征独立性。结合门控注意力融合模块，DMMRL能有效整合来自图、序列和几何结构的信息，在七个基准数据集上取得了领先的预测性能，显著增强了模型的可解释性与泛化能力。
selection_source: fresh_fetch
motivation: 现有分子属性预测模型往往存在表征纠缠且难以有效利用图、序列和几何等多模态间的互补信息。
method: 提出基于变分自编码器的解耦学习框架，将表征分为共享与私有空间，并采用正交正则化和门控注意力机制进行特征融合。
result: 在七个主流基准数据集上的实验验证了DMMRL相比于现有最先进方法具有更优越的预测性能。
conclusion: 通过解耦多模态表征并捕捉复杂的模态间依赖关系，DMMRL为药物发现和材料科学提供了更精准且具可解释性的预测工具。
---

## 摘要
分子性质预测是药物发现和材料科学的基石，需要能够解耦不同分子模态中复杂的结构-性质关系的模型。现有方法经常表现出纠缠的表示——将结构、化学和功能因素混为一谈——从而限制了可解释性和可迁移性。此外，传统方法未能充分利用来自图、序列和几何结构的互补信息，通常依赖于忽略模态间依赖关系的简单拼接。在这项工作中，我们提出了 DMMRL，它利用变分自编码器将分子表示解耦为共享（结构相关）和私有（模态特定）的潜空间，从而提高了可解释性和预测性能。所提出的变分解耦机制有效地分离了用于性质预测的最具信息量的特征，而正交性和对齐正则化则促进了统计独立性和跨模态一致性。此外，门控注意力融合模块自适应地整合共享表示，捕捉复杂的模态间关系。在七个基准数据集上的实验验证表明，DMMRL 的性能优于现有最先进的方法。本文的代码和数据可在 https://github.com/xulong0826/DMMRL 免费获取。

## Abstract
Molecular property prediction constitutes a cornerstone of drug discovery and materials science, necessitating models capable of disentangling complex structure-property relationships across diverse molecular modalities. Existing approaches frequently exhibit entangled representations--conflating structural, chemical, and functional factors--thereby limiting interpretability and transferability. Furthermore, conventional methods inadequately exploit complementary information from graphs, sequences, and geometries, often relying on naive concatenation that neglects inter-modal dependencies. In this work, we propose DMMRL, which employs variational autoencoders to disentangle molecular representations into shared (structure-relevant) and private (modality-specific) latent spaces, enhancing both interpretability and predictive performance. The proposed variational disentanglement mechanism effectively isolates the most informative features for property prediction, while orthogonality and alignment regularizations promote statistical independence and cross-modal consistency. Additionally, a gated attention fusion module adaptively integrates shared representations, capturing complex inter-modal relationships. Experimental validation across seven benchmark datasets demonstrates DMMRL's superior performance relative to state-of-the-art approaches. The code and data underlying this article are freely available at https://github.com/xulong0826/DMMRL.

---

## 论文详细总结（自动生成）

以下是对论文《DMMRL: Disentangled Multi-Modal Representation Learning via Variational Autoencoders for Molecular Property Prediction》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：分子性质预测（如溶解度、毒性、生物活性）在药物发现中至关重要，但现有深度学习模型面临两大挑战：
    1.  **表征纠缠（Representation Entanglement）**：结构、化学和功能因素在学习到的向量中混杂在一起，导致模型可解释性差且难以迁移。
    2.  **多模态整合不足**：分子拥有图（拓扑）、序列（SMILES）和几何（3D构象）等多种模态，现有方法多采用简单的拼接，忽略了模态间的复杂依赖和互补性。
*   **研究背景**：随着化学空间的指数级增长，亟需一种能解耦性质相关特征并有效融合多模态信息的稳健模型。

### 2. 方法论
DMMRL 框架的核心思想是利用**变分自编码器（VAE）**将分子表征分解为**共享潜空间（Shared Latent Space）**和**私有潜空间（Private Latent Space）**。
*   **多模态编码器**：
    *   **序列编码器**：双向 LSTM + Transformer，捕捉局部化学语境和长程依赖。
    *   **图编码器**：通信消息传递神经网络（CMPNN），提取拓扑连接特征。
    *   **几何编码器**：几何图神经网络，建模 3D 空间中的键长、键角和扭转角。
*   **解耦机制（关键技术）**：
    *   每个模态的特征被映射为两个分布：**共享空间 $z_{shared}$**（捕获跨模态一致的、与性质相关的结构信息）和**私有空间 $z_{private}$**（捕获模态特定的噪声或人工痕迹）。
    *   **信息瓶颈**：仅将共享表征传递给下游预测头，强制模型过滤无关信息。
*   **损失函数设计**：
    *   **变分解耦损失**：包括 KL 散度（正则化共享空间）和 MMD 损失（促进私有空间多样性）。
    *   **对齐损失（$L_{align}$）**：利用 InfoNCE 增强不同模态共享表征之间的一致性。
    *   **正交损失（$L_{ortho}$）**：通过惩罚共享与私有表征的内积，确保两者的统计独立性。
*   **门控注意力融合**：采用动态、上下文感知的门控机制，根据分子特征自适应地为不同模态分配权重，而非固定拼接。

### 3. 实验设计
*   **数据集**：选取了 MoleculeNet 中的 7 个基准数据集，涵盖分类（BACE, BBBP, ClinTox, Tox21）和回归（ESOL, FreeSolv, Lipo）任务。
*   **对比方法（Baselines）**：共对比了 12 种方法，包括：
    *   基于序列：RNN, Transformer。
    *   基于图：GCN, MPNN, GROVER, FP-GNN 等。
    *   多模态：GraSeq, MvMRL, SGGRL（最先进的 SOTA 方法）。
*   **评估指标**：分类任务使用 ROC-AUC，回归任务使用 RMSE。

### 4. 资源与算力
*   **硬件配置**：Intel Core i7-14700KF CPU，64GB 内存，**NVIDIA GeForce RTX 4060 Ti GPU (16GB VRAM)**。
*   **训练时长**：在上述配置下，完成所有数据集的完整训练大约需要 **2 天**。
*   **超参数**：Batch size 为 64，Epoch 为 200，使用 Adam 优化器和 Noam 学习率调度器。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同性质的数据集上进行了测试，每个实验重复 **10 次**（使用不同随机种子）并报告均值和标准差。
*   **消融实验**：设计了 LBL（仅预测）、BOT（加入变分瓶颈）和 ALL（完整 DMMRL）三组对比，验证了每个约束项的必要性。
*   **充分性评价**：实验设计较为充分，涵盖了不同规模（从 639 到 7831 个样本）和不同任务类型的分子数据，结果具有统计学意义。

### 6. 主要结论与发现
*   **性能领先**：DMMRL 在 5/7 个数据集上取得了最优性能，尤其在 BACE 和 FreeSolv 等结构多样且标注稀疏的数据集上提升显著。
*   **解耦的价值**：实验证明，即使不使用更复杂的编码器，仅通过解耦策略（分离共享与私有特征）就能显著提升预测精度。
*   **稳健性**：DMMRL 在多次运行中表现出较低的标准差，说明解耦表征增强了模型在不同化学骨架上的预测稳定性。

### 7. 优点（亮点）
*   **创新的解耦架构**：首次在分子多模态学习中引入 VAE 驱动的共享-私有空间分离，解决了特征混杂问题。
*   **严谨的正则化**：结合了 InfoNCE 对齐和正交性约束，从统计学角度保证了特征的纯净度。
*   **动态融合**：门控注意力机制比传统的拼接更符合化学直觉（即不同性质受不同模态信息主导）。

### 8. 不足与局限
*   **数据分割方式**：实验主要采用随机分割（Random Split），虽然是标准做法，但未能充分测试模型在**骨架分割（Scaffold Split）**下的外推泛化能力（论文在未来工作中提到了这一点）。
*   **领域知识融合**：目前的解耦过程主要依赖统计约束，尚未显式引入化学领域知识（如官能团规则）来引导解耦。
*   **计算开销**：由于涉及三个编码器和复杂的 VAE 结构，其训练成本（2天/4060Ti）对于超大规模筛选任务可能仍有优化空间。

（完）
