---
title: Solution for 10th Competition on Ambivalence/Hesitancy (AH) Video Recognition Challenge using Divergence-Based Multimodal Fusion
title_zh: 基于差异多模态融合的第十届矛盾/犹豫（AH）视频识别挑战赛解决方案
authors: "Aislan Gabriel O. Souza, Agostinho Freire, Leandro Honorato Silva, Igor Lucas B. da Silva, João Vinícius R. de Andrade, Gabriel C. de Albuquerque, Lucas Matheus da S. Oliveira, Mário Stela Guerra, Luciana Machado"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.16939v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 基于散度的多模态融合处理跨模态冲突
tldr: 本研究针对第10届ABAW竞赛中的矛盾/犹豫（A/H）视频识别挑战，提出了一种基于散度的多模态融合方案。该方法整合了视觉（AU）、音频（Wav2Vec 2.0）和文本（BERT）特征，利用BiLSTM与注意力机制提取时序信息，并通过计算模态间的绝对差异来捕捉表征矛盾心理的跨模态冲突。在BAH数据集上，该方法取得了0.6808的Macro F1分数，显著优于基准线，并揭示了AU的时间变异性是识别A/H的关键视觉特征。
selection_source: fresh_fetch
motivation: 旨在通过捕捉视觉、听觉和文本模态之间的不一致性，解决视频中复杂的矛盾与犹豫情绪识别难题。
method: 采用基于散度的多模态融合技术，通过计算各模态在共享嵌入空间中的成对绝对差异来显式建模跨模态冲突。
result: "在BAH数据集验证集上实现了0.6808的Macro F1得分，相比0.2827的挑战赛基准线提升了约140%。"
conclusion: 研究证实了跨模态冲突建模在情绪识别中的有效性，并确定了面部动作单元的时间变异性是区分矛盾状态的核心视觉指标。
---

## 摘要
我们针对 CVPR 2026 第十届 ABAW 竞赛中的矛盾/犹豫（A/H）视频识别挑战赛提出了解决方案。我们提出了一种基于差异的多模态融合方法，该方法显式地衡量了视觉、音频和文本通道之间的跨模态冲突。视觉特征被编码为通过 Py-Feat 提取的面部动作单元（AU），音频通过 Wav2Vec 2.0 编码，文本通过 BERT 编码。每个模态由带有注意力池化的双向长短期记忆网络（BiLSTM）处理，并投影到共享嵌入空间中。融合模块计算模态嵌入之间的成对绝对差异，直接捕捉表征 A/H 的不一致性。在 BAH 数据集上，我们的方法在验证测试集上达到了 0.6808 的 Macro F1 分数，优于 0.2827 的挑战赛基准。对 1,132 个视频的统计分析证实，AU 的时间变异性是 A/H 的主要视觉判别特征。

## Abstract
We address the Ambivalence/Hesitancy (A/H) Video Recognition Challenge at the 10th ABAW Competition (CVPR 2026). We propose a divergence-based multimodal fusion that explicitly measures cross-modal conflict between visual, audio, and textual channels. Visual features are encoded as Action Units (AUs) extracted via Py-Feat, audio via Wav2Vec 2.0, and text via BERT. Each modality is processed by a BiLSTM with attention pooling and projected into a shared embedding space. The fusion module computes pairwise absolute differences between modality embeddings, directly capturing the incongruence that characterizes A/H. On the BAH dataset, our approach achieves a Macro F1 of 0.6808 on the validation test set, outperforming the challenge baseline of 0.2827. Statistical analysis across 1{,}132 videos confirms that temporal variability of AUs is the dominant visual discriminator of A/H.

---

## 论文详细总结（自动生成）

以下是对论文《Solution for 10th Competition on Ambivalence/Hesitancy (AH) Video Recognition Challenge using Divergence-Based Multimodal Fusion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：识别视频中的“矛盾/犹豫”（Ambivalence/Hesitancy, A/H）情感状态。
*   **研究背景**：A/H 是一种复杂的心理状态，其特征是不同沟通渠道（视觉、听觉、文本）之间存在冲突或不一致。
*   **研究动机**：传统的多模态融合方法（如简单的特征拼接或后期融合）通常假设各模态是互补的，而忽略了模态间的“冲突”本身就是识别 A/H 的关键信号。作者希望通过显式建模跨模态的差异（Divergence）来提升识别精度。

### 2. 方法论
*   **核心思想**：通过计算不同模态嵌入向量之间的成对绝对差异，直接捕捉表征矛盾心理的“不一致性”。
*   **关键技术细节**：
    *   **特征提取**：
        *   **视觉**：使用 Py-Feat 提取 20 个面部动作单元（AU）。通过滑动窗口计算均值、标准差、斜率和范围，捕捉面部表情的动态变化。
        *   **音频**：使用预训练的 Wav2Vec 2.0 提取 768 维嵌入。
        *   **文本**：使用 BERT-base 提取 [CLS] 标记的嵌入，并微调最后两层。
    *   **时序建模**：每个模态分别输入一个 2 层双向 LSTM（BiLSTM），配合注意力池化（Attention Pooling）将序列投影到 128 维的共享嵌入空间。
    *   **融合策略（Fusion B）**：计算视觉-音频、视觉-文本、音频-文本三组嵌入向量之间的**成对绝对差异**（$|h'_1 - h'_2|$），将这些差异向量拼接后输入 3 层 MLP 进行分类。
    *   **训练优化**：使用带类别权重的交叉熵损失（BCEWithLogitsLoss），采用 AdamW 优化器和余弦退火学习率调度。

### 3. 实验设计
*   **数据集**：使用 BAH 数据集，包含 1,132 个视频（598 训练，107 验证，427 测试），涉及 239 名参与者。
*   **评估指标**：视频级 Macro F1 分数。
*   **对比方法**：
    *   **基准线**：挑战赛官方基准（Macro F1 = 0.2827）。
    *   **单模态**：分别评估视觉（XGBoost）、音频（LSTM）、文本（BERT）。
    *   **融合变体**：Fusion A（隐式拼接）、Fusion B（差异融合）、Fusion C（拼接+差异混合）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。仅提到了训练参数设置（如 30 个 epoch、早停机制等）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单模态与多模态的对比实验。
    *   针对三种不同的融合策略（拼接、差异、混合）进行了消融研究。
    *   对 1,132 个视频进行了统计学分析（Mann-Whitney U 检验），以验证 AU 特征的判别力。
*   **充分性评价**：实验设计较为客观，通过统计分析验证了特征选择的合理性。但由于数据集规模较小（训练集仅 598 个视频），实验结果可能存在一定的波动性。

### 6. 主要结论与发现
*   **性能提升**：提出的差异融合模型（Fusion B）在测试集上达到了 **0.6808** 的 Macro F1，远超官方基准（0.2827）。
*   **关键视觉特征**：统计分析表明，**AU 的时间变异性（标准差）**而非平均强度是区分 A/H 的主要视觉指标。这证实了 A/H 表现为面部表情的不稳定性。
*   **模态贡献**：音频是表现最强的单模态（F1 = 0.6141），而多模态融合通过引入视觉和文本信号进一步提升了性能。

### 7. 优点
*   **理论契合度高**：融合策略直接对应 A/H 的心理学定义（跨模态冲突），具有很强的解释性。
*   **特征工程精细**：不仅使用了深度学习特征，还结合了可解释的面部动作单元（AU）统计量，揭示了“面部不稳定性”这一重要发现。
*   **效果显著**：在极具挑战性的任务上实现了较基准线翻倍以上的性能提升。

### 8. 不足与局限
*   **数据量限制**：数据集规模较小，导致更复杂的特征表示在验证集上表现良好但在测试集上出现退化（过拟合风险）。
*   **时序对齐**：目前的方法在融合前分别对各模态进行池化，缺乏视觉和音频在细粒度时间尺度上的显式对齐。
*   **泛化性**：尚未在其他情感识别数据集上验证这种“差异融合”策略的通用性。

（完）
