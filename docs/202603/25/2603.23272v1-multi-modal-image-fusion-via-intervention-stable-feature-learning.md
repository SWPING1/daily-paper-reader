---
title: Multi-Modal Image Fusion via Intervention-Stable Feature Learning
title_zh: 基于干预稳定特征学习的多模态图像融合
authors: "Xue Wang, Zheng Guan, Wenhua Qian, Chengchao Wang, Runzhuo Ma"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23272v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 补偿模态中缺失信息的干预策略
tldr: 针对多模态图像融合中统计相关性易导致伪关联的问题，本文提出一种基于因果干预的特征学习框架。通过设计互补掩码、随机掩码和模态丢弃三种干预策略，并结合因果特征整合器（CFI）及自适应不变性门控，旨在提取在不同扰动下保持稳定的鲁棒跨模态依赖。实验证明该方法在多个基准测试和下游视觉任务中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 传统融合方法过度依赖统计相关性，容易捕获数据集中的伪关联，导致模型在分布偏移时鲁棒性不足。
method: 提出基于因果干预的框架，通过三种掩码策略探测模态关系，并利用因果特征整合器（CFI）识别干预稳定特征。
result: 实验结果表明，该方法在多个公开基准测试及下游视觉任务中均实现了最先进的性能。
conclusion: 通过因果干预学习稳定的跨模态依赖，能有效克服伪相关问题，显著提升多模态图像融合的质量与泛化性。
---

## 摘要
多模态图像融合将来自不同模态的互补信息整合到统一的表示中。当前的方法主要优化模态间的统计相关性，通常会捕捉到由数据集引起的伪相关，这些相关性在分布偏移下会发生退化。在本文中，我们受因果原理启发，提出了一种基于干预的框架，用于识别鲁棒的跨模态依赖关系。借鉴 Pearl 的因果层级理论，我们设计了三种原则性的干预策略来探测模态关系的不同方面：i) 具有空间不相交扰动的互补掩码，测试模态是否能真正补偿彼此缺失的信息；ii) 相同区域的随机掩码，识别在部分可观测性下仍保持信息量的特征子集；iii) 模态丢弃，评估每个模态不可替代的贡献。基于这些干预，我们引入了一种因果特征整合器（CFI），它通过自适应不变性门控，学习识别并优先处理在不同扰动模式下保持重要性的干预稳定特征，从而捕捉鲁棒的模态依赖关系而非伪相关。大量实验表明，我们的方法在公共基准测试和下游高级视觉任务上均达到了 SOTA 性能。

## Abstract
Multi-modal image fusion integrates complementary information from different modalities into a unified representation. Current methods predominantly optimize statistical correlations between modalities, often capturing dataset-induced spurious associations that degrade under distribution shifts. In this paper, we propose an intervention-based framework inspired by causal principles to identify robust cross-modal dependencies. Drawing insights from Pearl's causal hierarchy, we design three principled intervention strategies to probe different aspects of modal relationships: i) complementary masking with spatially disjoint perturbations tests whether modalities can genuinely compensate for each other's missing information, ii) random masking of identical regions identifies feature subsets that remain informative under partial observability, and iii) modality dropout evaluates the irreplaceable contribution of each modality. Based on these interventions, we introduce a Causal Feature Integrator (CFI) that learns to identify and prioritize intervention-stable features maintaining importance across different perturbation patterns through adaptive invariance gating, thereby capturing robust modal dependencies rather than spurious correlations. Extensive experiments demonstrate that our method achieves SOTA performance on both public benchmarks and downstream high-level vision tasks.

---

## 论文详细总结（自动生成）

### 论文总结：基于干预稳定特征学习的多模态图像融合

#### 1. 核心问题与整体含义（研究动机和背景）
多模态图像融合（MMIF）旨在整合不同传感器（如红外与可见光）的互补信息。当前主流深度学习方法主要基于**统计相关性（Association）**进行优化，即通过最大化输入与输出之间的统计依赖来学习融合规则。
*   **研究动机**：作者指出，单纯依赖相关性会导致模型捕获数据集中的**伪相关（Spurious Correlations）**。例如，如果训练集中热源总是出现在特定背景下，模型可能学会这种偶然的共现而非真正的物理互补性。当部署环境发生分布偏移（如光照变化、传感器退化）时，这些伪相关会失效，导致融合质量下降。
*   **核心目标**：借鉴 Pearl 的因果层级理论，将融合从“观察模式”提升到“干预模式”，通过主动扰动输入来识别真正鲁棒的跨模态依赖特征。

#### 2. 方法论：核心思想与关键技术
论文提出了一种基于因果干预的融合框架，核心在于通过主动“破坏”输入来测试特征的稳定性。

*   **核心思想**：真正重要的融合特征应在各种扰动下保持其重要性（干预不变性），而伪相关特征在扰动下会表现出不稳定性。
*   **三种原则性干预策略**：
    1.  **互补掩码（Complementary Masking）**：对两个模态进行空间不相交的遮挡，测试一模态是否能补偿另一模态缺失的信息，探测真正的跨模态补偿能力。
    2.  **随机掩码（Random Masking）**：对相同区域进行遮挡，识别在局部信息受损时依然稳健的特征子集。
    3.  **模态丢弃（Modality Dropout）**：完全移除某一模态，评估各模态的不可替代性，防止模型过度依赖单一模态。
*   **关键模块：因果特征整合器（CFI）**：
    *   利用**双向跨模态注意力**进行信息交换。
    *   引入**自适应不变性门控（Invariance Gating）**：通过学习一个门控权重，优先保留在干预实验中表现稳定的特征，抑制受扰动影响大的伪相关特征。
*   **损失函数**：
    *   $L = L_f + \alpha L_{inv} + \beta L_{nec}$
    *   $L_f$：融合忠实度损失（强度与梯度）。
    *   $L_{inv}$：干预一致性损失，强制门控选中的稳定区域在扰动前后保持一致。
    *   $L_{nec}$：模态必要性损失，确保每个模态都对最终结果有实质贡献。

#### 3. 实验设计
*   **数据集与场景**：
    *   **红外与可见光融合（IVIF）**：使用 MSRS、TNO、M3FD 三个主流基准数据集。
    *   **下游任务**：在 M3FD 上进行目标检测（YOLOv5），在 MSRS 上进行语义分割。
    *   **跨域泛化**：在 Harvard 医疗图像数据集上进行零样本（Zero-shot）迁移测试。
*   **对比方法**：对比了 9 种 SOTA 方法，包括 TIMFusion、Conti、SAGE、MUFusion、LUT-Fuse、LRRNet、IGNet、DCEvo 和 A2RNet。
*   **评价指标**：PSNR、SF（空间频率）、AG（平均梯度）、CC（相关系数）和 $Q_{abf}$。

#### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **NVIDIA GeForce RTX 4090 GPU** 的工作站。
*   **训练细节**：
    *   框架：PyTorch。
    *   训练时长/参数：训练了 50 个 epoch，batch size 为 16。
    *   优化器：Adam，学习率为 1e-4。
    *   图像处理：输入裁剪为 $256 \times 256$ 的补丁。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量和定性对比，涵盖了从基础融合指标到高级视觉任务（检测、分割）的全方位评估。
*   **消融实验**：详细验证了 CFI 模块、干预一致性损失 $L_{inv}$、模态必要性损失 $L_{nec}$ 的有效性。
*   **因果分析**：引入了 **ATE（平均处理效应）分析**，定量评估了不同干预策略对融合结果的具体影响。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集验证和跨任务（医疗图像）迁移，有力证明了该方法不仅在特定数据集上有效，且具有极强的泛化能力。

#### 6. 主要结论与发现
*   **干预学习的有效性**：通过干预策略训练的模型，在处理极端条件（如大雾、过曝光）时表现出更强的鲁棒性。
*   **克服伪相关**：实验证明，识别“干预稳定特征”能有效避免模型学习到数据集的统计偏差。
*   **下游任务增益**：融合质量的提升直接转化为目标检测（mAP 提升）和语义分割（mIoU 提升）的性能增长。
*   **零样本迁移能力**：在未见过的医疗图像上直接应用 IVIF 模型仍能取得 SOTA 效果，说明模型捕获的是通用的融合原理。

#### 7. 优点（亮点）
*   **理论创新**：首次将 Pearl 的因果干预思想系统性地引入多模态图像融合领域，突破了传统相关性学习的局限。
*   **策略科学**：设计的三种掩码策略（互补、随机、丢弃）逻辑严密，分别对应了模态关系的补偿性、充分性和必要性。
*   **可解释性**：通过不变性门控的权重可视化，直观展示了模型如何聚焦于稳健的特征区域。
*   **泛化性强**：跨传感器（红外到医疗）的成功迁移是该方法最大的亮点之一。

#### 8. 不足与局限
*   **超参数敏感性**：掩码的大小（如 $16 \times 16$）和数量（1 到 6 个）属于启发式设置，虽然文中做了参数分析，但在不同分辨率或极度复杂的场景下可能需要重新调整。
*   **计算开销**：虽然 CFI 采用了池化技术来降低注意力机制的复杂度，但相比于极轻量化的方法（如 LUT-Fuse），其在嵌入式设备上的实时性表现仍有待进一步验证。
*   **干预多样性**：目前的干预主要集中在空间掩码，未来可以探索更多形式的干预（如光谱扰动或噪声干预）以进一步增强鲁棒性。

（完）
