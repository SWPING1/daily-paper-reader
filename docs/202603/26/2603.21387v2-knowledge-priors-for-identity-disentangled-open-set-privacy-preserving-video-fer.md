---
title: Knowledge Priors for Identity-Disentangled Open-Set Privacy-Preserving Video FER
title_zh: 用于身份解耦开集隐私保护视频面部表情识别的知识先验
authors: "Feng Xu, Xun Li, Lars Petersson, Yulei Sui, David Ahmedt-Aristizabal, Dadong Wang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21387v2"
tags: ["query:missing-modality"]
score: 9.0
evidence: 视频面部表情识别的身份解耦表示
tldr: 针对视频面部表情识别（FER）中的身份隐私泄露问题，本文提出一种无需身份标签的开放集隐私保护框架。该框架利用视频内外的知识先验构建身份抑制网络，在匿名化身份的同时保留表情特征，并通过去噪模块恢复识别性能。此外，引入基于伪造的验证方法评估隐私鲁棒性。实验证明，该方法在保护隐私的同时，FER准确率可媲美有监督方法。
selection_source: fresh_fetch
motivation: 现有的隐私保护FER方法在缺乏身份标签的真实开放集视频场景下，难以有效解耦身份与表情信息并保护隐私。
method: 提出两阶段框架，利用视频知识先验训练身份抑制网络，并结合去噪模块恢复表情信息，同时引入基于识别先验的隐私验证方法。
result: 在三个视频数据集上的实验表明，该方法在无需身份标注的情况下，实现了与身份监督基准相当的表情识别精度。
conclusion: 本研究证明了利用知识先验在无监督开放集环境下实现高效、隐私安全的视频表情识别是切实可行的。
---

## 摘要
面部表情识别依赖于面部数据，这些数据本质上会暴露身份，从而引发严重的隐私担忧。当前的隐私保护方法在现实的开集视频场景中通常会失效，因为在这些场景中身份是未知的，且无法获得身份标签。我们针对具有挑战性的开集设置提出了一种基于视频的隐私保护面部表情识别（FER）两阶段框架，该框架在任何阶段都不需要身份标签。为了解耦隐私和效用，我们首先利用从无身份标签的真实视频中提取的视频内和视频间知识先验，训练了一个身份抑制网络。该网络在匿名化身份的同时保留了表情线索。随后的去噪模块恢复了与表情相关的信息，并有助于恢复面部表情识别性能。此外，我们引入了一种基于伪造的验证方法，该方法利用识别先验在不需要标注身份标签的情况下严格评估隐私鲁棒性。在三个视频数据集上的实验表明，我们的方法在有效保护隐私的同时，保持了与受身份监督的基准方法相当的面部表情识别准确率。

## Abstract
Facial expression recognition relies on facial data that inherently expose identity and thus raise significant privacy concerns. Current privacy-preserving methods typically fail in realistic open-set video settings where identities are unknown, and identity labels are unavailable. We propose a two-stage framework for video-based privacy-preserving FER in challenging open-set settings that requires no identity labels at any stage. To decouple privacy and utility, we first train an identity-suppression network using intra- and inter-video knowledge priors derived from real-world videos without identity labels. This network anonymizes identity while preserving expressive cues. A subsequent denoising module restores expression-related information and helps recover FER performance. Furthermore, we introduce a falsification-based validation method that uses recognition priors to rigorously evaluate privacy robustness without requiring annotated identity labels. Experiments on three video datasets demonstrate that our method effectively protects privacy while maintaining FER accuracy comparable to identity-supervised baselines.

---

## 论文详细总结（自动生成）

这篇论文针对视频面部表情识别（FER）中的隐私保护问题，提出了一种无需身份标签、适用于开集（Open-set）场景的新型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：面部表情识别（FER）在医疗、人机交互等领域应用广泛，但面部图像天然携带身份信息，存在严重的隐私泄露风险。
*   **核心挑战**：
    1.  **标签缺失**：现有隐私保护方法多为“闭集”设计，依赖训练集中的身份标签，而现实中往往无法获得身份标注。
    2.  **特征耦合**：身份信息与表情特征高度耦合，抑制身份往往会导致表情识别准确率大幅下降。
    3.  **验证困难**：在无身份标签的开集环境下，缺乏有效的量化指标来评估隐私保护的强度。
*   **整体含义**：本文旨在通过视频特有的时空先验知识，在不依赖身份标签的前提下，实现身份与表情的解耦，保护隐私的同时维持高精度的表情识别。

### 2. 方法论
该框架包含四个关键阶段：
*   **知识先验提取与数据集生成**：
    *   利用视频特性（如同一视频中同一位置的人脸具有身份连续性），通过人脸检测、对齐和跟踪生成**伪身份标签**。
    *   构建包含“锚点-正样本-负样本”的三元组：锚点和负样本来自同一视频（同身份），正样本来自具有相同表情标签但不同视频的样本（异身份、同表情）。
*   **身份解耦的隐私保护（$f_{pp}$）**：
    *   采用编码器-解码器结构。使用**三元组损失（Triplet Loss）**和**二元交叉熵损失（BCE）**进行训练。
    *   核心思想是让网络学习重构图像，使得重构后的图像在身份特征空间中无法被区分，但保留表情相关的视觉线索。
*   **去噪与表情识别训练（$f_{denoise}$）**：
    *   由于身份抑制可能导致表情扭曲，引入去噪模块。该模块在独立的数据集（如RAF-DB）上预训练，专门用于修复被匿名化处理后的表情特征。
*   **基于伪造的隐私验证（Falsification-based Validation）**：
    *   提出一种无需人工标注的评估协议。通过规则生成测试案例（如原始图 vs 匿名图），利用匹配模型 $f_{match}$ 判断是否为同一人。如果模型无法正确匹配（即判定为不同身份），则说明隐私保护成功。

### 3. 实验设计
*   **数据集**：
    *   **DFEW**：大规模野外视频表情数据集（开集场景，主要测试集）。
    *   **CREMA-D & RAVDESS**：受控环境下的视频数据集（用于闭集对比验证）。
    *   **RAF-DB**：用于去噪模块的预训练。
*   **基准模型（Backbones）**：采用 R(2+1)D 和 I3D 两种主流视频理解网络。
*   **对比方法**：
    1.  **高斯模糊（GB）**：传统图像处理方法。
    2.  **对抗性方法（Adver.）**：如 SPAct，基于自监督对抗学习。
    3.  **换脸技术（Face S.）**：如 MobileFaceSwap。
    4.  **频率控制法（Contr-HL）**：现有的基于频率分离的视频隐私保护方法（需身份标签）。

### 4. 资源与算力
*   **训练细节**：批大小（Batch Size）设为 1024（包含 341 个三元组），训练 400 个 Epoch。
*   **算力说明**：文中提到了“GPU 优化”，但**未明确说明具体的 GPU 型号、数量或总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了详尽测试，涵盖了从野外环境到受控环境的多种场景。
*   **消融实验**：
    *   测试了不同的人脸跟踪算法（MTCNN, SAMURAI）对结果的影响。
    *   分析了损失函数权重 $\alpha$ 的敏感度。
    *   验证了去噪模块 $f_{denoise}$ 对提升准确率的贡献（结果显示去噪后准确率提升了约 6-8%）。
*   **客观性**：通过 5 次随机种子运行取平均值，并提供了 95% 置信区间，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能领先**：在 DFEW 数据集上，该方法在隐私保护比率（$P_{pre}$）接近 96% 的情况下，FER 准确率显著优于模糊、换脸和对抗学习等开集基准方法。
*   **解耦有效性**：t-SNE 可视化证明，该方法能系统性地将身份嵌入向量移离原始分布，实现有效的身份匿名化。
*   **去噪必要性**：去噪模块不仅提升了识别精度，还意外地增强了隐私保护效果（通过进一步偏移身份特征）。

### 7. 优点（亮点）
*   **无需标签**：完全摆脱了对身份标注的依赖，极大地扩展了在真实世界未标注视频数据中的适用性。
*   **视频先验利用**：巧妙地将视频的“时空连续性”转化为训练约束，解决了开集环境下的身份定义问题。
*   **闭环验证**：不仅提出了保护方法，还配套设计了一套不依赖标签的隐私评估协议，填补了开集隐私评价的空白。

### 8. 不足与局限
*   **模型时效性**：实验使用的 FER 主干网络（R(2+1)D, I3D）相对较旧，未测试最新的视频 Transformer 模型。
*   **任务局限**：目前仅验证了表情识别任务，尚未探讨该框架在人体姿态估计等其他隐私敏感任务中的通用性。
*   **理论深度**：去噪模块为何能同时提升隐私性和效用的深层理论机制仍需进一步探讨。

（完）
