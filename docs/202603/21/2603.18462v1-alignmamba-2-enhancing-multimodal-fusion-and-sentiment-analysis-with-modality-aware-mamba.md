---
title: "AlignMamba-2: Enhancing Multimodal Fusion and Sentiment Analysis with Modality-Aware Mamba"
title_zh: AlignMamba-2：通过模态感知 Mamba 增强多模态融合与情感分析
authors: "Yan Li, Yifei Xing, Xiangyuan Lan, Xin Li, Haifeng Chen, Dongmei Jiang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18462v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 具有模态感知机制的多模态融合与情感分析
tldr: 针对多模态情感分析中Transformer计算复杂度高及Mamba全局对齐能力不足的问题，本文提出AlignMamba-2框架。该框架引入双重对齐策略，利用最优传输和最大均值差异增强模态间的一致性，并设计了包含专家混合机制（MoE）的模态感知Mamba层以处理数据异构性。实验证明，该模型在动态序列和静态图像任务上均达到SOTA水平，实现了性能与效率的平衡。
selection_source: fresh_fetch
motivation: 旨在解决Transformer处理长序列时的计算瓶颈，以及Mamba模型在捕捉非序列性全局跨模态关联方面的局限性。
method: 采用双重对齐策略优化模态间的几何与统计一致性，并结合模态感知Mamba层通过共享与特定专家架构显式处理异构数据融合。
result: 在CMU-MOSI、CMU-MOSEI等四个多模态基准测试中，AlignMamba-2在动态时间序列分析和静态图像分类任务上均取得了领先的性能。
conclusion: AlignMamba-2通过创新的对齐机制和专家混合架构，有效提升了多模态融合的效率与准确性，为情感分析任务提供了高效的通用解决方案。
---

## 摘要
在大规模预训练模型时代，将通用知识有效地迁移到特定的情感计算任务中仍然是一个挑战，特别是在计算效率和多模态异构性方面。虽然基于 Transformer 的方法在建模模态间依赖关系方面表现出色，但其二次方计算复杂度限制了它们在长序列数据中的应用。基于 Mamba 的模型已成为一种计算效率高的替代方案；然而，其固有的顺序扫描机制难以捕捉对于有效跨模态对齐至关重要的全局、非顺序关系。为了解决这些局限性，我们提出了 AlignMamba-2，这是一个用于多模态融合和情感分析的高效且有效的框架。我们的方法引入了一种双重对齐策略，利用最优传输（Optimal Transport）距离和最大均值差异（Maximum Mean Discrepancy）对模型进行正则化，在不增加推理时间开销的情况下，促进模态间的几何和统计一致性。更重要的是，我们设计了一个模态感知 Mamba 层，该层采用混合专家（Mixture-of-Experts）架构，包含模态特定专家和模态共享专家，以在融合过程中显式处理数据异构性。在四个具有挑战性的基准测试上进行的广泛实验，包括动态时间序列（CMU-MOSI 和 CMU-MOSEI 数据集）和静态图像相关任务（NYU-Depth V2 和 MVSA-Single 数据集），证明了 AlignMamba-2 在从动态时间序列分析到静态图文分类的各种模式识别任务中，在有效性和效率方面均达到了新的最先进水平（state-of-the-art）。

## Abstract
In the era of large-scale pre-trained models, effectively adapting general knowledge to specific affective computing tasks remains a challenge, particularly regarding computational efficiency and multimodal heterogeneity. While Transformer-based methods have excelled at modeling inter-modal dependencies, their quadratic computational complexity limits their use with long-sequence data. Mamba-based models have emerged as a computationally efficient alternative; however, their inherent sequential scanning mechanism struggles to capture the global, non-sequential relationships that are crucial for effective cross-modal alignment. To address these limitations, we propose \textbf{AlignMamba-2}, an effective and efficient framework for multimodal fusion and sentiment analysis. Our approach introduces a dual alignment strategy that regularizes the model using both Optimal Transport distance and Maximum Mean Discrepancy, promoting geometric and statistical consistency between modalities without incurring any inference-time overhead. More importantly, we design a Modality-Aware Mamba layer, which employs a Mixture-of-Experts architecture with modality-specific and modality-shared experts to explicitly handle data heterogeneity during the fusion process. Extensive experiments on four challenging benchmarks, including dynamic time-series (on the CMU-MOSI and CMU-MOSEI datasets) and static image-related tasks (on the NYU-Depth V2 and MVSA-Single datasets), demonstrate that AlignMamba-2 establishes a new state-of-the-art in both effectiveness and efficiency across diverse pattern recognition tasks, ranging from dynamic time-series analysis to static image-text classification.

---

## 论文详细总结（自动生成）

以下是对论文《AlignMamba-2: Enhancing Multimodal Fusion and Sentiment Analysis with Modality-Aware Mamba》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多模态情感分析中，如何在保持计算效率的同时，有效解决不同模态（如音、视、文）之间的**异构性差异**和**跨模态对齐**问题。
*   **研究背景**：
    *   **Transformer 的局限**：虽然擅长建模模态间依赖，但其自注意力机制具有二次方计算复杂度，难以处理长序列。
    *   **现有 Mamba 的不足**：Mamba 虽具有线性复杂度，但其顺序扫描机制难以捕捉非序列性的全局跨模态关联；且现有方法多将 Mamba 视为模态无关的处理器，忽略了模态特有的属性。
*   **研究动机**：在 AlignMamba-1 的基础上进一步优化，消除推理时的对齐开销，并增强模型对模态异构性的感知能力。

### 2. 方法论
AlignMamba-2 提出了一个双阶段增强框架：
*   **双重对齐策略（Dual Alignment Strategy）**：
    *   **局部几何对齐（OT Distance）**：利用**最优传输（Optimal Transport）**距离作为正则化损失，通过 Sinkhorn 算法寻找模态间的最小变换成本，实现细粒度的标记级对齐。
    *   **全局统计对齐（MMD）**：利用**最大均值差异（Maximum Mean Discrepancy）**匹配不同模态特征分布的高阶统计矩，确保全局属性的一致性。
    *   **关键点**：对齐仅作为训练时的损失函数（Regularization），**推理时无需计算对齐矩阵**，实现了零推理开销。
*   **模态感知 Mamba 层（Modality-Aware Mamba Layer）**：
    *   **核心架构**：引入**专家混合（MoE）**设计，将标准 Mamba 的线性投影层替换为由“模态特定专家”（针对音、视、文各一个）和“模态共享专家”组成的结构。
    *   **路由机制**：采用确定性路由，根据输入 Token 的来源模态分配给对应的专家，从而显式建模模态特有特征和跨模态共享模式。

### 3. 实验设计
*   **数据集**：
    *   **动态时间序列任务**：CMU-MOSI 和 CMU-MOSEI（多模态情感分析基准）。
    *   **静态图像相关任务**：NYU-Depth V2（RGB-D 场景识别）和 MVSA-Single（图文情感分类）。
*   **对比方法**：
    *   **Transformer 类**：MulT, MISA, Self-MM, GeminiFusion, EUAR 等。
    *   **Mamba 类**：Cobra, Sigma, AlignMamba-1 等。
*   **评估指标**：准确率（Acc）和 F1 分数。

### 4. 资源与算力
*   **硬件说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：论文详细对比了推理时的 GPU 显存占用和延迟。结果显示，在处理 100k 长度的序列时，AlignMamba-2 保持了线性增长的显存和时间开销，而 Transformer 模型在 20k 长度时即出现显存溢出（OOM）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流数据集上进行了主实验，验证了模型在动态和静态任务中的通用性。
    *   **消融实验**：分别验证了双重对齐损失、MoE 架构以及路由策略（确定性 vs 可学习）的有效性。
    *   **泛化性实验**：进行了跨数据集（MOSI 训练，MOSEI 测试）的零样本泛化评估。
    *   **超参数分析**：探讨了对齐损失权重（$\lambda_{OT}, \lambda_{MMD}$）对性能的影响。
*   **评价**：实验设计全面，涵盖了性能、效率、泛化性和定性案例分析，对比基准包含了最新的 SOTA 模型，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：AlignMamba-2 在所有四个基准数据集上均达到了 SOTA 水平，尤其在长序列和异构数据融合方面表现优异。
*   **效率优势**：相比 AlignMamba-1，新版本消除了推理时的对齐计算，显著提升了处理超长序列的能力（支持 100k+ Token）。
*   **模态感知的重要性**：实验证明，确定性的模态专家路由比通用的可学习路由更适合多模态融合任务，能更好地解耦模态特有信息。

### 7. 优点
*   **高效对齐**：通过将 OT 和 MMD 转化为训练约束，巧妙解决了 Mamba 全局对齐能力弱的问题，且不增加推理负担。
*   **架构创新**：将 MoE 引入 Mamba 投影层，为处理多模态异构性提供了新的思路。
*   **通用性强**：不仅适用于视频/音频序列，也能有效处理 RGB-D 和图文等静态融合任务。

### 8. 不足与局限
*   **训练开销**：虽然推理高效，但训练阶段计算 OT 距离和 MMD 损失会增加额外的计算负担和内存占用。
*   **硬件细节缺失**：未提供具体的训练环境参数，不便于其他研究者评估复现所需的算力成本。
*   **对齐指标的线性化**：论文最后提到，未来仍需探索具有线性复杂度的对齐指标，以进一步优化训练效率。

（完）
