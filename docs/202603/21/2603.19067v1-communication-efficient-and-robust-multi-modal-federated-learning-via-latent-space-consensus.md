---
title: Communication-Efficient and Robust Multi-Modal Federated Learning via Latent-Space Consensus
title_zh: 通过潜在空间共识实现通信高效且鲁棒的多模态联邦学习
authors: "Mohamed Badi, Chaouki Ben Issaid, Mehdi Bennis"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19067v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 联邦学习中对异构模态和异常值的鲁棒性
tldr: 针对多模态联邦学习中客户端模态异构、架构差异及通信开销大的挑战，本文提出CoMFed框架。该框架通过可学习的投影矩阵生成压缩的潜空间表示，并利用潜空间正则化器实现跨客户端的特征对齐。实验证明，CoMFed在人体活动识别任务中实现了高精度、低开销且对异常值具有鲁棒性，为分布式多模态学习提供了高效的对齐方案。
selection_source: fresh_fetch
motivation: 解决多模态联邦学习中由于客户端模态异构和模型架构差异导致的特征空间对齐困难及高通信成本问题。
method: 提出CoMFed框架，利用可学习投影矩阵生成压缩潜表示，并通过潜空间正则化器促进跨客户端的模态一致性。
result: 在人体活动识别基准测试中，该方法以极低的通信开销实现了具有竞争力的分类准确率和良好的鲁棒性。
conclusion: CoMFed为异构多模态联邦学习提供了一种通信高效、鲁棒且能有效处理模型异构性的特征对齐解决方案。
---

## 摘要
联邦学习 (FL) 能够在不共享原始数据的情况下，在分布式设备之间实现协作模型训练，但将 FL 应用于多模态场景会带来重大挑战。客户端通常拥有异构的模态和模型架构，这使得在保护隐私和最小化通信成本的同时，难以高效地对齐特征空间。为了解决这一问题，我们提出了 CoMFed，这是一个通信高效的多模态联邦学习框架，它使用可学习的投影矩阵来生成压缩的潜在表示。潜在空间正则化器在客户端之间对齐这些表示，从而提高了跨模态一致性和对离群值的鲁棒性。在人类活动识别基准上的实验表明，CoMFed 以极小的开销实现了具有竞争力的准确率。

## Abstract
Federated learning (FL) enables collaborative model training across distributed devices without sharing raw data, but applying FL to multi-modal settings introduces significant challenges. Clients typically possess heterogeneous modalities and model architectures, making it difficult to align feature spaces efficiently while preserving privacy and minimizing communication costs. To address this, we introduce CoMFed, a Communication-Efficient Multi-Modal Federated Learning framework that uses learnable projection matrices to generate compressed latent representations. A latent-space regularizer aligns these representations across clients, improving cross-modal consistency and robustness to outliers. Experiments on human activity recognition benchmarks show that CoMFed achieves competitive accuracy with minimal overhead.

---

## 论文详细总结（自动生成）

以下是对论文《Communication-Efficient and Robust Multi-Modal Federated Learning via Latent-Space Consensus》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**模态异构**（不同客户端拥有不同传感器）、**架构异构**（模型结构不同）且**通信受限**的分布式环境中，实现高效且鲁棒的多模态联邦学习（FL）。
*   **研究背景**：
    *   传统 FL 依赖参数平均，要求模型架构一致，无法处理多模态异构性。
    *   现有异构 FL 方法（如 FedMD）通常依赖公共数据集进行知识蒸馏，但在隐私敏感或资源受限的边缘场景中，公共数据往往不可得。
    *   多模态模型通常参数量巨大，直接交换模型参数会带来沉重的通信负担。

### 2. 方法论
*   **核心思想**：放弃交换模型参数，转而交换**类级别的低维潜在空间统计量**。通过学习“翻译器”（投影矩阵）将不同架构的本地特征映射到统一的潜在空间进行对齐。
*   **关键技术细节**：
    *   **潜在空间投影**：每个客户端 $i$ 学习一个本地投影矩阵 $P_i$，将高维特征 $v_i$ 映射到低维共享潜在空间 $R^d$（$d$ 远小于特征维度）。
    *   **潜在共识正则化器**：在本地损失函数中加入正则项，惩罚本地投影表示与邻居（或全局）共识表示之间的差异。
    *   **鲁棒对齐机制**：引入基于**几何中位数（Geometric Median）**的距离函数 $\phi$。相比算术平均，几何中位数能有效过滤掉来自异常值或拜占庭攻击者的恶意表示。
    *   **算法流程（CoMFed）**：
        1.  **本地更新**：客户端根据本地任务损失和潜在空间正则项更新模型参数 $w_i$。
        2.  **表示压缩与交换**：客户端计算每个类别的平均潜在表示 $u_{i,m}$ 并上传/交换。
        3.  **共识计算**：计算类级别的共识目标（均值或几何中位数）。
        4.  **投影矩阵更新**：客户端更新 $P_i$ 以缩小本地特征与共识目标之间的差距。

### 3. 实验设计
*   **数据集与场景**：
    1.  **USC-HAD（人体活动识别）**：包含加速度计（ACC）和陀螺仪（GYR）模态，模拟 14 个客户端的异构模态分布。
    2.  **DeepSense（遮挡时长预测）**：包含毫米波（mmWave）和 LiDAR 模态，模拟 6 个具有强模态差异的部署场景。
*   **对比基准（Benchmarks）**：
    *   **Harmony (UniFL)**：按模态组合划分客户端进行训练。
    *   **FedMD**：基于公共数据集的异构联邦蒸馏方法。
    *   **FedIoT**：利用自编码器和公共数据集的方法。
    *   **Centralized**：数据集中训练的理想上限。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **训练配置**：提到学习率为 $10^{-3}$，总迭代次数为 250 次，CoMFed 的投影维度 $d$ 在实验中分别测试了 8 到 64 维。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同领域（传感器、无线通信）的真实数据集上进行了验证。
    *   进行了**消融实验**，分析了投影维度 $d$ 对准确率与通信成本的权衡。
    *   进行了**鲁棒性实验**，模拟了不同数量的拜占庭客户端攻击。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还重点量化了通信开销（KB 级 vs GB 级），并验证了在无公共数据情况下的性能，对比实验具有较强的说服力和公平性。

### 6. 主要结论与发现
*   **高性能与低开销**：CoMFed 在准确率上达到或超过了现有基准，同时通信开销降低了数个数量级（例如在 USC 数据集上，达到 50% 准确率仅需 410 KB，而 Harmony 需要 9.57 GB）。
*   **无需公共数据**：证明了通过潜在空间对齐，可以在不依赖外部公共数据集的情况下实现有效的跨模态协作。
*   **强鲁棒性**：采用几何中位数的 CoMFed 变体在存在恶意攻击者的情况下，性能下降远小于传统均值方法。

### 7. 优点
*   **通信极度高效**：交换的信息量仅与类别数和投影维度相关，与模型参数量解耦，非常适合大规模模型。
*   **架构与模态无关**：支持完全异构的本地网络结构，解决了多模态 FL 中的核心兼容性问题。
*   **隐私与安全兼顾**：低维潜在表示比原始梯度更难逆向工程，且内置了抗攻击机制。

### 8. 不足与局限
*   **类级别信息的局限性**：仅交换类均值可能忽略了样本分布的方差和细粒度特征，在极度非独立同分布（Non-IID）的数据下可能存在性能瓶颈。
*   **同步性要求**：算法假设客户端同步通信，未探讨在异步或高丢包率网络环境下的表现。
*   **超参数依赖**：正则化权重 $\lambda$ 和投影维度 $d$ 对结果有影响，实际应用中可能需要针对不同任务进行调优。

（完）
