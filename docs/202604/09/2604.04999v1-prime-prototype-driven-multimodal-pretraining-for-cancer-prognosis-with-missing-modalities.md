---
title: "PRIME: Prototype-Driven Multimodal Pretraining for Cancer Prognosis with Missing Modalities"
title_zh: PRIME：针对模态缺失癌症预后的原型驱动多模态预训练
authors: "Kai Yu, Shuang Zhou, Yiran Song, Zaifu Zhan, Jie Peng, Kaixiong Zhou, Tianlong Chen, Feng Xie, Meng Wang, Huazhu Fu, Mingquan Lin, Rui Zhang"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04999v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 缺失感知的多模态预训练与潜空间语义插补
tldr: 针对癌症预后分析中多模态数据（病理图像、基因表达、病理报告）常面临缺失的问题，本文提出PRIME预训练框架。该框架通过共享原型记忆库在潜空间进行语义补全，并结合模态对齐与融合一致性目标，在不依赖完整数据的情况下学习鲁棒表征。在TCGA多项任务中表现优异，显著提升了模型在数据缺失场景下的预测性能和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04999v1/fig-001.webp\", \"caption\": \"TABLE II TASK-1 OVERALL SURVIVAL PERFORMANCE (C-INDEX) ON FIVE CANCER COHORTS. VALUES ARE MEAN±STD OVER 5-FOLD CV WITHIN EACH COHORT; AVG DENOTES THE MEAN±STD ACROSS COHORTS. BEST AND SECOND-BEST RESULTS ARE HIGHLIGHTED IN BOLD AND UNDERLINE.\", \"page\": 6, \"index\": 1, \"width\": 998, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2604.04999v1/fig-002.webp\", \"caption\": \"TABLE III AVERAGE AUROC ACROSS FIVE CANCER COHORTS FOR TASK-2 (3-YEAR MORTALITY) AND TASK-3 (3-YEAR RECURRENCE). VALUES ARE MEAN±STD ACROSS COHORTS. BEST AND SECOND-BEST METHODS ARE\", \"page\": 7, \"index\": 2, \"width\": 379, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2604.04999v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of the proposed PRIME framework for multimodal cancer prognosis with missing modalities. Left (Sample Pre-processing): Frozen encoders extract token embeddings from WSIs, RNA-seq, and pathology reports. Middle (Missing Modality Imputation): In a unified token space, a shared sequence-level prototype memory bank imputes missing modalities via patient-level consensus retrieval, followed by modality-specific refinement. Right (Missing-aware Pretraining): Two complementary objectives, inter-modality contrastive alignment and post-fusion consistency under modality/token dropout with prototype mixtures, jointly drive self-supervised pretraining.\", \"page\": 3, \"index\": 3, \"width\": 1076, \"height\": 483}, {\"url\": \"assets/figures/arxiv/2604.04999v1/fig-004.webp\", \"caption\": \"TABLE V ABLATION ON KEY COMPONENTS OF OUR PRETRAINING FRAMEWORK. ALL VARIANTS USE THE SAME DOWNSTREAM PROTOCOL (PRETRAINED + LP) AND ARE EVALUATED ON THE FIVE-COHORT AVERAGE (MEAN±STD OVER 5-FOLD CV). FULL METHOD USES MISSING-AWARE PRETRAINING, PROTOTYPES, AND THE COMBINED OBJECTIVE L = λLalign + (1− λ)Lfusion . “W/O Lalign” CORRESPONDS TO λ = 0, AND “W/O Lfusion” CORRESPONDS TO λ = 1.\", \"page\": 9, \"index\": 4, \"width\": 948, \"height\": 217}]"
motivation: 临床多模态数据常存在严重的模态缺失，限制了现有预训练方法在碎片化数据集上的应用。
method: 提出一种基于原型的缺失感知框架，利用共享记忆库进行潜空间语义插值，并通过模态对齐和结构化缺失增强下的融合一致性进行自监督学习。
result: 在TCGA的32种癌症预后任务中，PRIME在生存预测、死亡率和复发分类上均取得最优性能，且在测试时模态缺失情况下表现出极强的鲁棒性。
conclusion: PRIME证明了缺失感知预训练是处理碎片化临床数据、提升癌症预后建模效果的一种实用且高效的策略。
---

## 摘要
多模态自监督预训练通过整合组织病理学全切片图像、基因表达和病理报告，为癌症预后提供了一条极具前景的途径，但现有的大多数方法都要求输入完全配对且完整。在实践中，临床队列往往是碎片化的，经常缺失一个或多个模态，这限制了监督融合和可扩展的多模态预训练。我们提出了 PRIME，这是一个感知缺失的多模态自监督预训练框架，能够从部分观测的队列中学习鲁棒且可迁移的表示。PRIME 将异构模态嵌入映射到统一的 token 空间，并引入共享原型记忆库，通过患者级共识检索进行隐空间语义插补，在不重建原始信号的情况下生成结构对齐的 token。两个互补的预训练目标——模态间对齐和结构化缺失增强下的融合后一致性，共同学习在任意模态子集下仍保持预测能力的表示。我们在癌症基因组图谱（TCGA）上评估了 PRIME，在 32 种癌症类型上进行了无标签预训练，并在五个队列上针对总生存期预测、3 年死亡率分类和 3 年复发分类进行了下游五折交叉验证。PRIME 在所有对比方法中取得了最佳的宏平均性能，在三项任务中分别达到了 0.653 的 C-index、0.689 的 AUROC 和 0.637 的 AUROC，同时提高了测试时缺失情况下的鲁棒性，并支持参数高效和标签高效的适配。这些结果表明，感知缺失的多模态预训练是碎片化临床数据环境下预后建模的一种实用策略。

## Abstract
Multimodal self-supervised pretraining offers a promising route to cancer prognosis by integrating histopathology whole-slide images, gene expression, and pathology reports, yet most existing approaches require fully paired and complete inputs. In practice, clinical cohorts are fragmented and often miss one or more modalities, limiting both supervised fusion and scalable multimodal pretraining. We propose PRIME, a missing-aware multimodal self-supervised pretraining framework that learns robust and transferable representations from partially observed cohorts. PRIME maps heterogeneous modality embeddings into a unified token space and introduces a shared prototype memory bank for latent-space semantic imputation via patient-level consensus retrieval, producing structurally aligned tokens without reconstructing raw signals. Two complementary pretraining objectives: inter-modality alignment and post-fusion consistency under structured missingness augmentation, jointly learn representations that remain predictive under arbitrary modality subsets. We evaluate PRIME on The Cancer Genome Atlas with label-free pretraining on 32 cancer types and downstream 5-fold evaluation on five cohorts across overall survival prediction, 3-year mortality classification, and 3-year recurrence classification. PRIME achieves the best macro-average performance among all compared methods, reaching 0.653 C-index, 0.689 AUROC, and 0.637 AUROC on the three tasks, respectively, while improving robustness under test-time missingness and supporting parameter-efficient and label-efficient adaptation. These results support missing-aware multimodal pretraining as a practical strategy for prognosis modeling in fragmented clinical data settings.

---

## 论文详细总结（自动生成）

### 论文总结：PRIME——针对模态缺失癌症预后的原型驱动多模态预训练

#### 1. 核心问题与整体含义（研究动机和背景）
癌症预后分析依赖于多源异构数据（如病理图像 WSI、基因表达 RNA-seq 和病理报告 Text）。然而，在临床实践中，由于成本、检测条件或记录不全，多模态数据往往是**碎片化**的，即大量患者缺失一个或多个模态。
*   **现有挑战**：传统的多模态预训练方法通常假设输入是完整且配对的，导致无法利用仅有部分模态的大规模队列；同时，在推理阶段遇到模态缺失时，模型性能往往会剧烈下降。
*   **研究目标**：开发一种“缺失感知”的预训练框架，能够利用不完整的临床数据学习鲁棒的跨模态表征，并支持在任意模态组合下的可靠预测。

#### 2. 方法论：核心思想与关键技术
PRIME 框架的核心在于**隐空间语义插补**和**双重对比学习目标**：
*   **统一 Token 空间**：使用查询式交叉注意力（Query-based Cross-Attention）将异构模态（图像、基因、文本）映射为固定长度（$T_q=128$）的统一语义 Token。
*   **共享原型记忆库（Prototype Memory Bank）**：
    *   引入一组可学习的原型序列作为“语义代码本”。
    *   **患者级共识检索**：根据患者现有的模态计算与原型的相似度，生成共识分布。
    *   **隐空间插补**：对于缺失模态，直接从原型库中根据共识分布加权合成 Token，而非重建原始高维信号，从而避免了生成式插补的“幻觉”风险。
*   **模态感知 MoE 骨干网络**：采用稀疏专家混合（MoE）架构，通过模态索引嵌入引导 Token 路由，平衡模态特异性与跨模态交互。
*   **预训练目标**：
    1.  **模态间对齐（$L_{align}$）**：在配对的模态子集上进行 InfoNCE 对齐。
    2.  **融合后一致性（$L_{fusion}$）**：通过 Dirichlet 驱动的随机增强（模态/Token 丢弃后用原型混合物填充），强制模型在不同缺失视图下保持表征一致。

#### 3. 实验设计
*   **数据集**：使用 TCGA（癌症基因组图谱）数据。
    *   **预训练**：涵盖 32 种癌症类型，共 10,439 名患者（包含完整和部分观测样本）。
    *   **下游评估**：在 UCEC、LUAD、LGG、BRCA、BLCA 五个队列上进行五折交叉验证。
*   **任务（Benchmark）**：
    1.  总生存期（OS）预测（C-index）。
    2.  3 年死亡率分类（AUROC）。
    3.  3 年复发分类（AUROC）。
*   **对比方法**：
    *   **单模态**：ABMIL（图像）、SNN（基因）、Text-only。
    *   **多模态融合**：Early/Late Fusion、Cross-Attention、TensorFusion、MAGGate、MulT。
    *   **专用预后模型**：MCAT、Porpoise、PathOmics 等。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A100-SXM4-40GB GPU**。
*   **训练细节**：
    *   预训练：AdamW 优化器，学习率 1e-5，Batch Size 64，训练 200 个 Epoch。
    *   下游微调：Batch Size 16，训练 50 个 Epoch。
*   **时长**：文中未明确给出总训练时长，但提到了预训练是在 32 种癌症的联合队列上进行的，具有一定的计算规模。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的五折交叉验证，涵盖了 5 种不同的癌症类型和 3 种临床终点，实验组数较多。
*   **消融实验**：系统地验证了原型库、缺失感知预训练、对齐损失和融合损失的贡献。
*   **鲁棒性测试**：专门设计了测试时模态缺失（Leave-one-out 和 Only-one）的实验，验证模型在极端情况下的表现。
*   **公平性**：所有对比方法均使用相同的预计算特征嵌入和下游训练协议，确保了比较的客观性。

#### 6. 主要结论与发现
*   **性能领先**：PRIME 在三项任务的宏平均指标上均取得最优（C-index 0.653, AUROC 0.689/0.637）。
*   **参数高效**：预训练后的**线性探测（Linear Probing）**性能往往优于全参数微调，说明预训练捕捉到了极强的跨模态结构。
*   **缺失鲁棒性**：在测试时缺失模态的情况下，PRIME 的性能下降远小于传统融合方法，尤其在仅有单一模态输入时优势显著。
*   **标签效率**：在减少下游标注数据量（如仅用 50% 标签）时，PRIME 表现出更强的韧性。

#### 7. 优点（亮点）
*   **原型驱动的插补**：巧妙地利用共享原型库在隐空间解决缺失问题，既实现了结构对齐，又避免了复杂的生成任务。
*   **缺失感知设计**：将缺失视为数据的结构属性而非异常，通过 Dirichlet 增强提升了模型在碎片化数据上的泛化力。
*   **实用性强**：支持线性探测，降低了下游任务的适配成本，适合临床小样本场景。

#### 8. 不足与局限
*   **外部验证缺失**：实验主要集中在 TCGA 内部划分，缺乏独立外部队列（External Validation）来验证其在不同医疗机构间的迁移能力。
*   **特征依赖**：模型依赖于预先冻结的编码器（如 UNI, BioClinicalBERT），如果底层编码器存在偏差，PRIME 难以通过端到端训练完全纠正。
*   **计算复杂度**：虽然推理时高效，但 MoE 架构和原型检索在超大规模部署时可能增加一定的内存开销。

（完）
