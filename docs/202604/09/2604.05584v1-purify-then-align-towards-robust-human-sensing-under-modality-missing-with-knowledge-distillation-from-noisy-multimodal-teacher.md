---
title: "Purify-then-Align: Towards Robust Human Sensing under Modality Missing with Knowledge Distillation from Noisy Multimodal Teacher"
title_zh: Purify-then-Align：利用噪声多模态教师知识蒸馏实现模态缺失下的鲁棒人体感知
authors: "Pengcheng Weng, Yanyu Qian, Yangxin Xu, Fei Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05584v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 模态缺失下的鲁棒人体感知与知识扩散
tldr: 针对多模态人体感知中模态缺失及低质量模态带来的“表示鸿沟”和“污染效应”，本文提出PTA（先净化后对齐）框架。该框架结合元学习与知识扩散技术，首先通过元学习动态降低噪声模态权重以净化知识源，随后利用扩散模型驱动的知识蒸馏，将净化后的多模态教师知识传递给单模态编码器。实验证明，PTA在多个大型数据集上显著提升了单模态模型在复杂缺失场景下的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05584v1/fig-001.webp\", \"caption\": \"Table 1. MPJPE (mm) and PA-MPJPE (mm) on MM-Fi [46] dataset.\", \"page\": 7, \"index\": 1, \"width\": 852, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.05584v1/fig-002.webp\", \"caption\": \"Figure 1. The overall architecture of our proposed PTA framework, built on a Purify-then-Align paradigm. The model is trained in a nested loop: (a) The Purify Stage: The Purify Stage optimizes the meta-learning weights w to mitigate the Contamination Effect. (b) The Align Stage: The Align Stage uses these weights w to construct a “clean teacher” fT and then uses it in a diffusion-based process (LDiffKD) to refine each “student” feature fS , thereby bridging the Representation Gap.\", \"page\": 3, \"index\": 2, \"width\": 1036, \"height\": 360}]"
motivation: 旨在解决多模态感知中异构数据间的表示差异以及低质量模态对整体性能的污染问题。
method: 提出PTA框架，利用元学习权重机制净化噪声模态，并结合扩散模型进行跨模态知识蒸馏。
result: 在MM-Fi和XRF55数据集上达到了SOTA性能，显著增强了单模态编码器在模态缺失时的鲁棒性。
conclusion: 通过“先净化后对齐”的策略，可以有效利用含噪声的多模态数据训练出具备跨模态知识的高性能单模态模型。
---

## 摘要
鲁棒的多模态人体感知必须克服模态缺失这一关键挑战。两个主要障碍是异构数据之间的表示鸿沟（Representation Gap）以及低质量模态带来的污染效应（Contamination Effect）。这些障碍具有因果联系，因为污染引入的损坏从根本上阻碍了表示差异的缩小。在本文中，我们提出了 PTA，一种新颖的“先净化后对齐”（Purify-then-Align）框架，通过元学习与知识扩散的协同整合来解决这种因果依赖。为了净化知识源，PTA 首先采用了一种元学习驱动的加权机制，动态学习降低噪声及低贡献模态的影响权重。随后，为了对齐不同模态，PTA 引入了一种基于扩散的知识蒸馏范式，其中由净化后的共识形成的信息丰富的清洁教师负责优化每个学生模态的特征。这种“先净化后对齐”策略的最终成果是创建了注入跨模态知识的极其强大的单模态编码器。在大规模 MM-Fi 和 XRF55 数据集上进行的全面实验表明，在显著的表示鸿沟和污染效应下，PTA 达到了最先进的性能，并显著提高了单模态模型在各种模态缺失场景下的鲁棒性。

## Abstract
Robust multimodal human sensing must overcome the critical challenge of missing modalities. Two principal barriers are the Representation Gap between heterogeneous data and the Contamination Effect from low-quality modalities. These barriers are causally linked, as the corruption introduced by contamination fundamentally impedes the reduction of representation disparities. In this paper, we propose PTA, a novel "Purify-then-Align" framework that solves this causal dependency through a synergistic integration of meta-learning and knowledge diffusion. To purify the knowledge source, PTA first employs a meta-learning-driven weighting mechanism that dynamically learns to down-weight the influence of noisy, low-contributing modalities. Subsequently, to align different modalities, PTA introduces a diffusion-based knowledge distillation paradigm in which an information-rich clean teacher, formed from this purified consensus, refines the features of each student modality. The ultimate payoff of this "Purify-then-Align" strategy is the creation of exceptionally powerful single-modality encoders imbued with cross-modal knowledge. Comprehensive experiments on the large-scale MM-Fi and XRF55 datasets, under pronounced Representation Gap and Contamination Effect, demonstrate that PTA achieves state-of-the-art performance and significantly improves the robustness of single-modality models in diverse missing-modality scenarios.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **PTA (Purify-then-Align)** 的框架，旨在解决多模态人体感知在模态缺失（如传感器故障、环境干扰）情况下的鲁棒性问题。以下是对该论文的深度结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：多模态人体感知（如结合深度相机、LiDAR、Wi-Fi信号）在实际应用中常面临**模态缺失**。
*   **两大障碍**：
    1.  **表示鸿沟 (Representation Gap)**：不同传感器（如像素 vs. 稀疏点云 vs. 无线信号）的物理原理迥异，特征难以对齐。
    2.  **污染效应 (Contamination Effect)**：低质量或高噪声的模态在融合时会“污染”高质量模态，导致整体性能下降。
*   **研究动机**：作者发现这两者存在**因果联系**——噪声污染会阻碍表示鸿沟的缩小。因此，必须先“净化”知识源，才能有效地进行跨模态“对齐”。

### 2. 方法论
PTA 框架采用“先净化、后对齐”的协同范式：
*   **净化阶段 (Purify Stage)**：
    *   **核心思想**：利用**元学习 (Meta-Learning)** 动态学习各模态的权重。
    *   **技术细节**：采用嵌套优化策略。内层循环在训练集上优化模型参数；外层循环在验证集上根据性能表现更新模态权重 $w$。这使得模型能自动识别并降低噪声、低贡献模态的权重，从而构建一个“清洁”的教师表示。
*   **对齐阶段 (Align Stage)**：
    *   **核心思想**：基于**扩散模型 (Diffusion Model)** 的知识蒸馏。
    *   **技术细节**：
        *   将净化后的多模态融合特征作为“教师”，单模态特征作为“学生”。
        *   **噪声适配器 (Noise Adapter)**：预测一个系数 $\gamma$，将学生特征与高斯噪声融合，作为扩散过程的起点。
        *   **轻量化设计**：在压缩的潜空间（Latent Space）进行操作，并使用 **DDIM 采样策略**（仅需 5 步推理），大幅提升效率。
        *   **损失函数**：结合了扩散损失 ($L_{Diff}$) 和特征距离蒸馏损失 ($L_{KD}$)。

### 3. 实验设计
*   **数据集**：
    1.  **MM-Fi**：大型人体姿态估计（HPE）数据集，包含深度图、LiDAR 点云和 Wi-Fi CSI 信号。
    2.  **XRF55**：人体动作识别（HAR）数据集，包含毫米波雷达、Wi-Fi 和 RFID 信号。
*   **Benchmark 与对比方法**：
    *   **Base.1/Base.2**：特征级融合和决策级融合基准。
    *   **X-Fi**：目前最先进的（SOTA）模态不变基础模型。
*   **实验场景**：测试了所有可能的模态组合（单模态、双模态、全模态），模拟各种缺失情况。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU**。
*   **参数设置**：MM-Fi 批大小为 16，XRF55 批大小为 32。
*   **训练时长**：文中未明确给出具体的训练总时长，但强调了其扩散模块的轻量化设计（1x1 卷积、Bottleneck 结构）和 DDIM 的 5 步快速采样，暗示了推理开销较低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个任务（姿态估计、动作识别）上进行了全面对比。
    *   针对 MM-Fi 做了 7 种模态组合的 MPJPE 和 PA-MPJPE 测试。
    *   针对 XRF55 做了 7 种模态组合的准确率测试。
*   **消融实验**：分别去除了“扩散对齐模块”和“元学习权重模块”，验证了两者协同工作的必要性。
*   **充分性评价**：实验设计覆盖了从高分辨率（LiDAR）到极低分辨率（RFID/Wi-Fi）的异构模态，对比了最新的 SOTA 方法，实验结果详实，具有较强的说服力。

### 6. 主要结论与发现
*   **单模态能力飞跃**：通过从净化后的教师那里学习，单模态编码器的性能大幅提升（如 MM-Fi 中 LiDAR 单模态性能提升 47.5%）。
*   **成功抑制污染**：元学习机制能有效识别弱势模态（如 Wi-Fi），防止其在融合时拖累高质量模态（如 LiDAR）。
*   **扩散模型的优势**：相比传统 MSE 蒸馏，基于扩散的对齐能更好地处理异构数据间的复杂分布映射。

### 7. 优点与亮点
*   **因果建模**：首次识别并解决了污染效应与表示鸿沟之间的因果依赖关系。
*   **自适应性**：无需像 X-Fi 那样手动调节复杂的模态丢弃率（Dropout Rate），元学习自动完成权重分配。
*   **鲁棒性强**：在极端模态缺失（仅剩一个传感器）的情况下，依然能保持较高的感知精度。

### 8. 不足与局限
*   **结构化误差风险**：在 MM-Fi 实验中发现，虽然绝对位置误差（MPJPE）下降，但结构对齐误差（PA-MPJPE）在某些情况下略有上升，说明无线信号在恢复精细肢体结构方面仍有局限。
*   **生成伪影**：扩散模型的生成性质可能在极端噪声下引入微小的生成伪影。
*   **模态质量阈值**：论文未深入探讨模态质量差到什么程度时必须完全舍弃，而非仅仅降权。

（完）
