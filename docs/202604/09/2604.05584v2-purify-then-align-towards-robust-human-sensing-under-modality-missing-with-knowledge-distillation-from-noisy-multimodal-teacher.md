---
title: "Purify-then-Align: Towards Robust Human Sensing under Modality Missing with Knowledge Distillation from Noisy Multimodal Teacher"
title_zh: 先净化后对齐：利用噪声多模态教师知识蒸馏实现模态缺失下的鲁棒人体感知
authors: "Pengcheng Weng, Yanyu Qian, Yangxin Xu, Fei Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05584v2"
tags: ["query:missing-modality"]
score: 9.0
evidence: 模态缺失下的鲁棒人体感知
tldr: 针对多模态人体感知中模态缺失、异构数据表示差距及低质量模态污染等挑战，本文提出PTA（先净化后对齐）框架。该框架结合元学习动态加权机制以净化噪声模态，并利用基于扩散模型的知识蒸馏将净化后的多模态共识传递给单模态编码器。实验证明，PTA在MM-Fi和XRF55数据集上显著提升了单模态模型在复杂缺失场景下的鲁棒性，实现了最先进的感知性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05584v2/fig-001.webp\", \"caption\": \"Table 1. MPJPE (mm) and PA-MPJPE (mm) on MM-Fi [46] dataset.\", \"page\": 7, \"index\": 1, \"width\": 852, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.05584v2/fig-002.webp\", \"caption\": \"Figure 1. The overall architecture of our proposed PTA framework, built on a Purify-then-Align paradigm. The model is trained in a nested loop: (a) The Purify Stage: The Purify Stage optimizes the meta-learning weights w to mitigate the Contamination Effect. (b) The Align Stage: The Align Stage uses these weights w to construct a “clean teacher” fT and then uses it in a diffusion-based process (LDiffKD) to refine each “student” feature fS , thereby bridging the Representation Gap.\", \"page\": 3, \"index\": 2, \"width\": 1036, \"height\": 360}]"
motivation: 旨在解决多模态感知中异构数据表示差距与低质量模态污染之间相互交织、阻碍特征对齐的因果依赖问题。
method: 提出PTA框架，通过元学习动态降低噪声模态权重实现净化，并利用扩散模型引导的知识蒸馏将跨模态知识对齐至单模态编码器。
result: 在MM-Fi和XRF55等大规模数据集上的实验表明，PTA达到了SOTA性能，并显著增强了单模态模型在多种模态缺失场景下的鲁棒性。
conclusion: “先净化后对齐”策略能有效从含噪多模态教师中提取高质量知识，为构建鲁棒的单模态人体感知模型提供了新颖且高效的方案。
---

## 摘要
鲁棒的多模态人体感知必须克服模态缺失这一关键挑战。两个主要障碍是异构数据之间的表示鸿沟（Representation Gap）以及来自低质量模态的污染效应（Contamination Effect）。这些障碍具有因果联系，因为污染引入的损坏从根本上阻碍了表示差异的缩小。在本文中，我们提出了 PTA，这是一种新颖的“先净化后对齐”框架，通过元学习与知识扩散的协同整合来解决这种因果依赖。为了净化知识源，PTA 首先采用了一种元学习驱动的加权机制，动态学习降低噪声、低贡献模态的影响权重。随后，为了对齐不同模态，PTA 引入了一种基于扩散的知识蒸馏范式，其中由这种净化后的共识形成的信息丰富的纯净教师，负责细化每个学生模态的特征。这种“先净化后对齐”策略的最终成果是创建了注入跨模态知识的极其强大的单模态编码器。在大规模 MM-Fi 和 XRF55 数据集上进行的全面实验表明，在显著的表示鸿沟和污染效应下，PTA 达到了最先进的性能，并显著提高了单模态模型在各种模态缺失场景下的鲁棒性。

## Abstract
Robust multimodal human sensing must overcome the critical challenge of missing modalities. Two principal barriers are the Representation Gap between heterogeneous data and the Contamination Effect from low-quality modalities. These barriers are causally linked, as the corruption introduced by contamination fundamentally impedes the reduction of representation disparities. In this paper, we propose PTA, a novel "Purify-then-Align" framework that solves this causal dependency through a synergistic integration of meta-learning and knowledge diffusion. To purify the knowledge source, PTA first employs a meta-learning-driven weighting mechanism that dynamically learns to down-weight the influence of noisy, low-contributing modalities. Subsequently, to align different modalities, PTA introduces a diffusion-based knowledge distillation paradigm in which an information-rich clean teacher, formed from this purified consensus, refines the features of each student modality. The ultimate payoff of this "Purify-then-Align" strategy is the creation of exceptionally powerful single-modality encoders imbued with cross-modal knowledge. Comprehensive experiments on the large-scale MM-Fi and XRF55 datasets, under pronounced Representation Gap and Contamination Effect, demonstrate that PTA achieves state-of-the-art performance and significantly improves the robustness of single-modality models in diverse missing-modality scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：Purify-then-Align (PTA) 框架

#### 1. 核心问题与整体含义
本文针对多模态人体感知（如人体姿态估计、动作识别）在实际应用中面临的**模态缺失**（Modality Missing）问题展开研究。研究者指出，现有的多模态融合方法主要受制于两大瓶颈：
*   **表示鸿沟（Representation Gap）**：不同传感器（如 Wi-Fi 信号与深度图像）的数据特征差异巨大，难以直接对齐。
*   **污染效应（Contamination Effect）**：低质量或高噪声的模态在融合时会干扰高质量模态，导致整体性能下降。
论文的核心动机在于发现这两者存在**因果联系**：低质量模态带来的“污染”会阻碍特征的有效“对齐”。因此，论文提出了“先净化、后对齐”的策略。

#### 2. 方法论
PTA 框架由两个协同阶段组成：
*   **净化阶段（Purify）**：
    *   **核心思想**：通过元学习（Meta-Learning）动态调整各模态的权重。
    *   **技术细节**：采用嵌套优化循环。内层循环在训练集上优化模型参数；外层循环在验证集上根据任务损失更新模态权重 $w$。这使得模型能自动识别并降低噪声模态（如干扰严重的 Wi-Fi）的权重，从而构建一个“纯净”的多模态教师共识特征。
*   **对齐阶段（Align）**：
    *   **核心思想**：利用基于扩散模型（Diffusion Model）的知识蒸馏，将纯净教师的知识传递给单模态学生。
    *   **技术细节**：引入**噪声适配器（Noise Adapter）**，将学生特征映射为扩散过程的起点，通过去噪过程（DDIM 采样）引导学生特征在结构上向教师特征对齐。这种生成式对齐机制比传统的 MSE 蒸馏更能处理异构数据间的复杂分布差异。

#### 3. 实验设计
*   **数据集**：
    1.  **MM-Fi**：大规模多模态人体姿态估计数据集（包含深度图、LiDAR、Wi-Fi CSI）。
    2.  **XRF55**：大规模射频动作识别数据集（包含毫米波雷达、Wi-Fi、RFID）。
*   **Benchmark 与对比方法**：
    *   对比了特征级融合（Base.1）、决策级融合（Base.2）以及当前最先进的模态不变基础模型 **X-Fi**。
    *   评价指标包括 MPJPE（平均关节位置误差）、PA-MPJPE（对齐后的误差）和分类准确率（Accuracy）。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU**。
*   **训练细节**：Batch size 分别设为 16（MM-Fi）和 32（XRF55）。
*   **时长**：文中未明确提及具体的训练总时长，但提到扩散模型采用了轻量化设计（1x1 卷积、Bottleneck 结构）以及 5 步 DDIM 快速采样以优化推理效率。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同任务（姿态估计与动作识别）的 SOTA 数据集，实验组数充分。
*   **覆盖场景**：详尽测试了所有可能的模态缺失组合（如 D+L, L+W, 单 D, 单 L 等），共计十余种组合。
*   **消融实验**：分别验证了“无扩散对齐”和“无元学习加权”的情况，证明了两个模块的协同必要性。
*   **客观性**：通过与 SOTA 方法 X-Fi 在相同设置下对比，证明了 PTA 在提升单模态鲁棒性方面的显著优势。

#### 6. 主要结论与发现
*   **单模态能力大幅提升**：通过学习纯净教师的知识，单模态编码器的性能显著增强（例如 LiDAR 单模态在姿态估计上的误差降低了 47.5%）。
*   **成功抑制污染**：元学习机制能有效识别低质量模态，避免其在融合时拖累高质量模态。
*   **模态间隙影响**：在射频类模态（雷达、Wi-Fi、RFID）之间，由于表示鸿沟较小，低质量模态（RFID）有时能为高质量模态提供正向增益；而在异构程度极高的模态间，抑制污染更为关键。

#### 7. 优点
*   **因果洞察**：首次揭示了污染效应与表示鸿沟之间的因果依赖关系。
*   **自适应强**：无需像 X-Fi 那样手动调节复杂的模态丢弃率，元学习实现了自动化的重要性分配。
*   **对齐效果好**：引入扩散模型处理特征对齐，比传统线性映射更能捕捉跨模态的复杂关联。

#### 8. 不足与局限
*   **结构精度权衡**：在 PA-MPJPE 指标上，PTA 有时略逊于 X-Fi，表明扩散模型在提升全局位置准确性的同时，可能对精细的人体骨架约束保持不足。
*   **生成风险**：扩散过程作为一种生成式方法，在学生特征极度匮乏信息时，可能会引入微小的生成伪影。
*   **应用限制**：目前主要在人体感知领域验证，其在更广泛的多模态任务（如视觉-语言导航）中的通用性尚待观察。

（完）
