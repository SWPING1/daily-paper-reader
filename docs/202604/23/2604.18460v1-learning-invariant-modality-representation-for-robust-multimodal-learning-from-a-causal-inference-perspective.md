---
title: Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective
title_zh: 从因果推理视角学习不变模态表示以实现鲁棒多模态学习
authors: "Sijie Mai, Shiqin Han"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18460v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 用于鲁棒多模态情感计算和情绪预测的解耦方法
tldr: 针对多模态情感计算中模型易受虚假相关性影响、泛化性差的问题，本文从因果推理视角提出了一种因果模态不变表示学习框架（CmIR）。该框架通过解耦学习，将各模态分解为因果不变表示和环境特定虚假表示，并利用不变性、互信息和重构约束确保表示的稳定性和信息完整性。实验证明，CmIR在分布偏移和噪声数据下表现优异，显著提升了多模态学习的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18460v1/fig-001.webp\", \"caption\": \"Table 1: Comparisons on the CMU-MOSI and CMU-MOSEI datasets. The results labeled with † are obtained from original papers, and other results are obtained from our experiments. The best results are highlighted.\", \"page\": 8, \"index\": 1, \"width\": 908, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-002.webp\", \"caption\": \"Figure 7: The structures of encoder, decoder, and predictor. d ′ represents the hidden dimensionality.\", \"page\": 19, \"index\": 2, \"width\": 458, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-003.webp\", \"caption\": \"Table 4: Discussion on noisy modalities on CMU-MOSI and CMU-MOSEI. MuBo denotes Multimodal Boosting.\", \"page\": 9, \"index\": 3, \"width\": 867, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-004.webp\", \"caption\": \"Figure 3: The overall framework of CmIR and the visualization of the proposed constraints.\", \"page\": 5, \"index\": 4, \"width\": 902, \"height\": 329}]"
motivation: 现有多模态模型往往会学习到虚假的相关性，导致其在面对分布偏移或模态噪声时泛化性能下降。
method: 提出CmIR框架，通过因果解耦将模态分解为因果不变和环境特定表示，并结合不变性、互信息及重构约束进行训练。
result: 在多个多模态基准测试中达到SOTA水平，尤其在处理分布外数据和噪声数据时表现出卓越的鲁棒性。
conclusion: 从因果推理角度学习模态不变表示是实现稳健且泛化能力强的多模态学习的有效途径。
---

## 摘要
多模态情感计算旨在利用语言、音频和视觉模态来预测人类的情感、情绪、意图和观点。然而，现有模型通常会学习到虚假相关性，这在分布偏移或模态噪声的情况下会损害泛化能力。为解决这一问题，我们提出了一种用于鲁棒多模态学习的因果模态不变表示（CmIR）学习框架。其核心是，我们从因果推理的视角引入了一种具有理论依据的解耦方法，将每个模态分解为“因果不变表示”和“环境特定虚假表示”。CmIR 通过不变性约束、互信息约束和重构约束，确保所学习的不变表示在不同环境下与标签保持稳定的预测关系，同时保留来自原始输入的充足信息。在多个多模态基准测试上的实验表明，CmIR 达到了最先进的性能。CmIR 在分布外数据和噪声数据上表现尤为出色，证实了其鲁棒性和泛化能力。

## Abstract
Multimodal affective computing aims to predict humans' sentiment, emotion, intention, and opinion using language, acoustic, and visual modalities. However, current models often learn spurious correlations that harm generalization under distribution shifts or noisy modalities. To address this, we propose a causal modality-invariant representation (CmIR) learning framework for robust multimodal learning. At its core, we introduce a theoretically grounded disentanglement method that separates each modality into `causal invariant representation' and `environment-specific spurious representation' from a causal inference perspective. CmIR ensures that the learned invariant representations retain stable predictive relationships with labels across different environments while preserving sufficient information from the raw inputs via invariance constraint, mutual information constraint, and reconstruction constraint. Experiments across multiple multimodal benchmarks demonstrate that CmIR achieves state-of-the-art performance. CmIR particularly excels on out-of-distribution data and noisy data, confirming its robustness and generalizability.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态情感计算（MAC）旨在整合语言、音频和视觉信息来预测人类情感。然而，现有模型往往会学习到训练数据中的**虚假相关性**（Spurious Correlations），而非真正的因果关系。例如，如果训练集中某位演讲者总是带着微笑（视觉特征）说负面的话，模型可能会错误地将“微笑”与“积极情感”强行绑定。当测试环境发生变化（如光照、说话风格、背景噪声或分布偏移）时，这些虚假相关性会失效，导致模型泛化能力差。论文的研究动机在于从**因果推理**视角出发，提取跨环境稳定的“不变特征”，以提升模型在现实复杂场景下的鲁棒性。

### 2. 方法论：CmIR 框架
论文提出了**因果模态不变表示（CmIR）**学习框架，其核心思想是将每个模态的原始特征解耦为两个互补部分：
*   **因果不变表示 ($Z_{inv}$)**：捕捉与标签之间具有稳定因果关系的特征，不随环境变化。
*   **环境特定虚假表示 ($Z_{spu}$)**：捕捉与环境相关的噪声或非因果特征。

**关键技术细节与约束：**
*   **不变性约束 ($R_{inv}$)**：通过在原始特征中注入不同强度的噪声来模拟“虚拟环境”，强制模型在不同环境下提取出的 $Z_{inv}$ 保持一致，确保其预测能力的稳定性。
*   **互信息约束 ($R_{dec}$)**：利用特征正交化作为互信息最小化的代理手段，减少 $Z_{inv}$ 与 $Z_{spu}$ 之间的信息冗余，实现深度解耦。
*   **重构约束 ($R_{rec}$)**：要求解耦后的两个表示能够重新合成原始输入特征，防止在解耦过程中丢失关键信息。
*   **理论证明**：论文提供了严谨的数学证明，证实了不变表示的存在性、可提取性，并证明了基于不变表示的预测器在最坏情况下的分布外（OOD）风险低于原始特征预测器。

### 3. 实验设计
*   **任务与数据集**：
    *   **多模态情感分析 (MSA)**：CMU-MOSI, CMU-MOSEI, CH-SIMS-v2。
    *   **幽默检测 (MHD)**：UR-FUNNY。
    *   **讽刺检测 (MSD)**：MUStARD。
    *   **分布外 (OOD) 测试**：专门使用了 CMU-MOSI (OOD) 基准。
*   **Benchmark 与对比方法**：对比了包括 Self-MM, MISA, ITHP 等强基线，以及 SuCI, CLUE, GEAR, AtCAF 等最新的基于因果推理的解耦方法。
*   **噪声场景**：在 10% 到 70% 的高斯噪声、拉普拉斯噪声及随机擦除噪声下测试模型的鲁棒性。

### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA RTX 3090 GPU**。
*   **软件环境**：PyTorch 1.13.1, CUDA 11.6。
*   **训练细节**：使用了 AdamW 优化器，通过 50 次随机网格搜索确定超参数，并对最终结果进行了 5 次重复实验取平均值。

### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度验证**：涵盖了 6 个主流数据集，跨越了情感、幽默、讽刺三个不同任务。
*   **鲁棒性专项实验**：不仅做了标准测试，还专门设计了 OOD 偏移实验和 7 种不同噪声强度的对比实验。
*   **消融实验**：逐一验证了不变性、互信息和重构三个约束项的必要性。
*   **客观性**：通过显著性检验（t-test）和探测实验（Probing Experiments）验证了 $Z_{inv}$ 和 $Z_{spu}$ 是否真的实现了语义分离。

### 6. 主要结论与发现
*   **性能领先**：CmIR 在所有标准基准测试中均达到了 SOTA（最先进）水平。
*   **OOD 优势显著**：在分布偏移较大的 CMU-MOSI (OOD) 数据集上，CmIR 的领先优势比在标准数据集上更明显，证明了因果学习的价值。
*   **噪声抗性强**：随着噪声比例增加，CmIR 的性能下降速度远慢于基线模型，表现出极强的实用鲁棒性。
*   **解耦有效性**：可视化和探测实验证明，模型确实提取到了与标签高度相关且对环境不敏感的因果特征。

### 7. 优点（亮点）
*   **理论与实践结合**：不仅提出了算法，还给出了完备的因果推理理论证明，增强了模型的可解释性。
*   **通用性强**：不依赖于特定的偏置假设（如演讲者偏置或模态偏置），而是一个通用的不变特征学习框架。
*   **即插即用**：其解耦思想可以较容易地集成到其他多模态架构中。
*   **高效性**：相比于一些大型预训练模型，CmIR 的参数量和计算量（FLOPs）更低，但性能更优。

### 8. 不足与局限
*   **环境模拟简化**：虽然通过噪声注入模拟环境变化很有效，但可能无法完全覆盖现实世界中复杂的语义偏移（如文化背景差异导致的表达变化）。
*   **互信息代理**：使用正交化作为互信息最小化的代理虽然计算高效，但在处理非线性相关性时可能不够精确。
*   **超参数敏感度**：虽然论文做了敏感性分析，但 λ1, λ2, λ3 等多个约束权重的调优在不同数据集上可能需要较多的人工干预。

（完）
