---
title: Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective
title_zh: 从因果推理视角学习不变模态表示以实现鲁棒的多模态学习
authors: "Sijie Mai, Shiqin Han"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18460v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 针对鲁棒多模态情感计算和噪声模态的解耦方法
tldr: 针对多模态情感计算中模型易学习虚假相关性导致泛化性差的问题，本文提出了一种基于因果推理的因果模态不变表示学习框架（CmIR）。该框架通过解耦学习各模态的因果不变表示与环境特定虚假表示，并结合不变性、互信息和重构约束，显著提升了模型在分布偏移和噪声数据下的鲁棒性与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18460v1/fig-001.webp\", \"caption\": \"Table 1: Comparisons on the CMU-MOSI and CMU-MOSEI datasets. The results labeled with † are obtained from original papers, and other results are obtained from our experiments. The best results are highlighted.\", \"page\": 8, \"index\": 1, \"width\": 908, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-002.webp\", \"caption\": \"Figure 7: The structures of encoder, decoder, and predictor. d ′ represents the hidden dimensionality.\", \"page\": 19, \"index\": 2, \"width\": 458, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-003.webp\", \"caption\": \"Table 4: Discussion on noisy modalities on CMU-MOSI and CMU-MOSEI. MuBo denotes Multimodal Boosting.\", \"page\": 9, \"index\": 3, \"width\": 867, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-004.webp\", \"caption\": \"Figure 3: The overall framework of CmIR and the visualization of the proposed constraints.\", \"page\": 5, \"index\": 4, \"width\": 902, \"height\": 329}]"
motivation: 现有多模态学习模型容易捕捉到虚假相关性，导致在面对分布偏移或噪声模态时泛化性能下降。
method: 提出CmIR框架，利用因果推理将模态特征解耦为因果不变表示和环境特定虚假表示，并施加多种约束以确保表示的稳定性。
result: 在多个基准测试中达到SOTA水平，特别是在分布外（OOD）数据和噪声数据上表现出卓越的鲁棒性。
conclusion: 通过因果解耦学习不变特征是提高多模态学习系统鲁棒性和泛化性的有效途径。
---

## 摘要
多模态情感计算旨在利用语言、声学和视觉模态来预测人类的情感、情绪、意图和观点。然而，现有模型通常会学习到虚假相关性，这在分布偏移或模态噪声的情况下会损害泛化能力。为了解决这一问题，我们提出了一种用于鲁棒多模态学习的因果模态不变表示（CmIR）学习框架。其核心是，我们从因果推理的角度引入了一种具有理论依据的解耦方法，将每个模态分解为“因果不变表示”和“环境特定虚假表示”。CmIR 通过不变性约束、互信息约束和重构约束，确保所学习的不变表示在不同环境下与标签保持稳定的预测关系，同时保留原始输入中的充分信息。在多个多模态基准测试上的实验表明，CmIR 达到了最先进的性能。CmIR 在分布外数据和噪声数据上表现尤为出色，证实了其鲁棒性和泛化能力。

## Abstract
Multimodal affective computing aims to predict humans' sentiment, emotion, intention, and opinion using language, acoustic, and visual modalities. However, current models often learn spurious correlations that harm generalization under distribution shifts or noisy modalities. To address this, we propose a causal modality-invariant representation (CmIR) learning framework for robust multimodal learning. At its core, we introduce a theoretically grounded disentanglement method that separates each modality into `causal invariant representation' and `environment-specific spurious representation' from a causal inference perspective. CmIR ensures that the learned invariant representations retain stable predictive relationships with labels across different environments while preserving sufficient information from the raw inputs via invariance constraint, mutual information constraint, and reconstruction constraint. Experiments across multiple multimodal benchmarks demonstrate that CmIR achieves state-of-the-art performance. CmIR particularly excels on out-of-distribution data and noisy data, confirming its robustness and generalizability.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective》的深度结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：多模态情感计算（MAC）模型在训练时容易捕捉到**虚假相关性（Spurious Correlations）**。例如，模型可能过度依赖某个说话者特有的表情（如习惯性微笑）来预测情感，而非理解其语义逻辑。
*   **研究动机**：当测试环境发生变化（分布偏移，OOD）或模态存在噪声时，这些虚假相关性会导致模型性能剧烈下降。现有的解耦或因果方法往往依赖于特定的先验偏置假设（如说话人偏置），缺乏通用性。

### 2. 方法论：CmIR 框架
该论文提出了**因果模态不变表示（CmIR）**学习框架，旨在从因果推理视角将每个模态解耦。

*   **核心思想**：将每个模态 $X_m$ 分解为两个互补部分：
    1.  **因果不变表示 ($Z_{inv}$)**：在不同环境下与标签保持稳定因果关系的特征。
    2.  **环境特定虚假表示 ($Z_{spu}$)**：捕获与环境相关的噪声，与标签无因果联系。
*   **关键技术细节**：
    *   **环境模拟**：由于现实数据缺乏环境标签，作者通过向原始特征注入不同强度的噪声来模拟 $K$ 个虚拟环境。
    *   **三大约束损失函数**：
        1.  **不变性约束 ($R_{inv}$)**：强制 $Z_{inv}$ 在不同噪声环境下的预测输出保持一致，确保其对环境干扰不敏感。
        2.  **互信息约束 ($R_{dec}$)**：通过特征正交化作为互信息的代理，最小化 $Z_{inv}$ 与 $Z_{spu}$ 之间的相关性，实现彻底解耦。
        3.  **重构约束 ($R_{rec}$)**：确保解耦后的两个表示能够重构原始输入，防止关键信息丢失。
*   **理论保证**：论文提供了数学证明，证实了在多环境训练下不变表示的存在性、可提取性，并证明了基于不变表示的预测器在最坏情况下的 OOD 风险低于原始特征预测器。

### 3. 实验设计
*   **数据集**：
    *   **情感分析**：CMU-MOSI, CMU-MOSEI, CH-SIMS-v2。
    *   **分布外测试**：CMU-MOSI (OOD)（专门用于评估鲁棒性）。
    *   **幽默与讽刺检测**：UR-FUNNY, MUStARD。
*   **Benchmark 与对比方法**：
    *   对比了超过 20 种方法，包括经典模型（Self-MM, MISA）、最新 SOTA（ITHP, GSCon）以及专门的因果/去偏方法（CLUE, GEAR, SuCI）。
*   **噪声场景**：在 10% 到 70% 的高斯噪声、拉普拉斯噪声及随机擦除（Random Erasing）条件下进行鲁棒性测试。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU** (24GB 显存)。
*   **软件**：PyTorch 1.13.1, CUDA 11.6。
*   **训练细节**：使用了 AdamW 优化器，通过 50 次随机网格搜索确定超参数，并对最终结果取 5 次运行的平均值。文中未明确给出总训练时长，但提到模型复杂度较低（参数量与 FLOPs 均优于部分 SOTA）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个主流数据集，横跨情感、幽默、讽刺三个任务，实验设计非常全面。
*   **消融实验**：针对因果推理模块、不变性约束、互信息约束和重构约束分别做了消融，验证了各组件的必要性。
*   **充分性与公平性**：
    *   不仅在标准测试集上对比，还专门在 OOD 数据集和多种噪声比例下对比，证明了其“鲁棒性”并非偶然。
    *   提供了显著性检验（t-test）和超参数敏感性分析，实验结果具有统计学意义且客观。

### 6. 主要结论与发现
*   **性能领先**：CmIR 在所有标准基准测试中均达到或超过了 SOTA 水平。
*   **OOD 优势显著**：在 CMU-MOSI (OOD) 上，CmIR 的 Acc2 提升了约 3.1%，Acc7 提升了约 7.2%，证明因果解耦能有效抵御分布偏移。
*   **噪声抗性**：随着噪声比例增加，CmIR 相对于基准模型的领先优势扩大，表明其提取的因果特征确实比原始特征更稳定。
*   **可视化验证**：T-SNE 可视化显示，CmIR 学习到的不变特征在特征空间中类别边界更清晰，受噪声影响更小。

### 7. 优点与亮点
*   **理论与实践结合**：不仅提出了算法，还从因果推理和信息论角度给出了严谨的理论证明。
*   **通用性强**：不依赖于特定的偏置类型（如 Speaker ID），通过噪声模拟环境，使其能够处理各种未知的分布偏移。
*   **高效性**：在提升性能的同时，保持了较低的参数量和计算开销（FLOPs 低于同类 SOTA）。

### 8. 不足与局限
*   **环境模拟的局限性**：虽然噪声注入能模拟环境变化，但现实世界的分布偏移（如文化差异、光照变化）可能比简单的加性噪声更复杂。
*   **互信息代理**：使用正交化作为互信息最小化的代理虽然高效，但在处理非线性相关性时可能不够精确。
*   **超参数敏感性**：实验显示噪声强度 $\alpha(1)$ 对结果有一定影响，在不同数据集上可能需要精细调优。

（完）
