---
title: "Purify-then-Align: Towards Robust Human Sensing under Modality Missing with Knowledge Distillation from Noisy Multimodal Teacher"
title_zh: Purify-then-Align：通过噪声多模态教师知识蒸馏实现模态缺失下的鲁棒人体感知
authors: "Pengcheng Weng, Yanyu Qian, Yangxin Xu, Fei Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05584v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 利用知识扩散实现缺失模态下的鲁棒人体感知
tldr: 针对多模态人体感知中模态缺失、异构数据表示差异及低质量模态污染等挑战，本文提出了一种名为PTA（先净化后对齐）的创新框架。该框架结合元学习与知识扩散技术，首先通过元学习权重机制动态降低噪声模态影响以净化知识源，随后利用扩散模型驱动的知识蒸馏将净化后的多模态共识传递给单模态编码器。实验证明，PTA在MM-Fi和XRF55数据集上显著提升了单模态模型在复杂缺失场景下的鲁棒性，达到了领域领先水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05584v1/fig-001.webp\", \"caption\": \"Table 1. MPJPE (mm) and PA-MPJPE (mm) on MM-Fi [46] dataset.\", \"page\": 7, \"index\": 1, \"width\": 852, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.05584v1/fig-002.webp\", \"caption\": \"Figure 1. The overall architecture of our proposed PTA framework, built on a Purify-then-Align paradigm. The model is trained in a nested loop: (a) The Purify Stage: The Purify Stage optimizes the meta-learning weights w to mitigate the Contamination Effect. (b) The Align Stage: The Align Stage uses these weights w to construct a “clean teacher” fT and then uses it in a diffusion-based process (LDiffKD) to refine each “student” feature fS , thereby bridging the Representation Gap.\", \"page\": 3, \"index\": 2, \"width\": 1036, \"height\": 360}]"
motivation: 旨在解决多模态感知中异构数据间的表示鸿沟以及低质量模态带来的污染效应，提高模态缺失时的系统鲁棒性。
method: 提出PTA框架，利用元学习动态加权净化噪声模态，并结合扩散模型进行跨模态知识蒸馏以对齐特征。
result: 在MM-Fi和XRF55等大规模数据集上的实验表明，该方法在多种模态缺失场景下均取得了最先进的性能。
conclusion: 通过“先净化后对齐”策略，PTA成功构建了具备跨模态知识的强力单模态编码器，有效应对了多模态感知的鲁棒性难题。
---

## 摘要
鲁棒的多模态人体感知必须克服模态缺失这一关键挑战。两个主要障碍是异构数据之间的表示鸿沟（Representation Gap）以及来自低质量模态的污染效应（Contamination Effect）。这些障碍具有因果联系，因为污染引入的损坏从根本上阻碍了表示差异的缩小。在本文中，我们提出了 PTA，一种新颖的“先净化后对齐”（Purify-then-Align）框架，通过元学习与知识扩散的协同整合来解决这种因果依赖。为了净化知识源，PTA 首先采用了一种元学习驱动的加权机制，动态学习降低噪声、低贡献模态的影响权重。随后，为了对齐不同模态，PTA 引入了一种基于扩散的知识蒸馏范式，其中由这种净化后的共识形成的富含信息的干净教师，负责细化每个学生模态的特征。这种“先净化后对齐”策略的最终成果是创建了注入跨模态知识的极其强大的单模态编码器。在大型 MM-Fi 和 XRF55 数据集上进行的全面实验表明，在显著的表示鸿沟和污染效应下，PTA 达到了最先进的性能，并显著提高了单模态模型在各种模态缺失场景下的鲁棒性。

## Abstract
Robust multimodal human sensing must overcome the critical challenge of missing modalities. Two principal barriers are the Representation Gap between heterogeneous data and the Contamination Effect from low-quality modalities. These barriers are causally linked, as the corruption introduced by contamination fundamentally impedes the reduction of representation disparities. In this paper, we propose PTA, a novel "Purify-then-Align" framework that solves this causal dependency through a synergistic integration of meta-learning and knowledge diffusion. To purify the knowledge source, PTA first employs a meta-learning-driven weighting mechanism that dynamically learns to down-weight the influence of noisy, low-contributing modalities. Subsequently, to align different modalities, PTA introduces a diffusion-based knowledge distillation paradigm in which an information-rich clean teacher, formed from this purified consensus, refines the features of each student modality. The ultimate payoff of this "Purify-then-Align" strategy is the creation of exceptionally powerful single-modality encoders imbued with cross-modal knowledge. Comprehensive experiments on the large-scale MM-Fi and XRF55 datasets, under pronounced Representation Gap and Contamination Effect, demonstrate that PTA achieves state-of-the-art performance and significantly improves the robustness of single-modality models in diverse missing-modality scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《Purify-then-Align: Towards Robust Human Sensing under Modality Missing with Knowledge Distillation from Noisy Multimodal Teacher》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多模态人体感知（如姿态估计、动作识别）中，硬件故障或环境干扰常导致“模态缺失”。现有方法难以同时解决两个核心障碍：**表示鸿沟（Representation Gap）**（异构数据间差异大）和**污染效应（Contamination Effect）**（低质量模态干扰高质量模态）。
*   **研究动机**：作者认为这两个障碍存在因果联系——低质量模态带来的污染会阻碍表示鸿沟的缩小。因此，需要一种机制先“净化”知识源，再进行跨模态“对齐”，从而使单模态模型在其他模态缺失时仍能具备强大的感知能力。

### 2. 方法论
论文提出了 **PTA (Purify-then-Align)** 框架，包含两个协同阶段：
*   **净化阶段 (Purify)**：
    *   **核心思想**：利用**元学习（Meta-Learning）**动态学习各模态的权重。
    *   **技术细节**：采用嵌套优化策略。内层循环在训练集上优化模型参数；外层循环在验证集上优化模态权重 $w$。通过这种方式，系统能自动识别并降低噪声或低贡献模态的权重，确保生成的“教师”表示是纯净且高质量的。
*   **对齐阶段 (Align)**：
    *   **核心思想**：基于**扩散模型（Diffusion Model）**的知识蒸馏。
    *   **技术细节**：将净化后的多模态融合特征作为“教师”，单模态特征作为“学生”。引入**噪声适配器（Noise Adapter）**将学生特征映射到潜在空间，并利用扩散模型的反向去噪过程（结合 DDIM 加速采样）来细化学生特征，使其在结构上与富含信息的教师特征对齐。

### 3. 实验设计
*   **数据集**：
    *   **MM-Fi**：大规模人体姿态估计数据集，包含深度图（Depth）、LiDAR 点云和 Wi-Fi CSI 信号。
    *   **XRF55**：射频人体动作识别数据集，包含毫米波雷达（mmWave）、Wi-Fi 和 RFID 信号。
*   **Benchmark 与对比方法**：
    *   对比了特征融合基准（Base.1）、决策融合基准（Base.2）以及当前最先进的（SOTA）模态不变基础模型 **X-Fi**。
*   **评估指标**：姿态估计使用 MPJPE（平均关节位置误差）和 PA-MPJPE；动作识别使用分类准确率（Accuracy）。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：MM-Fi 任务 Batch Size 为 16，XRF55 任务 Batch Size 为 32。使用了 Adam 优化器，学习率分别设为 $5 \times 10^{-4}$（主模型）和 $1 \times 10^{-2}$（元学习权重）。
*   **时长**：文中未明确提及具体的训练总时长，但强调了扩散模块采用了轻量化设计（如 1x1 卷积和 5 步 DDIM 采样）以提升效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同任务（姿态估计与动作识别）的大规模数据集，测试了所有可能的模态组合（如 D+L, L+W, D+L+W 等）。
*   **消融实验**：专门设计了实验分别移除“扩散对齐模块”和“元学习权重模块”，验证了各组件的必要性。
*   **充分性评价**：实验设计较为全面，通过跨数据集和多模态组合的对比，客观地证明了 PTA 在处理模态缺失和噪声污染方面的优越性。

### 6. 主要结论与发现
*   **单模态能力大幅提升**：通过 PTA 蒸馏，单模态编码器（如纯 Wi-Fi 或纯 LiDAR）的性能显著优于基准方法，证明了跨模态知识转移的成功。
*   **有效抑制污染**：在 MM-Fi 中，Wi-Fi 被识别为低质量模态，元学习机制成功降低了其对 LiDAR 等高质量模态的负面影响。
*   **扩散模型的优势**：相比传统的 MSE 蒸馏，基于扩散的对齐能更好地处理异构数据间的复杂分布映射。

### 7. 优点
*   **因果解耦**：首次识别并解决了污染效应与表示鸿沟之间的因果联系。
*   **自适应性强**：无需像 X-Fi 那样手动调节复杂的模态丢弃率（Dropout Rate），元学习权重是自动学习的。
*   **鲁棒性高**：在极端模态缺失（仅剩一个传感器）的情况下，依然能保持较高的感知精度。

### 8. 不足与局限
*   **结构化细节丢失**：在姿态估计中，虽然 MPJPE（绝对位置）表现优异，但 PA-MPJPE（结构对齐误差）有时会略微上升，表明模型在处理极度噪声信号（如 Wi-Fi）时，可能生成结构合理但局部细节不准确的“平均姿态”。
*   **生成伪影风险**：消融实验显示，在某些极低质量模态组合下，扩散过程可能会引入微小的生成伪影或放大固有噪声。
*   **应用限制**：目前主要针对人体感知任务，在其他更通用的多模态任务（如视觉-语言理解）上的泛化能力尚待验证。

（完）
