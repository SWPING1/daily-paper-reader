---
title: "GraphPL: Leveraging GNN for Efficient and Robust Modalities Imputation in Patchwork Learning"
title_zh: GraphPL：利用图神经网络在拼凑学习中实现高效且鲁棒的模态插补
authors: "Xingjian Hu, Zuoyu Yan, Jianhua Zhu, Liangcai Gao, Fei Wang, Tengfei Ma"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25352v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 在拼贴学习中为每个客户端插补缺失模态
tldr: 本研究针对分布式多模态学习中客户端模态缺失的“拼凑学习”问题，指出传统方法无法充分利用观测模态且鲁棒性差。为此提出 GraphPL 框架，通过引入图神经网络（GNN）灵活整合所有可用模态信息，实现高效且鲁棒的无监督模态补全。实验证明 GraphPL 在基准测试和真实电子健康记录数据集上均达到领先水平，显著提升了下游任务的预测性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25352v1/fig-001.webp\", \"caption\": \"Fig. 2: Existing POE-based method faced the modality collapse issue, due to its over-reliance on specific modality (M1 here). In contrast, our GNN-based method merge the input information adaptively, exhibiting stronger robustness.\", \"page\": 2, \"index\": 1, \"width\": 1014, \"height\": 200}, {\"url\": \"assets/figures/arxiv/2604.25352v1/fig-002.webp\", \"caption\": \"Fig. 3: GraphPL’s overall pipeline (here, the client lacks the medication modality): During training, it masks one modality as target modality and imputes it via other observed modalities as conditional ones. During inference, it uses all observed modalities as conditional modalities to predict the missing modality’s features for either imputation or downstream tasks.\", \"page\": 2, \"index\": 2, \"width\": 496, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.25352v1/fig-003.webp\", \"caption\": \"Table 1: Generation quality (%) and representation quality (%) on multiple benchmark datasets.\", \"page\": 3, \"index\": 3, \"width\": 690, \"height\": 200}, {\"url\": \"assets/figures/arxiv/2604.25352v1/fig-004.webp\", \"caption\": \"Fig. 1: The performance of different methods on the realworld EHR dataset eICU [1]. By incorporating imputation tasks and leveraging GNNs to mitigate the modality collapse issue identified in CLAP [2], GraphPL achieves superior performance in both tasks.\", \"page\": 1, \"index\": 4, \"width\": 501, \"height\": 328}]"
motivation: 现有分布式多模态学习方法假设模态完整，但在实际应用中客户端常面临模态缺失且现有补全方法利用率不足的问题。
method: 提出 GraphPL 框架，利用图神经网络（GNN）灵活整合所有观测到的模态，实现无监督的缺失模态补全。
result: 在基准数据集和真实电子健康记录（EHR）数据集上均达到 SOTA 性能，并能有效支持疾病预测等下游任务。
conclusion: GraphPL 通过 GNN 提升了拼凑学习中的模态补全效率与鲁棒性，为处理不完整分布式多模态数据提供了强有力的方案。
---

## 摘要
当前关于分布式多模态学习的研究通常假设客户端可以访问所有模态的完整信息，但这在实践中可能并不成立。在本文中，我们探讨了拼凑学习（patchwork learning），其中不同客户端可用的模态各不相同，目标是以无监督的方式为每个客户端插补缺失的模态。现有方法被证明未能充分利用模态信息，因为它们往往仅依赖于观察到的模态子集。为了解决这一问题，我们提出了 GraphPL，它将图神经网络与拼凑学习相结合，以灵活地整合所有观察到的模态，并在输入存在噪声时保持鲁棒性。实验结果表明，GraphPL 在基准数据集上达到了 SOTA 性能。我们在真实世界分布式电子健康记录数据集上的结果显示，GraphPL 学习到了强大的下游特征，并通过卓越的模态插补实现了疾病预测等任务。

## Abstract
Current research on distributed multi-modal learning typically assumes that clients can access complete information across all modalities, which may not hold in practice. In this paper, we explore patchwork learning, in which the modalities available to different clients vary, and the objective is to impute the missing modalities for each client in an unsupervised manner. Existing methods are shown not to fully utilize the modality information as they tend to rely on only a subset of the observed modalities. To address this issue, we propose GraphPL, which combines graph neural networks with patchwork learning to flexibly integrate all observed modalities and remains robust with noisy inputs. Experimental results show that GraphPL achieves SOTA performance on benchmark datasets. Our results on real-world distributed electronic health record dataset show GraphPL learns strong downstream features and enables tasks like disease prediction via superior modality imputation.

---

## 论文详细总结（自动生成）

这是一份关于论文《GraphPL: Leveraging GNN for Efficient and Robust Modalities Imputation in Patchwork Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的分布式多模态学习中，不同客户端（如不同医院）往往无法拥有完整的模态数据（例如，有的医院只有病历，有的只有影像），这种现象被称为**拼凑学习（Patchwork Learning）**。
*   **现有挑战**：现有的多模态插补方法（如 CLAP）通常依赖于“专家乘积模型”（Product-of-Experts, POE）进行模态融合。
*   **核心缺陷**：POE 容易导致**模态崩溃（Modality Collapse）**，即模型过度依赖某些特定模态，而忽略了其他模态的补充信息，导致在输入存在噪声或模态缺失时鲁棒性较差。
*   **研究目标**：提出一种能够灵活整合所有观测模态、缓解模态崩溃并提高插补效率和鲁棒性的框架。

### 2. 论文提出的方法论：GraphPL
GraphPL 结合了变分自编码器（VAE）和图神经网络（GNN），其核心流程如下：
*   **个体编码层**：为每个模态实现独立的 VAE 编码器，将观测到的“条件模态”映射到潜在空间特征 $z$。
*   **GNN 融合模块**：
    *   **构图策略**：将每个模态视为图中的一个节点，构建一个全连接的模态-模态图。
    *   **虚拟节点插补**：将待插补的“目标模态”作为虚拟节点引入图中，通过消息传递机制从所有观测节点聚合信息。
    *   **技术细节**：采用分组图卷积（Grouped GCN-Conv）配合通道洗牌（Channel Shuffle）来促进跨组信息流动，并使用 LayerNorm 稳定训练。
*   **训练机制**：
    *   **本地训练**：在客户端内部，通过掩码（Masking）策略，轮流将一个观测模态设为目标，其余为条件，最小化条件对数似然损失（插补损失）和单模态重建损失。
    *   **全局聚合**：利用联邦学习算法（FedAvg）在不共享原始数据的前提下，同步各客户端的编码器、解码器及 GNN 参数。

### 3. 实验设计
*   **数据集/场景**：
    *   **模拟基准**：PolyMNIST（多模态数字）、MST、Quad-CelebA（人脸属性与分割）。
    *   **真实世界场景**：eICU 电子健康记录数据集（包含人口统计、诊断、治疗、药物、实验室测试、生命体征 6 种模态）。
*   **Benchmark（对比方法）**：MVAE、MMVAE、MoPoE 以及 SOTA 方法 CLAP。
*   **评估指标**：
    *   **生成质量 (GQ)**：插补出的模态是否能被预训练分类器正确识别。
    *   **表示质量 (RQ)**：插补后的特征在下游任务（如死亡率预测、疾病诊断）中的分类准确率或 AUPRC。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了使用 Adam 优化器，Batch Size 设置为 256，实验在分布式联邦学习框架下进行（FedAvg）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个模拟数据集和 1 个复杂的真实医疗数据集。
    *   在 eICU 数据集上分别测试了 10、20、50 个医院（客户端）参与的情况。
*   **充分性与客观性**：
    *   每组实验均运行 5 次并报告均值与标准差，具有统计学意义。
    *   专门设计了**噪声鲁棒性实验**，通过在推理阶段加入不同比例的噪声，验证了模型在缓解“模态崩溃”方面的优越性。
    *   实验设置考虑了类别不平衡（使用 AUPRC 指标）和统计异质性（Non-IID 采样），符合实际应用场景。

### 6. 主要结论与发现
*   **性能领先**：GraphPL 在模拟数据集上的生成质量平均提升了 **9.2%**。
*   **医疗应用价值**：在 eICU 数据集上，GraphPL 在疾病诊断、药物推荐和治疗推荐任务中分别实现了 11.5%、6.5% 和 8.0% 的性能增益。
*   **鲁棒性增强**：相比于基于 POE 的方法，GraphPL 在高噪声环境下依然能保持稳定的插补质量，证明了 GNN 动态融合机制能有效利用所有可用模态，而非盲目依赖单一模态。

### 7. 优点
*   **架构灵活性**：利用 GNN 处理变长的输入模态，天然适应拼凑学习中模态组合不确定的情况。
*   **缓解模态崩溃**：通过图交互机制强制模型学习模态间的复杂关联，而非简单的分布乘积。
*   **隐私保护**：在联邦学习框架下运行，适合医疗等对数据隐私高度敏感的领域。

### 8. 不足与局限
*   **计算开销**：虽然 GNN 提升了效果，但随着模态数量增加，全连接图的计算复杂度会上升，论文未深入讨论在大规模模态场景下的扩展性。
*   **超参数敏感度**：插补损失与重建损失之间的平衡系数 $\lambda$ 可能需要针对不同数据集进行精细调优。
*   **模态类型限制**：实验主要集中在结构化数据和简单图像上，对于更复杂的跨模态（如长文本与高分辨率视频）的插补效果尚待验证。

（完）
