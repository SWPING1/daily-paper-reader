---
title: Multicentric thrombus segmentation using an attention-based recurrent network with gradual modality dropout
title_zh: 基于注意力循环网络与渐进式模态丢弃的多中心血栓分割
authors: "Sofia Vargas-Ibarra, Vincent Vigneron, Hichem Maaref, Sonia Garcia-Salicetti"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00817v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 针对频繁缺失序列的渐进式模态丢弃
tldr: 针对缺血性中风中血栓体积小、对比度低且多模态表现不一的挑战，本研究提出了一种基于注意力机制的循环分割网络（UpAttLLSTM）。该方法结合渐进式模态丢弃策略，通过2.5D结构聚合切片上下文并融合多序列特征，有效解决了多中心数据中的各向异性、类别不平衡及序列缺失问题，显著提升了小病灶分割的鲁棒性与泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决3D脑部扫描中微小血栓因对比度低、多模态表现差异及多中心数据序列缺失导致的检测难题。
method: 提出UpAttLLSTM网络，利用循环单元聚合切片上下文，并结合注意力门控与渐进式模态丢弃策略进行特征融合与正则化。
result: "单中心测试中血栓检出率超90%（Dice 0.65），在序列缺失的多中心场景下检出率约80%（Dice 0.35）。"
conclusion: 该方法在血栓分割上表现稳健，且可推广至其他具有稀疏、微小且模态依赖特征的3D医学影像小病灶检测任务。
---

## 摘要
在3D脑部扫描中检测和描绘微小目标是医学影像领域的一个核心但尚未得到充分解决的挑战。例如，在缺血性中风中，致病血栓体积小、对比度低，且在不同模态（如磁敏感加权T2*晕染效应、DWI/ADC上的扩散受限）中表现各异，而现实世界的多中心数据则引入了领域偏移、各向异性和频繁的序列缺失。我们提出了一种方法，将基于注意力的循环分割网络（UpAttLLSTM）、逐步增加异构模态学习难度的训练计划以及渐进式模态丢弃相结合。UpAttLLSTM通过循环单元（2.5D）聚合切片间的上下文，并利用注意力门融合可用序列中的互补线索，使其对各向异性和类别不平衡具有鲁棒性。渐进式模态丢弃在训练过程中系统地模拟了中心异质性、噪声和模态缺失，既作为数据增强又作为正则化手段，以提高多中心泛化能力。在单中心队列中，我们的方法在超过90%的病例中检测到了血栓，Dice分数为0.65。在存在模态缺失的多中心设置下，它实现了约80%的检测率，Dice分数约为0.35。除了中风，所提出的方法还可以直接迁移到3D医学影像中的其他小病灶任务，这些任务的目标稀少、细微且依赖于特定模态。

## Abstract
Detecting and delineating tiny targets in 3D brain scans is a central yet under-addressed challenge in medical imaging.In ischemic stroke, for instance, the culprit thrombus is small, low-contrast, and variably expressed across modalities(e.g., susceptibility-weighted T2 blooming, diffusion restriction on DWI/ADC), while real-world multi-center dataintroduce domain shifts, anisotropy, and frequent missing sequences. We introduce a methodology that couples an attention-based recurrent segmentation network (UpAttLLSTM), a training schedule that progressively increases the difficulty of hetero-modal learning, with gradual modality dropout, UpAttLLSTM aggregates context across slices via recurrent units (2.5D) and uses attention gates to fuse complementary cues across available sequences, making it robust to anisotropy and class imbalance. Gradual modality dropout systematically simulates site heterogeneity,noise, and missing modalities during training, acting as both augmentation and regularization to improve multi-center generalization. On a monocentric cohort, our approach detects thrombi in >90% of cases with a Dice score of 0.65. In a multi-center setting with missing modalities, it achieves-80% detection with a Dice score around 0.35. Beyond stroke, the proposed methodology directly transfers to other small-lesion tasks in 3D medical imaging where targets are scarce, subtle, and modality-dependent

---

## 论文详细总结（自动生成）

### 论文总结：基于注意力循环网络与渐进式模态丢弃的多中心血栓分割

#### 1. 核心问题与整体含义
该研究针对缺血性中风（Ischemic Stroke）诊断中的核心挑战——**脑部微小血栓的自动分割**。血栓在3D MRI扫描中体积微小、对比度低，且在不同序列（如DWI、ADC、SWI/SWAN）中的表现具有高度异质性。此外，现实临床环境中的多中心数据存在设备差异（如GE与西门子）、各向异性以及频繁的**模态缺失**问题。研究动机在于开发一种既能利用多模态互补信息，又能对序列缺失和中心偏移保持鲁棒性的深度学习框架。

#### 2. 方法论
论文提出了 **UpAttLLSTM** 架构及 **渐进式模态丢弃（Gradual Modality Dropout）** 策略：
*   **UpAttLLSTM 网络：**
    *   **交叉注意力模块（Cross-Attention）：** 以病灶信息丰富的 DWI 序列作为 Query，引导模型在血栓特征明显的磁敏感加权序列（SWAN/PHASE）中定位目标。
    *   **上采样残差连接（Upsampling）：** 通过残差结构恢复在注意力计算过程中损失的图像细节。
    *   **逻辑长短期记忆网络（Logic LSTM, LLSTM）：** 一种轻量化的循环单元，通过处理连续切片（2.5D 方式）来捕捉空间上下文，模拟医生连续翻看切片的诊断过程，且参数量远低于标准 ConvLSTM。
*   **渐进式模态丢弃：**
    *   **核心思想：** 不同于传统的随机丢弃（0或1），该方法引入时间相关函数 $g_j(t)$。在训练初期保留所有模态，随训练进行逐渐降低某些模态的对比度直至完全变黑（丢弃）。
    *   **作用：** 这种“由易到难”的训练计划既起到了数据增强的作用，又作为正则化手段提升了模型在模态缺失情况下的泛化能力。

#### 3. 实验设计
*   **数据集：** 整合了 6 个数据集，包括公开数据集（ISLES, JHU）和多个单中心/多中心私有数据集（CHSF, MATAR, MATAR2, FOCH），涵盖了不同品牌扫描仪和多种中风类型。
*   **Benchmark 与对比方法：**
    *   **分割基准：** nnUnet（医学影像分割标杆）、CLSTM（标准卷积LSTM）。
    *   **缺失模态处理对比：** MultiUnet（固定概率丢弃）、ModDrop++（动态滤波器网络）。
*   **实验场景：** 分为域内评估（In-domain）、跨中心域外评估（Out-domain，模拟未见中心且模态缺失）以及针对血栓的单中心/多中心专项测试。

#### 4. 资源与算力
*   论文提到使用了法国国家高性能计算资源 **IDRIS (GENCI)** 的算力支持。
*   **具体细节：** 文中未明确列出具体的 GPU 型号和数量，但提到在迁移学习阶段额外进行了 400 个 epoch 的训练。模型参数量约为 200 万（2M），远小于 nnUnet 的 3000 万（30M），表明该方法对算力需求相对较低。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了多组详尽的实验，包括针对不同丢弃概率 $p$ 的敏感性分析、针对不同切片数量的消融实验、以及针对不同架构组件（注意力机制、LLSTM、上采样）的对比。
*   **充分性：** 实验设计较为全面，通过跨中心验证（如在不含 ISLES 数据的情况下训练并在其上测试）证明了泛化性。对比了多种 SOTA 缺失模态处理技术，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升：** UpAttLLSTM 在单中心血栓检测率上超过 90%（Dice 0.65），显著优于 nnUnet。
*   **鲁棒性：** 渐进式模态丢弃在处理“未见中心+模态缺失”场景时表现优异，Dice 分数接近“全模态已知”的上界。
*   **轻量化优势：** LLSTM 在保持甚至提升性能的同时，大幅减少了模型参数，降低了过拟合风险。
*   **临床价值：** 模型推理时间小于 3 分钟，能够为急诊中风治疗提供快速的血栓位置、形状和体积信息。

#### 7. 优点
*   **针对性强：** 专门为医学影像中“微小目标”和“模态依赖”问题设计了交叉注意力机制。
*   **策略创新：** 渐进式丢弃策略比传统的随机丢弃更稳定，有助于模型平滑地学习缺失模态的表征。
*   **实用性高：** 考虑了现实中扫描仪品牌不同导致的序列差异（如 PHASE 缺失），具有很强的临床落地潜力。

#### 8. 不足与局限
*   **性能鸿沟：** 在极端缺失模态的多中心场景下，Dice 分数从 0.65 降至 0.35 左右，说明复杂环境下的精确分割仍有提升空间。
*   **假阳性风险：** 尽管引入了病灶距离约束等后处理，但在处理极小血栓时，背景噪声引起的假阳性（FP）依然是一个挑战。
*   **模态依赖：** 方法高度依赖于磁敏感加权序列（SWAN/SWI），如果该类序列完全缺失，模型性能可能会大幅受限。

（完）
