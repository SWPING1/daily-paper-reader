---
title: "CLIP-AUTT: Test-Time Personalization with Action Unit Prompting for Fine-Grained Video Emotion Recognition"
title_zh: CLIP-AUTT：基于动作单元提示的测试时个性化细粒度视频情感识别
authors: "Muhammad Osama Zeeshan, Masoumeh Sharafi, Benoît Savary, Alessandro Lameiras Koerich, Marco Pedersoli, Eric Granger"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.27999v2"
tags: ["query:missing-modality"]
score: 9.0
evidence: 使用CLIP进行细粒度视频情感识别
tldr: 针对细粒度视频情感识别中个性化不足及现有CLIP方法依赖噪声文本的问题，本文提出CLIP-AU和CLIP-AUTT。CLIP-AU利用动作单位（AU）作为结构化文本提示来捕捉细微表情，而CLIP-AUTT通过熵引导的时间窗口选择和提示微调实现测试时个性化适配。实验证明该方法在多个数据集上优于现有SOTA方法，实现了鲁棒且个性化的情感识别。
selection_source: fresh_fetch
motivation: 现有的CLIP情感识别方法依赖噪声较大的LLM生成文本，且难以捕捉细粒度及受试者特有的表情变化。
method: 提出将动作单位（AU）作为结构化提示，并结合熵引导的时间窗口选择进行测试时提示微调，以实现受试者相关的动态适配。
result: 在BioVid、StressID和BAH三个挑战性视频数据集上，该方法显著优于现有的CLIP基础模型和测试时自适应（TTA）方法。
conclusion: 通过结合AU语义提示与测试时个性化技术，本研究为细粒度视频情感识别提供了一种高效、可解释且具备强泛化能力的解决方案。
---

## 摘要
情感识别（ER）中的个性化对于准确解释细微且具有受试者特异性的表达模式至关重要。视觉语言模型（VLM）（如 CLIP）的最新进展展示了在情感识别中利用图像-文本联合表示的巨大潜力。然而，基于 CLIP 的方法要么依赖于 CLIP 的对比预训练，要么依赖于大语言模型（LLM）生成描述性文本提示，这些提示存在噪声、计算成本高，且无法捕捉细粒度的表情，从而导致性能下降。在这项工作中，我们将动作单元（AU）作为 CLIP 中的结构化文本提示，用于建模细粒度的面部表情。AU 编码了表情背后的细微肌肉激活，为更稳健的情感识别提供了局部且可解释的语义线索。我们引入了 CLIP-AU，这是一种轻量级的 AU 引导时序学习方法，将可解释的 AU 语义集成到 CLIP 中。它通过将 AU 提示与面部动态对齐来学习通用的、与受试者无关的表示，从而在无需 CLIP 微调或 LLM 生成文本监督的情况下实现细粒度情感识别。尽管 CLIP-AU 建模了细粒度的 AU 语义，但它无法适应细微表情中受试者特有的变异性。为了解决这一局限性，我们提出了 CLIP-AUTT，这是一种基于视频的测试时个性化方法，能够动态地将 AU 提示调整为来自未知受试者的视频。通过结合熵引导的时序窗口选择与提示微调，CLIP-AUTT 在保持时序一致性的同时实现了受试者特异性的自适应。我们在 BioVid、StressID 和 BAH 三个具有挑战性的基于视频的细微情感识别数据集上进行了广泛实验，结果表明 CLIP-AU 和 CLIP-AUTT 优于最先进的基于 CLIP 的面部表情识别（FER）和测试时自适应（TTA）方法，实现了稳健且个性化的细微情感识别。我们的代码已公开：https://github.com/osamazeeshan/CLIP-AUTT。

## Abstract
Personalization in emotion recognition (ER) is essential for an accurate interpretation of subtle and subject-specific expressive patterns. Recent advances in vision-language models (VLMs) such as CLIP demonstrate strong potential for leveraging joint image-text representations in ER. However, CLIP-based methods either depend on CLIP's contrastive pretraining or on LLMs to generate descriptive text prompts, which are noisy, computationally expensive, and fail to capture fine-grained expressions, leading to degraded performance. In this work, we leverage Action Units (AUs) as structured textual prompts within CLIP to model fine-grained facial expressions. AUs encode the subtle muscle activations underlying expressions, providing localized and interpretable semantic cues for more robust ER. We introduce CLIP-AU, a lightweight AU-guided temporal learning method that integrates interpretable AU semantics into CLIP. It learns generic, subject-agnostic representations by aligning AU prompts with facial dynamics, enabling fine-grained ER without CLIP fine-tuning or LLM-generated text supervision. Although CLIP-AU models fine-grained AU semantics, it does not adapt to subject-specific variability in subtle expressions. To address this limitation, we propose CLIP-AUTT, a video-based test-time personalization method that dynamically adapts AU prompts to videos from unseen subjects. By combining entropy-guided temporal window selection with prompt tuning, CLIP-AUTT enables subject-specific adaptation while preserving temporal consistency. Our extensive experiments on three challenging video-based subtle ER datasets, BioVid, StressID, and BAH, indicate that CLIP-AU and CLIP-AUTT outperform state-of-the-art CLIP-based FER and TTA methods, achieving robust and personalized subtle ER. Our code is publicly available at: https://github.com/osamazeeshan/CLIP-AUTT.

---

## 论文详细总结（自动生成）

以下是对论文《CLIP-AUTT: Test-Time Personalization with Action Unit Prompting for Fine-Grained Video Emotion Recognition》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 CLIP 的情感识别方法在处理**细粒度（Fine-grained）**和**受试者特异性（Subject-specific）**的微弱表情时表现不佳。
*   **研究动机**：
    *   **提示词局限性**：通用类别提示（如“一个表情为痛苦的人”）或 LLM 生成的描述过于笼统且带有噪声，无法捕捉面部肌肉微小的激活模式。
    *   **缺乏个性化**：不同个体表达同一情感的方式存在差异（如同样的皱眉对某人是痛苦，对另一人可能是中性），现有模型难以在推理阶段针对新受试者进行动态调整。
    *   **数据依赖**：细粒度表情（如疼痛、压力）的标注成本高，且 Action Unit（AU）的逐帧标注极度耗时。

### 2. 方法论
论文提出了两个阶段的方法：**CLIP-AU**（基础对齐）和 **CLIP-AUTT**（测试时个性化）。

*   **CLIP-AU（AU 引导的时序学习）**：
    *   **核心思想**：将 46 个面部动作单元（AU）的自然语言描述作为结构化文本提示（如“内眉提升”、“鼻部皱缩”），利用 CLIP 的跨模态空间建立面部动态与肌肉动作的语义关联。
    *   **关键技术**：
        *   **时序模块**：使用轻量级 **1D-CNN + 门控线性单元（GLU）** 处理视频帧特征，捕捉微弱表情的动态演变。
        *   **AU 适配器（AU Adapter）**：在冻结的 CLIP 文本编码器后添加轻量级 MLP，将通用文本嵌入转化为 AU 特异性语义。
        *   **弱监督学习**：通过计算视频特征与 AU 提示的余弦相似度得到“AU 激活向量”，并在此基础上训练情感分类器，**无需 AU 标签**即可学习细粒度特征。

*   **CLIP-AUTT（测试时个性化自适应）**：
    *   **熵引导的时间窗口选择**：由于视频中并非所有帧都包含有效表情，利用滑动窗口计算 AU 预测的**香农熵**，自动筛选出预测最自信（熵最低）、表达最明显的关键片段进行自适应。
    *   **AU 提示微调（AU Prompt Tuning）**：在推理阶段，针对特定受试者的视频，通过**最小化熵损失**动态优化 AU 文本嵌入。这使得模型能根据该受试者的面部特征调整 AU 的权重和语义对齐，实现“一人一模”。

### 3. 实验设计
*   **数据集**：
    *   **BioVid Heat Pain**：热诱导疼痛表情数据集（受控环境）。
    *   **StressID**：多模态压力识别数据集（受控环境）。
    *   **BAH**：行为矛盾/犹豫数据集（半受控、真实世界环境，极具挑战性）。
*   **Benchmark 与对比方法**：
    *   **CLIP 基础模型**：Zero-shot CLIP, Fine-tuned CLIP。
    *   **视频情感识别 SOTA**：EmoCLIP, X-CLIP, Exp-CLIP。
    *   **测试时自适应（TTA）方法**：TPT, TDA, DPE, PromptAlign, ReTA, T3AL。
*   **评价指标**：加权平均召回率（WAR）、F1 分数。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU (48 GB)**。
*   **训练细节**：
    *   CLIP-AU 预训练阶段：10 个 Epoch，Batch Size 为 8。
    *   CLIP-AUTT 测试时自适应：针对每个受试者视频进行 10 次迭代优化，Batch Size 为 1。
    *   优化器：AdamW，学习率 $1 \times 10^{-3}$。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同性质的挑战性数据集上进行了验证，涵盖了从受控实验室到真实世界的场景。
*   **受试者分析**：对每个数据集的 10 名目标受试者进行了逐一的性能对比（Subject-wise analysis），展示了个性化带来的具体增益。
*   **消融实验**：非常充分。探讨了时序模块设计（Transformer vs CNN）、提示词模板选择、AU 提示与类别提示的对比、窗口长度的影响以及 AU 适配器的作用。
*   **客观性**：对比了多种最新的 TTA 方法，并使用了外部 AU 检测器（OpenFace）进行定性验证，确保了结果的客观性。

### 6. 主要结论与发现
*   **AU 提示的优越性**：相比于通用的类别提示，AU 提示能提供更细粒度的语义引导，显著提升了对微弱表情的识别能力。
*   **个性化的必要性**：CLIP-AUTT 通过测试时自适应，在所有数据集上均刷新了 SOTA 记录，尤其是在 F1 分数上提升巨大（如在 StressID 上 F1 提升了约 14%），证明了捕捉个体差异的重要性。
*   **时序一致性**：熵引导的窗口选择能有效过滤视频中的中性帧噪声，使自适应过程更加稳健。

### 7. 优点
*   **可解释性强**：利用 AU 作为中介，模型不仅能给出情感分类，还能通过 AU 权重展示哪些肌肉动作支撑了该判断。
*   **高效轻量**：无需微调庞大的 CLIP 主干网络，仅优化极少量的提示参数和适配器，适合测试时快速部署。
*   **无需细粒度标注**：在没有 AU 标签的情况下实现了 AU 级别的特征对齐，具有很强的实用价值。

### 8. 不足与局限
*   **极端环境挑战**：在 BAH 数据集（真实世界、光照变化大、存在说话动作）上，虽然优于对比方法，但绝对性能仍有提升空间，说明环境噪声仍是细粒度识别的瓶颈。
*   **自适应漂移风险**：虽然使用了熵最小化，但在极短或信息极度匮乏的视频中，测试时优化可能存在过拟合到噪声的风险。
*   **应用限制**：目前主要针对单人视频，对于多人交互或复杂背景下的细粒度表情识别尚未深入探讨。

（完）
