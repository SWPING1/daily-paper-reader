---
title: Towards Emotion Recognition with 3D Pointclouds Obtained from Facial Expression Images
title_zh: 迈向基于面部表情图像获取的 3D 点云的情绪识别
authors: "Laura Rayón Ropero, Jasper De Laet, Filip Lemic, Pau Sabater Nácher, Nabeel Nisar Bhat, Sergi Abadal, Jeroen Famaey, Eduard Alarcón, Xavier Costa-Pérez"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27798v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 使用替代传感的脸部情感识别
tldr: "针对传统2D面部表情识别存在的隐私泄露和难以持续监测的问题，本文提出利用高频无线传感生成的3D点云进行识别。为解决3D标注数据匮乏，研究者开发了基于FLAME的方法将2D图像转换为3D点云，构建了AffectNet3D数据集。通过PointNet++模型验证，该方法在隐私保护和持续监测方面表现出巨大潜力，准确率可达70%以上，为可穿戴情感计算提供了新思路。"
selection_source: fresh_fetch
motivation: 旨在解决传统2D图像表情识别中的隐私安全隐患及无法在可穿戴设备上进行持续、实时监测的局限性。
method: 开发了一种基于FLAME模型从现有2D图像库生成3D面部点云的流程，并构建了AffectNet3D数据集及相应的点云细化与识别框架。
result: "实验表明，在合成数据上预训练并在少量真实3D数据上微调后，模型准确率超过70%，且在部分遮挡的模拟可穿戴场景下表现稳健。"
conclusion: 研究证明了通过合成3D点云数据实现隐私感知、持续性面部表情识别的技术可行性，为未来高频无线传感器的应用奠定了基础。
---

## 摘要
面部情绪识别（FER）是情感计算（Affective Computing）中的一个关键研究领域，在人机交互、心理健康评估和疲劳监测等方面具有广泛的应用。目前的 FER 方法主要依赖于在 2D 图像数据上训练的深度学习技术，这带来了显著的隐私问题，且不适用于持续的实时监测。作为替代方案，我们提出将高频无线传感（HFWS）作为实现持续且具备隐私保护意识的 FER 的手段，通过嵌入可穿戴设备的随身传感器生成详细的面部 3D 点云。我们提出了支持 HFWS 相比传统 2D 成像在隐私方面具有优势的论据，特别是在日益严格的数据保护法规背景下。将 HFWS 应用于 FER 的一个主要障碍是带标签的 3D FER 数据集的匮乏。为了解决这一问题，我们引入了一种基于 FLAME 的方法，从现有的公开 2D 数据集中生成 3D 面部点云。利用该方法，我们创建了 AffectNet 数据库的 3D 版本——AffectNet3D。为了评估生成数据的质量和可用性，我们设计了一个专注于隔离面部区域的点云精细化流水线，并在精细化后的点云上训练了流行的 PointNet++ 模型。在未见过的 3D FER 数据集 BU-3DFE 的一小部分子集上对模型进行微调，分类准确率超过 70%，与 Oracle 级别的性能相当。为了进一步研究基于 HFWS 的 FER 在持续监测方面的潜力，我们通过遮蔽部分生成的点云来模拟可穿戴传感条件。实验结果表明，在 AffectNet3D 上训练并仅使用 25% 的 BU-3DFE 进行微调的模型，其表现优于仅在 BU-3DFE 上训练的模型。这些发现突显了我们流水线的可行性，并支持了通过可穿戴 HFWS 系统实现持续且具备隐私保护意识的 FER 的可行性。

## Abstract
Facial Emotion Recognition is a critical research area within Affective Computing due to its wide-ranging applications in Human Computer Interaction, mental health assessment and fatigue monitoring. Current FER methods predominantly rely on Deep Learning techniques trained on 2D image data, which pose significant privacy concerns and are unsuitable for continuous, real-time monitoring. As an alternative, we propose High-Frequency Wireless Sensing (HFWS) as an enabler of continuous, privacy-aware FER, through the generation of detailed 3D facial pointclouds via on-person sensors embedded in wearables. We present arguments supporting the privacy advantages of HFWS over traditional 2D imaging, particularly under increasingly stringent data protection regulations. A major barrier to adopting HFWS for FER is the scarcity of labeled 3D FER datasets. Towards addressing this issue, we introduce a FLAME-based method to generate 3D facial pointclouds from existing public 2D datasets. Using this approach, we create AffectNet3D, a 3D version of the AffectNet database. To evaluate the quality and usability of the generated data, we design a pointcloud refinement pipeline focused on isolating the facial region, and train the popular PointNet++ model on the refined pointclouds. Fine-tuning the model on a small subset of the unseen 3D FER dataset BU-3DFE yields a classification accuracy exceeding 70%, comparable to oracle-level performance. To further investigate the potential of HFWS-based FER for continuous monitoring, we simulate wearable sensing conditions by masking portions of the generated pointclouds. Experimental results show that models trained on AffectNet3D and fine-tuned with just 25% of BU-3DFE outperform those trained solely on BU-3DFE. These findings highlight the viability of our pipeline and support the feasibility of continuous, privacy-aware FER via wearable HFWS systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Emotion Recognition with 3D Pointclouds Obtained from Facial Expression Images》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统基于 2D 图像的面部情绪识别（FER）面临两大挑战：一是**隐私风险**，2D 图像包含丰富的纹理和身份信息，容易违反如欧盟《人工智能法案》（EU AI Act）等数据保护法规；二是**监测局限性**，摄像头难以在可穿戴设备上实现非侵入式的持续监测。
*   **研究背景**：3D 点云数据（可通过高频无线传感 HFWS 如太赫兹雷达生成）仅保留几何结构而去除纹理，具有天然的隐私保护优势。然而，高质量、大规模的标注 3D FER 数据集极度匮乏，限制了深度学习模型在该领域的应用。

### 2. 论文提出的方法论
*   **核心思想**：利用现有的海量 2D FER 数据集，通过统计面部模型将其转换为 3D 点云，从而解决 3D 数据短缺问题，并构建一套适用于可穿戴设备的隐私感知识别流水线。
*   **关键技术细节**：
    1.  **数据生成（AffectNet3D）**：采用 **FLAME**（Faces Learned with an Articulated Model and Expressions）模型，从 2D 图像中回归面部参数，结合 **ZoeDepth** 和 **Boost Your Own Depth** 技术生成高精度 3D 面部网格，并采样为点云。
    2.  **点云细化流水线（Refinement Pipeline）**：
        *   **2D 处理段**：通过投影和霍夫圆变换（Hough Circle Transform）定位双眼中心及半径。
        *   **3D 处理段**：以眼睛为解剖锚点，建立裁剪平面（距离设为眼半径的 2.3 倍），剔除与情绪无关的后脑勺和颈部区域。
    3.  **特征提取与分类**：使用 **PointNet++** 作为骨干网络，通过采样（Sampling）、分组（Grouping）和 PointNet 层提取层级化局部特征。
    4.  **预训练与微调策略**：在合成的 AffectNet3D 上进行大规模预训练，然后在少量真实 3D 数据（BU-3DFE）上进行微调，以弥合合成数据与真实传感数据之间的领域鸿沟。

### 3. 实验设计
*   **数据集**：
    *   **AffectNet**：原始 2D 数据集，用于生成 30,000 个 3D 样本（AffectNet3D）。
    *   **BU-3DFE**：权威的真实 3D 面部表情数据库，用于验证和微调。
*   **Benchmark（基准）**：
    *   **Oracle 方案**：使用 80% 的 BU-3DFE 真实数据直接训练。
    *   **Baseline 方案**：仅使用 10% 或 25% 的 BU-3DFE 数据训练。
*   **对比场景**：
    *   全脸点云识别。
    *   **模拟可穿戴遮挡场景**：通过掩码（Masking）模拟智能眼镜（仅覆盖眼部周围）和头显（HMD，覆盖上半脸）的传感范围，评估模型在有限观测下的稳健性。

### 4. 资源与算力
*   **硬件环境**：NVIDIA A100 80GB PCIe GPU，4 核 CPU，16GB RAM。
*   **训练时长**：
    *   在 AffectNet3D 上训练（约 3 万样本）：耗时约 **1764 至 2040 分钟**（约 30 小时）。
    *   在 BU-3DFE 上微调（15 个 Epoch）：仅需约 **8 分钟**。
*   **推理效率**：文中提到模型量化后在嵌入式 NPU 上推理延迟约为 10-20ms，满足实时性要求。

### 5. 实验数量与充分性
*   **实验组数**：共设计了 6 组核心实验（E1-E6），涵盖了从原始数据训练、细化数据训练到不同比例微调的对比。
*   **充分性与客观性**：
    *   实验设计较为全面，通过对比“仅用少量真实数据”与“合成数据预训练+少量真实数据微调”，清晰地证明了合成数据的价值。
    *   引入了混淆矩阵分析和遮挡敏感性分析，增强了结果的说服力。
    *   **公平性**：所有对比实验均在相同的 PointNet++ 架构下进行，确保了变量控制。

### 6. 主要结论与发现
*   **合成数据的有效性**：在 AffectNet3D 上预训练并用 25% 的 BU-3DFE 微调后，准确率达到 **70.64%**，显著优于直接在少量真实数据上训练的结果（49.12%），且接近全量训练的 Oracle 水平（78.9%）。
*   **细化的重要性**：点云细化（裁剪非面部区域）虽然没有显著提升最终准确率，但将训练时间缩短了约 **4 倍**，大幅提升了效率。
*   **可穿戴可行性**：在模拟眼镜或头显的局部遮挡情况下，模型仍能保持较高的识别率，证明了 HFWS 结合可穿戴设备进行持续监测的潜力。

### 7. 优点（亮点）
*   **隐私感知设计**：从底层表征出发，利用 3D 几何代替 2D 纹理，符合现代 AI 伦理和法规要求。
*   **数据增强新思路**：提出了一种从 2D 图像“无中生有”创造大规模 3D 标注数据的完整流水线，解决了 3D FER 领域长期存在的数据饥渴问题。
*   **跨模态迁移**：成功实现了从“2D 图像生成的合成 3D”到“真实 3D 扫描数据”的知识迁移。

### 8. 不足与局限
*   **领域鸿沟（Domain Gap）**：直接推理准确率极低（约 16%），说明合成 3D 数据与真实 3D 扫描数据在分布上仍有较大差异，必须依赖微调。
*   **硬件模拟局限**：目前的 HFWS 传感效果是通过对干净的 3D 数据进行掩码模拟的，尚未在真实的太赫兹雷达硬件原型上进行端到端验证，实际环境中的噪声（如多径效应、身体运动）可能影响性能。
*   **身份偏见风险**：虽然去除了纹理，但 3D 几何结构仍可能携带身份特征，论文未进行严谨的抗重构攻击测试。
*   **模型单一**：仅使用了 PointNet++，未对比更先进的 Graph CNN 或 Transformer 架构在点云 FER 上的表现。

（完）
