---
title: Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective
title_zh: 从因果推理视角学习不变模态表示以实现鲁棒的多模态学习
authors: "Sijie Mai, Shiqin Han"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18460v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 用于鲁棒多模态情感计算的因果解耦
tldr: 针对多模态情感计算中模型易受虚假相关性影响、泛化能力差的问题，本文提出了一种基于因果推理的因果模态不变表示（CmIR）学习框架。该框架通过解耦学习，将各模态分解为因果不变表示和环境特定虚假表示，并结合不变性、互信息和重构约束，确保表示的稳定性和信息完整性。实验证明，CmIR在多项基准测试中达到SOTA，尤其在处理分布偏移和噪声数据时表现出极强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18460v1/fig-001.webp\", \"caption\": \"Table 1: Comparisons on the CMU-MOSI and CMU-MOSEI datasets. The results labeled with † are obtained from original papers, and other results are obtained from our experiments. The best results are highlighted.\", \"page\": 8, \"index\": 1, \"width\": 908, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-002.webp\", \"caption\": \"Figure 7: The structures of encoder, decoder, and predictor. d ′ represents the hidden dimensionality.\", \"page\": 19, \"index\": 2, \"width\": 458, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-003.webp\", \"caption\": \"Table 4: Discussion on noisy modalities on CMU-MOSI and CMU-MOSEI. MuBo denotes Multimodal Boosting.\", \"page\": 9, \"index\": 3, \"width\": 867, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-004.webp\", \"caption\": \"Figure 3: The overall framework of CmIR and the visualization of the proposed constraints.\", \"page\": 5, \"index\": 4, \"width\": 902, \"height\": 329}]"
motivation: 现有多模态模型容易学习到虚假相关性，导致在分布偏移或存在噪声的情况下泛化性能下降。
method: 提出CmIR框架，利用因果推理将模态解耦为因果不变表示和环境特定虚假表示，并施加多种约束以保证表示的稳定性。
result: 在多个多模态基准测试中取得SOTA性能，特别是在分布外（OOD）和噪声数据上表现优异。
conclusion: 通过因果解耦学习不变表示能有效提升多模态学习的鲁棒性和泛化能力。
---

## 摘要
多模态情感计算旨在利用语言、声学和视觉模态来预测人类的情感、情绪、意图和观点。然而，当前模型通常会学习到虚假相关性，这在分布偏移或存在噪声模态的情况下会损害泛化能力。为解决这一问题，我们提出了一种因果模态不变表示（CmIR）学习框架，用于鲁棒的多模态学习。其核心是，我们从因果推理的角度引入了一种具有理论依据的解耦方法，将每个模态分解为“因果不变表示”和“特定于环境的虚假表示”。CmIR 通过不变性约束、互信息约束和重构约束，确保学习到的不变表示在不同环境下与标签保持稳定的预测关系，同时保留来自原始输入的充分信息。在多个多模态基准测试上的实验表明，CmIR 达到了最先进的性能。CmIR 在分布外数据和噪声数据上表现尤为出色，证实了其鲁棒性和泛化能力。

## Abstract
Multimodal affective computing aims to predict humans' sentiment, emotion, intention, and opinion using language, acoustic, and visual modalities. However, current models often learn spurious correlations that harm generalization under distribution shifts or noisy modalities. To address this, we propose a causal modality-invariant representation (CmIR) learning framework for robust multimodal learning. At its core, we introduce a theoretically grounded disentanglement method that separates each modality into `causal invariant representation' and `environment-specific spurious representation' from a causal inference perspective. CmIR ensures that the learned invariant representations retain stable predictive relationships with labels across different environments while preserving sufficient information from the raw inputs via invariance constraint, mutual information constraint, and reconstruction constraint. Experiments across multiple multimodal benchmarks demonstrate that CmIR achieves state-of-the-art performance. CmIR particularly excels on out-of-distribution data and noisy data, confirming its robustness and generalizability.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective》（从因果推理视角学习不变模态表示以实现鲁棒的多模态学习）的结构化深度分析总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态情感计算（MAC）模型在训练时容易捕获**虚假相关性**（Spurious Correlations），而非真正的因果关系。例如，模型可能过度依赖某个说话者特有的微笑（虚假视觉特征）来预测正面情感，当测试环境发生变化（如说话风格、光照、背景噪声改变）时，模型性能会大幅下降。
*   **研究背景**：现有的领域自适应或解耦方法缺乏因果解释性，且往往依赖于对特定偏见（如说话者偏见）的先验假设，难以应对未知的分布偏移（OOD）和模态噪声。

### 2. 方法论：CmIR 框架
论文提出了**因果模态不变表示（CmIR）**学习框架，旨在将每个模态解耦为两个互补部分：
*   **核心思想**：利用因果推理和信息论，将原始模态 $X_m$ 分解为：
    1.  **因果不变表示 ($Z_{inv}$)**：在不同环境下与标签保持稳定因果关系的特征。
    2.  **环境特定虚假表示 ($Z_{spu}$)**：捕获与环境相关的噪声，与标签无因果联系。
*   **关键技术细节与约束**：
    *   **虚拟环境模拟**：由于现实数据缺乏环境标签，通过向原始特征注入不同强度的噪声来模拟 $K$ 个虚拟环境。
    *   **不变性约束 ($R_{inv}$)**：强制 $Z_{inv}$ 在不同虚拟环境下保持一致，确保其对环境扰动不敏感。
    *   **互信息约束 ($R_{dec}$)**：通过特征正交化作为互信息的代理指标，最小化 $Z_{inv}$ 与 $Z_{spu}$ 之间的相关性，实现彻底解耦。
    *   **重构约束 ($R_{rec}$)**：使用解码器从 $(Z_{inv}, Z_{spu})$ 对中重构原始输入，防止信息丢失。
*   **理论保证**：论文提供了数学证明，确保了不变表示的存在性、可提取性，并证明了基于不变表示的预测器在最坏情况下的 OOD 风险低于原始特征预测器。

### 3. 实验设计
*   **任务与数据集**：
    *   **情感分析 (MSA)**：CMU-MOSI, CMU-MOSEI, CH-SIMS-v2。
    *   **幽默检测 (MHD)**：UR-FUNNY。
    *   **讽刺检测 (MSD)**：MUStARD。
    *   **分布外测试 (OOD)**：专门使用了 CMU-MOSI (OOD) 版本。
*   **对比方法**：包括经典的 Self-MM, MISA, 以及最新的因果推理方法（如 SuCI, CLUE, GEAR, AtCAF）和信息论方法（如 ITHP, C-MIB）。
*   **评估指标**：Acc7（七分类准确率）、Acc2（二分类准确率）、F1 分数、MAE（平均绝对误差）和 Corr（相关系数）。

### 4. 资源与算力
*   **硬件环境**：使用了一台 **NVIDIA RTX 3090 GPU** (24GB 显存)。
*   **软件框架**：PyTorch 1.13.1, CUDA 11.6。
*   **训练细节**：使用了 AdamW 优化器，通过 50 次随机网格搜索确定超参数，并对最终结果进行了 5 次重复实验取平均值。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 **6 个主流数据集**上进行了性能对比。
    *   针对 **OOD 场景**进行了专门验证。
    *   进行了 **7 种不同噪声强度**（10%-70%）下的鲁棒性测试，甚至包括训练未见的噪声类型（Laplace 噪声和随机擦除）。
    *   包含详尽的**消融实验**（验证因果推理、三种约束的必要性）。
    *   包含**超参数敏感性分析**（$\lambda$ 权重、环境数量 $K$、噪声强度 $\alpha$）。
    *   包含 **T-SNE 可视化**和**显著性检验**（t-test）。
*   **评价**：实验设计非常全面且严谨，不仅覆盖了标准基准，还深入探讨了模型在极端条件下的生存能力，具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：CmIR 在所有任务中均达到了 SOTA 性能，尤其在 CMU-MOSI (OOD) 上显著超过了之前的因果方法。
*   **鲁棒性极强**：在模态噪声增加时，CmIR 的性能下降速度远慢于基准模型，证明了因果不变特征的稳定性。
*   **解耦有效性**：探测实验（Probing Experiments）证明 $Z_{inv}$ 包含预测标签的信息，而 $Z_{spu}$ 主要包含环境/噪声信息，两者实现了有效的语义分离。

### 7. 优点与亮点
*   **理论深度**：不仅提出了算法，还从因果图模型（SCM）出发提供了严密的数学证明，提升了论文的学术厚度。
*   **通用性**：不依赖于特定的偏见假设（如性别或说话者 ID），通过噪声模拟环境，使其能够处理更广泛的隐式偏见。
*   **高效性**：模型复杂度分析显示，CmIR 在参数量和计算量（FLOPs）上均优于许多复杂的 SOTA 模型（如 EMOE, GSCon）。

### 8. 不足与局限
*   **环境模拟简化**：虽然噪声注入有效，但它主要模拟的是观测噪声，可能无法完全覆盖现实世界中复杂的结构性分布偏移（如文化背景差异导致的表达变化）。
*   **互信息代理**：使用正交性作为互信息的代理虽然高效，但在处理非线性相关性时可能不够精确。
*   **超参数依赖**：模型引入了多个超参数（$\lambda_1, \lambda_2, \lambda_3$ 及噪声强度），在不同数据集上可能需要精细调优才能达到最佳效果。

（完）
