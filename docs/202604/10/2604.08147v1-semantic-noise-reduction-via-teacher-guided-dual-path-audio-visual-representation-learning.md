---
title: Semantic Noise Reduction via Teacher-Guided Dual-Path Audio-Visual Representation Learning
title_zh: 通过教师引导的双路径视听表征学习减少语义噪声
authors: "Linge Wang, Yingying Chen, Bingke Zhu, Lu Zhou, Jinqiao Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08147v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 在视听学习中解耦重建与对齐
tldr: 本研究针对音视频表示学习中对比对齐与掩码重建目标冲突的问题，提出了TG-DP框架。该框架通过双路径设计将重建与对齐任务解耦，并引入教师模型引导对比分支的可见令牌组织，有效减少了语义噪声和优化干扰。实验表明，TG-DP在零样本检索和线性探测任务上均达到了SOTA水平，显著提升了大规模音视频预训练的效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08147v1/fig-001.webp\", \"caption\": \"Figure 2. Overall pipeline of our proposed framework. The model consists of two objective-specific forward passes: (1) the reconstruction branch (left) learns modality-specific generative representations by randomly masking both audio and visual patches and reconstructing them through separate decoders. (2) the contrastive branch (right) adopts a teacher-student scheme for cross-modal alignment under a lower masking ratio. The teacher model observes full, unmasked inputs and produces attention-based token priority cues to guide the student’s masking strategy. The student encodes the masked tokens, appends modality-specific global tokens (gv, ga), and aligns them via the InfoNCE-based contrastive loss Lcontra. An additional distillation loss Ldis encourages consistency between teacher and student global embeddings. The encoders in the reconstruction branch share weights with the student encoders in the contrastive branch. The final training objective combines all terms as Lall = λ1Lrec + λ2Ldis + λ3Lcontra.\", \"page\": 4, \"index\": 1, \"width\": 964, \"height\": 564}]"
motivation: 传统的单路径联合优化迫使对比分支使用为重建设计的随机掩码，导致语义噪声和跨模态对齐干扰。
method: 提出TG-DP框架，通过解耦重建与对齐路径并利用教师模型提供辅助引导，优化了对比分支的可见令牌组织。
result: "在AudioSet上，视频转音频和音频转视频的零样本检索R@1分别提升至37.4%和37.1%，并在多个数据集上取得SOTA线性探测性能。"
conclusion: 解耦多模态目标并引入教师引导结构是提升大规模音视频预训练表示鲁棒性的有效途径。
---

## 摘要
视听表征学习的最新进展表明，将对比对齐与掩码重建相结合具有重要价值。然而，在单次前向传播中联合优化这些目标会迫使对比分支依赖于为重建设计的随机可见补丁，而非跨模态对齐，从而引入了语义噪声和优化干扰。我们提出了 TG-DP，这是一个教师引导的双路径框架，它将重建和对齐解耦到独立的优化路径中。通过解耦这两个分支的掩码机制，TG-DP 使对比路径能够使用更适合跨模态对齐的可见性模式。教师模型进一步为该分支中可见标记的组织提供辅助引导，有助于减少干扰并稳定跨模态表征学习。TG-DP 在零样本检索中达到了最先进的性能。在 AudioSet 数据集上，它将视频到音频检索的 R@1 从 35.2% 提升至 37.4%，将音频到视频检索的 R@1 从 27.9% 提升至 37.1%。所学习的表征还保持了语义鲁棒性，在 AS20K 和 VGGSound 上实现了最先进的线性探测性能。综上所述，我们的结果表明，解耦多模态目标并在对比路径中引入教师引导结构，为改进大规模视听预训练提供了一个有效的框架。

## Abstract
Recent advances in audio-visual representation learning have shown the value of combining contrastive alignment with masked reconstruction. However, jointly optimizing these objectives in a single forward pass forces the contrastive branch to rely on randomly visible patches designed for reconstruction rather than cross-modal alignment, introducing semantic noise and optimization interference. We propose TG-DP, a Teacher-Guided Dual-Path framework that decouples reconstruction and alignment into separate optimization paths. By disentangling the masking regimes of the two branches, TG-DP enables the contrastive pathway to use a visibility pattern better suited to cross-modal alignment. A teacher model further provides auxiliary guidance for organizing visible tokens in this branch, helping reduce interference and stabilize cross-modal representation learning. TG-DP achieves state-of-the-art performance in zero-shot retrieval. On AudioSet, it improves R@1 from 35.2\% to 37.4\% for video-to-audio retrieval and from 27.9\% to 37.1\% for audio-to-video retrieval. The learned representations also remain semantically robust, achieving state-of-the-art linear-probe performance on AS20K and VGGSound. Taken together, our results suggest that decoupling multimodal objectives and introducing teacher-guided structure into the contrastive pathway provide an effective framework for improving large-scale audio-visual pretraining. Code is available at https://github.com/wanglg20/TG-DP.

---

## 论文详细总结（自动生成）

这篇论文由中国科学院自动化研究所等机构的研究人员撰写，提出了一种名为 **TG-DP** 的新型视听表征学习框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
当前的视听表征学习通常结合了**掩码自编码（MAE）**和**对比学习（CL）**。然而，现有方法（如 CAV-MAE）通常在单次前向传播中联合优化这两个目标。这导致了两个主要问题：
*   **语义噪声：** 为重建设计的随机掩码可能会保留无意义的区域（如静音音频、背景图像），这些噪声会污染用于对比对齐的全局表征。
*   **优化干扰：** 重建任务（关注局部细节）与对比任务（关注语义一致性）的目标本质不同，共享同一套标记（Tokens）会导致梯度信号相互干扰，损害优化效果。

### 2. 论文提出的方法论
论文提出了 **TG-DP（Teacher-Guided Dual-Path）** 框架，核心思想是将重建和对齐任务在空间和时间上进行解耦：
*   **双路径解耦（Dual-Path Decoupling）：** 将模型分为“重建分支”和“对比分支”。
    *   **重建分支：** 使用高掩码率（75%），专注于学习模态内的结构信息。
    *   **对比分支：** 使用较低的掩码率（50%），专注于跨模态语义对齐。
*   **教师-学生引导机制（Teacher-Student Guidance）：** 引入一个教师编码器（学生模型的指数移动平均 EMA），输入完整的、未掩码的数据。
    *   **蒸馏损失（Distillation Loss）：** 通过均方误差（MSE）强制学生模型的掩码全局表征与教师模型的完整视图表征保持一致。
*   **教师引导的结构化掩码（Teacher-Guided Masking）：** 利用教师模型联合编码器中的注意力权重，识别对全局语义贡献最大的标记，并优先保留这些标记作为对比分支的输入，从而减少背景噪声。

### 3. 实验设计
*   **数据集：** 
    *   **AudioSet-2M：** 大规模视听数据集（实际使用约 140 万有效样本进行预训练）。
    *   **VGGSound：** 包含 20 万个高质量视听对的数据集。
*   **评估任务（Benchmark）：**
    *   **零样本检索（Zero-shot Retrieval）：** 视频转音频（V→A）和音频转视频（A→V）的 R@1, R@5, R@10。
    *   **线性/注意力探测（Linear/Attention Probing）：** 在 AS20K 和 VGGSound 上进行分类性能评估。
*   **对比方法：** 包括 CAV-MAE, ImageBind, DenseAV, AVSiam, LanguageBind 等 SOTA 模型。

### 4. 资源与算力
*   **训练时长：** 论文对比了预训练运行时间。基准模型（baseline）每轮（epoch）耗时 730 秒，总计 7.1 小时；TG-DP 每轮耗时 1045 秒，总计 **10.2 小时**。
*   **算力细节：** 文中**未明确说明**具体的 GPU 型号和数量，但提到由于增加了额外的前向传播和 EMA 教师模型，训练开销增加了约 40%。
*   **推理成本：** 强调教师模型和双路径设计仅在训练阶段使用，**推理时无额外参数或计算开销**。

### 5. 实验数量与充分性
*   **实验规模：** 论文在两个主流大规模数据集上进行了详尽测试，并提供了 4 组关键的消融实验：
    1.  双路径方案的有效性。
    2.  对比分支不同掩码率的影响（0.0 到 0.75）。
    3.  教师-学生蒸馏损失的作用。
    4.  不同掩码策略（随机 vs 教师引导 vs 概率引导）的对比。
*   **充分性评价：** 实验设计逻辑严密，消融实验覆盖了框架的各个核心组件，对比了多种 SOTA 方法，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **解耦是关键：** 将重建和对齐任务分开处理能显著提升性能，尤其是在更具挑战性的“音频转视频”检索任务中。
*   **掩码率不对称：** 对比分支适合较低的掩码率（50%），而重建分支适合高掩码率（75%）。
*   **教师引导有效：** 教师模型提供的全局语义锚点和结构化掩码策略能有效稳定训练并减少语义噪声。
*   **SOTA 性能：** 在 AudioSet 零样本检索上，A→V 的 R@1 从 27.9% 大幅提升至 37.1%。

### 7. 优点
*   **架构创新：** 巧妙地解决了 MAE 与对比学习在联合优化时的冲突问题。
*   **性能卓越：** 在不增加推理成本的前提下，显著提升了跨模态检索和分类的精度。
*   **鲁棒性强：** 教师引导的掩码机制使模型对背景噪声和静音片段更具鲁棒性。

### 8. 不足与局限
*   **训练成本增加：** 引入双路径和教师模型增加了约 40% 的训练时间和内存消耗。
*   **数据依赖：** 实验中提到由于 YouTube 视频失效，实际训练数据量少于原始 AudioSet-2M，这可能影响与在完整数据集上训练的其他模型的绝对公平对比。
*   **超参数敏感：** 框架引入了多个损失项的平衡系数（λ1, λ2, λ3）以及对比分支的掩码率，这些超参数可能需要针对不同数据集进行精细调优。

（完）
