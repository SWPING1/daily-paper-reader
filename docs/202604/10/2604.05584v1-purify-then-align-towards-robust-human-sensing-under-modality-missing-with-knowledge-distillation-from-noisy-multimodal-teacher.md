---
title: "Purify-then-Align: Towards Robust Human Sensing under Modality Missing with Knowledge Distillation from Noisy Multimodal Teacher"
title_zh: 先净化后对齐：利用噪声多模态教师知识蒸馏实现模态缺失下的鲁棒人体感知
authors: "Pengcheng Weng, Yanyu Qian, Yangxin Xu, Fei Wang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05584v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 模态缺失下的鲁棒人类感知
tldr: 针对多模态人体感知中模态缺失、异构数据表示差异及低质量模态污染等挑战，本文提出了一种名为PTA（先净化后对齐）的创新框架。该框架结合元学习与知识扩散技术，首先通过元学习动态降低噪声模态权重以净化知识源，随后利用基于扩散的知识蒸馏将净化后的多模态共识传递给单模态编码器。实验证明，PTA在MM-Fi和XRF55数据集上显著提升了单模态模型在模态缺失场景下的鲁棒性，达到了SOTA水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05584v1/fig-001.webp\", \"caption\": \"Table 1. MPJPE (mm) and PA-MPJPE (mm) on MM-Fi [46] dataset.\", \"page\": 7, \"index\": 1, \"width\": 852, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.05584v1/fig-002.webp\", \"caption\": \"Figure 1. The overall architecture of our proposed PTA framework, built on a Purify-then-Align paradigm. The model is trained in a nested loop: (a) The Purify Stage: The Purify Stage optimizes the meta-learning weights w to mitigate the Contamination Effect. (b) The Align Stage: The Align Stage uses these weights w to construct a “clean teacher” fT and then uses it in a diffusion-based process (LDiffKD) to refine each “student” feature fS , thereby bridging the Representation Gap.\", \"page\": 3, \"index\": 2, \"width\": 1036, \"height\": 360}]"
motivation: 旨在解决多模态人体感知中由于异构数据表示差异和低质量模态污染导致的模态缺失鲁棒性不足问题。
method: 提出PTA框架，利用元学习动态加权净化噪声模态，并结合扩散模型进行知识蒸馏，将多模态共识对齐到单模态编码器。
result: 在MM-Fi和XRF55等大规模数据集上的实验表明，PTA在存在严重表示差异和污染的情况下，显著优于现有方法。
conclusion: PTA通过“先净化后对齐”策略有效增强了单模态编码器的跨模态感知能力，为鲁棒的多模态人体感知提供了新方案。
---

## 摘要
鲁棒的多模态人体感知必须克服模态缺失这一关键挑战。两个主要障碍是异构数据之间的表示鸿沟（Representation Gap）以及低质量模态带来的污染效应（Contamination Effect）。这些障碍之间存在因果联系，因为污染引入的损坏从根本上阻碍了表示差异的缩小。在本文中，我们提出了 PTA，一种新颖的“先净化后对齐”框架，通过元学习与知识扩散的协同整合来解决这种因果依赖。为了净化知识源，PTA 首先采用了一种元学习驱动的加权机制，动态学习并降低噪声及低贡献模态的影响权重。随后，为了对齐不同模态，PTA 引入了一种基于扩散的知识蒸馏范式，其中由净化后的共识形成的、信息丰富的纯净教师模型，对每个学生模态的特征进行精炼。这种“先净化后对齐”策略的最终成果是创建了蕴含跨模态知识的、极其强大的单模态编码器。在大型数据集 MM-Fi 和 XRF55 上进行的全面实验表明，在显著的表示鸿沟和污染效应下，PTA 达到了最先进的性能，并显著提高了单模态模型在各种模态缺失场景下的鲁棒性。

## Abstract
Robust multimodal human sensing must overcome the critical challenge of missing modalities. Two principal barriers are the Representation Gap between heterogeneous data and the Contamination Effect from low-quality modalities. These barriers are causally linked, as the corruption introduced by contamination fundamentally impedes the reduction of representation disparities. In this paper, we propose PTA, a novel "Purify-then-Align" framework that solves this causal dependency through a synergistic integration of meta-learning and knowledge diffusion. To purify the knowledge source, PTA first employs a meta-learning-driven weighting mechanism that dynamically learns to down-weight the influence of noisy, low-contributing modalities. Subsequently, to align different modalities, PTA introduces a diffusion-based knowledge distillation paradigm in which an information-rich clean teacher, formed from this purified consensus, refines the features of each student modality. The ultimate payoff of this "Purify-then-Align" strategy is the creation of exceptionally powerful single-modality encoders imbued with cross-modal knowledge. Comprehensive experiments on the large-scale MM-Fi and XRF55 datasets, under pronounced Representation Gap and Contamination Effect, demonstrate that PTA achieves state-of-the-art performance and significantly improves the robustness of single-modality models in diverse missing-modality scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《Purify-then-Align: Towards Robust Human Sensing under Modality Missing with Knowledge Distillation from Noisy Multimodal Teacher》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多模态人体感知（如姿态估计、动作识别）中，如何应对**模态缺失**（由于硬件故障、环境干扰或部署成本导致）的挑战。
*   **研究背景**：现有的多模态学习面临两大障碍：
    1.  **表示鸿沟（Representation Gap）**：不同传感器（如LiDAR与Wi-Fi）的物理原理迥异，导致特征空间难以对齐。
    2.  **污染效应（Contamination Effect）**：低质量或高噪声模态在融合时会污染高质量模态，降低整体性能。
*   **核心动机**：作者认为这两者存在**因果联系**——噪声污染会阻碍表示鸿沟的缩小。因此，必须先“净化”知识源，再进行跨模态“对齐”。

### 2. 方法论：PTA 框架
PTA 采用“先净化后对齐”（Purify-then-Align）的范式，结合了元学习与知识扩散技术：
*   **净化阶段（Purify）**：
    *   利用**元学习（Meta-Learning）驱动的加权机制**。通过嵌套优化循环（内循环优化模型参数，外循环在验证集上优化模态权重 $w$），动态识别并降低噪声、低贡献模态的权重。
    *   这种机制避免了手动调节模态丢弃率（Dropout Rate）的繁琐，确保生成一个“纯净”的多模态教师共识特征。
*   **对齐阶段（Align）**：
    *   引入**基于扩散的知识蒸馏（DiffKD）**。将净化后的教师特征作为目标，引导单模态学生特征进行对齐。
    *   **关键技术**：
        *   **噪声适配器（Noise Adapter）**：预测缩放系数，将学生特征与高斯噪声融合，作为扩散逆过程的起点。
        *   **轻量化设计**：在压缩的潜空间（Latent Space）进行扩散，并使用 **DDIM 采样策略**（仅需 5 步推理），显著降低计算开销。

### 3. 实验设计
*   **数据集**：
    1.  **MM-Fi**：大规模人体姿态估计（HPE）数据集，包含深度图（Depth）、LiDAR 点云和 Wi-Fi CSI 信号。
    2.  **XRF55**：人体动作识别（HAR）数据集，包含毫米波雷达（mmWave）、Wi-Fi 和 RFID 信号。
*   **Benchmark 与对比方法**：
    *   **指标**：HPE 任务使用 MPJPE 和 PA-MPJPE；HAR 任务使用分类准确率（Accuracy）。
    *   **对比方法**：特征融合基准（Base.1）、决策融合基准（Base.2）以及当前最先进的模态不变基础模型 **X-Fi**。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **参数设置**：HPE 任务 Batch Size 为 16，HAR 任务为 32。
*   **时长**：文中未明确说明具体的训练总时长，但强调了扩散模块通过潜空间操作和 DDIM 采样实现了轻量化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同任务（姿态估计与动作识别）的大规模数据集。
*   **组合覆盖**：针对所有可能的模态缺失组合（如 D+L, D+W, L+W, 单模态等）进行了详尽测试。
*   **消融实验**：分别验证了“扩散对齐模块”和“元学习加权模块”的有效性。
*   **客观性**：通过与 SOTA 方法 X-Fi 在相同预处理和划分下的对比，证明了 PTA 在单模态鲁棒性上的显著优势（如在 MM-Fi 的 LiDAR 单模态上提升了 47.5%）。

### 6. 主要结论与发现
*   **单模态增强**：通过从纯净的多模态教师处学习，单模态编码器的性能大幅提升，使其在其他传感器失效时仍能保持高精度。
*   **污染抑制**：元学习能有效识别低质量模态（如 Wi-Fi CSI），防止其在融合时拖累高质量模态（如 LiDAR）。
*   **表示对齐**：扩散模型比传统的 MSE 蒸馏更能有效桥接异构模态间的表示鸿沟。

### 7. 优点与亮点
*   **因果视角**：首次提出并解决了污染效应与表示鸿沟之间的因果依赖关系。
*   **自适应性**：元学习机制实现了模态权重的自动分配，无需人工干预。
*   **效率平衡**：通过潜空间扩散和少步采样（5步），在保持生成式对齐优势的同时兼顾了推理速度。

### 8. 不足与局限
*   **结构化误差权衡**：在 MM-Fi 实验中，虽然绝对位置误差（MPJPE）大幅下降，但结构对齐误差（PA-MPJPE）在某些场景下略有上升，表明模型在提取全局位置信息时可能牺牲了部分精细的肢体结构。
*   **生成伪影风险**：消融实验显示，在极极端噪声下，扩散过程可能引入微小的生成伪影或放大固有噪声。
*   **模态范围**：目前主要验证了视觉与射频（RF）模态，对于音频、IMU 等更多元模态的泛化性尚待进一步验证。

（完）
