---
title: Semantic Compensation via Adversarial Removal for Robust Zero-Shot ECG Diagnosis
title_zh: 通过对抗性移除进行语义补偿以实现鲁棒的零样本心电图诊断
authors: "Hongjun Liu, Rujun Han, Leyu Zhou, Chao Yao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01498v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 对部分观测和缺失关键导联的鲁棒性
tldr: 针对心电图（ECG）在实际应用中常因电极脱落或伪影导致信号缺失的问题，本文提出了SCAR框架。该框架通过对抗性掩码移除关键时空特征，并利用语义监督的自适应选择器从剩余信号中补偿诊断信息，增强了ECG-语言预训练模型的鲁棒性。实验证明，SCAR在多种缺失场景下的零样本诊断和线性探测任务中均表现优异，并引入了新的鲁棒性评估指标CMRS。
selection_source: fresh_fetch
motivation: 现有的ECG-语言预训练模型在面对导联缺失或信号中断等部分观测情况时，跨模态语义对齐能力会严重下降。
method: 提出SCAR框架，利用可微对抗掩码移除关键特征，并结合语义监督的自适应选择器来挖掘剩余信号中的次要诊断线索。
result: 在6个数据集上的实验显示，SCAR显著提升了模型在复杂缺失场景下的语义鲁棒性和迁移学习能力。
conclusion: SCAR通过对抗性移除和语义补偿机制，有效解决了ECG诊断中的信号不完整问题，为鲁棒的零样本医疗诊断提供了新思路。
---

## 摘要
近期的心电图-语言预训练方法通过将心脏信号与临床文本对齐实现了零样本诊断，但它们并未显式地对部分观测下的鲁棒性进行建模，且通常是在完全观测的心电图场景下进行研究。在实践中，由于电极脱落、运动伪影或信号损坏，诊断关键导联或时间片段可能会缺失，从而导致跨模态语义对齐的严重退化。在本文中，我们提出了 SCAR，这是一个通过对抗性移除进行语义补偿（Semantic Compensation via Adversarial Removal）的鲁棒心电图-语言预训练框架。SCAR 通过显式训练模型在语义关键信息缺失的情况下保持语义对齐，并从剩余的可见证据中恢复诊断含义，从而提高鲁棒性。具体而言，我们引入了一个可微对抗掩码器，在训练期间移除对对齐最关键的时空心电图标记（tokens），迫使心电图编码器学习即使在主要诊断证据缺失时也能与临床文本保持语义对齐的表示。在这种对抗性损坏下，我们为心电图编码器配备了一个语义监督的自适应选择器，该选择器学习对剩余的可见标记进行重新加权，并利用次要但具有诊断信息量的形态学线索进行补偿。为了评估分类准确率之外的鲁棒性，我们进一步引入了反事实缺失分辨率得分（CMRS），用于量化特征在缺失情况下保留诊断语义的能力。在 6 个数据集上的实验表明，SCAR 在联合导联和时间缺失下一致地提高了语义鲁棒性，在主要诊断证据不可用的困难情况下优势尤为明显，同时也产生了更强的线性探测迁移能力。

## Abstract
Recent ECG--language pretraining methods enable zero-shot diagnosis by aligning cardiac signals with clinical text, but they do not explicitly model robustness to partial observation and are typically studied under fully observed ECG settings. In practice, diagnostically critical leads or temporal segments may be missing due to electrode detachment, motion artifacts, or signal corruption, causing severe degradation of cross-modal semantic alignment. In this paper, we propose \textbf{SCAR}, a robust ECG--language pretraining framework for \textbf{S}emantic \textbf{C}ompensation via \textbf{A}dversarial \textbf{R}emoval. SCAR improves robustness by explicitly training the model to remain semantically aligned with semantically critical missingness and to recover diagnostic meaning from the remaining visible evidence. Specifically, we introduce a differentiable adversarial masker to remove the most alignment-critical spatio-temporal ECG tokens during training, forcing the ECG encoder to learn representations that remain semantically aligned with clinical text even when primary diagnostic evidence is missing. Under such adversarial corruption, we equip the ECG encoder with a semantically supervised adaptive selector that learns to reweight the remaining visible tokens and compensate with secondary yet diagnostically informative morphological cues. To evaluate robustness beyond classification accuracy, we further introduce Counterfactual Missingness Resolution Score (CMRS), which quantifies how well feature preserve diagnostic semantics under missingness. Experiments on $6$ datasets show that SCAR consistently improves semantic robustness under joint lead and temporal missingness, with particularly clear advantages in harder cases where primary diagnostic evidence is unavailable, while also yielding stronger linear-probing transferability.

---

## 论文详细总结（自动生成）

这篇论文提出了 **SCAR**（Semantic Compensation via Adversarial Removal），旨在解决心电图（ECG）-语言预训练模型在面对信号缺失（如导联脱落、伪影干扰）时的鲁棒性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 ECG-语言预训练模型（如 MERL, MELP）主要在完整观测的 ECG 数据上进行研究。但在临床实际中，ECG 经常出现部分观测（Partial Observation）的情况，如特定导联缺失或时间片段损坏。
*   **研究动机**：随机缺失对诊断影响有限，但如果缺失的是包含主要诊断证据的“关键部分”，模型性能会剧烈下降。作者认为，模型需要具备在主要证据丢失时，通过“次要诊断线索”进行语义补偿的能力。

### 2. 方法论
SCAR 框架的核心思想是通过对抗性训练模拟最困难的缺失场景，并学习补偿机制。
*   **可微对抗掩码器（Differentiable Adversarial Masker）**：
    *   引入一个掩码器 $f_{mask}$，利用 Gumbel-Sigmoid 松弛技术生成可微的掩码。
    *   **对抗机制**：掩码器被训练去寻找并移除对“ECG-文本对齐”最关键的时空标记（Tokens），而编码器则努力在这些关键信息缺失的情况下维持对齐。这是一个 Min-Max 博弈过程。
*   **语义监督自适应选择器（Semantically-Supervised Adaptive Selector）**：
    *   在关键标记被移除后，选择器 $f_{sel}$ 对剩余的可见标记进行评分和重新加权。
    *   通过报告层面的语义监督，学习识别并放大那些虽然微弱但具有诊断价值的“次要形态学线索”。
*   **损失函数设计**：
    *   **对齐损失 ($L_{align}$)**：保持部分观测 ECG 与临床报告的对比学习对齐。
    *   **一致性损失 ($L_{cons}$)**：强制部分观测的表示与完整观测（Full-view）的表示在语义空间保持一致。
    *   **预算约束 ($L_{budget}$)**：控制掩码的比例，防止过度遮蔽。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：MIMIC-IV-ECG（约 80 万条 ECG-报告对）。
    *   **下游评估**：PTB-XL（包含 4 个子任务：Superclass, Subclass, Form, Rhythm）、CPSC2018、Chapman-Shaoxing-Ningbo (CSN)。
*   **评估场景**：
    *   **零样本诊断（Zero-Shot）**：直接匹配 ECG 嵌入与类别提示词。
    *   **线性探测（Linear Probing）**：冻结编码器，评估表示的迁移能力。
    *   **缺失模拟**：联合导联丢弃（概率 0.1）和时间片段丢弃（比例 5%-20%）。
*   **对比方法**：
    *   多模态基准：MERL, MELP。
    *   自监督/表示学习基准：TS-TCC, CLOCS, ST-MEM, ECGFM 等。
*   **新指标**：引入 **CMRS**（反事实缺失分辨率得分），用于量化模型在缺失情况下保留“完整视图语义”的能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量。
*   **训练细节**：预训练 100 个 epoch，batch size 为 64，初始学习率为 $1 \times 10^{-4}$，使用了混合精度训练。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的下游任务/数据集上进行了广泛测试。
*   **消融实验**：非常充分。分别验证了随机掩码 vs. 对抗掩码、平均池化 vs. 自适应选择器、以及一致性正则化的贡献。
*   **鲁棒性分析**：区分了“随机缺失”和“困难缺失（Hard Missingness）”，并针对导联缺失、时间缺失及联合缺失分别做了分解实验。
*   **客观性**：通过可视化嵌入空间和 Token 权重图，直观展示了模型如何从主要证据转向次要线索，增强了结论的说服力。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在所有数据集的零样本任务中，SCAR 在联合缺失场景下的表现均优于现有最强基准（如 MELP）。
*   **语义保留能力强**：CMRS 指标显示，SCAR 在关键证据丢失时，保留原始诊断语义的能力远超传统方法（例如在 CSN 数据集上 CMRS 提升了约 35%）。
*   **迁移性优异**：在标签极少（1% 标签）的线性探测任务中，SCAR 展现出极强的少样本学习能力。
*   **效率优势**：相比 MELP，SCAR 的参数量更少、计算量（GFLOPs）大幅降低，且推理延迟更低。

### 7. 优点
*   **视角独特**：不再将信号缺失视为随机噪声，而是将其建模为一种“语义压力测试”，通过对抗学习主动寻找弱点。
*   **端到端补偿**：自适应选择器能够自动识别次要线索，无需人工干预。
*   **评估体系完善**：提出的 CMRS 指标填补了鲁棒性评估中“语义一致性”量化的空白。

### 8. 不足与局限
*   **依赖参考模型**：CMRS 指标和“困难缺失”模式的构建依赖于一个预训练好的强力参考模型（如 ECGFM），如果参考模型本身存在偏见，评估结果可能受影响。
*   **预训练数据单一**：虽然使用了大规模的 MIMIC 数据集，但所有预训练均基于同一来源，跨医疗机构的泛化性仍有待在更多样化的预训练集上验证。
*   **掩码比例固定**：实验中主要使用了固定的掩码预算，对于极端高比例缺失（如只剩 1 个导联）的性能边界探讨较少。

（完）
