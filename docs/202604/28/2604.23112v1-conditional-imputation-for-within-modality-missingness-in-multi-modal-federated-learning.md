---
title: Conditional Imputation for Within-Modality Missingness in Multi-Modal Federated Learning
title_zh: 多模态联邦学习中模态内缺失的条件插补
authors: "Wugeng Zheng, Ziwen Kan, Katie Wang, Chen Chen, Song Wang"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23112v1"
tags: ["query:missing-modality"]
score: 9.5
evidence: 多模态学习中缺失数据的条件插补
tldr: 针对多模态联邦学习中常见的模态内数据缺失问题，本文提出CondI框架。该框架利用条件扩散模型显式修复缺失的原始数据，通过两阶段训练流程优化模态提取器和联合嵌入空间。在三个临床数据集上的实验证明，CondI能有效恢复语义结构，显著提升模型在严重数据缺失情况下的鲁棒性和性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23112v1/fig-001.webp\", \"caption\": \"Table 3. Classification performance for the MIMIC-IV dataset across different tasks under varying missing-data configurations.\", \"page\": 8, \"index\": 1, \"width\": 446, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.23112v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of the proposed CondI framework. (a) Global Federated Workflow. The server coordinates the distribution and aggregation of global model parameters Θ and prompt embeddings W across clients via FedAvg. (b) Local Two-Phase Pipeline & Inference. Inside each client, the process is structurally decoupled. In Phase A, the missing pattern (Wcond) is extracted to explicitly guide the diffusion model in imputing missing data. In Phase B, the model optimizes the modality profile (Wmod) and trains the instance encoder. During the inference stage (dotted line), the explicitly imputed data is fed back into the encoder to extract refined instance features (Wins), which are then concatenated with Wmod and Wcond for downstream task prediction.\", \"page\": 4, \"index\": 2, \"width\": 882, \"height\": 389}]"
motivation: 现有的多模态联邦学习方法在处理模态内数据缺失时，往往无法准确恢复数据分布，导致模型性能受限。
method: 提出CondI框架，利用条件扩散模型结合多模态上下文进行显式数据补全，并采用两阶段训练优化特征提取与融合。
result: 在PTB-XL、SLEEP-EDF和MIMIC-IV三个临床数据集上的实验表明，CondI达到了当前最先进的性能水平。
conclusion: 显式的数据补全能确保模型在完整的语义结构上运行，显著增强了多模态联邦学习对严重数据缺失的抵御能力。
---

## 摘要
多模态联邦学习（MMFL）支持隐私保护的协作训练，但在实际临床应用中，由于传感器间歇性或不规则采样，经常会出现模态内缺失（within-modality missingness）的问题。现有方法通过架构对齐或缺失嵌入（missing embeddings）隐式地表示未观测数据，通常无法恢复真实分布，导致性能欠佳。我们提出了 CondI，这是一个利用条件扩散模型（conditional diffusion models）显式解决此类缺失问题的联邦框架。CondI 采用两阶段训练流水线：首先，利用可用的多模态上下文和条件嵌入插补未观测的时间分量；其次，优化模态特定的提取器和联合嵌入空间。在推理过程中，插补后的原始数据通过训练好的提取器生成鲁棒特征，为下游任务提供整体表示。显式数据插补确保模型在完整的语义结构上运行，显著增强了对严重数据不完整性的韧性。在三个临床数据集（PTB-XL、SLEEP-EDF、MIMIC-IV）上的实验表明，CondI 取得了与现有最先进基准相当的结果。代码：https://github.com/ZhengWugeng/CondI

## Abstract
Multimodal Federated Learning (MMFL) enables privacy-preserving collaborative training, but real-world clinical applications often suffer from within-modality missingness caused by sensor intermittency or irregular sampling. Existing methods implicitly represent unobserved data via architectural alignment or missing embeddings, often failing to recover the true distribution and yielding sub-optimal performance. We propose CondI, a federated framework explicitly addressing this missingness using conditional diffusion models. CondI employs a two-phase training pipeline: first, imputing unobserved temporal components using available multimodal context and conditional embeddings; second, optimizing modality-specific extractors and joint embedding spaces. During inference, imputed raw data pass through trained extractors to generate robust features, providing a holistic representation for downstream tasks. Explicit data imputation ensures models operate on complete semantic structures, significantly enhancing resilience against severe data incompleteness. Experiments on three clinical datasets (PTB-XL, SLEEP-EDF, MIMIC-IV) demonstrate CondI achieves comparable results to state-of-the-art baselines. Code: https://github.com/ZhengWugeng/CondI

---

## 论文详细总结（自动生成）

这篇论文针对多模态联邦学习（MMFL）中极具挑战性的“模态内数据缺失”问题提出了名为 **CondI** 的新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实临床场景（如 ICU 监测）中，多模态数据不仅存在整个模态缺失的情况，更常见的是**模态内缺失**（Within-Modality Missingness），即由于传感器间歇性脱落或采样频率不一致导致的时间序列断裂。
*   **研究动机**：
    *   现有方法多采用“隐式”处理，如通过架构对齐或学习一个“缺失标记嵌入”，这无法恢复数据的真实分布，导致语义不连贯。
    *   传统的插补方法通常假设数据是中心化的，不适用于隐私受限的联邦学习环境。
    *   论文旨在通过**显式生成**缺失数据，确保模型在完整的语义结构上进行推理。

### 2. 方法论：CondI 框架
CondI 核心思想是利用**条件扩散模型（Conditional Diffusion Models）**在本地客户端显式修复缺失信号。其流程分为两个阶段：

*   **阶段 A：条件估计（本地插补训练）**
    *   **核心技术**：引入可学习的跨模态条件嵌入 $W_{cond}$。它捕捉不同传感器间的关联（例如血压下降与心率上升的相关性）。
    *   **扩散过程**：将观测到的数据 $x_O$ 通过混合专家网络（MoE）提取上下文，结合 $W_{cond}$ 作为引导条件，驱动扩散模型生成未观测部分 $x_U$。
    *   **自监督学习**：在本地使用掩码策略（Masking Strategy）训练扩散模型，使其具备从部分观测恢复原始信号的能力。
*   **阶段 B：任务优化（下游分类训练）**
    *   **特征提取**：训练实例编码器提取特征 $W_{ins}$，并结合静态模态嵌入 $W_{mod}$。
    *   **联邦聚合**：服务器通过 FedAvg 算法聚合各客户端的模型参数 $\Theta$ 以及三种嵌入矩阵（$W_{mod}, W_{ins}, W_{cond}$），实现跨客户端的知识共享。
*   **推理阶段**：先通过训练好的扩散模型生成完整的原始数据，再输入编码器进行分类，确保推理时的语义一致性。

### 3. 实验设计
*   **数据集**：
    1.  **PTB-XL**：12 导联心电图（ECG）诊断分类。
    2.  **SLEEP-EDF**：多导睡眠图（PSG）信号，进行 5 类睡眠阶段评分。
    3.  **MIMIC-IV**：重症监护室（ICU）电子健康记录，预测住院死亡率（48-IHM）和住院时长（48-LOS）。
*   **基准方法（Benchmarks）**：对比了 FedAvg、FedMM、PEPSY（目前最强的处理缺失数据的 MMFL 基准）等。
*   **缺失模拟**：设置了样本级缺失率 $p_s$ 和模态内缺失率 $p_w$，范围从 0.2 到 0.8，模拟从轻微到极度严重的数据缺失场景。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：提到 PTB-XL 和 SLEEP-EDF 训练了 70 轮，MIMIC-IV 训练了 30 轮，客户端参与率为 0.5。代码已开源。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同类型的临床数据集上进行了测试，涵盖了波形信号和临床时间序列。
*   **消融实验**：针对“无插补”和“无条件嵌入”等变体进行了对比，验证了各模块的必要性。
*   **特征重建分析**：通过 L2 距离和余弦相似度量化了插补特征与真实特征的接近程度，证明了扩散模型确实恢复了有意义的信号。
*   **评价**：实验设计较为充分，涵盖了多种缺失组合，对比基准具有代表性，结果展示了在极端缺失（80% 缺失）下的稳健性。

### 6. 主要结论与发现
*   **性能领先**：CondI 在大多数设置下优于 PEPSY 等隐式方法，尤其在严重缺失（$p_s=0.8, p_w=0.8$）时，准确率和 F1 分数提升显著。
*   **稳定性强**：随着缺失率增加，CondI 的性能下降幅度远小于基准方法，显示出极强的韧性。
*   **显式插补的价值**：实验证明，显式恢复原始数据比单纯在特征空间“打补丁”更能捕捉跨模态的生理相关性。

### 7. 优点
*   **创新性**：首次在联邦学习框架下引入条件扩散模型来解决细粒度的模态内缺失问题。
*   **显式建模**：通过生成原始信号而非抽象特征，增强了模型的可解释性和临床验证的可能性。
*   **解耦设计**：两阶段训练有效分离了“数据恢复”和“任务分类”两个目标，降低了训练难度。

### 8. 不足与局限
*   **计算开销**：扩散模型的推理过程通常涉及多次迭代，相比隐式嵌入方法，其在客户端的计算负担和推理延迟会更高。
*   **低缺失场景优势不明显**：在数据缺失较少（如 $p_s=0.2$）时，CondI 的表现有时略逊于 PEPSY，这可能是因为扩散模型在样本量不足时难以学习到精确的分布。
*   **隐私边界**：虽然不传输原始数据，但扩散模型生成的合成数据是否会通过模型参数泄露成员隐私，论文未做深入的隐私攻击防御分析。

（完）
