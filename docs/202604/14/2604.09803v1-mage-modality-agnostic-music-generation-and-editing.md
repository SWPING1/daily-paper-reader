---
title: "MAGE: Modality-Agnostic Music Generation and Editing"
title_zh: MAGE：模态无关的音乐生成与编辑
authors: "Muhammad Usama Saleem, Tejasvi Ravi, Tianyu Xu, Rajeev Nongpiur, Ishan Chatterjee, Mayur Jagdishbhai Patel, Pu Wang"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09803v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 当引导信息部分缺失时的鲁棒性
tldr: MAGE是一个统一的多模态音乐生成与编辑框架，旨在解决现有模型在处理模糊、错位或缺失引导信息时的脆弱性。它采用连续潜空间表征，核心是受控多模态FluxFormer，通过音频-视觉关系对齐和交叉门控调制机制增强跨模态关联。通过动态模态掩码训练，MAGE实现了在文本、视觉或混合引导下的鲁棒推理，为音乐创作提供了灵活且高质量的编辑与生成能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09803v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of the proposed framework for multimodal music generation and separation. (a) MixWavCodec, a mixture-aware audio codec that learns latent tokens z from audio mixtures and reconstructs waveform signals. (b) Controlled Multimodal FluxFormer, a flow-based generative transformer that enables controllable music generation and source separation from mixtures. The model aligns audio, visual scene cues, and text prompts through Audio–Visual Nexus Alignment (AVNA) and Temporal Scene Attention, while a Visual-Aware Mixture Audio Filter guides the generation process. This design enables multimodal-guided editing and extraction of target sounds from complex audio mixtures.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.09803v1/fig-002.webp\", \"caption\": \"Fig. 3: Qualitative comparison with state-of-the-art separation methods on diverse time–frequency structures. Each row shows the mixture, reference visual frame, groundtruth magnitude, and MAGE prediction. Unlike mask-regression models, MAGE recovers suppressed components (circled) by modeling conditional generative dynamics rather than deterministic masking.\", \"page\": 11, \"index\": 2, \"width\": 725, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.09803v1/fig-003.webp\", \"caption\": \"Table 1: Comparison of multimodal fusion operators. All variants share the same backbone and training setup, differing only in how aligned visual features are fused into the audio latent stream.\", \"page\": 25, \"index\": 3, \"width\": 673, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2604.09803v1/fig-004.webp\", \"caption\": \"Table 2: Ablation of visual conditioning strategies. The static-image variant replaces the temporal visual sequence with a randomly sampled frame repeated across the clip.\", \"page\": 26, \"index\": 4, \"width\": 608, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2604.09803v1/fig-005.webp\", \"caption\": \"Fig. 1: Illustration of Audio–Visual Nexus Alignment (AVNA). Video frames and audio events are sampled at different temporal resolutions, leading to a mismatch between the visual stream and the audio timeline. AVNA resolves this by performing nearestneighbor temporal resampling on normalized timestamps, assigning each audio event to the closest visual observation. The aligned visual sequence therefore matches the audio timeline and provides temporally consistent conditioning for multimodal generation and mixture-grounded editing.\", \"page\": 19, \"index\": 5, \"width\": 725, \"height\": 565}]"
motivation: 现有音乐生成系统通常受限于单一任务或固定接口，且在处理不完整或错位的多模态引导时容易出现内容漂移。
method: 提出基于流匹配Transformer的MAGE框架，引入音频-视觉对齐与交叉门控调制，并采用动态模态掩码课程学习。
result: 在MUSIC基准测试中，MAGE在多模态引导的生成和针对性编辑任务上均表现出色，达到了极具竞争力的生成质量。
conclusion: MAGE通过模态无关的架构设计，为实际音乐创作流程提供了一个轻量、灵活且鲁棒的统一生成与编辑解决方案。
---

## 摘要
多模态音乐创作要求模型既能根据高层提示生成音频，又能以针对性的方式编辑现有的混音。然而，大多数多模态音乐系统是为单一任务和固定提示接口构建的，当引导信息模糊、时间不对应或部分缺失时，其条件约束会变得脆弱。常见的加性融合或特征拼接进一步削弱了跨模态关联，在生成和编辑过程中常导致提示漂移和伪音乐内容。我们提出了 MAGE，这是一个模态无关的框架，在单一的连续潜空间表述中统一了多模态音乐生成和基于混音的编辑。其核心是 MAGE 使用了受控多模态 FluxFormer，这是一种基于流的 Transformer，用于在任何可用条件子集下学习合成与编辑的可控潜轨迹。为了提高关联性，我们引入了音视频纽带对齐，为音频时间线选择时间一致的视觉证据；以及一种交叉门控调制机制，将来自对齐的视觉和文本提示的乘性控制应用于音频潜变量，从而抑制不支持的成分而非注入它们。最后，我们采用动态模态掩码课程进行训练，使模型接触到仅文本、仅视觉、联合多模态以及混音引导的设置，从而在无需训练独立模型的情况下，实现缺失模态下的鲁棒推理。在 MUSIC 基准测试上的实验表明，MAGE 支持有效的多模态引导音乐生成和针对性编辑，在达到具有竞争力的质量的同时，提供了一个专为实际音乐工作流定制的轻量且灵活的接口。

## Abstract
Multimodal music creation requires models that can both generate audio from high-level cues and edit existing mixtures in a targeted manner. Yet most multimodal music systems are built for a single task and a fixed prompting interface, making their conditioning brittle when guidance is ambiguous, temporally misaligned, or partially missing. Common additive fusion or feature concatenation further weakens cross-modal grounding, often causing prompt drift and spurious musical content during generation and editing. We propose MAGE, a modality-agnostic framework that unifies multimodal music generation and mixture-grounded editing within a single continuous latent formulation. At its core, MAGE uses a Controlled Multimodal FluxFormer, a flow-based Transformer that learns controllable latent trajectories for synthesis and editing under any available subset of conditions. To improve grounding, we introduce Audio-Visual Nexus Alignment to select temporally consistent visual evidence for the audio timeline, and a cross-gated modulation mechanism that applies multiplicative control from aligned visual and textual cues to the audio latents, suppressing unsupported components rather than injecting them. Finally, we train with a dynamic modality-masking curriculum that exposes the model to text-only, visual-only, joint multimodal, and mixture-guided settings, enabling robust inference under missing modalities without training separate models. Experiments on the MUSIC benchmark show that MAGE supports effective multimodal-guided music generation and targeted editing, achieving competitive quality while offering a lightweight and flexible interface tailored to practical music workflows.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **MAGE**（Modality-Agnostic Music Generation and Editing）的统一框架，旨在通过单一模型实现多模态引导下的音乐生成与针对性编辑。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态音乐生成系统通常存在“任务孤立”和“条件脆弱”的问题。具体表现为：
    *   **任务孤立**：生成模型（从无到有）与编辑/分离模型（基于现有混音修改）通常是分开设计的，缺乏统一的工作流。
    *   **条件脆弱**：当引导信号（如文本或视频）模糊、时间上不对齐或部分缺失时，模型容易产生“提示漂移”（Prompt Drift）或生成无关的伪音。
    *   **融合缺陷**：传统的加性融合（Additive Fusion）或特征拼接难以精确控制音频内容，往往无法有效抑制混音中的干扰项。
*   **研究背景**：实际音乐创作需要迭代精修，开发者希望有一个轻量级、能处理不完整输入且能同时胜任生成与编辑任务的统一引擎。

### 2. 方法论
MAGE 的核心思想是将音乐生成和编辑统一建模为**连续潜空间中的条件流轨迹学习**。
*   **MixWavCodec（混合感知编解码器）**：
    *   一种连续潜变量编解码器，在训练时同时输入独立音轨和随机合成的混音，并引入高斯噪声增强鲁棒性。这使得潜空间能够稳定表征重叠的音乐信号。
*   **Controlled Multimodal FluxFormer**：
    *   基于**流匹配（Flow Matching）**的 Transformer 架构。它学习从高斯噪声到目标音频潜变量的向量场。
    *   **统一推理**：当不提供混音输入时，它执行“零混音生成”；当提供混音潜变量作为条件时，它执行“基于混音的编辑”。
*   **关键技术细节**：
    *   **音频-视觉纽带对齐（AVNA）**：利用最近邻匹配，将视频帧序列按时间戳重采样，使其与音频潜变量的时间线精确对齐。
    *   **交叉门控调制（CGM）**：采用**乘性控制**（Multiplicative Control）而非加性融合。通过 Sigmoid 门控机制，利用视觉和文本特征来“过滤”音频特征，从而抑制不支持的成分，而非盲目注入新特征。
    *   **动态模态掩码课程学习**：在训练期间随机丢弃文本、视觉或混音信号，使模型学会处理仅文本、仅视觉或多模态组合的各种推理场景。

### 3. 实验设计
*   **数据集**：主要使用 **MUSIC 训练集**（包含多种乐器演奏的野外视频，具有强音视频对应关系）。
*   **Benchmark 与指标**：
    *   **编辑/分离任务**：使用 BSS_Eval 指标（SDR, SIR, SAR）衡量信号重建和干扰抑制能力。
    *   **生成任务**：使用 FAD（感知真实度）、CLAP 相似度（语义一致性）和 CLAPScore（文本对齐度）。
*   **对比方法**：
    *   **分离专用模型**：Sound-of-Pixels, Co-Separation, CCoL, iQuery, DAVIS 等。
    *   **大规模统一模型**：SAM Audio（拥有 30 亿参数的巨型模型）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号和数量。
*   **训练细节**：使用了 AdamW 优化器，学习率为 $10^{-4}$，Batch Size 为 32，训练时长为 2000 个 Epoch。音频采样率为 11.025 kHz，片段长度约为 5.86 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了与 6 种主流音视频分离模型的性能对比。
    *   针对不同模态组合（仅文本、仅视觉、文本+视觉）在生成和编辑任务下分别进行了测试。
    *   **消融实验**：详细测试了 AVNA（对齐）、CGM（门控调制）以及不同融合策略（加性、自注意力、交叉注意力）的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从信号质量到语义对齐的多个维度。通过消融实验有力地证明了乘性门控比加性融合在抑制干扰方面的优越性。然而，实验主要集中在 MUSIC 数据集上，该数据集乐器种类相对有限，对于更复杂的现代流行音乐混音的泛化能力尚待验证。

### 6. 主要结论与发现
*   **统一性**：证明了生成和编辑可以作为同一条件生成过程的两种操作模式共存。
*   **门控优势**：乘性门控调制（CGM）在处理多模态引导时显著优于加性融合，尤其在提高 SIR（信号干扰比）方面表现突出。
*   **鲁棒性**：通过动态掩码训练，模型在缺失模态（如只有文本没有视频）的情况下仍能保持极具竞争力的性能，不会崩溃。
*   **轻量化**：相比于 SAM Audio 等巨型模型，MAGE 以更小的参数量在特定任务上取得了更优或相当的效果。

### 7. 优点
*   **模态无关性**：极大地提升了实际应用中的灵活性，用户可以根据现有素材自由选择引导方式。
*   **对齐机制**：AVNA 解决了音视频采样率不一致导致的对齐难题，提升了时间上的精确度。
*   **抑制能力**：通过门控机制解决了生成模型容易“幻听”出无关乐器的问题。

### 8. 不足与局限
*   **音频质量限制**：实验采用 11.025 kHz 的采样率，这对于高质量音乐创作（通常需要 44.1 kHz 或更高）来说明显不足，高频细节丢失严重。
*   **数据集单一**：主要依赖 MUSIC 数据集，其场景多为单人或简单组合，对于复杂环境（如大型交响乐或嘈杂背景）的鲁棒性未知。
*   **推理速度**：虽然使用了流匹配，但 ODE 求解器的积分步数仍可能影响实时交互体验（文中提到使用了 Euler 求解器，但未给出具体推理延迟数据）。

（完）
