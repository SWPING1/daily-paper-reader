---
title: Head-wise Modality Specialization within MLLMs for Robust Fake News Detection under Missing Modality
title_zh: 多模态大语言模型中基于头级模态专门化的模态缺失下鲁棒虚假新闻检测
authors: "Kai Qian, Weijie Shi, Jiaqi Wang, Mengze Li, Hao Chen, Yue Cui, Hanghui Guo, Ziyi Liu, Jia Zhu, Jiajie Xu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.09711v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 缺失模态下的多模态大模型鲁棒检测
tldr: 针对多模态虚假新闻检测中常见的模态缺失问题，本文研究了多模态大语言模型（MLLM）中注意力头的模态专业化特性。提出了一种头级模态专业化机制，通过显式分配注意力头并施加注意力约束，结合单模态知识保留策略，增强了模型在模态缺失情况下的鲁棒性，同时保持了完整输入下的检测性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09711v1/fig-001.webp\", \"caption\": \"Figure 6: Ranked attention-share distributions of top-ranked heads under different training settings. (a) Image modality; (b) text modality. Finetuning strengthens modality-related top-ranked heads relative to the pretrained model, while unimodal finetuning yields a more concentrated distribution than multimodal finetuning, indicating a less diffuse specialization pattern.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2604.09711v1/fig-002.webp\", \"caption\": \"Figure 1: Illustration of unequal modality contribution and veracity change under missing modality.\", \"page\": 2, \"index\": 2, \"width\": 498, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.09711v1/fig-003.webp\", \"caption\": \"Figure 2: Overviewof the proposed framework. (a)Modality head identification.Wefirst run the pretrainedMLLMonmultimodal training data, compute image and text attention shares for all heads, rank them by modality allocation, and obtain the selected image and text head assignments used in later training. (b) Stage-wise training loop. Each epoch takes the current MLLM together with the modality-head assignments and performs three stages, namely image-only training, text-only training, and multimodal training. In each stage, Head-wise Modality Specialization applies modality-specific lower-bound constraints to the selected heads, while Unimodal Knowledge Retention shrinks updates on mismatched modality-specific heads.\", \"page\": 4, \"index\": 3, \"width\": 1022, \"height\": 610}, {\"url\": \"assets/figures/arxiv/2604.09711v1/fig-004.webp\", \"caption\": \"Figure 4: Head-layer attention-share heatmaps comparing the pretrained model and multimodal finetuning. (a) and (b) show image attention share before and after multimodal finetuning, respectively; (c) and (d) show the corresponding text attention share. Compared with the pretrained model, multimodal finetuning activates substantially stronger modality-related head responses for both image and text.\", \"page\": 5, \"index\": 4, \"width\": 1056, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2604.09711v1/fig-005.webp\", \"caption\": \"Figure 5: Head-layer attention-share heatmaps comparing unimodal and multimodal finetuning. (a) and (b) compare imageside specialization under image-only and multimodal finetuning, respectively; (c) and (d) compare text-side specialization under text-only and multimodal finetuning, respectively. In both modalities, unimodal finetuning shows more concentrated high-response heads, whereas multimodal finetuning exhibits a comparatively broader and more diffuse specialization pattern.\", \"page\": 5, \"index\": 5, \"width\": 1056, \"height\": 221}]"
motivation: 现实场景中多模态新闻常面临模态缺失，且模型往往对低贡献模态学习不足，导致检测鲁棒性差。
method: 提出头级模态专业化机制，通过注意力约束显式分配模态关键头，并利用单模态知识保留策略防止知识漂移。
result: 实验证明该方法在模态缺失情况下显著提升了鲁棒性，且不损害完整多模态输入的性能。
conclusion: 通过优化MLLM内部注意力头的模态专业化，可以有效解决多模态虚假新闻检测中的模态缺失挑战。
---

## 摘要
多模态虚假新闻检测（MFND）旨在通过共同利用文本和视觉证据来验证新闻的可信度。然而，在现实世界的新闻传播中，由于图片删除、截图损坏等问题，经常会出现模态缺失的情况。因此，在这种场景下的鲁棒检测需要保持对每种模态的强大验证能力，但由于对低贡献模态的学习不足以及单模态标注的稀缺，这在 MFND 中具有挑战性。为解决这一问题，我们提出了多模态大语言模型（MLLMs）中的头级模态专门化（Head-wise Modality Specialization），用于模态缺失下的鲁棒 MFND。具体而言，我们首先系统地研究了 MLLMs 中的注意力头及其与模态缺失下性能的关系，结果表明，模态关键头通过其模态专门化，成为单模态验证能力的关键载体。基于这一观察，为了更好地保留低贡献模态的验证能力，我们引入了一种头级专门化机制，将这些头显式地分配给不同的模态，并通过注意力下界约束来保持它们的专门化。此外，为了更好地利用稀缺的单模态标注，我们提出了一种单模态知识保留策略，防止这些头偏离从有限监督中学习到的单模态知识。实验表明，我们的方法在提高模态缺失下鲁棒性的同时，也保持了完整多模态输入下的性能。

## Abstract
Multimodal fake news detection (MFND) aims to verify news credibility by jointly exploiting textual and visual evidence. However, real-world news dissemination frequently suffers from missing modality due to deleted images, corrupted screenshots, and similar issues. Thus, robust detection in this scenario requires preserving strong verification ability for each modality, which is challenging in MFND due to insufficient learning of the low-contribution modality and scarce unimodal annotations. To address this issue, we propose Head-wise Modality Specialization within Multimodal Large Language Models (MLLMs) for robust MFND under missing modality. Specifically, we first systematically study attention heads in MLLMs and their relationship with performance under missing modality, showing that modality-critical heads serve as key carriers of unimodal verification ability through their modality specialization. Based on this observation, to better preserve verification ability for the low-contribution modality, we introduce a head-wise specialization mechanism that explicitly allocates these heads to different modalities and preserves their specialization through lower-bound attention constraints. Furthermore, to better exploit scarce unimodal annotations, we propose a Unimodal Knowledge Retention strategy that prevents these heads from drifting away from the unimodal knowledge learned from limited supervision. Experiments show that our method improves robustness under missing modality while preserving performance with full multimodal input.

---

## 论文详细总结（自动生成）

这是一份关于论文《Head-wise Modality Specialization within MLLMs for Robust Fake News Detection under Missing Modality》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：多模态虚假新闻检测（MFND）通常依赖文本和图像的联合证据。但在现实传播中，常因图片删除或损坏导致“模态缺失”。
*   **核心挑战**：
    1.  **模态贡献不均**：模型往往过度依赖高贡献模态（如文本），导致低贡献模态（如图像）学习不足，一旦主导模态缺失，性能会剧烈下降。
    2.  **单模态标注稀缺**：现有数据集多为成对标注，缺乏单独针对文本或图像的真实性标签，难以构建鲁棒的单模态验证能力。
*   **研究目的**：在多模态大语言模型（MLLM）内部，通过优化注意力头的专门化，提升模型在模态缺失情况下的检测鲁棒性。

### 2. 核心方法论
论文提出了一个结合“头级模态专门化”与“知识保留”的框架：
*   **模态关键头识别**：首先通过预训练模型在训练集上的推理，计算每个注意力头对图像和文本Token的注意力分配比例，识别出排名前 $K$ 的“图像关键头”和“文本关键头”。
*   **头级模态专门化（HMS）**：
    *   引入**注意力下界约束**：在微调过程中，强制选定的模态关键头对对应模态的注意力权重不低于阈值 $\tau$，防止在多模态训练中这些专门化的路径被稀释或破坏。
*   **单模态知识保留（UKR）**：
    *   采用**梯度收缩策略**：在阶段化训练中，如果当前是图像训练阶段，则收缩文本关键头的梯度更新（乘以因子 $\gamma$），反之亦然。这能防止稀缺的单模态知识被后续的其他模态更新所覆盖（灾难性遗忘）。
*   **阶段化训练流程**：每个 Epoch 内依次进行“仅图像”、“仅文本”和“多模态”三个训练阶段，分别应用对应的任务损失和专门化约束。

### 3. 实验设计
*   **数据集**：
    *   **Weibo**：中等规模，中文环境。由于缺乏单模态标签，作者手动标注了部分测试集用于评估。
    *   **DGM4**：大规模多模态篡改数据集，英文环境。通过模拟 0.25%、1% 和 5% 三种极低比例的单模态监督信号来测试性能。
*   **Benchmark 与对比方法**：
    *   传统 MFND 方法：MTS, COOLANT。
    *   缺失模态专用方法：MoMKE。
    *   基准模型：基于 Qwen3-VL-2B-Instruct 的朴素全量微调（Qwen + SFT）。
*   **评估指标**：Macro F1 分数（针对多模态、仅文本、仅图像三种推理场景）。

### 4. 资源与算力
*   **模型骨干**：Qwen3-VL-2B-Instruct。
*   **微调技术**：使用 LoRA 进行高效参数微调。
*   **训练参数**：Batch size 为 24，训练 5 个 Epoch。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但考虑到 2B 规模模型及 LoRA 技术，该方法对算力的需求相对适中，属于消费级显卡可负担范围。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同语言、不同规模的数据集，并设置了三种不同的单模态监督预算（0.25%~5%）。
*   **消融实验**：详细对比了“无关键头选择”、“无 HMS”、“无 UKR”等变体，验证了各组件的有效性。
*   **敏感性分析**：对注意力阈值 $\tau$ 和梯度收缩因子 $\gamma$ 进行了参数敏感性测试。
*   **评价**：实验设计较为充分，特别是在 Weibo 数据集上进行了人工标注以确保评估的客观性，对比基准涵盖了从传统模型到最新 MLLM 的多种路径，具有较强的说服力。

### 6. 主要结论与发现
*   **专门化路径的存在**：预训练 MLLM 内部确实存在对特定模态敏感的注意力头，这些头是单模态验证能力的核心载体。
*   **鲁棒性显著提升**：在模态缺失（尤其是图像缺失）的情况下，该方法显著优于朴素微调。在 Weibo 数据集上，图像单模态 F1 从 0.3187 提升至 0.6063。
*   **性能平衡**：该方法在增强缺失模态鲁棒性的同时，几乎不损害完整多模态输入下的检测精度。

### 7. 优点与亮点
*   **机制可解释性**：从 MLLM 内部的注意力机制入手，而非仅仅在外部增加融合模块，具有较好的机械解释性。
*   **解决标注稀缺问题**：通过 UKR 策略，在极少量的单模态标注下也能有效保留验证能力，具有很强的实际应用价值。
*   **软约束设计**：HMS 采用下界约束而非硬性锁定，允许模型在保持专门化的同时适应下游任务。

### 8. 不足与局限
*   **超参数依赖**：$K$ 值（关键头数量）和阈值 $\tau$ 需要根据不同模型和数据集进行调整，缺乏自适应机制。
*   **训练开销**：阶段化训练（每个 Epoch 三个阶段）相比普通训练会增加一定的训练时长。
*   **模型架构限制**：该研究主要基于 Transformer 架构的 MLLM，对于非注意力机制驱动的模型可能不适用。
*   **偏差风险**：关键头的识别依赖于预训练模型的初始状态，如果预训练模型本身存在严重的模态偏见，可能会影响后续的专门化效果。

（完）
