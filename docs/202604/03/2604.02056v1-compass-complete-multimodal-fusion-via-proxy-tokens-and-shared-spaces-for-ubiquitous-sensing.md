---
title: "COMPASS: Complete Multimodal Fusion via Proxy Tokens and Shared Spaces for Ubiquitous Sensing"
title_zh: COMPASS：通过代理令牌和共享空间实现泛在感知的完整多模态融合
authors: "Hao Wang, Yanyu Qian, Pengcheng Weng, Zixuan Xia, William Dan, Yangxin Xu, Fei Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02056v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 为缺失模态合成代理令牌以确保融合完整性
tldr: 针对多模态感知中模态缺失导致的融合不完整问题，本文提出COMPASS框架。该框架遵循“融合完整性”原则，通过在共享潜空间中利用观测模态生成目标代理Token，确保融合层始终接收固定结构的N槽输入。COMPASS结合了代理对齐、共享空间正则化和判别性监督，在多个数据集和缺失场景下表现优于现有方法，为鲁棒多模态感知提供了新思路。
selection_source: fresh_fetch
motivation: 现有方法在模态缺失时会改变融合层的输入结构，导致跨模态交互受损和融合性能下降。
method: 通过在共享空间内利用源到目标的生成器合成缺失模态的代理Token，确保融合头始终接收固定数量的输入。
result: 在XRF55、MM-Fi和OctoNet等数据集的多种单模态及多模态缺失场景下，COMPASS均取得了领先的性能。
conclusion: 保持模态完整的融合接口是提升多模态感知系统鲁棒性的一种简单且有效的设计原则。
---

## 摘要
模态缺失仍然是多模态感知面临的一个主要挑战，因为大多数现有方法通过丢弃缺失分支、使用特定子集的融合或重建缺失特征，使融合过程适应观测到的子集。结果，融合头接收到的输入结构往往与训练期间看到的结构不同，导致融合不完整以及跨模态交互性能下降。我们提出了 COMPASS，这是一个基于融合完整性原则构建的缺失模态融合框架：融合头始终接收固定的 N 插槽多模态输入，每个模态插槽对应一个令牌。对于每个缺失模态，COMPASS 在共享潜空间中使用成对的源到目标生成器，从观测模态中合成特定目标的代理令牌，并将其聚合为单个替换令牌。为了使这些代理既具备表示兼容性又包含任务信息，我们结合了代理对齐、共享空间正则化以及针对每个代理的判别性监督。在 XRF55、MM-Fi 和 OctoNet 数据集上进行的多种单模态和多模态缺失设置下的实验表明，COMPASS 在绝大多数场景中均优于现有方法。我们的结果表明，保持模态完整的融合接口是实现鲁棒多模态感知的一种简单且有效的设计原则。

## Abstract
Missing modalities remain a major challenge for multimodal sensing, because most existing methods adapt the fusion process to the observed subset by dropping absent branches, using subset-specific fusion, or reconstructing missing features. As a result, the fusion head often receives an input structure different from the one seen during training, leading to incomplete fusion and degraded cross-modal interaction. We propose COMPASS, a missing-modality fusion framework built on the principle of fusion completeness: the fusion head always receives a fixed N-slot multimodal input, with one token per modality slot. For each missing modality, COMPASS synthesizes a target-specific proxy token from the observed modalities using pairwise source-to-target generators in a shared latent space, and aggregates them into a single replacement token. To make these proxies both representation-compatible and task-informative, we combine proxy alignment, shared-space regularization, and per-proxy discriminative supervision. Experiments on XRF55, MM-Fi, and OctoNet under diverse single- and multiple-missing settings show that COMPASS outperforms prior methods on the large majority of scenarios. Our results suggest that preserving a modality-complete fusion interface is a simple and effective design principle for robust multimodal sensing.

---

## 论文详细总结（自动生成）

以下是对论文《COMPASS: Complete Multimodal Fusion via Proxy Tokens and Shared Spaces for Ubiquitous Sensing》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在泛在感知（如智能家居、行为识别）中，多模态系统（集成WiFi、雷达、摄像头等）虽性能强大，但在实际部署中常面临**模态缺失**（传感器故障、遮挡、隐私限制等）的挑战。
*   **现有问题**：传统方法通常通过丢弃缺失分支、切换特定子集的融合模块或重建原始特征来应对。作者指出，这不仅是信息丢失问题，更是**“融合接口不匹配”**问题——融合层在训练时习惯处理固定数量的输入，而在推理时输入结构的变化会导致跨模态交互性能大幅下降。
*   **核心动机**：提出“融合完整性”原则，即无论模态是否缺失，融合头始终接收固定结构的 $N$ 个输入槽位。

### 2. 方法论：核心思想与关键技术
COMPASS 框架通过**代理令牌（Proxy Tokens）**和**共享空间**来维持融合接口的稳定性。其流程包含四个阶段：
1.  **模态编码与投影**：各模态通过专用编码器提取特征，并投影到统一维度的共享潜空间中。
2.  **代理令牌生成**：当模态 $j$ 缺失时，利用所有观测到的模态 $i$ 作为源，通过“源到目标”的定向生成器（单层 Transformer）合成针对模态 $j$ 的代理令牌。
3.  **令牌聚合与固定融合**：将针对同一缺失模态的多个代理估计值进行平均聚合，填补缺失槽位。最终，融合头对 $N$ 个令牌（真实令牌或代理令牌）进行**求和（Summation）**融合。
4.  **三级训练策略**：
    *   **代理对齐损失 ($L_{align}$)**：使生成的代理令牌在特征上接近真实的全局令牌。
    *   **共享空间正则化 ($L_{ss}$)**：利用 VICReg 算法约束潜空间的几何结构，防止表示坍缩并增强跨模态兼容性。
    *   **判别性监督 ($L_{proxy}$)**：要求每个生成的代理令牌都能独立完成分类任务，确保其包含任务相关信息。

### 3. 实验设计
*   **数据集**：
    *   **MM-Fi**：包含 RGB、深度、LiDAR、毫米波雷达四种模态，27 类人体动作。
    *   **XRF55**：包含毫米波雷达、WiFi、RFID 三种射频模态，55 类动作。
    *   **OctoNet**：大规模 IoT 数据集，选取 IMU、UWB、WiFi、ToF、毫米波雷达五种非视觉模态，62 类活动。
*   **Benchmark 与对比方法**：
    *   对比了 **Feature-level fusion**（为每种组合单独训练模型）和 **X-Fi**（目前最先进的模态无关基础模型）。
    *   评估了所有可能的模态缺失组合（如 OctoNet 的 31 种组合）。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA H100 GPU**。
*   **训练细节**：Batch Size 为 16 或 32；在 XRF55 上训练 100 轮，MM-Fi 50 轮，OctoNet 20 轮。
*   **效率**：由于采用了简单的求和融合而非复杂的多层注意力机制，COMPASS 的推理速度比 X-Fi 快 **1.9 至 2.6 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同性质的数据集上进行了详尽测试，涵盖了从单模态缺失到多模态缺失的所有排列组合（共计 7 + 15 + 31 = 53 种场景）。
*   **消融实验**：针对代理令牌的有效性、掩码概率 ($p_{drop}$)、融合策略（求和 vs 注意力）以及表示几何结构（可视化分析）进行了深入探讨。
*   **客观性**：采用了与基准模型相同的骨干网络（ResNet-18, Point Transformer），确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：COMPASS 在绝大多数缺失场景下优于基准方法，尤其在观测模态较少的极端情况下提升显著（如 OctoNet 单模态场景平均提升 28%）。
*   **几何对齐**：通过可视化证明，COMPASS 成功构建了一个模态边界模糊但类别边界清晰的共享空间，这是代理令牌能够有效替代真实令牌的前提。
*   **接口价值**：保持固定的融合接口比动态调整融合路径更有效，且计算开销更低。

### 7. 优点与亮点
*   **设计简洁**：通过“填坑”而非“改路”的思想解决缺失模态问题，逻辑清晰且易于实现。
*   **高效性**：求和融合不引入额外参数，显著降低了推理延迟，适合资源受限的泛在感知设备。
*   **鲁棒性强**：三级损失函数的设计兼顾了特征对齐和任务判别，使生成的代理令牌具有高质量。

### 8. 不足与局限
*   **融合上限**：求和融合虽然高效，但在处理极其复杂的跨模态非线性交互时，可能不如可学习的注意力机制。
*   **弱模态瓶颈**：对于本身信息量极弱的模态（如 RFID），代理生成的提升效果有限，且强行对齐可能导致该模态的判别力受损。
*   **任务覆盖**：目前仅在分类任务（HAR）上验证，尚未在回归任务（如人体姿态估计）中测试其空间对齐能力。

（完）
