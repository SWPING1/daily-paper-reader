---
title: A Clinical Point Cloud Paradigm for In-Hospital Mortality Prediction from Multi-Level Incomplete Multimodal EHRs
title_zh: 一种基于多级不完整多模态电子健康记录的院内死亡率预测临床点云范式
authors: "Bohao Li, Tao Zou, Junchen Ye, Yan Gong, Bowen Du"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04614v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 处理包括缺失模态在内的多级不完整多模态数据
tldr: 针对电子健康记录（EHR）中普遍存在的不规则采样、模态缺失和标签稀疏等三层不完整性问题，本文提出了一种名为HealthPoint (HP) 的临床点云范式。该方法将异构临床事件映射到包含内容、时间、模态和病例的4D连续空间中，利用低秩关系注意力机制捕捉高阶依赖关系。通过分层交互和自监督学习，HP在处理不完整数据时表现出极强的鲁棒性，在多项院内死亡率预测任务中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的多模态EHR建模方法难以有效处理临床数据中普遍存在的多层级不完整性，导致时间错位和模态失衡。
method: 提出HealthPoint框架，将临床事件视为4D点云，并采用低秩关系注意力机制和分层采样策略进行高效建模与自监督学习。
result: 在多项大规模EHR数据集上的实验证明，HealthPoint在不同程度的数据缺失下均保持了最优的预测性能和极强的鲁棒性。
conclusion: 临床点云范式为处理高度不完整且异构的医疗大数据提供了一种灵活、高效且鲁棒的统一建模方案。
---

## 摘要
基于深度学习的多模态电子健康记录（EHRs）建模已成为临床诊断和风险预测的重要方法。然而，由于临床工作流程的多样性和隐私限制，原始 EHRs 本质上存在多级不完整性，包括不规则采样、模态缺失和稀疏标签。这些问题导致了时间错位、模态失衡和监督受限。大多数现有的多模态方法假设数据相对完整，即使是针对不完整性设计的方法，通常也只是孤立地解决其中一两个问题。因此，它们往往依赖于僵化的时间/模态对齐，或者丢弃不完整的数据，这可能会扭曲原始的临床语义。为解决这一问题，我们提出了 HealthPoint (HP)，这是一种针对多级不完整 EHRs 的统一临床点云范式。HP 将异构临床事件表示为由内容、时间、模态和病例定义的连续 4D 空间中的点。为了建模任意点对之间的交互，我们引入了一种低秩关系注意力机制，能够高效地捕捉跨这四个维度的高阶依赖关系。我们进一步开发了一种分层交互和采样策略，以平衡细粒度建模和计算效率。基于该框架，HP 实现了灵活的事件级交互和细粒度自监督，支持鲁棒的模态恢复和对无标签数据的有效利用。在大规模 EHR 数据集上进行的风险预测实验表明，HP 在不同程度的不完整性下始终保持最先进的性能和强大的鲁棒性。

## Abstract
Deep learning-based modeling of multimodal Electronic Health Records (EHRs) has become an important approach for clinical diagnosis and risk prediction. However, due to diverse clinical workflows and privacy constraints, raw EHRs are inherently multi-level incomplete, including irregular sampling, missing modalities, and sparse labels. These issues cause temporal misalignment, modality imbalance, and limited supervision. Most existing multimodal methods assume relatively complete data, and even methods designed for incompleteness usually address only one or two of these issues in isolation. As a result, they often rely on rigid temporal/modal alignment or discard incomplete data, which may distort raw clinical semantics. To address this problem, we propose HealthPoint (HP), a unified clinical point cloud paradigm for multi-level incomplete EHRs. HP represents heterogeneous clinical events as points in a continuous 4D space defined by content, time, modality, and case. To model interactions between arbitrary point pairs, we introduce a Low-Rank Relational Attention mechanism that efficiently captures high-order dependencies across these four dimensions. We further develop a hierarchical interaction and sampling strategy to balance fine-grained modeling and computational efficiency. Built on this framework, HP enables flexible event-level interaction and fine-grained self-supervision, supporting robust modality recovery and effective use of unlabeled data. Experiments on large-scale EHR datasets for risk prediction show that HP consistently achieves state-of-the-art performance and strong robustness under varying degrees of incompleteness.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Clinical Point Cloud Paradigm for In-Hospital Mortality Prediction from Multi-Level Incomplete Multimodal EHRs》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：多模态电子健康记录（EHR）建模对临床诊断至关重要，但现实中的 EHR 数据普遍存在**多级不完整性**：不规则采样（时间级）、模态缺失（模态级）和标签稀疏（标签级）。
*   **核心挑战**：现有方法通常只能孤立地解决其中一两个问题，且往往依赖僵化的时间对齐或直接剔除不完整样本，这破坏了临床观察的语义完整性。
*   **整体含义**：本文提出了一种统一的**临床点云范式 HealthPoint (HP)**，将异构临床事件视为 4D 空间中的独立点，旨在通过一种灵活的几何视角统一处理 EHR 的异构性和不完整性。

### 2. 方法论
*   **核心思想**：将每个临床事件建模为 4D 坐标系（内容、时间、模态、病例）中的一个“点”，利用点云处理的思想（如局部交互、采样）来应对非固定拓扑的数据。
*   **关键技术细节**：
    *   **临床点构建**：通过模态特定的编码器（MLP、Clinical BERT、DenseNet）将原始内容转化为特征，并赋予其时间戳、模态类型和病例索引坐标。
    *   **低秩关系注意力 (LRRL)**：为了计算 4D 空间中点对之间的复杂依赖，采用 **CP 分解（Canonical Polyadic Decomposition）** 将高阶交互张量近似为低秩向量乘积，大幅降低计算复杂度。
    *   **分层交互与采样 (LRRSL)**：设计了五层架构（局部、模态内、跨模态、跨样本、全局融合），并引入可学习的下采样层来平衡建模粒度与计算效率。
    *   **细粒度自监督学习**：
        *   **细粒度对齐 (FGA)**：在事件级别对齐不同模态的疾病演化轨迹。
        *   **细粒度重建 (FGR)**：利用跨模态和跨样本的先验信息，重建缺失模态的特征表示。
    *   **自适应熵推理**：推理时根据各分支预测结果的熵（置信度）自动选择输出，减少噪声干扰。

### 3. 实验设计
*   **数据集**：使用两大权威公开数据集 **MIMIC-III**（生理时序+临床笔记）和 **MIMIC-IV**（生理时序+出院小结+胸部X光）。
*   **任务场景**：院内死亡率（IHM）预测。
*   **Benchmark 与对比方法**：
    *   对比了 **14 种** 最近的先进方法，涵盖了处理不规则采样的（如 MIPM）、处理标签稀疏的（如 VecoCare）、处理模态缺失的（如 M3Care、RedCore、DrFuse）以及综合性方法（如 PRIME、UMM、MUSE）。
*   **评估指标**：AUROC、AUPRC 和 F1-score。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **4 台 NVIDIA H200 GPU**。
*   **训练细节**：MIMIC-III 训练 30 个 epoch，MIMIC-IV 训练 10 个 epoch。
*   **效率分析**：文中专门进行了推理延迟分析，证明 HP 在保持高性能的同时，通过采样策略达到了与基线模型相当甚至更优的推理速度（约 14-24ms/样本）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在两个大型数据集上验证了综合性能。
    *   **鲁棒性实验**：模拟了极端的标签缺失（25%-90%）和模态缺失（53%-90%）场景。
    *   **消融实验**：测试了 11 种变体，验证了 4D 维度、自监督模块、低秩机制等的必要性。
    *   **案例研究**：通过注意力热图可视化验证了模型捕捉跨样本相似性的能力。
*   **充分性评价**：实验设计非常全面，不仅对比了大量基线，还深入探讨了不同不完整性水平下的表现，实验结果经过 3 次重复取平均值，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：HP 在所有指标上均显著优于现有 SOTA 方法，尤其在 AUPRC 和 F1 分数上提升明显。
*   **极端鲁棒性**：在 90% 标签缺失或 90% 模态缺失的极端情况下，HP 依然能保持稳定的预测能力，证明了自监督学习和跨样本先验的有效性。
*   **效率平衡**：分层采样机制成功解决了事件级建模带来的计算压力，实现了精度与速度的平衡。

### 7. 优点
*   **范式创新**：首次将 EHR 建模抽象为 4D 点云问题，打破了传统序列建模对对齐和完整性的依赖。
*   **统一框架**：一个模型同时解决了采样不规则、模态缺失、标签稀疏三大痛点，具有极强的通用性。
*   **数学严谨**：利用低秩分解处理高阶关系，既保证了表达能力又控制了复杂度。
*   **推理智能**：引入基于熵的自适应推理，增强了模型在面对噪声数据时的可靠性。

### 8. 不足与局限
*   **超参数敏感性**：自监督损失项的权重（$\lambda_a, \lambda_r$）对结果影响较大，在不同数据集上需要精细调优。
*   **跨样本依赖**：模型性能部分依赖于 Batch 内是否存在相似病例，在极小样本量或罕见病场景下的表现可能受限。
*   **实现复杂度**：相比于简单的 RNN 或 Transformer，该模型的分层架构和点云操作实现起来相对复杂。
*   **任务覆盖**：目前仅验证了死亡率预测这一分类任务，尚未在多标签分类或回归任务（如住院时长预测）中进行验证。

（完）
