---
title: "CAGMamba: Context-Aware Gated Cross-Modal Mamba Network for Multimodal Sentiment Analysis"
title_zh: CAGMamba：用于多模态情感分析的上下文感知门控跨模态 Mamba 网络
authors: "Minghai Jiao, Jing Xiao, Peng Xiao, Ende Zhang, Shuang Kan, Wenyan Jiang, Jinyao Li, Yixian Liu, Haidong Xin"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03650v1"
tags: ["query:missing-modality"]
score: 8.0
evidence: 多模态情感分析与跨模态交互
tldr: 针对多模态情感分析中Transformer计算复杂度高及上下文建模不足的问题，本文提出CAGMamba框架。该方法利用Mamba的线性复杂度优势，通过构建时间有序的二元序列显式捕捉对话情感演变，并设计门控跨模态网络（GCMN）平衡模态融合与保留。实验证明，该模型在多个基准数据集上取得了领先性能，为高效、长序列的对话情感分析提供了新方案。
selection_source: fresh_fetch
motivation: 现有的多模态情感分析方法受限于Transformer的平方复杂度，且难以有效捕捉对话中跨轮次的情感演变规律。
method: 提出一种基于Mamba的上下文感知门控跨模态网络，通过时间排序序列建模上下文，并利用门控机制和多任务学习优化模态融合。
result: 在三个主流基准数据集上的实验结果显示，CAGMamba在多项评估指标上均达到了最先进或极具竞争力的水平。
conclusion: 该研究验证了Mamba架构在多模态对话情感分析中的潜力，实现了计算效率与预测精度的双重提升。
---

## 摘要
多模态情感分析 (MSA) 要求在保持计算效率的同时，对跨模态交互和上下文依赖进行有效建模。现有的融合方法主要依赖于基于 Transformer 的跨模态注意力机制，这会导致相对于序列长度的二次复杂度，并限制了可扩展性。此外，来自前序话语的上下文信息通常通过拼接或独立融合的方式引入，缺乏捕获对话轮次间情感演变的显式时序建模。为了解决这些局限性，我们提出了 CAGMamba，这是一种用于基于对话的情感分析的上下文感知门控跨模态 Mamba 框架。具体而言，我们将上下文特征和当前话语特征组织成一个按时间排序的二元序列，为 Mamba 提供显式的时序结构以建模情感演变。为了进一步实现可控的跨模态集成，我们提出了门控跨模态 Mamba 网络 (GCMN)，该网络通过可学习的门控机制整合跨模态和单模态路径，以平衡信息融合与模态保留，并采用针对文本、音频和融合预测的三分支多任务目标进行训练。在三个基准数据集上的实验表明，CAGMamba 在多个评估指标上达到了最先进或具有竞争力的结果。所有代码均可在 https://github.com/User2024-xj/CAGMamba 获取。

## Abstract
Multimodal Sentiment Analysis (MSA) requires effective modeling of cross-modal interactions and contextual dependencies while remaining computationally efficient. Existing fusion approaches predominantly rely on Transformer-based cross-modal attention, which incurs quadratic complexity with respect to sequence length and limits scalability. Moreover, contextual information from preceding utterances is often incorporated through concatenation or independent fusion, without explicit temporal modeling that captures sentiment evolution across dialogue turns. To address these limitations, we propose CAGMamba, a context-aware gated cross-modal Mamba framework for dialogue-based sentiment analysis. Specifically, we organize the contextual and the current-utterance features into a temporally ordered binary sequence, which provides Mamba with explicit temporal structure for modeling sentiment evolution. To further enable controllable cross-modal integration, we propose a Gated Cross-Modal Mamba Network (GCMN) that integrates cross-modal and unimodal paths via learnable gating to balance information fusion and modality preservation, and is trained with a three-branch multi-task objective over text, audio, and fused predictions. Experiments on three benchmark datasets demonstrate that CAGMamba achieves state-of-the-art or competitive results across multiple evaluation metrics. All codes are available at https://github.com/User2024-xj/CAGMamba.

---

## 论文详细总结（自动生成）

以下是对论文《CAGMamba: Context-Aware Gated Cross-Modal Mamba Network for Multimodal Sentiment Analysis》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态情感分析（MSA）旨在整合文本、音频和视觉信号来推断情感。当前该领域面临两个主要挑战：
*   **计算效率瓶颈**：主流的融合模型多依赖 Transformer 的跨模态注意力机制，其计算复杂度随序列长度呈二次方增长，限制了长序列处理的扩展性。
*   **上下文建模不足**：在对话式情感分析中，当前话语的情感往往受前序话语影响。然而，现有方法通常将上下文视为辅助特征进行简单拼接或独立融合，缺乏能够捕捉对话轮次间情感演变的显式时序建模。
*   **研究动机**：利用 Mamba 架构的线性复杂度优势，结合显式的上下文时序构造和自适应门控机制，提升对话情感分析的准确性与效率。

### 2. 论文提出的方法论
CAGMamba 采用文本-音频双流架构，核心流程分为四个阶段：
*   **特征提取与序列构造**：
    *   使用 RoBERTa-Large 提取文本特征，Data2Vec-Audio-Large 提取音频特征。
    *   **核心创新**：将上下文特征（$t-1$ 轮）和当前话语特征（$t$ 轮）组织成一个**时间有序的二元序列**。这种“上下文到当前”的排序允许 Mamba 的选择性状态空间机制显式地模拟情感随对话推进的演变过程。
*   **双向选择性扫描模块 (BSSM)**：
    *   作为 GCMN 的基础单元，BSSM 结合了前向和后向的选择性扫描，并通过学习到的门控信号调制双向输出，以捕捉双向序列依赖。
*   **门控跨模态 Mamba 网络 (GCMN)**：
    *   **三流架构**：包含一条跨模态路径和两条单模态路径。
    *   **自适应门控**：设计了可学习的门控机制，根据实例级别动态平衡跨模态融合信息与单模态特征保留，避免过度融合导致模态特有信息的丢失。
*   **多任务预测目标**：
    *   采用三分支监督学习，同时对文本分支、音频分支和融合分支进行损失计算（MSE Loss），通过联合优化增强特征的判别力。

### 3. 实验设计
*   **数据集**：在三个主流基准上进行评估：**CMU-MOSI**（英文）、**CMU-MOSEI**（大规模英文）和 **CH-SIMS**（中文，用于跨语言验证）。
*   **评估指标**：二分类准确率 (Acc-2)、F1 分数、七分类准确率 (Acc-7)、平均绝对误差 (MAE) 和皮尔逊相关系数 (Corr)。
*   **对比基线**：
    *   **表示学习类**：MISA, Self-MM, ConFEDE, CLGSI 等。
    *   **交互融合类**：Semi-IIN, MFMB-Net, MISR 等。
    *   **相关架构类**：MMML（引入上下文的 SOTA）、MSAmba（首个引入 Mamba 的 MSA 模型）。

### 4. 资源与算力
*   **模型规模**：CAGMamba 极其轻量，参数量仅为 **0.80M**，计算量为 **0.10G FLOPs**。
*   **训练细节**：使用 AdamW 优化器，训练 200 个 epoch，batch size 为 16，采用早停机制（patience=8）。
*   **算力说明**：论文**未明确提及具体的 GPU 型号和数量**，但从其极小的参数量和 FLOPs 来看，该模型对算力的需求远低于基于 Transformer 的模型。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的实验，包括：
    *   在三个不同语言和规模的数据集上进行主实验对比。
    *   **消融实验**：验证了上下文建模策略（拼接 vs. 有序序列）、门控机制（固定 vs. 可学习）的有效性。
    *   **窗口分析**：研究了不同长度的文本/音频上下文窗口对性能的影响。
    *   **效率分析**：对比了 Transformer、Vision Mamba 等架构的参数量与计算成本。
    *   **可视化分析**：通过 t-SNE 展示了特征空间的聚类效果。
*   **充分性评价**：实验设计全面，涵盖了性能、效率、鲁棒性和可解释性，对比基线包含了最新的 SOTA 模型，实验结果具有较高的说服力和客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：CAGMamba 在 CMU-MOSI 上取得了 Acc-2 88.19% 和 F1 90.09% 的优异成绩，超越了现有的 Transformer 和 Mamba 基准模型。
*   **上下文的重要性**：显式的时间序列构造比简单的特征拼接更能有效利用上下文信息，帮助消除当前话语情感的歧义。
*   **高效性**：相比 Transformer 基准，参数量减少了约 82%，计算成本降低了 74%，证明了 Mamba 在多模态任务中的高效潜力。
*   **门控价值**：自适应门控能有效抑制冗余的跨模态信号，保留关键的单模态证据，从而在 t-SNE 可视化中表现出更清晰的类间界限。

### 7. 优点
*   **架构创新**：巧妙地将对话上下文转化为 Mamba 可处理的时序序列，解决了对话情感演变的建模难题。
*   **极致轻量化**：在保持高性能的同时，将模型参数压缩到 1M 以下，非常适合端侧设备部署。
*   **融合策略灵活**：GCMN 的三流门控设计比传统的单路径融合更具鲁棒性，能够平衡模态间的互补性与独立性。

### 8. 不足与局限
*   **模态覆盖有限**：目前仅关注文本和音频（Bimodal），未包含视觉模态（Visual），虽然论文提到是受 MMML 启发，但在完整的多模态场景下表现尚待验证。
*   **上下文深度限制**：实验发现增加过长的上下文窗口（尤其是音频）并不能持续提升性能，甚至可能引入噪声，说明模型对长程对话历史的处理仍有优化空间。
*   **标注依赖**：依然依赖于话语级的标量标注，这种简化可能无法捕捉复杂对话中细微的情感波动。

（完）
