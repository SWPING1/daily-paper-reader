---
title: Incomplete Multi-View Multi-Label Classification via Shared Codebook and Fused-Teacher Self-Distillation
title_zh: 基于共享码本和融合教师自蒸馏的不完整多视图多标签分类
authors: "Xu Yan, Jun Yin, Shiliang Sun, Minghua Wan"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04170v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 不完全多视图与跨视图重建
tldr: 针对多视图多标签学习中视图和标签双重缺失的挑战，本文提出了一种基于共享码本和融合教师自蒸馏的方法（SCSD）。该方法通过共享码本学习离散的一致性表示以减少冗余，利用权重估计优化预测融合，并引入自蒸馏机制将全局知识反馈至单视图分支，显著提升了模型在复杂缺失场景下的判别能力和泛化性能。
selection_source: fresh_fetch
motivation: 现有方法在处理视图与标签双重缺失时，因缺乏显式结构约束而难以捕获稳定且具有判别性的共享语义。
method: 提出利用共享码本学习离散一致性表示，并结合基于标签相关性的权重估计与融合教师自蒸馏框架进行模型训练。
result: 在五个基准数据集上的实验结果证明，该方法在各项性能指标上均优于现有的先进多视图多标签分类算法。
conclusion: 通过引入结构化表示学习和全局知识反馈机制，本研究有效解决了双重缺失环境下的多视图特征对齐与分类泛化问题。
---

## 摘要
尽管多视图多标签学习已得到广泛研究，但在视图和标签均不完整的双缺失场景下的研究仍有待深入探索。现有方法主要依赖对比学习或信息瓶颈理论在视图缺失条件下学习一致性表示，但缺乏显式结构约束的基于损失的对齐限制了捕获稳定且具有判别性的共享语义的能力。为解决这一问题，我们引入了一种更具结构化的机制来进行一致性表示学习：我们通过多视图共享码本和跨视图重构来学习离散的一致性表示，这在有限的共享码本嵌入中自然地对齐了不同视图，并减少了特征冗余。在决策层面，我们设计了一种权重估计方法，用于评估每个视图保持标签相关性结构的能力，并据此分配权重以提高融合预测的质量。此外，我们引入了一个融合教师自蒸馏框架，其中融合预测引导特定视图分类器的训练，并将全局知识反馈到单视图分支中，从而增强模型在标签缺失条件下的泛化能力。通过在五个基准数据集上与先进方法进行的大量对比实验，充分证明了所提方法的有效性。代码可在 https://github.com/xuy11/SCSD 获取。

## Abstract
Although multi-view multi-label learning has been extensively studied, research on the dual-missing scenario, where both views and labels are incomplete, remains largely unexplored. Existing methods mainly rely on contrastive learning or information bottleneck theory to learn consistent representations under missing-view conditions, but loss-based alignment without explicit structural constraints limits the ability to capture stable and discriminative shared semantics. To address this issue, we introduce a more structured mechanism for consistent representation learning: we learn discrete consistent representations through a multi-view shared codebook and cross-view reconstruction, which naturally align different views within the limited shared codebook embeddings and reduce feature redundancy. At the decision level, we design a weight estimation method that evaluates the ability of each view to preserve label correlation structures, assigning weights accordingly to enhance the quality of the fused prediction. In addition, we introduce a fused-teacher self-distillation framework, where the fused prediction guides the training of view-specific classifiers and feeds the global knowledge back into the single-view branches, thereby enhancing the generalization ability of the model under missing-label conditions. The effectiveness of our proposed method is thoroughly demonstrated through extensive comparative experiments with advanced methods on five benchmark datasets. Code is available at https://github.com/xuy11/SCSD.

---

## 论文详细总结（自动生成）

这是一份关于论文《Incomplete Multi-View Multi-Label Classification via Shared Codebook and Fused-Teacher Self-Distillation》（SCSD）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**不完整多视图多标签分类（IMVMLC）**中的“双重缺失”挑战，即在实际应用中，样本的多个描述视图（如图像的不同特征）和多标签标注往往都是不完整的。
*   **研究动机**：现有方法多依赖对比学习或信息瓶颈理论来对齐视图，但缺乏显式的结构约束，导致在视图缺失时难以捕获稳定的共享语义。
*   **核心目标**：在视图和标签同时存在大量缺失的情况下，学习具有判别性的一致性表示，并有效利用标签之间的相关性来提升分类性能。

### 2. 方法论：核心思想与技术细节
论文提出了 **SCSD 框架**，主要包含三个关键模块：
*   **一致性离散表示学习（Shared Codebook）**：
    *   **核心思想**：通过一个多视图共享的“码本”（Codebook）将连续特征量化为离散索引。
    *   **技术细节**：各视图特征经编码器映射到统一维度后，通过向量量化（VQ）寻找码本中最接近的嵌入向量。这种离散化机制天然地在有限的嵌入空间内对齐了不同视图，减少了特征冗余。
    *   **跨视图重构**：引入重构损失，要求一个视图的离散表示能够重构出其他视图的原始特征，从而强化视图间的一致性。
*   **基于标签相关性的决策融合**：
    *   **核心思想**：不依赖额外的学习网络，而是通过评估各视图预测结果对“标签相关性结构”的保持程度来分配权重。
    *   **技术细节**：计算全局标签相关性矩阵 $S$ 与单视图预测生成的局部相关性矩阵 $S^{(v)}$ 之间的 Frobenius 范数。差异越小，说明该视图预测质量越高，分配的融合权重越大。
*   **融合教师自蒸馏（Self-Distillation）**：
    *   **核心思想**：将多视图融合后的预测作为“教师信号”，指导各单视图分类器（学生）的训练。
    *   **技术细节**：通过 KL 散度损失将全局整合的知识反馈给单视图分支，增强模型在推理阶段（尤其是某些视图缺失时）的鲁棒性和泛化能力。

### 3. 实验设计
*   **数据集**：使用了 5 个主流的多视图多标签基准数据集：Corel5k, Pascal07, Espgame, Iaprtc12, 和 Mirflickr。
*   **实验场景**：模拟真实缺失环境，设置了 **50% 的视图缺失率**和 **50% 的标签缺失率**。
*   **对比方法（Benchmark）**：对比了 8 种先进（SOTA）方法，包括 iMvWL (2018), NAIM3L (2022), DDINet (2023), DICNet (2023), MTD (2024), SIP (2024), RANK (2025), 和 DRLS (2025)。
*   **评价指标**：采用 AP (Average Precision), Hamming Loss, AUC, Ranking Loss, OneError 和 Coverage 等 6 项多标签分类标准指标。

### 4. 资源与算力
*   **硬件环境**：论文明确指出实验在 **Ubuntu** 系统上完成，配置为 **NVIDIA RTX 4090 GPU** 和 **Intel i9-13900K CPU**。
*   **训练细节**：使用 AdamW 优化器，学习率为 0.001，Batch Size 为 128。码本大小 $k$ 设为 2048，嵌入维度 $d_c$ 设为 4。
*   **训练时长**：文中未明确给出具体的总训练时长，但通过复杂度分析指出其计算开销主要集中在多视图编解码过程，且随样本量 $n$ 呈线性增长，具有良好的扩展性。

### 5. 实验数量与充分性
论文的实验设计非常详尽且具有说服力：
*   **主实验**：在 5 个数据集上进行了全指标对比，SCSD 在绝大多数指标上排名第一。
*   **消融实验**：分别验证了自蒸馏损失、重构损失、向量量化（VQ）模块、跨视图重构机制以及标签相关性融合策略的有效性。
*   **鲁棒性分析**：测试了不同视图缺失率（0%-70%）、标签缺失率（0%-70%）以及不同训练集比例（10%-70%）下的表现。
*   **参数敏感性**：对损失权重 $\alpha, \lambda$、温度参数 $\tau$、码本大小 $k$ 和维度 $d_c$ 进行了深入分析。
*   **客观性**：实验遵循了 IMVMLC 领域的标准设置，对比方法均为近三年的顶会/顶刊成果，实验结果客观、公平。

### 6. 主要结论与发现
*   **性能领先**：SCSD 在处理复杂标签空间（如 Espgame 和 Iaprtc12）时优势尤为明显，AP 指标比次优方法提升了 5.8% 到 8.1%。
*   **离散表示的优势**：共享码本不仅能减少冗余，还能在信息缺失严重时提供更稳定的特征对齐。
*   **自蒸馏的价值**：通过将融合后的全局知识反馈给单视图分支，显著提升了模型在测试阶段应对视图缺失的能力。
*   **码本利用率**：实验发现 SCSD 能在几个 Epoch 内达到 100% 的码本利用率，有效避免了向量量化中常见的“码本崩溃”问题。

### 7. 优点（亮点）
*   **结构化对齐**：引入共享码本为多视图对齐提供了显式的离散结构约束，比单纯的对比损失更稳定。
*   **无需额外参数的融合**：利用标签相关性矩阵进行权重估计，既利用了监督信号的内在结构，又避免了引入复杂的注意力机制或额外的学习网络。
*   **全局知识反馈**：自蒸馏机制巧妙地解决了训练（多视图）与推理（可能单视图）之间的信息不对称问题。

### 8. 不足与局限
*   **计算开销**：引入码本和向量量化过程增加了额外的内存存储和计算距离矩阵的开销。
*   **对齐假设**：该方法假设不同视图的特征可以映射到同一个潜在空间。如果视图间差异极端巨大（如极度异构），共享码本的对齐效果可能会打折扣。
*   **极端缺失风险**：虽然在 70% 缺失率下表现稳定，但若视图缺失率进一步提高，跨视图重构所需的配对信息将极度匮乏，可能导致模型性能骤降。

（完）
