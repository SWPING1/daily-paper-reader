---
title: Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective
title_zh: 从因果推理视角学习不变模态表示以实现鲁棒多模态学习
authors: "Sijie Mai, Shiqin Han"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18460v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 用于鲁棒多模态情感计算和情感分析的解耦表示
tldr: 针对多模态情感计算中模型易受虚假相关性影响、泛化能力差的问题，本文提出了一种基于因果推理的因果模态不变表示（CmIR）学习框架。该框架通过解耦学习，将各模态分解为因果不变表示和环境特定虚假表示，并结合不变性、互信息和重构约束，确保表示的稳定性和信息完整性。实验证明，CmIR在多项基准测试中达到SOTA，尤其在处理分布偏移和噪声数据时表现出极强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18460v1/fig-001.webp\", \"caption\": \"Table 1: Comparisons on the CMU-MOSI and CMU-MOSEI datasets. The results labeled with † are obtained from original papers, and other results are obtained from our experiments. The best results are highlighted.\", \"page\": 8, \"index\": 1, \"width\": 908, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-002.webp\", \"caption\": \"Figure 7: The structures of encoder, decoder, and predictor. d ′ represents the hidden dimensionality.\", \"page\": 19, \"index\": 2, \"width\": 458, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-003.webp\", \"caption\": \"Table 4: Discussion on noisy modalities on CMU-MOSI and CMU-MOSEI. MuBo denotes Multimodal Boosting.\", \"page\": 9, \"index\": 3, \"width\": 867, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.18460v1/fig-004.webp\", \"caption\": \"Figure 3: The overall framework of CmIR and the visualization of the proposed constraints.\", \"page\": 5, \"index\": 4, \"width\": 902, \"height\": 329}]"
motivation: 现有多模态模型往往学习到虚假相关性，导致在分布偏移或存在噪声的情况下泛化性能下降。
method: 提出CmIR框架，利用因果推理将模态解耦为因果不变表示和环境特定表示，并施加不变性、互信息及重构约束。
result: 在多个多模态基准测试中取得SOTA性能，特别是在分布外（OOD）和噪声数据上表现优异。
conclusion: 通过因果解耦学习不变表示能有效提升多模态学习的鲁棒性和泛化能力。
---

## 摘要
多模态情感计算旨在利用语言、声学和视觉模态来预测人类的情感、情绪、意图和观点。然而，当前模型通常会学习到虚假相关性，这在分布偏移或存在噪声模态的情况下会损害泛化能力。为解决这一问题，我们提出了一种用于鲁棒多模态学习的因果模态不变表示（CmIR）学习框架。其核心是，我们从因果推理的视角引入了一种具有理论依据的解耦方法，将每个模态分解为“因果不变表示”和“环境特定虚假表示”。CmIR 通过不变性约束、互信息约束和重构约束，确保所学习的不变表示在不同环境下与标签保持稳定的预测关系，同时保留来自原始输入的充分信息。在多个多模态基准测试上的实验表明，CmIR 达到了最先进的性能。CmIR 在分布外数据和噪声数据上表现尤为出色，证实了其鲁棒性和泛化能力。

## Abstract
Multimodal affective computing aims to predict humans' sentiment, emotion, intention, and opinion using language, acoustic, and visual modalities. However, current models often learn spurious correlations that harm generalization under distribution shifts or noisy modalities. To address this, we propose a causal modality-invariant representation (CmIR) learning framework for robust multimodal learning. At its core, we introduce a theoretically grounded disentanglement method that separates each modality into `causal invariant representation' and `environment-specific spurious representation' from a causal inference perspective. CmIR ensures that the learned invariant representations retain stable predictive relationships with labels across different environments while preserving sufficient information from the raw inputs via invariance constraint, mutual information constraint, and reconstruction constraint. Experiments across multiple multimodal benchmarks demonstrate that CmIR achieves state-of-the-art performance. CmIR particularly excels on out-of-distribution data and noisy data, confirming its robustness and generalizability.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Invariant Modality Representation for Robust Multimodal Learning from a Causal Inference Perspective》的结构化深度分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
多模态情感计算（MAC）旨在整合语言、视觉和声学信息来预测人类情感。然而，现有模型在训练时极易捕获**虚假相关性（Spurious Correlations）**。例如，如果训练集中某位演讲者总是面带微笑（视觉特征）表达负面评论，模型可能会错误地将“微笑”与“积极情感”强绑定。
当测试环境发生变化（如光照、说话风格、背景噪声或分布偏移）时，这些虚假关联会导致模型性能大幅下降。论文的研究动机在于：如何从因果推理的角度，提取出在不同环境下与标签保持稳定因果关系的**不变特征**，从而提升模型的鲁棒性和泛化能力。

### 2. 方法论：CmIR 框架
论文提出了**因果模态不变表示（CmIR）**学习框架，其核心思想是将每个模态的原始特征解耦为两个独立部分：
*   **因果不变表示 ($Z_{inv}$)**：携带跨环境稳定的因果信息，用于最终预测。
*   **环境特定虚假表示 ($Z_{spu}$)**：捕获与环境相关的噪声或非因果特征。

**关键技术细节与约束机制：**
*   **虚拟环境模拟**：由于现实数据通常缺乏环境标签，CmIR 通过向原始特征注入不同强度的噪声（$\alpha^{(e)}$）来模拟 $K$ 个不同的虚拟环境。
*   **不变性约束 ($R_{inv}$)**：强制要求 $Z_{inv}$ 在不同虚拟环境下保持一致，确保其对环境扰动不敏感。
*   **互信息约束 ($R_{dec}$)**：通过特征正交化（Orthogonality）作为互信息的代理指标，最小化 $Z_{inv}$ 与 $Z_{spu}$ 之间的相关性，实现彻底解耦。
*   **重构约束 ($R_{rec}$)**：使用解码器从 $(Z_{inv}, Z_{spu})$ 对中重构原始输入 $X_m$，确保解耦过程不丢失关键信息。
*   **理论证明**：论文提供了三个定理，从数学上证明了不变表示的存在性、可提取性，以及在最坏情况下的分布外（OOD）风险优势。

### 3. 实验设计
*   **数据集**：
    *   **情感分析 (MSA)**：CMU-MOSI, CMU-MOSEI, CH-SIMS-v2。
    *   **分布外测试 (OOD)**：CMU-MOSI (OOD) 专门用于评估分布偏移。
    *   **幽默与讽刺检测 (MHD/MSD)**：UR-FUNNY, MUStARD。
*   **对比方法**：
    *   **标准基准**：Self-MM, MISA, ITHP, GSCon 等。
    *   **因果/去偏方法**：SuCI, CLUE, GEAR, AtCAF 等。
    *   **鲁棒性/去噪方法**：TMDC, C-MIB, Multimodal Boosting。
*   **场景设置**：涵盖了标准测试、OOD 测试以及在不同噪声比例（10%-70%）下的鲁棒性测试。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA RTX 3090 GPU**。
*   **软件**：PyTorch 1.13.1, CUDA 11.6。
*   **训练细节**：使用 AdamW 优化器，通过 50 次随机网格搜索确定超参数，并对最终结果取 5 次运行的平均值。论文指出 CmIR 的参数量和计算量（FLOPs）低于或等同于主流 SOTA 模型（如 EMOE, GSCon），具有较高的计算效率。

### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多任务覆盖**：不仅做了情感分析，还扩展到了幽默和讽刺检测，验证了通用性。
*   **鲁棒性深度测试**：除了高斯噪声，还引入了拉普拉斯噪声和随机擦除（Random Erasing）来模拟未见过的 OOD 噪声。
*   **消融实验**：逐一验证了不变性、互信息和重构三个约束项的必要性。
*   **可视化与探测实验**：通过 T-SNE 可视化和专门的探测任务（Probing Experiments）证明了 $Z_{inv}$ 确实捕获了情感信息，而 $Z_{spu}$ 捕获的是非因果偏置。

### 6. 主要结论与发现
*   **SOTA 性能**：CmIR 在所有主流 MAC 数据集上均达到了最先进水平，尤其在 Acc7 和 Acc2 指标上提升显著。
*   **OOD 优势**：在 CMU-MOSI (OOD) 上，CmIR 的领先优势比在标准数据集上更明显，证明了因果学习对处理分布偏移的有效性。
*   **噪声鲁棒性**：随着噪声比例增加，CmIR 的性能下降速度远慢于基准模型，表现出极强的实用性。
*   **解耦有效性**：探测实验显示，仅靠虚假表示 $Z_{spu}$ 无法进行有效的情感预测（准确率接近标签分布偏置），证明了因果特征被成功分离。

### 7. 优点
*   **理论完备**：不同于许多纯经验性的深度学习论文，本文提供了严谨的因果推理理论支撑。
*   **无需先验偏置知识**：CmIR 不需要预先知道具体的偏置类型（如演讲者偏置或性别偏置），通过环境模拟自动学习不变性，具有更强的通用性。
*   **端到端解耦**：通过简洁的损失函数组合实现了复杂的因果解耦，易于集成到现有架构中。

### 8. 不足与局限
*   **环境模拟的局限性**：虽然噪声注入有效，但它模拟的是特征层面的扰动，可能无法完全覆盖现实世界中复杂的语义级分布偏移（如文化背景差异）。
*   **互信息代理**：使用正交化作为互信息的代理虽然高效，但在处理非线性相关性时可能不够精确。
*   **超参数敏感性**：实验显示噪声强度 $\alpha^{(1)}$ 对结果有一定影响，在不同数据集上可能需要精细调优。

（完）
