---
title: Pretraining Reusable Inference Across Views with Synthetic Task Priors
title_zh: 利用合成任务先验预训练跨视图可复用推理
authors: "Jielong Lu, Zhihao Wu, Jiajun Yu, Zhaoliang Chen, Haishuai Wang"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.19115v1"
tags: ["query:mm"]
score: 8.0
evidence: 多视图学习，处理视图缺失与可靠性，提出可复用推理
tldr: 传统多视图学习方法需为每个下游任务重新学习视图效用和证据组合，导致知识重复丢弃。本文提出SIMPLE，将多视图学习重构为任务条件化的可复用推理过程，利用合成任务先验生成多样化情节，并通过层级架构进行视图内外及跨样本推理。冻结SIMPLE即可达竞争性能，轻量适配器校准在多数数据集上取得领先，同时支持one-shot和缺失视图。证明多视图推理能力本身可预训练和复用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.19115v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1695, \"height\": 786, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19115v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 844, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19115v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 843, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19115v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 843, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19115v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 840, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19115v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 863, \"height\": 640, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.19115v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1708, \"height\": 782, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.19115v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1583, \"height\": 752, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.19115v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1616, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.19115v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1477, \"height\": 343, \"label\": \"Table\"}]"
motivation: 预训练编码器使视图表示可复用，但视图效用判定与证据组合仍需逐任务重学，知识未跨任务迁移。
method: 提出SIMPLE，基于合成任务先验生成多样支持-查询情节，用层级架构完成视图内、跨视图及跨样本推理。
result: 冻结推理骨干已具竞争力，轻量适配器校准在多数数据集上领先，并支持one-shot和缺失视图场景。
conclusion: 多视图推理可被预训练并复用，适配器校准仅在需要时提供任务特定对齐。
---

## 摘要
现代预训练编码器使得来自异构视图的表征越来越可复用，但决定视图效用并整合证据的过程仍需要为每个下游任务重新学习。因此，关于视图相关性、互补性、可靠性和缺失性的知识在任务间被反复丢弃，而非迁移使用。为此，我们将多视图学习重新构建为学习一种可复用的、任务条件下的推理过程，而非固定的融合函数。基于这一视角，我们提出SIMPLE，一种先验拟合的多视图上下文学习器，通过以小型标注支持集为条件来预测查询标签。由于现有真实世界数据集仅覆盖有限的视图配置和任务结构，我们在嵌入空间中构建了一个可控的合成任务先验。它生成多样的支持-查询片段，具有不同的类别结构、共享与特定视图因素、表征几何、跨视图依赖、可靠性水平、缺失模式以及分布偏移。随后，一种分层推理架构在视图内、跨视图以及跨支持集和查询样本之间进行推理。在多视图和多组学基准上的实验表明，SIMPLE的冻结变体在不更新推理主干的情况下达到了具有竞争力的性能，而轻量级适配器校准在大多数评估数据集上取得了领先性能。综合来看，冻结、单样本和缺失视图设置下的结果支持了核心假设：多视图推理本身可以被预训练和复用，而轻量级适配器校准则在需要时提供任务特定的对齐。

## Abstract
Modern pretrained encoders make representations from heterogeneous views increasingly reusable, but the procedure that determines view utility and combines evidence is still relearned for each downstream task. Consequently, knowledge about view relevance, complementarity, reliability, and missingness is repeatedly discarded rather than transferred across tasks. We therefore reformulate multi-view learning as learning a reusable, task-conditioned inference procedure rather than a fixed fusion function. Based on this perspective, we propose SIMPLE, a prior-fitted multi-view in-context learner that predicts query labels by conditioning on a small labeled support set. Since existing real-world datasets cover only a limited range of view configurations and task structures, we construct a controllable synthetic task prior in embedding space. It generates diverse support-query episodes with varying class structures, shared and view-specific factors, representation geometries, cross-view dependencies, reliability levels, missingness patterns, and distribution shifts. A hierarchical inference architecture then performs reasoning within views, across views, and across support and query samples. Experiments on multi-view and multi-omics benchmarks demonstrate that the frozen variant of SIMPLE achieves competitive performance without updating the inference backbone, while lightweight adapter calibration attains leading performance on most evaluated datasets. Together, the results under frozen, one-shot, and missing-view settings support the central hypothesis that multi-view reasoning itself can be pretrained and reused, while lightweight adapter calibration provides task-specific alignment when needed.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：现代预训练编码器（如 CLIP、BLIP 等）使得图像、文本、传感器等多种视图的表征越来越可复用，但后续的多视图推理阶段（决定哪些视图有用、如何组合、如何处理缺失等）仍然需要为每个下游任务重新学习。已有方法通常每次训练新的融合模块和预测头，导致“视图表征可复用，而视图推理能力被反复丢弃”的根本不对称。
- **核心问题**：多视图学习的推理过程本身能否被预训练一次，并复用到从未见过的下游任务？
- **整体意义**：论文主张将多视图学习重新定义为“可复用的、任务条件化的推理过程”，而非为每个数据集拟合一个固定融合函数。基于支持集（labeled support set）和查询集（query set）的上下文学习，模型应能从当前任务中推断出合适的预测规则。

## 2. 论文提出的方法论

- **核心思想**：提出 SIMPLE（Synthetic-prior In-context Multi-view Pretrained LearnEr），一种基于合成任务先验的多视图上下文学习器。支持集充当“数据集级别的任务描述”，预训练推理骨干通过前向计算构造对应的预测规则，目标是分摊（amortize）多视图任务的推理过程。
- **合成任务先验**：
  - 由于真实数据集只覆盖有限的视图组合和任务结构，论文在嵌入空间中构建了可控的合成任务先验 p(T)，生成多样化的 support-query 片段。
  - 每个任务动态采样：类别数（2~64）、视图数（1~16）、潜语义维度（8~128）、类别分布（均衡/不均衡/长尾）等。
  - 潜语义生成采用类条件混合高斯分布（含多个模式）：`z_i = μ + L ε`，并加入非线性依赖结构 `z* = z + α φ(zA_T)` 来控制潜因子间的纠缠程度。
  - 视图生成考虑共享/私有信息，通过 α_m 控制跨视图冗余与互补：`x_i^(m) = α_m O_sm(z_i) + (1−α_m) O_pm(u_i^(m)) + ε`。
  - 还支持条件缺失（missing-not-at-random）、块缺失、整视图 dropout、支持-查询分布偏移、类别不平衡、噪声标签、离群点、虚假相关等。
- **层级多视图上下文 Transformer**：
  - **特征级**：将每个视图的 embedding 分为多个特征补丁，经 MLP 注入位置信息，再通过特征级 Transformer 学习视图内依赖，并用注意力聚合得到视图级表示。
  - **视图级**：视图 token 加入身份嵌入，通过带可用掩码的自注意力建模跨视图交互，并由掩码感知的归一化和对可用视图加权聚合，天然支持缺失视图。
  - **样本级**：支持样本加入标签嵌入，查询样本使用未知 token；支持与查询样本通过角色嵌入区分，并在样本级 Transformer 中联合上下文推理，最终通过学习到的分类器或余弦相似度对支持原型进行预测。
- **预训练目标**：对每个 batch 在线采样多个合成任务，仅用查询集的交叉熵损失进行优化；该过程隐含了掩码视图和分布鲁棒训练。
- **部署方式**：
  - **SIMPLE-F**：冻结视图编码器和推理骨干，直接用支持集作为上下文做前向推理，不更新任何参数。
  - **SIMPLE-A**：冻结推理骨干，仅优化轻量级的视图适配器、输入 LayerNorm、低秩残差、温度参数和分类偏置。

## 3. 实验设计

- **多视图分类数据集**：HW（手写数字，6视图）、OutScene（户外场景，4视图）、ESP-Game（图像-文本）、Flickr（图像-文本）、NUSWIDE（6视图）、YouTube（视频-音频，6视图）、Flowers（花卉图像，7视图）。
- **多组学数据集**：GS-BRCA、GS-COAD、GS-GBM、GS-LGG、GS-OV、Pan-Cancer。每个患者包含基因表达、DNA甲基化、miRNA表达、拷贝数变异4个分子视图。
- **对比方法**：
  - 传统/表示学习：MLP、SVM、XGBoost、DCCA、DMF；
  - 多视图融合：Co-GCN、RCML、TUNED；
  - Transformer 方法：ECMGD、ViHMGD；
  - 多组学方法：DeepMO、MOGONET、MoGCN、GTMancer。
- **实验设置**：每个数据集按类别比例取 10% 作为支持集，剩下 90% 为查询集；所有方法使用相同的 5 个随机种子（42~46），报告平均 ACC 和 Macro-F1；查询标签严格隔离，不用于训练、适配、归一化或超参选择。
- **额外实验**：
  - **One-shot 学习**：每个类别仅用 1 个标注样本，在 Pan-Cancer 和 Flowers 上评估。
  - **缺失视图鲁棒性**：在 Flowers 上随机移除 10%~90% 的视图，嵌套掩码保证一致性。
  - **跨数据集迁移**：以 Pan-Cancer 为源域训练，冻结骨干，在 7 个多视图目标数据集上用目标侧线性分类器评估，并用排名分数可视化。
  - **消融研究**：在 Pan-Cancer 和 Flowers 上渐进添加组件。
  - **效率分析**：比较准确率、下游执行时间和峰值 GPU 内存。

## 4. 资源与算力

- 论文**未明确说明**具体的 GPU 型号、数量、训练时长或总计算量。
- 文中仅提到：SIMPLE 主干约 1.25 亿参数（125,005,122）；使用 AdamW 优化器（学习率 1e-4、权重衰减 1e-4、梯度裁剪 1.0）；每个优化步包含 4 个独立合成任务；使用 bfloat16 自动混合精度和 TF32 矩阵乘法；训练种子为 42。
- 效率分析中也只报告了相对运行时间和峰值内存，未给出绝对算力细节。

## 5. 实验数量与充分性

- **实验数量较丰富**：包括 13 个标准基准数据集的完整对比（表1、表2）、两个 one-shot 实验、一个缺失视图实验、一个跨域迁移实验、两个数据集的消融实验、以及一个效率对比实验。
- **充分性与公平性**：
  - 统一支持-查询划分，所有方法用相同种子、相同缺失掩码，查询标签隔离，这增强了结论的可靠性。
  - 对比方法覆盖了传统、深度表示学习、图网络、Transformer、多组学专门方法，代表性尚可。
- **不足**：
  - 表中只给出平均值，未报告标准差，也没有统计显著性检验（如 t 检验），难以判断差异是否显著。
  - 主要结果中部分数据集上基线表现异常偏低（如 MOGONET 在某些多组学数据集上只有 38% 左右），可能存在基线调优不充分的风险。
  - 缺失视图实验仅在 Flowers 上开展，范围有限。
  - 跨域迁移实验使用线性探测，并非 SIMPLE-F 的上下文推理范式，因此不能直接视为“零样本迁移”证据。

## 6. 论文的主要结论与发现

- **SIMPLE-F**：在不更新推理骨干的前提下，其性能已可与许多完全训练的任务特定方法竞争，说明多视图推理能力可以跨任务迁移。
- **SIMPLE-A**：仅轻量校准适配器后，在多数评估数据集上取得领先（多视图 7 个数据集中 4 个最佳，多组学 6 个中 4 个最佳），表明任务特定对齐可以在保持骨干冻结的情况下获得进一步提升。
- **One-shot 场景**：SIMPLE 在极低监督下优于传统方法，显示其对任务上下文的有效利用。
- **缺失视图场景**：SIMPLE 的性能下降更慢，且在高缺失率下保持较高精度，得益于掩码感知的视图级推理。
- **消融实验**：合成任务先验、上下文条件化、视图推理等组件均带来一致增益，验证了各模块的独立贡献。
- **总体结论**：多视图推理过程可以被预训练并复用；轻量适配器校准仅在需要时提供任务特定对齐，而不是必须重新训练融合模块。

## 7. 优点

- **视角新颖**：将多视图学习从“学习固定融合函数”转向“学习可复用的推理过程”，并借鉴上下文学习范式，具有较好的理论价值。
- **合成任务先验设计精细**：在嵌入空间覆盖类别结构、视图依赖、缺失机制、分布偏移等大量变化，弥补了真实数据集多样性不足的问题。
- **层级架构与推理逻辑匹配**：特征级 → 视图级 → 样本级的分解清晰，且视图级掩码机制天然支持动态视图组合和缺失视图。
- **部署灵活**：SIMPLE-F 适合快速直接推理，SIMPLE-A 在少量适配后即可进一步提升性能，兼顾效率与准确率。
- **实验规范性强**：查询标签严格隔离、统一数据划分、嵌套缺失掩码等设计体现了较好的实验严谨性。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU 型号、数量、训练时长，难以评估方法的资源可复现性。
- **统计证明不足**：主要结果只报告均值，缺少标准差和显著性检验，部分性能优势可能不显著。
- **合成先验与现实差距**：合成任务基于潜变量和观察算子生成，与真实编码器表征的复杂结构可能存在偏差；模型规模较大（125M），对下游部署也有一定门槛。
- **实验覆盖有限**：缺失视图测试只在 Flowers 上；one-shot 只在两个数据集上；跨域迁移实验采用线性探测而非 SIMPLE-F 的上下文推理，因此对“可复用推理”的迁移性证据是间接的。
- **基线公平性疑问**：个别基线在特定数据集上表现异常低，可能因为默认超参或未充分调优，导致对比略有失公允。
- **应用限制**：方法依赖支持集质量、标签语义和预训练任务先验的覆盖面；若下游任务的视图数、特征分布或类别结构远超出先验范围，性能可能受到影响。

（完）
