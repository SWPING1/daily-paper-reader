---
title: Multimodal Emotion Recognition via Bi-directional Cross-Attention and Temporal Modeling
title_zh: 基于双向交叉注意力与时序建模的多模态情感识别
authors: "Junhyeong Byeon, Jeongyeol Kim, Sejoon Lim"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11971v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 使用交叉注意力的多模态情感识别框架
tldr: 针对野外环境下情感识别的复杂性，本文提出一种多模态框架。利用预训练的 CLIP 和 Wav2Vec 2.0 提取视听特征，通过 TCN 捕捉视觉时序依赖，并引入双向交叉注意力机制实现模态融合。此外，结合文本引导的对比学习增强语义对齐。在 ABAW 第十届挑战赛中表现优异，证明了时序建模与跨模态融合在复杂环境下的有效性。
selection_source: fresh_fetch
motivation: 单一模态难以应对野外视频中光照、姿态及背景噪声对情感识别带来的挑战。
method: 结合 CLIP 和 Wav2Vec 2.0 提取特征，利用 TCN 进行时序建模，并通过双向交叉注意力模块与文本引导对比学习进行多模态融合。
result: 在 ABAW 第十届 EXPR 基准测试中，该框架显著优于单模态模型，提供了强有力的多模态基准。
conclusion: 结合时序视觉建模、音频表征学习及跨模态融合，能有效提升非受限真实环境下的情感识别鲁棒性。
---

## 摘要
野外（in-the-wild）视频数据中的情感识别由于面部外观、头部姿态、光照、背景噪声的巨大变化以及人类情感固有的动态特性，仍然是一个具有挑战性的问题。仅依赖单一模态（如面部表情或语音）通常不足以捕捉这些复杂的情感线索。为了解决这一问题，我们针对第10届野外情感行为分析（ABAW）挑战赛中的表情（EXPR）识别任务，提出了一种多模态情感识别框架。我们的方法利用大规模预训练模型，即用于视觉编码的 CLIP 和用于音频表示学习的 Wav2Vec 2.0，作为冻结的主干网络。为了建模面部表情序列中的时序依赖关系，我们在固定长度的视频窗口上采用了时序卷积网络（TCN）。此外，我们引入了一个双向交叉注意力融合模块，其中视觉和音频特征对称地进行交互，以增强跨模态上下文关联并捕捉互补的情感信息。随后使用一个轻量级分类头进行最终的情感预测。我们进一步结合了基于 CLIP 文本特征的文本引导对比目标，以促进语义对齐的视觉表示。在 ABAW 第10届 EXPR 基准测试上的实验结果表明，所提出的框架提供了一个强大的多模态基准，并取得了优于单模态建模的性能。这些结果证明了结合时序视觉建模、音频表示学习和跨模态融合对于在不受限的真实世界环境中实现鲁棒情感识别的有效性。

## Abstract
Emotion recognition in in-the-wild video data remains a challenging problem due to large variations in facial appearance, head pose, illumination, background noise, and the inherently dynamic nature of human affect. Relying on a single modality, such as facial expressions or speech, is often insufficient to capture these complex emotional cues. To address this issue, we propose a multimodal emotion recognition framework for the Expression (EXPR) Recognition task in the 10th Affective Behavior Analysis in-the-wild (ABAW) Challenge.   Our approach leverages large-scale pre-trained models, namely CLIP for visual encoding and Wav2Vec 2.0 for audio representation learning, as frozen backbone networks. To model temporal dependencies in facial expression sequences, we employ a Temporal Convolutional Network (TCN) over fixed-length video windows. In addition, we introduce a bi-directional cross-attention fusion module, in which visual and audio features interact symmetrically to enhance cross-modal contextualization and capture complementary emotional information. A lightweight classification head is then used for final emotion prediction. We further incorporate a text-guided contrastive objective based on CLIP text features to encourage semantically aligned visual representations.   Experimental results on the ABAW 10th EXPR benchmark show that the proposed framework provides a strong multimodal baseline and achieves improved performance over unimodal modeling. These results demonstrate the effectiveness of combining temporal visual modeling, audio representation learning, and cross-modal fusion for robust emotion recognition in unconstrained real-world environments.

---

## 论文详细总结（自动生成）

### 论文总结：基于双向交叉注意力与时序建模的多模态情感识别

#### 1. 核心问题与研究动机
*   **核心问题**：在“野外”（in-the-wild）环境下的视频情感识别。这类场景存在面部姿态多变、光照不均、遮挡、背景噪声大以及情感表达动态演变等复杂挑战。
*   **研究动机**：单一模态（仅视觉或仅音频）难以提供足够的信息来消除歧义。作者旨在通过结合大规模预训练模型、时序建模技术以及对称的跨模态融合机制，提升情感识别的鲁棒性和准确性。

#### 2. 方法论
该框架由五个核心组件构成：
*   **特征提取（冻结主干）**：
    *   **视觉流**：使用 **CLIP (ViT-B/32)** 提取帧级特征（512维）。
    *   **音频流**：使用 **Wav2Vec 2.0** 提取音频特征（768维）。
    *   **文本引导**：利用 CLIP 文本编码器生成“A face expressing [Emotion]”的语义嵌入。
*   **时序建模与适配**：
    *   **视觉 TCN**：采用 6 层扩张因果卷积（Dilated Causal Convolutions）的 **时序卷积网络（TCN）**，在不损失分辨率的情况下扩大感受野，捕捉表情的动态演变。
    *   **音频适配器**：通过线性层和归一化将音频特征投影至 512 维，实现与视觉特征的维度对齐。
*   **双向交叉注意力融合（Bi-directional Cross-Attention）**：
    *   采用对称设计：视觉特征作为 Query 去查询音频特征（V-to-A），同时音频特征作为 Query 去查询视觉特征（A-to-V）。
    *   这种机制允许两个模态相互补充，当某一模态信息模糊时，利用另一模态的上下文进行增强。
*   **分类与对比学习目标**：
    *   **分类损失 ($L_{cls}$)**：使用加权交叉熵损失处理类别不平衡。
    *   **文本引导对比损失 ($L_{con}$)**：将融合后的视觉特征投影回 CLIP 空间，与对应的文本标签嵌入计算余弦相似度，增强特征的语义对齐。

#### 3. 实验设计
*   **数据集**：第 10 届 ABAW 挑战赛的表情识别（EXPR）任务基准（基于 Aff-Wild2 数据集）。
*   **Benchmark**：官方提供的基准模型（基于预训练的 VGGFace）。
*   **对比设置**：主要对比了不同时序窗口长度（30 帧 vs 60 帧）对模型性能的影响。
*   **评价指标**：准确率（Accuracy）和 Macro F1-score（官方主要指标）。

#### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA RTX A5000 GPU**。
*   **训练细节**：
    *   优化器：AdamW，学习率 $1 \times 10^{-5}$，采用余弦退火调度。
    *   时长/规模：训练 30 个 epoch，Batch Size 为 64，使用了梯度累积技术。
    *   主干网络（CLIP 和 Wav2Vec 2.0）在训练过程中保持**冻结**状态，以减少过拟合并提高稳定性。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了在验证集上的性能对比，重点考察了时序窗口大小的影响。
*   **充分性评价**：
    *   **优点**：实验直接对比了官方基准，证明了多模态及 TCN 建模的优越性。
    *   **不足**：消融实验相对较少。文中未详细展示去掉“双向交叉注意力”或“对比学习损失”后的性能变化，难以量化每个具体模块的贡献度。

#### 6. 主要结论与发现
*   **多模态优于单模态**：提出的多模态框架在 Macro F1 指标上显著超过了官方基准（从 0.25 提升至 0.3334）。
*   **时序上下文的重要性**：60 帧窗口的表现优于 30 帧窗口，说明更长的时序信息有助于识别复杂的野外表情。
*   **语义对齐有效**：引入 CLIP 文本引导的对比学习有助于模型学习到更具判别力的视觉特征。

#### 7. 优点与亮点
*   **对称融合机制**：双向交叉注意力比简单的特征拼接更能捕捉模态间的深层交互。
*   **利用预训练先验**：巧妙结合了视觉语言模型（CLIP）和自监督音频模型（Wav2Vec 2.0）的强大表征能力。
*   **时序效率**：使用 TCN 代替传统的 RNN/LSTM，在处理长序列时具有更好的并行性和计算效率。

#### 8. 不足与局限
*   **主干网络冻结**：虽然冻结主干能防止过拟合，但也限制了模型针对特定情感任务微调底层特征的能力。
*   **消融实验缺失**：缺乏对 TCN 层数、注意力头数以及对比损失权重 $\lambda$ 的详细参数敏感性分析。
*   **应用限制**：模型依赖于固定长度的视频窗口，对于极短或极长的情感爆发可能存在处理瓶颈。

（完）
