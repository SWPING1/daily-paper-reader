---
title: "EgoAdapt: Enhancing Robustness in Egocentric Interactive Speaker Detection Under Missing Modalities"
title_zh: EgoAdapt：增强缺失模态下第一视角交互式说话人检测的鲁棒性
authors: "Xinyuan Qian, Xinjia Zhu, Alessio Brutti, Dong Liang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18082v1"
tags: ["query:missing-modality"]
score: 10.0
evidence: 缺失模态下的鲁棒说话人检测
tldr: 本研究针对第一视角交互中“谁在跟我说话”（TTM）任务，提出了EgoAdapt框架。针对现实场景中视觉缺失、背景噪声及忽略头部姿态等挑战，该框架集成了视觉目标识别、并行共享权重音频编码及视觉模态缺失感知模块。通过融合言语与非言语信号并动态调整模态权重，EgoAdapt在Ego4D数据集上显著提升了检测精度和鲁棒性。
selection_source: fresh_fetch
motivation: 传统TTM模型在处理视觉数据缺失、背景噪声以及忽略头部姿态等现实复杂场景时表现不佳。
method: 提出了包含视觉目标识别、并行音频编码及模态缺失感知模块的自适应框架EgoAdapt，综合利用言语与非言语线索。
result: "在Ego4D数据集的TTM基准测试中，该方法在准确率上超越现有最先进技术4.96%，平均精度达到67.39%。"
conclusion: EgoAdapt通过多模态融合与缺失感知机制，有效增强了第一视角交互式说话人检测在复杂环境下的鲁棒性。
---

## 摘要
TTM（Talking to Me）任务是理解人类社交互动的关键组成部分，旨在确定谁正在与摄像机佩戴者进行对话。由于视觉数据缺失、忽视头部朝向的作用以及背景噪声，传统模型在现实场景中经常面临挑战。本研究通过引入 EgoAdapt 解决了这些局限性，这是一个专为缺失模态下鲁棒的第一视角“对我说话”说话人检测而设计的自适应框架。具体而言，EgoAdapt 包含三个关键模块：(1) 视觉说话人目标识别（VSTR）模块，它捕捉作为非语言线索的头部朝向和作为语言线索的嘴唇运动，从而能够对语言和非语言信号进行全面解释以解决 TTM 问题，这使其区别于仅关注检测说话状态的任务；(2) 并行权值共享音频（PSA）编码器，用于在噪声环境中增强音频特征提取；(3) 视觉模态缺失感知（VMMA）模块，用于估计每一帧中各模态的存在或缺失，从而动态调整系统响应。在 Ego4D 数据集的 TTM 基准上进行的全面评估表明，EgoAdapt 实现了 67.39% 的平均精度均值（mAP）和 62.01% 的准确率（Acc），在准确率上显著超过现有最先进方法 4.96%，在 mAP 上超过 1.56%。

## Abstract
TTM (Talking to Me) task is a pivotal component in understanding human social interactions, aiming to determine who is engaged in conversation with the camera-wearer. Traditional models often face challenges in real-world scenarios due to missing visual data, neglecting the role of head orientation, and background noise. This study addresses these limitations by introducing EgoAdapt, an adaptive framework designed for robust egocentric "Talking to Me" speaker detection under missing modalities. Specifically, EgoAdapt incorporates three key modules: (1) a Visual Speaker Target Recognition (VSTR) module that captures head orientation as a non-verbal cue and lip movement as a verbal cue, allowing a comprehensive interpretation of both verbal and non-verbal signals to address TTM, setting it apart from tasks focused solely on detecting speaking status; (2) a Parallel Shared-weight Audio (PSA) encoder for enhanced audio feature extraction in noisy environments; and (3) a Visual Modality Missing Awareness (VMMA) module that estimates the presence or absence of each modality at each frame to adjust the system response dynamically.Comprehensive evaluations on the TTM benchmark of the Ego4D dataset demonstrate that EgoAdapt achieves a mean Average Precision (mAP) of 67.39% and an Accuracy (Acc) of 62.01%, significantly outperforming the state-of-the-art method by 4.96% in Accuracy and 1.56% in mAP.

---

## 论文详细总结（自动生成）

以下是对论文《EgoAdapt: Enhancing Robustness in Egocentric Interactive Speaker Detection Under Missing Modalities》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是第一视角（Egocentric）视频理解中的**“对我说话”（Talking to Me, TTM）**任务。其核心目标是识别视频中哪个人正在与摄像机佩戴者进行交互。
*   **研究背景**：在现实的社交场景中，第一视角相机经常面临剧烈运动导致的视觉目标丢失（遮挡或出画）、严重的背景噪声干扰，以及难以区分“正在说话”与“正在对我说话”的问题。
*   **核心挑战**：传统方法往往假设模态完整且环境理想，但在实际应用中，视觉模态缺失和环境噪声会导致模型性能大幅下降。

### 2. 方法论：EgoAdapt 框架
为了提升鲁棒性，作者提出了 **EgoAdapt** 框架，包含三个核心模块：
*   **视觉说话人目标识别（VSTR）模块**：
    *   **非言语线索**：利用 RepVGG 提取头部作物（Head Crops）特征，并将其映射为 6D 旋转表示，以估计头部的偏航（Yaw）、俯仰（Pitch）和翻滚（Roll）角，判断说话人的朝向。
    *   **言语线索**：利用 CLIP 的 ViT 骨干网络提取嘴唇区域（Lip Crops）的运动特征。
*   **并行共享权重音频（PSA）编码器**：
    *   采用 Whisper-small 作为骨干网络。
    *   **核心思想**：同时输入干净语音和混合了噪声的语音，通过共享权重的编码器提取特征，并利用 **均方误差（MSE）损失函数** 最小化两者特征间的距离，从而迫使模型学习抗噪的鲁棒音频表示。
*   **视觉模态缺失感知（VMMA）模块**：
    *   **细粒度感知**：在帧级别标记视觉数据是否存在。
    *   **粗粒度感知**：计算片段内缺失帧的比例 $\Delta_c = T_{miss} / T$，并根据自适应阈值 $\beta_T$ 生成动态 Prompt，引导模型在视觉缺失时更多地依赖音频线索。
*   **多模态融合**：采用两两交叉注意力机制（Head-Lip, Lip-Audio, Audio-Head）进行特征聚合，最后通过自注意力层输出预测。

### 3. 实验设计
*   **数据集**：主要使用 **Ego4D** 社交交互基准数据集（包含约 32.4 小时训练数据）。
*   **噪声模拟**：引入 **WHAM!** 数据集中的真实环境噪声，以 50% 的概率添加到音频中，并设置 -10dB 到 10dB 的不同信噪比（SNR）进行测试。
*   **对比方法**：
    *   Random Guess（随机猜测）
    *   ResNet-18 Bi-LSTM（Ego4D 官方基准）
    *   EgoT2（任务翻译模型）
    *   QuAVF（当前的 SOTA 方法，基于质量感知的融合）

### 4. 资源与算力
*   **骨干网络**：使用了 6DRepNet（头部姿态）、CLIP ViT-B/32（视觉特征）和 Whisper-small（音频特征）。
*   **训练细节**：使用 Adam 优化器，学习率为 $1 \times 10^{-5}$，采用 Focal Loss 解决类别不平衡问题，训练 40 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到 Batch Size 设置为 1，并使用了 4 个 Data Loader 线程。

### 5. 实验数量与充分性
*   **实验规模**：在 Ego4D 的验证集和测试集上进行了全面评估。
*   **消融实验**：针对 VSTR、PSA 和 VMMA 三个模块分别做了组合消融，验证了每个模块的增益。
*   **鲁棒性分析**：专门针对不同信噪比（SNR）环境和不同视觉缺失比例进行了对比实验。
*   **定性分析**：提供了多说话人、高噪声、视觉缺失三种典型挑战场景的可视化对比。
*   **评价**：实验设计较为充分，涵盖了从定量指标（mAP, Acc）到定性可视化的多个维度，客观地展示了模型在极端情况下的优势。

### 6. 主要结论与发现
*   **性能提升**：EgoAdapt 在测试集上达到了 **62.01% 的准确率** 和 **67.39% 的 mAP**，分别比之前的 SOTA（QuAVF）高出 **4.96%** 和 **1.56%**。
*   **模态互补**：实验发现，单纯增加视觉模态有时会降低性能（由于数据缺失），但通过 VMMA 模块的动态感知，可以有效利用视觉线索并超越纯音频模型。
*   **抗噪性**：PSA 模块在低信噪比（如 -10dB）下表现出极强的鲁棒性，显著优于直接使用预训练音频模型。

### 7. 优点
*   **多维度线索**：不仅关注“说话状态”（嘴唇），还引入了“交互意图”（头部朝向），更符合 TTM 任务的本质。
*   **动态自适应**：VMMA 模块通过 Prompt 机制解决了多模态学习中常见的“模态缺失”导致的性能崩溃问题。
*   **训练策略巧妙**：PSA 模块利用共享权重和 MSE 约束，在不增加推理负担的前提下提升了抗噪能力。

### 8. 不足与局限
*   **计算复杂度**：多阶段的交叉注意力融合机制相比简单的拼接融合具有更高的计算开销，可能影响实时部署。
*   **同步依赖**：模型假设音视频是严格同步的，未讨论在音视频对齐偏移（Synchronization Offset）情况下的表现。
*   **泛化性验证**：实验主要集中在 Ego4D 数据集，对于其他领域（如第三方视角监控或不同类型的交互）的迁移能力尚待验证。

（完）
