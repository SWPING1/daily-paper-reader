---
title: Multimodal Self-Attention Network with Temporal Alignment for Audio-Visual Emotion Recognition
title_zh: 用于视听情感识别的具有时间对齐功能的多模态自注意力网络
authors: "Inyong Koo, yeeun Seong, Minseok Son, Jaehyuk Jang, Changick Kim"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11095v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 具有时间对齐功能的视听情感识别
tldr: 本研究针对音视频情感识别（AVER）中常见的模态采样率不匹配问题，提出了一种基于Transformer的对齐框架。该框架利用多模态自注意力编码器捕捉模态内外的依赖关系，并引入时间对齐旋转位置嵌入（TaRoPE）和跨时间匹配（CTM）损失函数，实现了不同采样率特征的隐式同步与一致性引导。实验证明，该方法在CREMA-D和RAVDESS数据集上均优于现有基准，有效提升了跨模态融合效果。
selection_source: fresh_fetch
motivation: 现有的音视频情感识别方法往往忽略了不同模态间采样频率不一致的问题，导致时间特征融合不够精确。
method: 提出一种包含时间对齐旋转位置嵌入（TaRoPE）和跨时间匹配（CTM）损失的Transformer架构，用于同步和对齐多模态特征。
result: 在CREMA-D和RAVDESS数据集上的实验结果显示，该方法在情感识别准确率上显著超过了近年来的主流基线模型。
conclusion: 显式解决模态间的帧率失配问题有助于保留关键的时间线索，从而显著增强多模态情感识别的性能。
---

## 摘要
视听情感识别（AVER）方法通常融合话语级特征，即使是帧级注意力模型也极少处理跨模态的帧率不匹配问题。在本文中，我们提出了一种基于 Transformer 的框架，专注于多模态特征的时间对齐。我们的设计采用了一个多模态自注意力编码器，在共享特征空间内同时捕捉模态内和模态间的依赖关系。为了解决异构采样率问题，我们引入了时间对齐旋转位置嵌入（TaRoPE），它隐式地同步了音频和视频标记（tokens）。此外，我们引入了跨时间匹配（CTM）损失，强制要求时间相近的对之间保持一致性，从而引导编码器实现更好的对齐。在 CREMA-D 和 RAVDESS 数据集上的实验表明，该方法相比于近期的基准模型有持续的改进，这表明显式地解决帧率不匹配问题有助于保留时间线索并增强跨模态融合。

## Abstract
Audio-visual emotion recognition (AVER) methods typically fuse utterance-level features, and even frame-level attention models seldom address the frame-rate mismatch across modalities. In this paper, we propose a Transformer-based framework focusing on the temporal alignment of multimodal features. Our design employs a multimodal self-attention encoder that simultaneously captures intra- and inter-modal dependencies within a shared feature space. To address heterogeneous sampling rates, we incorporate Temporally-aligned Rotary Position Embeddings (TaRoPE), which implicitly synchronize audio and video tokens. Furthermore, we introduce a Cross-Temporal Matching (CTM) loss that enforces consistency among temporally proximate pairs, guiding the encoder toward better alignment. Experiments on CREMA-D and RAVDESS datasets demonstrate consistent improvements over recent baselines, suggesting that explicitly addressing frame-rate mismatch helps preserve temporal cues and enhances cross-modal fusion.

---

## 论文详细总结（自动生成）

以下是对论文《Multimodal Self-Attention Network with Temporal Alignment for Audio-Visual Emotion Recognition》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：音视频情感识别（AVER）中，音频（通常为 50 FPS）与视频（通常为 30 FPS）的**采样率不匹配（Frame-rate Mismatch）**。
*   **研究动机**：
    *   传统方法多在话语级（Utterance-level）融合，丢失了细粒度时间动态。
    *   现有的 Transformer 模型虽使用位置嵌入，但无法感知跨模态的相对时间结构，导致跨模态注意力可能分散在无关位置，削弱了融合效果。
    *   需要一种能够显式处理异构采样率并实现时间同步的对齐框架。

### 2. 方法论
该论文提出了一种基于 Transformer 的统一框架，核心包含以下三个部分：
*   **多模态自注意力编码器（Multimodal Self-Attention, MSA）**：
    *   将音频（xlsr-Wav2Vec 2.0 提取）和视频（OpenFace 提取的动作单位 AU）特征投影到共享嵌入空间。
    *   使用统一的 Transformer 块同时捕捉模态内（Intra-modal）和模态间（Inter-modal）的依赖关系。
*   **时间对齐旋转位置嵌入（TaRoPE）**：
    *   改进了传统的旋转位置嵌入（RoPE）。通过根据模态采样率比例缩放旋转频率（$\theta_v = \frac{\eta_a}{\eta_v} \theta_a$），将视频位置重新缩放到音频时间线上。
    *   这使得跨模态注意力能够基于一致的物理时间距离进行计算，实现隐式同步。
*   **跨时间匹配（CTM）损失**：
    *   引入基于时间戳的**高斯亲和力（Gaussian Affinity）**。
    *   通过双向交叉熵损失，强制要求在物理时间轴上接近的音视频帧具有相似的特征表示，从而引导编码器实现显式的时间一致性。

### 3. 实验设计
*   **数据集**：
    *   **CREMA-D**：包含 7,442 个剪辑，涉及 91 位演员的 6 种基本情感。
    *   **RAVDESS**：包含 1,440 个话语，涉及 24 位专业演员的 8 种情感。
*   **Benchmark 与对比方法**：
    *   对比了 2019 年至 2025 年间的 12 种主流方法，包括 TA-AVN、AttA-Net、HiCMAE、ATTSF-Net 等。
*   **评估协议**：采用独立于说话人（Speaker-independent）的训练/验证划分，RAVDESS 使用 5 折交叉验证。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，训练 50 个 epoch，批大小（Batch Size）为 4。
*   **超参数**：学习率 $5 \times 10^{-5}$（线性衰减），$d_{model}=512$，$d_{emb}=128$。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提到 MSA 层在处理 2 秒输入时约引入 0.5 GFLOPs 的计算量，属于计算高效型设计。

### 5. 实验数量与充分性
*   **实验规模**：在两个标准数据集上进行了完整的性能对比。
*   **消融实验**：
    *   对比了不同的融合策略（拼接、ISA、ICA、MSA），证明了 MSA 在参数量更少的情况下效果更好。
    *   对比了多种位置嵌入（正弦、可学习、RoPE、TaRoPE），验证了 TaRoPE 的优越性。
    *   验证了 CTM 损失对不同位置嵌入方案的增益。
*   **充分性评价**：实验设计较为全面，不仅有量化指标，还通过特征动力学可视化（Feature Dynamics）分析了 CTM 损失对时间对齐的实际影响，逻辑闭环较好。

### 6. 主要结论与发现
*   **性能领先**：在 CREMA-D 上达到 89.49%（提升 4.43%），在 RAVDESS 上达到 89.25%（提升 0.58%），均创下新纪录。
*   **对齐的重要性**：显式处理帧率失配比单纯增加模型深度或复杂度更有效。
*   **TaRoPE 的有效性**：通过调整旋转频率，Transformer 能够更好地理解跨模态的物理时间关联。
*   **CTM 损失的作用**：该损失能显著增强音视频特征在时间趋势上的一致性（导数符号一致性更高）。

### 7. 优点
*   **创新性对齐方案**：TaRoPE 是一种巧妙且轻量级的改进，解决了 Transformer 处理异构序列时的固有缺陷。
*   **架构简洁高效**：采用统一的 MSA 编码器，相比堆叠多个特定模态的注意力层，参数量更少且性能更强。
*   **理论与可视化结合**：通过高斯亲和力和特征轨迹图，直观证明了时间对齐的物理意义。

### 8. 不足与局限
*   **特征提取依赖预处理**：视频特征依赖于 OpenFace 提取的 AU（动作单位），而非端到端的原始像素处理，这可能限制了模型捕捉 AU 之外的视觉信息。
*   **批大小限制**：Batch Size 仅为 4，可能受限于显存或数据集样本长度，这在一定程度上可能影响训练的稳定性。
*   **场景局限性**：实验主要在受控的实验室数据集（CREMA-D, RAVDESS）上完成，尚未在复杂的“野外”（In-the-wild）噪声环境下验证鲁棒性。

（完）
