---
title: Multiscale Structure-Guided Latent Diffusion for Multimodal MRI Translation
title_zh: 多尺度结构引导的潜在扩散模型用于多模态 MRI 翻译
authors: "Jianqiang Lin, Zhiqiang Shen, Peng Cao, Jinzhu Yang, Osmar R. Zaiane, Xiaoli Liu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12581v1"
tags: ["query:missing-modality"]
score: 9.0
evidence: 通过风格-结构解耦处理任意缺失模态场景
tldr: 针对多模态MRI翻译中解剖结构不一致和纹理细节丢失的问题，本文提出了一种基于潜在扩散模型的多尺度结构引导框架（MSG-LDM）。该方法通过在潜在空间中解耦模态特定的风格与共享的结构特征，并结合多尺度特征空间建模解剖布局与边界细节，实现了在任意模态缺失情况下的高质量图像合成。实验证明，该方法在BraTS2020和WMH数据集上优于现有方法，尤其在结构重建方面表现出色。
selection_source: fresh_fetch
motivation: 现有的扩散模型在处理多模态MRI翻译时，容易出现解剖结构不一致或纹理细节退化的问题。
method: 提出MSG-LDM框架，通过在潜在空间解耦风格与结构特征，并利用多尺度特征显式建模高频边界与低频布局。
result: 在BraTS2020和WMH数据集上的实验表明，该方法在图像合成质量和结构完整性方面均优于现有主流方法。
conclusion: 该研究通过多尺度结构引导和特征解耦，有效提升了多模态MRI翻译的结构一致性和细节保真度。
---

## 摘要
尽管扩散模型在多模态磁共振成像（MRI）翻译任务中取得了显著进展，但现有方法在处理任意模态缺失的情况时，仍往往面临解剖结构不一致或纹理细节退化的问题。为了解决这些问题，我们提出了一种基于潜在扩散的多模态 MRI 翻译框架，称为 MSG-LDM。通过利用可用模态，该方法推断出完整的结构信息，从而保留了可靠的边界细节。具体而言，我们在潜在空间中引入了一种风格-结构解耦机制，将特定模态的风格特征与共享的结构表示显式分离，并在多尺度特征空间中联合建模低频解剖布局和高频边界细节。在结构解耦阶段，显式融入高频结构信息以增强特征表示，引导模型在学习模态不变的低频解剖表示时，关注细粒度的结构线索。此外，为了减少特定模态风格的干扰并提高结构表示的稳定性，我们设计了风格一致性损失和结构感知损失。在 BraTS2020 和 WMH 数据集上的广泛实验表明，所提方法优于现有的 MRI 合成方法，特别是在重建完整结构方面。源代码可在 https://github.com/ziyi-start/MSG-LDM 公开获取。

## Abstract
Although diffusion models have achieved remarkable progress in multi-modal magnetic resonance imaging (MRI) translation tasks, existing methods still tend to suffer from anatomical inconsistencies or degraded texture details when handling arbitrary missing-modality scenarios. To address these issues, we propose a latent diffusion-based multi-modal MRI translation framework, termed MSG-LDM. By leveraging the available modalities, the proposed method infers complete structural information, which preserves reliable boundary details. Specifically, we introduce a style--structure disentanglement mechanism in the latent space, which explicitly separates modality-specific style features from shared structural representations, and jointly models low-frequency anatomical layouts and high-frequency boundary details in a multi-scale feature space. During the structure disentanglement stage, high-frequency structural information is explicitly incorporated to enhance feature representations, guiding the model to focus on fine-grained structural cues while learning modality-invariant low-frequency anatomical representations. Furthermore, to reduce interference from modality-specific styles and improve the stability of structure representations, we design a style consistency loss and a structure-aware loss. Extensive experiments on the BraTS2020 and WMH datasets demonstrate that the proposed method outperforms existing MRI synthesis approaches, particularly in reconstructing complete structures. The source code is publicly available at https://github.com/ziyi-start/MSG-LDM.

---

## 论文详细总结（自动生成）

以下是对论文《Multiscale Structure-Guided Latent Diffusion for Multimodal MRI Translation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在临床实践中，由于采集时间长或成本限制，多模态 MRI 数据（如 T1, T2, T1CE, FLAIR）往往不完整。现有的跨模态合成方法（尤其是扩散模型）在处理**任意模态缺失**时，容易出现解剖结构扭曲、高频纹理细节丢失以及结构信息与模态风格耦合的问题。
*   **研究背景**：扩散模型虽在生成质量上优于 GAN，但其本质上对医疗图像的解剖结构不敏感。本文旨在通过显式的结构引导，提升模型在复杂缺失场景下的合成保真度和结构一致性。

### 2. 方法论
MSG-LDM 框架的核心思想是在 VAE 的潜在空间中实现**风格与结构的显式解耦**，并利用多尺度结构先验引导扩散过程。
*   **风格-结构解耦机制**：
    *   **结构编码器 (Estr)**：提取模态不变的解剖特征。
    *   **风格编码器 (Esty)**：提取特定模态的风格特征。
*   **关键技术模块**：
    *   **高频注入块 (HFIB)**：利用可学习的动态高斯滤波器分解特征，提取高频残差（边缘、纹理）并重新注入，以增强细粒度结构。
    *   **多模态结构特征融合 (MMSF)**：通过注意力门控机制，自适应地融合来自不同可用模态的结构特征。
    *   **多尺度结构特征增强 (MSSE)**：利用跨尺度注意力机制，将低层的高频细节注入高层语义表示，形成统一的结构表示 $F_s$。
*   **损失函数设计**：
    *   **风格一致性损失 ($L_{sc}$)**：采用类似对比学习的思路，拉近同模态风格、推开异模态风格，减少风格对结构的干扰。
    *   **结构感知损失 ($L_{sa}$)**：结合 $L_1$ 强度损失和**频域 SSIM 损失**（基于离散余弦变换 DCT），确保全局布局和局部细节的准确性。
    *   **总损失**：包含分割辅助损失、风格一致性损失、结构感知损失和潜在扩散损失。

### 3. 实验设计
*   **数据集**：
    *   **BraTS2020**：脑肿瘤多模态 MRI（T1, T1CE, T2, FLAIR），包含 369 个病例。
    *   **WMH**：白质高信号数据集，用于验证跨数据集的泛化能力。
*   **实验场景**：模拟任意模态缺失，分别测试 1 个、2 个、3 个可用模态作为输入来合成目标模态的情况。
*   **Benchmark（对比方法）**：
    *   **MM-GAN**：基于 GAN 的多模态合成方法。
    *   **SynDiff**：基于对抗扩散模型的合成方法。
    *   **MISA-LDM**：目前最先进的基于潜在扩散的结构感知翻译模型。
*   **评价指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、Dice 分数（评估合成图像对下游分割任务的贡献）。

### 4. 资源与算力
*   **硬件环境**：使用了 **3 台 NVIDIA 4090 GPU**。
*   **训练参数**：Batch size 为 9，学习率为 $1 \times 10^{-4}$，使用 Adam 优化器。
*   **训练时长**：共训练了 **100 个 epoch**（具体小时数未标明，但 4090 算力充足，通常此类任务需数十小时）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 BraTS2020 上进行了详尽的定量对比，涵盖了 4 种模态作为目标、不同数量可用模态作为输入的多种组合。
    *   在 WMH 数据集上进行了跨数据集验证。
    *   **消融实验**：针对解耦机制、MMSF、HFIB、MSSE 以及两种核心损失函数（$L_{sa}, L_{sc}$）分别进行了剔除实验。
*   **充分性评价**：实验设计较为全面，不仅对比了图像质量指标（PSNR/SSIM），还通过下游分割任务（Dice）验证了合成图像的临床实用性。消融实验逻辑清晰，证明了每个模块的必要性。

### 6. 主要结论与发现
*   **性能领先**：MSG-LDM 在所有指标上均优于现有 SOTA 方法，尤其在 SSIM 和 Dice 分数上提升显著，表明其在结构保持方面具有明显优势。
*   **结构引导的价值**：显式引入结构先验不仅提高了合成精度，还显著**加速了扩散模型的收敛过程**（见论文 Fig. 1 的去噪对比）。
*   **模态增益**：随着可用模态数量的增加，合成图像的清晰度和结构完整性呈线性提升趋势。

### 7. 优点（亮点）
*   **多尺度高频建模**：通过 HFIB 和 MSSE 显式处理高频信息，解决了传统扩散模型在医疗图像边缘重建上的模糊问题。
*   **频域约束**：引入 DCT 频域损失来约束结构，比单纯的空间域损失更能捕捉全局解剖布局。
*   **解耦彻底**：通过风格一致性损失增强了特征的纯净度，使得结构表示在不同模态间更具鲁棒性。

### 8. 不足与局限
*   **维度限制**：该方法目前基于 **2D 轴向切片**处理，虽然在 2D 上表现优异，但未充分利用 MRI 的 3D 空间上下文信息。
*   **计算开销**：尽管在潜在空间运行，但扩散模型的多步迭代推理速度仍慢于传统的 GAN 或单步生成模型。
*   **极端缺失测试**：实验主要集中在至少有一个模态可用的情况，未深入探讨在极低信噪比或严重伪影输入下的表现。

（完）
