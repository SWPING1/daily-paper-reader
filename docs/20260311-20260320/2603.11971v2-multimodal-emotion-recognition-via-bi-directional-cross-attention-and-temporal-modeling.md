---
title: Multimodal Emotion Recognition via Bi-directional Cross-Attention and Temporal Modeling
title_zh: 基于双向交叉注意力与时序建模的多模态情感识别
authors: "Junhyeong Byeon, Jeongyeol Kim, Sejoon Lim"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11971v2"
tags: ["query:missing-modality"]
score: 9.0
evidence: 通过交叉注意力的多模态情感识别
tldr: 针对野外视频中表情识别的复杂性，本文提出一种多模态情感识别框架。该框架结合大规模预训练的视觉和音频模型，通过时间视觉建模捕捉动态特征，并利用双向交叉注意力机制实现模态间的深度融合。此外，引入文本引导的对比学习以增强语义表示。在第10届ABAW挑战赛中，该方法显著提升了识别性能，证明了多模态融合在复杂环境下的有效性。
selection_source: fresh_fetch
motivation: 单一模态难以应对野外视频中复杂的背景噪声、光照变化及动态情感表达带来的挑战。
method: 结合预训练音视频模型，采用时间视觉建模、双向交叉注意力融合模块以及文本引导的对比学习目标。
result: 在ABAW第10届EXPR基准测试中，Macro F1分数从基准的0.25提升至0.32。
conclusion: 结合时间建模、音频表征和跨模态融合能有效提升非受限真实环境下的情感识别鲁棒性。
---

## 摘要
由于面部外观、背景条件、音频噪声的巨大差异以及人类情感固有的动态特性，野外（in-the-wild）视频数据中的表情识别仍然具有挑战性。仅依赖单一模态（如面部表情或语音）通常不足以捕捉这些复杂的情感线索。为了解决这一局限性，我们针对第10届野外情感行为分析（ABAW）挑战赛中的表情（EXPR）任务，提出了一种多模态情感识别框架。我们的框架基于大规模预训练的视觉和音频表示学习模型，并将其集成到一个统一的多模态架构中。为了更好地捕捉面部表情序列中的时序模式，我们在视频窗口上引入了时序视觉建模。我们进一步引入了一个双向交叉注意力融合模块，使视觉和音频特征能够以对称方式进行交互，从而促进跨模态上下文关联和互补的情感理解。此外，我们采用文本引导的对比目标，通过与情感相关的文本提示对齐，来促进具有语义意义的视觉表示。在 ABAW 第10届 EXPR 基准测试上的实验结果证明了所提框架的有效性，其 Macro F1 分数达到 0.32（基准分数为 0.25），并突显了结合时序视觉建模、音频表示学习和跨模态融合在不受限真实世界环境中实现鲁棒情感识别的优势。

## Abstract
Expression recognition in in-the-wild video data remains challenging due to substantial variations in facial appearance, background conditions, audio noise, and the inherently dynamic nature of human affect. Relying on a single modality, such as facial expressions or speech, is often insufficient for capturing these complex emotional cues. To address this limitation, we propose a multimodal emotion recognition framework for the Expression (EXPR) task in the 10th Affective Behavior Analysis in-the-wild (ABAW) Challenge. Our framework builds on large-scale pre-trained models for visual and audio representation learning and integrates them in a unified multimodal architecture. To better capture temporal patterns in facial expression sequences, we incorporate temporal visual modeling over video windows. We further introduce a bi-directional cross-attention fusion module that enables visual and audio features to interact in a symmetric manner, facilitating cross-modal contextualization and complementary emotion understanding. In addition, we employ a text-guided contrastive objective to encourage semantically meaningful visual representations through alignment with emotion-related text prompts. Experimental results on the ABAW 10th EXPR benchmark demonstrate the effectiveness of the proposed framework, achieving a Macro F1 score of 0.32 compared to the baseline score of 0.25, and highlight the benefit of combining temporal visual modeling, audio representation learning, and cross-modal fusion for robust emotion recognition in unconstrained real-world environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对第10届野外情感行为分析（ABAW）挑战赛中表情识别（EXPR）任务的多模态框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在“野外”（in-the-wild）环境下的视频表情识别面临面部姿态变化、光照不均、遮挡、背景噪声以及情感表达的动态演变等巨大挑战。
*   **研究动机**：单一模态（仅视觉或仅音频）难以提供足够的信息来应对复杂环境。现有的多模态方法往往缺乏对模态间深度交互的建模，且在捕捉面部表情随时间变化的动态特征方面存在不足。

### 2. 提出的方法论
该框架整合了视觉、音频和文本三种信息，核心组件包括：
*   **特征提取（冻结骨干网络）**：
    *   **视觉流**：使用预训练的 **CLIP (ViT-B/32)** 提取帧级特征。
    *   **音频流**：使用预训练的 **Wav2Vec 2.0** 提取音频特征。
    *   **文本流**：利用 CLIP 文本编码器将“A face expressing [Emotion]”等提示词转化为语义嵌入。
*   **时序建模与适配**：
    *   **视觉 TCN**：引入 6 层堆叠的**时间卷积网络（TCN）**，通过扩张因果卷积扩大感受野，捕捉面部表情的短程和长程动态。
    *   **音频适配器**：通过线性投影和归一化将音频特征维度对齐至视觉特征空间。
*   **双向交叉注意力融合（Bi-directional Cross-Attention）**：
    *   采用对称的注意力机制：视觉特征作为查询（Query）去关注音频特征，同时音频特征作为查询去关注视觉特征。这种双向交互促进了跨模态上下文的互补理解。
*   **文本引导的对比学习**：
    *   在训练阶段，通过对比损失（Contrastive Loss）将融合后的视觉表示与对应的情感文本标签对齐，增强特征的语义一致性。
*   **分类器**：经过时间池化和特征拼接后，通过一个三层 MLP 输出 8 类表情的预测结果。

### 3. 实验设计
*   **数据集**：使用第 10 届 ABAW 挑战赛的官方数据集（基于 Aff-Wild2），包含大量非受限环境下的视频。
*   **Benchmark**：官方提供的基准线（Baseline）Macro F1 分数为 **0.25**。
*   **对比实验**：主要对比了不同**时间窗口大小**（10、15、30、60 帧）对模型性能的影响。

### 4. 资源与算力
*   **硬件**：使用了 **1 张 NVIDIA RTX A5000 GPU**。
*   **训练细节**：采用 AdamW 优化器，学习率为 $1 \times 10^{-5}$，使用余弦退火调度器，训练 30 个 epoch，batch size 为 64。
*   **策略**：为了提高稳定性并防止过拟合，CLIP 和 Wav2Vec 2.0 的骨干网络在训练过程中保持**冻结**状态。

### 5. 实验数量与充分性
*   **实验规模**：论文在验证集和测试集上分别测试了四种不同的时间窗口设置。
*   **充分性评价**：
    *   **优点**：实验涵盖了从短时（10帧）到长时（60帧）的对比，验证了时序上下文的重要性。
    *   **不足**：缺乏针对核心组件（如 TCN、交叉注意力、对比损失）的详细**消融实验**，难以量化每个模块的具体贡献。此外，对比的方法主要限于官方基准，未广泛对比其他 SOTA 多模态模型。

### 6. 主要结论与发现
*   **性能提升**：该框架在测试集上达到了 **0.32** 的 Macro F1 分数，显著优于官方基准（0.25）。
*   **时序窗口影响**：在验证集上，30 帧窗口表现最佳（0.3670）；而在测试集上，60 帧窗口表现最优（0.32）。这表明更广泛的时序上下文有助于在复杂环境中实现更鲁棒的识别。
*   **多模态优势**：双向交叉注意力机制能有效整合音视频线索，弥补单一模态在噪声环境下的不足。

### 7. 优点与亮点
*   **对称融合机制**：双向交叉注意力比简单的特征拼接更能捕捉模态间的深层关联。
*   **引入文本先验**：利用 CLIP 的文本嵌入作为辅助监督信号，为抽象的情感分类提供了额外的语义指导。
*   **时序增强**：通过 TCN 解决了静态帧特征无法反映表情演变过程的问题。

### 8. 不足与局限
*   **骨干网络冻结**：虽然冻结预训练模型能节省算力并防止过拟合，但也限制了模型针对特定情感数据集进行深度微调的能力。
*   **计算开销**：双向交叉注意力的计算复杂度随序列长度增加而显著上升，可能影响实时性。
*   **消融分析缺失**：未明确展示如果不使用音频或不使用文本引导，性能会下降多少，导致难以判断各部分的必要性。

（完）
