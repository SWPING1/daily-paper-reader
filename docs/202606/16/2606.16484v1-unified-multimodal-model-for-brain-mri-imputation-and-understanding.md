---
title: Unified Multimodal Model for Brain MRI Imputation and Understanding
title_zh: 面向脑部MRI补全与理解的统一多模态模型
authors: "Zhiyun Song, Che Liu, Tian Xia, Avinash Kori, Wenjia Bai"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16484v1"
tags: ["query:mm"]
score: 8.0
evidence: 联合成像模态插补与脑图像理解以处理缺失MRI数据
tldr: 多模态大语言模型在医学领域潜力巨大，但面临训练数据稀缺和临床模态缺失的挑战。本文提出UniBrain，一个统一的脑MRI分析与填补模型，通过统一训练策略同时实现模态填补和理解。采用自回归训练、自对齐学习解剖特征及动态隐藏状态机制缓解暴露偏差。在多种脑疾病数据集上，UniBrain在不同缺失程度下均取得优异性能，为临床诊断提供了可靠支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16484v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 792, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16484v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 530, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16484v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1259, \"height\": 483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16484v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1258, \"height\": 400, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16484v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1251, \"height\": 283, \"label\": \"Table\"}]"
motivation: 现有医学多模态大语言模型受限于高质量数据稀缺和临床模态缺失问题，限制了其实际应用。
method: UniBrain采用统一训练策略联合模态填补与理解，利用交错数据流自回归训练、自对齐学习解剖特征及动态隐藏状态机制缓解长上下文暴露偏差。
result: 在多疾病脑MRI数据集上，UniBrain在图像填补、理解和疾病诊断任务中均达到高水平，尤其在不同模态缺失场景下表现稳定。
conclusion: UniBrain有效解决了模态缺失下的脑MRI分析难题，为临床鲁棒的多模态诊断提供了新方案。
---

## 摘要
多模态大语言模型在医学领域具有巨大潜力，因为它们继承了LLM的知识，并允许以自然语言整合、分析和解释多种数据模态。然而，医学MLLM领域面临着重大挑战，尤其是高质量训练数据的稀缺以及真实临床场景中频繁出现的数据缺失问题。本文提出了一种新颖的统一多模态模型UniBrain，用于脑部磁共振图像分析。为解决可能的脑MRI模态缺失问题，我们采用统一训练策略，联合执行成像模态补全与脑图像理解。训练过程中，构建交错且描述丰富的数据流，以自回归方式训练模型，实现基于生成的多模态数据的医学推理。引入自对齐策略，利用密集图像嵌入学习细粒度解剖特征，无需详细的图像描述。此外，我们提出动态隐藏状态机制，以缓解长上下文多模态推理中的暴露偏差。在多疾病脑MRI数据集上的大量实验表明，UniBrain在不同程度模态不完整情况下，在脑图像补全、理解和疾病诊断方面均表现出高性能。

## Abstract
Multimodal large language models (MLLMs) hold great potential for medicine, as they inherit knowledge from LLM and allow multiple data modalities to be integrated, analysed and interpreted in natural language. However, the field of medical MLLMs is constrained by non-trivial challenges, notably the scarcity of high-quality training data and the frequent occurrence of missing data in the real-world clinical setting. Here, we propose a novel unified multimodal model, UniBrain, for brain magnetic resonance image (MRI) analysis. To address potential missing brain MRI modalities, we employ a unified training strategy to perform joint imaging modality imputation and brain image understanding. During training, an interleaved and description-enriched data flow is constructed to train the model in an autoregressive manner, enabling medical reasoning with generated multimodal data. A self-alignment strategy is introduced to leverage dense image embeddings to learn fine-grained anatomical features without requiring detailed image captions. Furthermore, we propose a dynamic hidden state mechanism to alleviate the exposure bias during long-context multimodal inference. Extensive experiments on multi-disease brain MRI dataset demonstrate that UniBrain achieves high performance for brain image imputation, understanding, and disease diagnosis under various extents of modality incompleteness.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多模态大语言模型（MLLM）在医学领域潜力巨大，但面临两大关键挑战：
  1. **高质量训练数据稀缺**：医学图像-文本对难以获取，放射学报告提供的语义描述缺乏像素级监督信号。
  2. **临床模态缺失**：实际诊断中脑MRI（如T1、T2、FLAIR等）常因扫描条件或资源限制缺失部分模态，影响全面诊断。
- **现有方法的局限**：
  - 显式补全（如ResViT、M2DN）将补全与下游诊断分离为两阶段流程，可能导致特征不匹配。
  - 隐式表示学习（如ShaSpec、SimMLM）专注于诊断性能但缺乏图像生成结果，不可解释。
- **本文意义**：提出**UniBrain**，一个**统一的、自回归的多模态模型**，同时执行**模态补全**（生成缺失MRI）和**图像理解**（疾病诊断与报告生成），使补全过程辅助下游推理，实现互惠提升。

## 2. 论文提出的方法论
### 核心思想
- 将脑MRI分析建模为**自回归序列建模**问题：给定部分可用模态，模型依次生成缺失模态（作为中间推理步骤），最后输出诊断印象。
- 基于**BAGEL**（Mixture-of-Transformer-Experts架构）进行医学适应，包含理解专家和生成专家。

### 关键技术细节
1. **交错数据流构建**
   - 将输入模态、生成任务描述、放射学发现、目标图像逐一排列为序列：`x1, z1, f1, x2, ..., zk, R`。
   - 每个生成任务`zi`包含思考指令（如“需要将T1和FLAIR转换为T2”）和已有模态的发现。
2. **自对齐策略（Self-Alignment, SA）**
   - 利用ViT提取的密集视觉嵌入作为条件，指导VAE生成专家重建原图。
   - 损失函数为流匹配损失，无需文本描述，仅依赖图像自身信号：
     \[
     \mathcal{L}_{\text{SA}} = \mathbb{E}_{t,\epsilon} \left[ \| V_\theta(e_t^i, t, w_i, p_{\text{rec}}) - (\epsilon - e^i) \|^2 \right]
     \]
3. **动态隐藏状态机制（Dynamic Hidden State, DHS）**
   - 训练时使用**KV-cache**实施端到端自回归展开：先处理已知信息，在生成令牌处进行有限步扩散（10步），得到自生成图像，再将其隐藏状态加入KV cache。
   - 强制模型基于自身生成结果（含伪影）继续推理，缓解暴露偏差。
4. **统一训练目标**
   - 文本任务：Next-token prediction损失（LNTP）。
   - 生成任务：Flow matching损失（Lflow）和自对齐损失。
   - 总体损失：\(\mathcal{L} = \frac{1}{k}\sum_i (\mathcal{L}_{\text{SA}} + \mathcal{L}_{\text{NTP}} + \mathcal{L}_{\text{flow}})\).

## 3. 实验设计
### 数据集与场景
- **数据集**：公开的RadGenome-Brain MRI数据集，包含3408个脑MRI扫描，6种模态（T1、T2、FLAIR、DWI、ADC、T1ce），5种疾病（胶质瘤、脑膜瘤、转移瘤、卒中、白质高信号），配有放射科医生撰写的报告。
- **分割**：训练2382例，验证338例，测试688例。
- **场景**：不同缺失模态组合（仅T1w、T1w+T2w、T1w+T2w+T2f、完整数据），评估补全质量和下游诊断能力。

### 基准方法
- **隐式表示学习**：ShaSpec、SimMLM（仅输出诊断，无生成）。
- **显式模态补全**：ResViT + Lingshu（两阶段）、ResViT + UniBrain理解模块、M2DN + UniBrain理解模块。
- **多模态大语言模型**：BAGEL、UniMedVL、Lingshu。
- **消融基线**：UniBrain Und.（仅理解，无统一训练）。

### 评估指标
- 诊断：Top-1准确率。
- 报告生成：ROUGE-1、RaTEScore。
- 图像补全质量：PSNR、SSIM、下游诊断Top-1（诊断可用性）。

## 4. 资源与算力
- 文中明确说明：**所有实验在8块Nvidia A100 GPU上执行**。
- 训练步骤：
  - 自对齐预训练：2000步。
  - 非KV-cache训练：2000步。
  - DHS机制训练：200步。
- **推理显存需求**：超过32GB GPU内存。
- **未明确给出总训练时间**，但可推测总步数较少（约4200步），单步处理序列长度为13408 tokens，由于模型尺寸为14B参数，训练时间应较长。

## 5. 实验数量与充分性
- **实验组数**：共3组主要实验（表1~表3），覆盖诊断+报告生成、模态补全质量、消融分析。
- **对比广度**：涵盖了隐式方法、显式方法、其他MLLM，类型较全面。
- **缺失场景**：设置了4种不同程度的模态缺失情况，评估稳定性。
- **消融实验**：从基础BAGEL（A）→加入统一建模（B）→加入自对齐（C）→加入DHS（UniBrain），逐步验证各组件贡献。
- **充分性评价**：实验设计较为规范，但**仅使用了单一数据集**，且模型在2D切片上训练，未评估3D体积信息。此外，未提及在更多疾病或真实临床环境中的验证。总体实验量适中，基本支撑了结论，但泛化性证据有待加强。

## 6. 论文的主要结论与发现
- UniBrain在**所有缺失模态场景下**的诊断准确率和报告生成质量均优于对比的隐式方法、显式两阶段方法和现有MLLM。
- 在**仅T1w可用**的极具挑战场景下，UniBrain达到74.47%诊断准确率（次优为隐式方法63.61%）；**完整模态下**达82.06%。
- 在**模态补全方面**，UniBrain生成图像的质量（PSNR/SSIM）与专门生成模型（ResViT等）相当或更好，且**下游诊断可用性显著更高**（Top-1高出约10~15%）。
- 消融证实：统一建模、自对齐、动态隐藏状态均对最终性能有正向贡献，其中统一建模贡献最大（+5%准确率），DHS平衡了生成与理解。

## 7. 优点（方法或实验设计的亮点）
- **统一框架优势**：将补全与理解有机融合，避免了传统两阶段流程的次优性，使得生成图像更有利于下游诊断。
- **自对齐策略创新**：利用图像自身监督信号学习细粒度特征，解决了医学图像-文本对稀缺问题，且无需昂贵的手工描述。
- **动态隐藏状态机制**：通过训练时模拟自回归推理中自生成伪影，提升了模型对长上下文误差的鲁棒性，是解决暴露偏差的新思路。
- **实验评估全面**：同时报告了补全质量（PSNR/SSIM）和诊断可用性（Top-1），揭示了补全精度不等于诊断收益，强调了统一建模的价值。
- **优秀跨场景表现**：尤其在极端缺失（仅T1w）下仍保持高水平，说明模型具有较强鲁棒性。

## 8. 不足与局限
- **计算资源要求高**：推理时需超过32GB GPU显存，限制了低资源环境部署；训练虽步数少但参数量大（14B），训练成本不菲。
- **仅使用2D切片**：将3D体积MRI投影为2D切片并修改报告描述，丢失了体积空间信息，与临床实际3D分析有差距。
- **单数据集验证**：只在RadGenome-brain MRI上评测，未在更大规模或多机构数据集上测试，泛化性存疑。
- **对比方法待扩展**：未与最新的医学专用MLLM（如Med-PaLM等）比较；隐式方法仅报告了诊断准确率但未提供生成或报告指标。
- **缺乏专家评估**：报告生成指标为自动指标（ROUGE、RaTEScore），未邀请临床医生进行临床实用性评估。
- **未讨论不同缺失比例对性能的边际影响**：实验仅设定了固定缺失组合，未系统模拟随机缺失场景。
- **消融实验揭示自对齐可能轻微干扰诊断精度**（表3中C vs B，诊断准确率略降），需要进一步分析原因。

（完）
