---
title: "MAF: Multimodal Adaptive Few-shot Prompting for Sentiment Analysis with MLLMs"
title_zh: MAF：面向情感分析的多模态自适应少样本提示方法（基于MLLMs）
authors: Hangling Xie
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15694v1"
tags: ["query:mm"]
score: 8.0
evidence: 使用自适应提示的多模态情感分析
tldr: 针对多模态大语言模型在情感分析中对提示设计敏感的问题，提出MAF框架，动态检索与查询相关的演示样本。该框架通过编码面部表情、场景和文本语义，并引入唇部运动检测实现多人场景的说话人识别，利用轻量级系数生成网络实时输出查询条件融合权重，加权聚合多模态相似度检索最具信息量的演示。通过多数投票增强稳定性。实验表明，MAF显著优于基线，并与强基线相当。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15694v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 843, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1142, \"height\": 610, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15694v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15694v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1270, \"height\": 987, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15694v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1264, \"height\": 305, \"label\": \"Table\"}]"
motivation: 静态统一提示无法捕捉情感分析中多模态线索的变化，导致MLLM性能对提示设计极度敏感。
method: MAF构建演示检索模块，以唇部运动检测辅助说话人识别，并用轻量级网络输出查询条件融合权重，聚合多模态相似度检索top-K演示，最后通过多数投票稳定预测。
result: 在公开基准上，MAF相比对应骨干模型取得一致且显著的性能提升，与多模态情感分析强基线保持竞争力。
conclusion: 自适应检索演示的MAF有效提升了MLLM在情感分析中的鲁棒性和准确性，推动了提示优化方法的发展。
---

## 摘要
多模态大语言模型（MLLMs）在理解复杂多模态内容方面展现出显著能力。然而，它们在情感分析中的性能对提示设计极为敏感，静态、统一应用的提示本质上无法有效捕捉不同输入中变化的微妙多模态线索。为解决这一局限，我们提出了多模态自适应少样本提示（MAF）框架，该框架动态检索并整合与查询相关的示例，以情境敏感的方式激发MLLMs的情感推理能力。MAF构建了一个示例检索模块，整体编码面部表情、场景背景和文本语义，并引入唇动幅度检测机制以在多人物场景中准确识别说话者。与传统的固定权重融合不同，我们训练了一个轻量级系数生成网络实时输出查询条件下的融合权重，实现对多模态相似度分数的加权聚合，从而检索最具信息量的前K个示例。通过MLLM生成的多个候选输出进行多数投票进一步增强了预测稳定性。在公开基准数据集上的大量实验表明，MAF相比相应的骨干变体取得了显著且一致的性能提升，并且与强大的多模态情感分析基线方法保持竞争力。

## Abstract
Multimodal large language models (MLLMs) have demonstrated remarkable capabilities in understanding complex multimodal content. However, their performance in sentiment analysis exhibits acute sensitivity to prompt design, rendering static, uniformly applied prompts inherently suboptimal for capturing the nuanced multimodal cues that vary across inputs. To address this limitation, we propose a Multimodal Adaptive Few-Shot Prompting (MAF) framework, which dynamically retrieves and integrates query-relevant demonstrations to elicit the sentiment reasoning capabilities of MLLMs in a context-sensitive manner. MAF constructs a demonstration retrieval module that holistically encodes facial expressions, scene context, and textual semantics, with a lip movement amplitude detection mechanism introduced for accurate speaker identification in multi-person scenarios. Departing from conventional fixed-weight fusion, a lightweight coefficient generation network is trained to output query-conditioned fusion weights in real time, enabling weighted aggregation of multimodal similarity scores to retrieve the top-K most informative demonstrations. Prediction stability is further enhanced through majority voting over multiple candidate outputs generated by the MLLM. Extensive experiments on public benchmark datasets demonstrate that MAF achieves substantial and consistent performance improvements over the corresponding backbone variants and remains competitive with strong multimodal sentiment-analysis baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态大语言模型（MLLMs）在情感分析任务中表现出对提示设计的高度敏感性。静态、统一风格的少样本提示无法有效适应不同输入中变化的微妙多模态线索（如面部表情、场景、文本），导致预测结果不稳定甚至错误。
- **研究动机**：现有方法要么依赖大规模标注数据和任务特定重训练，要么使用固定融合策略和文本依赖，缺乏对多模态输入的动态适配能力。MLLM的推理能力需要被“激发”，而非通过参数更新来“教导”，因此如何动态选择与查询相关的多模态示例成为关键。
- **整体含义**：本文提出一种无需更新MLLM参数的轻量级框架，通过检索增强和自适应融合提升情感分析的准确性和鲁棒性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

#### 核心思想
MAF（Multimodal Adaptive Few-shot Prompting）框架结合**检索增强提示**与**轻量级可训练系数生成器**，为每个查询动态检索最相关的多模态示例，并自适应地融合多种模态相似度分数，最后通过多数投票稳定预测。

#### 关键技术细节（分模块描述）

##### ① 混合示例检索模块
- **输入**：查询 \( q \)（图像序列 \( q_{img} \) 和文本 \( q_{txt} \)），示例库 \( D \)（含 \( N=1000 \) 个视频-文本-标签三元组）。
- **特征提取**（三种模态）：
  - **面部特征** \( e_{face}^q \)：对每帧用OpenFace 3.0提取；多人场景下通过计算上下唇关键点欧氏距离变化之和定位活跃说话者；最终时间平均池化得到视频级特征。
  - **场景特征** \( e_{scene}^q \)：用PlaceCNN提取逐帧特征，后接时间平均池化。
  - **文本特征** \( e_{text}^q \)：用LLM文本编码器得嵌入序列，经最大池化和归一化。
- **相似度计算**：查询与每个示例 \( p_i \) 分别计算内积相似度 \( s_{face} \)、\( s_{scene} \)、\( s_{text} \)。
- **加权融合**：最终检索分数 \( s_{rank}(q,p_i) = \alpha_1 s_{face} + \alpha_2 s_{scene} + \alpha_3 s_{text} \)，系数由自适应系数生成器动态输出。

##### ② 自适应系数生成器
- **输入**：拼接的多模态特征向量 \( h_q = [e_{face}^q; e_{scene}^q; e_{text}^q] \in \mathbb{R}^{4096} \)。
- **网络结构**：两层MLP（隐藏层256维，ReLU），sigmoid输出层，输出维度 \( C=66 \)（对应权重组合数）。
- **权重空间离散化**：约束 \( \alpha_1+\alpha_2+\alpha_3=1 \) 且 \( \alpha_i \geq 0 \)，步长0.1，共66种组合。
- **训练目标**：对每个训练样本 \( x_j \) 和权重组合 \( c \)，记录预测正确性 \( t_{j,c} \)，用二元交叉熵损失训练（多标签设置）。
- **推理**：选择预测概率最高的权重组合作为最终融合系数。

##### ③ 多数投票模块
- **流程**：给定查询和K个检索示例组成提示 \( P(q) \)，LLM通过概率采样生成M个候选标签 \( \{\hat{y}_m\}_{m=1}^M \)，取众数作为最终预测 \( y^* \)。
- **特点**：无需额外参数；通过并行API调用，延迟与单次调用接近。

### 3. 实验设计：数据集、基准、对比方法

#### 数据集（3个公开基准）
| 数据集       | 语言   | 场景         | 样本数   | 标签类型                     |
|--------------|--------|--------------|----------|------------------------------|
| CMU-MOSEI    | 英语   | YouTube独白  | 23,453   | 连续情感值[-3,+3]，也用于7类分类 |
| CH-SIMS v2.0 | 中文   | 影视、综艺   | 4,361    | [-1,+1]连续情感              |
| MELD         | 英语   | Friends对话  | 13,707   | 7类情感类别                  |

#### 对比方法
- **传统多模态方法**：TFN, LMF, MulT, MAG-BERT, Self-MM, AV-MC, UFEN-MTFN, TextMI, CHFN, UniMSE, MMGCN, MM-DFN, EmoKEG, GA2MIF。
- **LLM/MLLM基线**：UniSA系列（GPT2, T5, BART）、MSE-Adapter、ATGFB。
- **骨干变体**：Qwen-1.8B, LLaMA2-7B, ChatGLM3-6B（不含MAF的版本作为直接对比）。

#### 评价指标
- **CMU-MOSEI**：MAE, Corr, Acc-7, Acc-2, F1（Acc-2为二分类准确率）。
- **CH-SIMS v2.0**：MAE, Corr, Acc-2, F1, Acc2_weak（弱情感样本准确率）。
- **MELD**：Acc, WF1。

### 4. 资源与算力
- **文中明确信息**：所有实验在 **NVIDIA GeForce RTX 4090** 上运行，采用固定随机种子以保证可复现。
- **未说明的信息**：未提及训练系数生成器的具体时长、多卡训练情况、LLM推理批次大小等细节。

### 5. 实验数量与充分性

#### 实验组数及类型
- **主实验结果**（Table 2）：在3个数据集上对比6组骨干变体（MSE-*、ATGFB-*、MAF-*）及大量历史基线，共约20+行比较。
- **消融实验**（Table 3）：在3个数据集上对ChatGLM3-6B进行5种变体移除（w/o RAG、w/o adaptive retrieval、w/o adaptive weighting、w/o voting、完整MAF），共15组结果。
- **超参数分析**（Figure 4）：在MELD和CH-SIMS v2.0上研究K（1~5）和M（1~16）的影响。

#### 充分性与公平性评判
- **优点**：覆盖了不同语言、不同场景（独白/对话）、不同标注类型（连续值/类别）的数据集；对比基线全面，包括传统方法和最新LLM适配方法；消融实验完整，验证了每个组件的贡献；超参数分析合理。
- **潜在不足**：未报告统计显著性检验（如p值或置信区间）；未在更多元的数据集（如不同情感粒度、噪声场景）上验证；仅使用单一种子可能降低结论泛化性；消融实验中仅测试了ChatGLM3-6B一种骨干，未在Qwen和LLaMA2上重复消融。

### 6. 论文的主要结论与发现

- MAF在三个数据集上**一致且显著**优于对应的骨干变体（MSE-Adapter和ATGFB），并在多数指标上超越或匹敌传统强基线。
- 在CMU-MOSEI上，MAF-ChatGLM3-6B达到 **Acc-2 90.23%**、**F1 89.83%**、**Acc-7 59.58%**、**Corr 0.805**；在CH-SIMS v2.0上达到 **Acc-2 86.89%**、**F1 85.94%**、**MAE 0.279**；在MELD上达到 **Acc 69.94%**、**WF1 69.25%**。
- 消融实验表明：
  - **自适应检索**（动态选择示例）比简单RAG或无检索提升更大（错误示例会误导模型）。
  - **自适应权重**比固定融合更优，尤其对回归指标（MAE）改善明显。
  - **多数投票**提高稳定性，对概率波动较大的大模型更有效。
- 最佳超参数：**K=3**（检索示例数）、**M=8**（投票候选数）。

### 7. 优点：方法或实验设计上的亮点

- **方法创新性**：
  - 首次在MLLM情感分析中引入**查询条件自适应融合权重**，通过轻量级网络动态优化多模态相似度加权。
  - 利用**唇动检测机制**解决多人场景说话人定位问题，提升面部特征提取的准确性。
  - 将**多数投票**融入少样本提示管道，无需额外训练即增强稳定性。
- **实用性**：不更新MLLM参数，仅需训练一个极小的MLP（两层网络），计算成本低；可直接适配多种MLLM骨干（Qwen、LLaMA2、ChatGLM3）。
- **实验设计**：在三个互补数据集上验证（不同语言、不同场景、不同标注类型），消融实验全面，超参数分析清晰。

### 8. 不足与局限

- **数据集覆盖有限**：仅使用3个数据集，且均为公开、相对干净的数据；未在更多挑战性场景（如极度不平衡、跨域、噪声标注）上测试。
- **特征提取依赖**：OpenFace、PlaceCNN、文本编码器均为预训练模型，其偏差可能影响检索质量；未分析这些特征提取器对最终结果的敏感度。
- **权重离散化粗糙**：将连续权重空间以0.1步长离散化为66种组合，可能错过最优权重；也导致训练成为多标签分类问题而非回归，信息有损失。
- **统计可靠性**：未报告多次运行的平均值和方差，也未进行显著性检验；单种子实验结果可能受偶然因素影响。
- **推理效率**：多数投票需要M次LLM调用，尽管论文声称并行调用可近似无延迟，但实际中仍受限于API并发限制和资源开销。
- **通用性验证不足**：消融实验仅在ChatGLM3-6B上进行，未在Qwen和LLaMA2上重复验证各模块贡献；可能无法完全排除骨干模型的影响。

（完）
